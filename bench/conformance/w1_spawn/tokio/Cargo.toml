[package]
name = "bench"
version = "0.1.0"
edition = "2021"

# ISC License -- see LICENSE in the repository root.

[[bin]]
name = "bench"
path = "src/main.rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
