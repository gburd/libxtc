# Jekyll configuration for the libxtc documentation site.
#
# Builds on both GitHub Pages and Codeberg Pages.  The rendered API
# reference (Doxygen) is generated into api/ and linked from the nav.
#
# Local preview:
#   cd docs && bundle install && bundle exec jekyll serve

title: libxtc
description: >-
  A C11 library for Tokio/Seastar/BEAM-style asynchronous concurrency:
  fibers, an event loop, lightweight processes with links and monitors,
  supervisors, and deterministic simulation testing.
lang: en

# just-the-docs: a reference + guide theme with search and side nav.
# Used as a gem theme (no remote fetch, no openssl needed at build time);
# both Pages builders and a local `bundle exec jekyll build` resolve it.
theme: just-the-docs

# Diagrams: just-the-docs renders ```mermaid fenced blocks client-side.
mermaid:
  version: "10.9.1"

# Default page layout: our hero-styled wrapper (big title + lede), so
# every content page carries the landing-page look.
defaults:
  - scope:
      path: ""
    values:
      layout: content

# Site-relative base path.  Both GitHub Pages (user.github.io/libxtc)
# and Codeberg Pages (user.codeberg.page/libxtc) serve under /libxtc.
baseurl: "/libxtc"

# Search across all pages.
search_enabled: true
search:
  heading_level: 3
  previews: 3

# Code highlighting.
highlighter: rouge
markdown: kramdown
kramdown:
  syntax_highlighter: rouge
  input: GFM
  hard_wrap: false

# just-the-docs nav ordering uses front-matter `nav_order`.
heading_anchors: true

# Footer: a back-to-top link; the nav-panel footer links (Codeberg /
# GitHub) come from _includes/nav_footer_custom.html, which also removes
# the theme's default "Just the Docs" attribution line.
back_to_top: true
back_to_top_text: "Back to top"

# Footer / repo links (rendered by the theme).
aux_links:
  "Source (Codeberg)":
    - "https://codeberg.org/gregburd/libxtc"
  "Mirror (GitHub)":
    - "https://github.com/gburd/libxtc"
  "API reference (Doxygen)":
    - "/libxtc/api/"

# Callout styles used by the book (note / rationale / not-chosen).
callouts:
  note:
    title: Note
    color: blue
  rationale:
    title: Why this way
    color: green
  not_chosen:
    title: Alternative not chosen
    color: yellow
  warning:
    title: Careful
    color: red

# Files Jekyll must not try to render or copy.
exclude:
  - snippets/          # tested .c files, not site pages
  - Doxyfile
  - api/               # Doxygen output is copied in at deploy time
  - vendor/
  - Gemfile
  - Gemfile.lock
  - "*.3"
