.\" Copyright (c) 2026, The XTC Project
.\" Use of this source code is governed by the ISC License.
.Dd May 25, 2026
.Dt XTC_EXEC 3
.Os
.Sh NAME
.Nm xtc_exec_init ,
.Nm xtc_exec_fini ,
.Nm xtc_exec_run ,
.Nm xtc_exec_set_service_mode ,
.Nm xtc_exec_get_service_mode ,
.Nm xtc_exec_set_eager_rebalance ,
.Nm xtc_exec_get_eager_rebalance ,
.Nm xtc_exec_set_steal_backoff ,
.Nm xtc_exec_get_steal_backoff ,
.Nm xtc_exec_set_preempt ,
.Nm xtc_exec_stop ,
.Nm xtc_exec_n_loops ,
.Nm xtc_exec_loop ,
.Nm xtc_exec_loop_stats ,
.Nm xtc_exec_loop_id ,
.Nm xtc_shard_id ,
.Nm xtc_shard_count ,
.Nm xtc_exec_spawn ,
.Nm xtc_exec_spawn_on ,
.Nm xtc_exec_async ,
.Nm xtc_exec_async_on ,
.Nm xtc_exec_class_create ,
.Nm xtc_exec_class_shares ,
.Nm xtc_exec_class_latency ,
.Nm xtc_exec_class_runs ,
.Nm xtc_exec_class_vruntime ,
.Nm xtc_exec_set_stall_budget ,
.Nm xtc_loop_set_stall_budget ,
.Nm xtc_loop_set_stall_cb ,
.Nm xtc_loop_stall_count
.Nd L2 multi-loop work-stealing executor
.Sh SYNOPSIS
.In xtc_exec.h
.Ft int
.Fn xtc_exec_init "xtc_exec_t **out" "int n_loops"
.Ft int
.Fn xtc_exec_fini "xtc_exec_t *exec"
.Ft int
.Fn xtc_exec_run "xtc_exec_t *exec"
.Ft void
.Fn xtc_exec_set_service_mode "xtc_exec_t *exec" "int on"
.Ft int
.Fn xtc_exec_get_service_mode "xtc_exec_t *exec"
.Ft void
.Fn xtc_exec_set_eager_rebalance "xtc_exec_t *exec" "int on"
.Ft int
.Fn xtc_exec_get_eager_rebalance "xtc_exec_t *exec"
.Ft void
.Fn xtc_exec_set_steal_backoff "xtc_exec_t *exec" "int on"
.Ft int
.Fn xtc_exec_get_steal_backoff "xtc_exec_t *exec"
.Ft int
.Fn xtc_exec_set_preempt "xtc_exec_t *exec" "int64_t interval_ns"
.Ft int
.Fn xtc_exec_stop "xtc_exec_t *exec"
.Ft int
.Fn xtc_exec_n_loops "xtc_exec_t *exec"
.Ft "xtc_loop_t *"
.Fn xtc_exec_loop "xtc_exec_t *exec" "int idx"
.Ft int
.Fn xtc_exec_loop_stats "xtc_exec_t *exec" "int idx" "xtc_loop_stats_t *out"
.Ft int
.Fn xtc_exec_loop_id "void"
.Ft int
.Fn xtc_shard_id "void"
.Ft int
.Fn xtc_shard_count "void"
.Ft int
.Fn xtc_exec_spawn "xtc_exec_t *exec" "xtc_task_fn fn" "void *user" "xtc_task_t **out"
.Ft int
.Fn xtc_exec_spawn_on "xtc_exec_t *exec" "int idx" "xtc_task_fn fn" "void *user" "xtc_task_t **out"
.Ft int
.Fn xtc_exec_async "xtc_exec_t *exec" "xtc_coro_fn fn" "void *arg" "xtc_task_t **out"
.Ft int
.Fn xtc_exec_async_on "xtc_exec_t *exec" "int idx" "xtc_coro_fn fn" "void *arg" "xtc_task_t **out"
.Ft int
.Fn xtc_exec_class_create "xtc_loop_t *loop" "int shares" "int64_t latency_ns" "xtc_exec_class_t *out"
.Ft int
.Fn xtc_exec_class_shares "xtc_exec_class_t cls"
.Ft int64_t
.Fn xtc_exec_class_latency "xtc_exec_class_t cls"
.Ft uint64_t
.Fn xtc_exec_class_runs "xtc_exec_class_t cls"
.Ft uint64_t
.Fn xtc_exec_class_vruntime "xtc_exec_class_t cls"
.Ft void
.Fn xtc_exec_set_stall_budget "xtc_exec_t *exec" "int64_t budget_ns"
.Ft void
.Fn xtc_loop_set_stall_budget "xtc_loop_t *loop" "int64_t budget_ns"
.Ft void
.Fn xtc_loop_set_stall_cb "xtc_loop_t *loop" "xtc_stall_cb cb" "void *user"
.Ft uint64_t
.Fn xtc_loop_stall_count "const xtc_loop_t *loop"
.Sh DESCRIPTION
.Nm xtc_exec
manages
.Em N
event loops, one per worker thread.  Each loop owns a Chase-Lev
work-stealing deque as its run queue plus an MPSC inbox for cross-thread
wakers and remote spawns.
.Sh LIFECYCLE
.Fn xtc_exec_init
constructs an executor with
.Fa n_loops
loops; non-positive values default to a small reasonable count.
.Fn xtc_exec_fini
joins every worker, frees every loop.
.Pp
.Fn xtc_exec_run
spawns
.Fa n_loops
worker threads (one per loop) and blocks on the calling thread until
the executor reaches a quiescent state \u2014 every spawned task
.Dv DONE ,
every timer fired or cancelled, every inbox drained, every deque
empty \u2014 or until
.Fn xtc_exec_stop
is called from any thread.
.Pp
.Fn xtc_exec_set_service_mode
opts the executor out of that quiescent-state auto-stop.  When
.Fa on
is non-zero,
.Fn xtc_exec_run
blocks until
.Fn xtc_exec_stop
is called explicitly and never stops merely because every loop went
idle.  Use it for a long-running service (a supervised
.Xr xtc_app 3 ) ,
where a transient all-idle window during startup -- before every
supervised child has been scheduled across loops -- must not terminate
the application.  A bare work-pool leaves it off so the executor
drains and returns when its tasks finish.
.Pp
.Fn xtc_exec_set_preempt
enables cooperative-assisted preemption: each worker arms a per-worker
CPU-time interval timer at
.Fa interval_ns
(of that worker thread's own CPU time), and a timer tick makes that
worker's
.Xr xtc_yield_if_due 3
callers yield, so a long compute fiber that periodically consults a
yield check is time-sliced without a manual budget.
.Fa interval_ns
of 0 disables it (the default).  Call before
.Fn xtc_exec_run ;
workers read the setting at start.  Returns
.Dv XTC_E_NOSYS
where the platform lacks per-thread CPU-time timers (the setting is
stored but arming is a no-op).  This does not preempt a fiber that never
reaches a yield check.
.Pp
Workers steal from peers when their own deque empties before going
to
.Xr xtc_io 3
.Sy poll .
M5 uses a random victim policy; NUMA-near steal ordering arrives in
M5.5.
.Pp
.Fn xtc_exec_set_eager_rebalance
(OFF by default) controls WHEN a loop steals a migratable
.Pq Xr xtc_proc 3 , Fa migratable
peer proc.  By default a loop steals only when FULLY idle -- its run
queue empty AND no parked fibers or timers -- and discovers a
sibling's stealable work only on its next poll edge.  Under a load
where every loop owns parked fibers (e.g. many server backends parked
on client sockets while a peer loop has a runnable query), no loop is
ever fully idle, so migratable work sits on the stealable deque and is
never taken.  With eager rebalance ON, a loop whose RUN QUEUE is empty
(even if it owns parked fibers or timers) steals before it blocks in
the poller, and enqueuing a migratable task nudges one idle peer to
steal it promptly.  This trades some cross-loop migration (and its
cache/NUMA cost) for reclaiming idle capacity under partial load; only
migratable tasks are ever moved, so pinned work (the default) is
unaffected either way.  A supervised server whose backends park on
sockets opts in; a cache-locality-bound workload leaves it off.
.Pp
.Fn xtc_exec_set_steal_backoff
(OFF by default) controls how hard an IDLE loop scans for work.  By
default an idle worker re-polls (and re-attempts a steal) on a fixed
1 ms tick, so on a large executor that is mostly idle -- many carriers,
tiny or infrequent work -- the aggregate cost of repeated empty steal
scans is visible even though the machine is nearly idle.  With steal
backoff ON, a worker that has taken no work for several consecutive
turns grows its idle poll timeout exponentially (from 1 ms toward a
32 ms cap); any real work resets it immediately.  This cuts the
idle-scan CPU of an over-provisioned executor at the cost of a few
extra milliseconds of worst-case latency before a deeply-idle loop
notices stealable work (a cross-thread enqueue still nudges it at once
when eager rebalance is also on).  It is complementary to eager
rebalance: eager controls WHEN an idle-but-not-drained loop steals;
backoff controls how often a fully-idle loop bothers to look.
.Pp
.Fn xtc_exec_get_service_mode ,
.Fn xtc_exec_get_eager_rebalance ,
and
.Fn xtc_exec_get_steal_backoff
read back the current setting (0 for a NULL exec).
.Sh POLICY KNOB CONVENTION
Every per-
.Vt xtc_exec_t
scheduler policy
.Pq service mode, eager rebalance, and any future addition -- steal
policy, NUMA affinity mode, migration eagerness, etc.
follows one shape:
.Bl -bullet -compact
.It
Named
.Fn xtc_exec_set_ Ns Ar name
/
.Fn xtc_exec_get_ Ns Ar name ;
a boolean knob takes/returns a plain
.Vt int
(0/1).
.It
Takes effect immediately; no re-init needed, and safe to call from any
thread at any time (each knob is a single atomic field on the
executor).
.It
Documented with its COST as well as its effect -- a policy that trades
CPU or syscalls for responsiveness
.Pq like eager rebalance
says so, so a caller can judge whether to opt in.
.El
.Pp
This is deliberately distinct from two other things that look similar:
.Bl -tag -width indent
.It Xr xtc_proc 3 Ns 's Va migratable
is a per-PROC ELIGIBILITY bit decided once at spawn
.Pq can this proc's coro be moved at all ,
not an executor-wide policy
.Pq how hard the executor tries to move eligible work .
Keeping eligibility and policy separate means a consumer who wants some
procs movable does not pay an executor-wide policy's runtime cost, and
vice versa.
.It Xr xtc_preempt 3 Ns 's Fn xtc_preempt_set_involuntary
is a PROCESS-WIDE toggle
.Pq no Vt xtc_exec_t argument
governing the signal-context preemption redirect, not scoped to one
executor.
.It Xr xtc_cfg 3
is a separate, PROCESS-GLOBAL named-tunable registry
.Pq log levels, backpressure thresholds, ops-facing settings meant to
survive a config-file reload
with no per-instance scoping yet, so it does not back a per-exec knob
like these -- the two mechanisms serve different scopes on purpose.
.El
.Sh SPAWN PLACEMENT
.Fn xtc_exec_spawn
puts the new task on a loop chosen by a round-robin counter.
.Fn xtc_exec_spawn_on
forces placement on the given loop.
The coroutine variants
.Fn xtc_exec_async
and
.Fn xtc_exec_async_on
mirror the contract.
.Pp
Spawn calls are safe from any thread, including before
.Fn xtc_exec_run :
work queued in advance is processed when the workers start.  Spawns
from a non-owner thread are routed through the target loop's inbox
and surface on the loop's next step.
.Sh PROPORTIONAL-SHARE SCHEDULING
The run queue is a plain FIFO plus the work-stealing deque by default,
which splits CPU roughly evenly among equally busy fibers.  The
optional
.Em proportional-share
scheduler adds weighted-fair scheduling: create a scheduling
.Em class
with
.Fn xtc_exec_class_create ,
giving it
.Fa shares
(1..1000) and an optional
.Fa latency_ns
bound, then place a process's tasks in it via
.Va xtc_proc_opts_t.sched_class
at spawn or
.Xr xtc_proc_set_class 3
from inside the process.  A loop that carries more than one class picks
the minimum-virtual-runtime class on each dispatch, so each class
receives a CPU fraction proportional to its shares — a class with 3x
the shares of another gets about 3x the CPU.  A non-zero
.Fa latency_ns
shrinks the loop's cooperative yield interval so the class is serviced
promptly.
.Pp
The feature is OFF by default with ZERO overhead: a loop with no class
created runs the exact plain-FIFO + work-stealing path, byte-for-byte,
and the virtual-runtime accounting activates only once a class exists.
Untagged tasks race an implicit default lane so background work is
never starved by always-ready class work.
.Fn xtc_exec_class_shares ,
.Fn xtc_exec_class_latency ,
.Fn xtc_exec_class_runs ,
and
.Fn xtc_exec_class_vruntime
read a class handle's parameters and telemetry (run count, accumulated
virtual runtime) back.
.Pp
This is INSPIRED BY Glommio (Glauber Costa / ScyllaDB), whose executor
gives each task queue shares and a latency class and picks the next
queue by a CFS-style virtual-runtime heap
.Pq executor/mod.rs account_vruntime , shares.rs .
The accrual formula is Glommio's:
.Fa vruntime
+= (cost_ns * reciprocal) >> 12, where reciprocal = (1<<22)/shares.
.Sh OVER-BUDGET STALL WATCHDOG
When a single task run monopolizes a loop past a budget, the optional
stall watchdog reports which task did it.  Arm a per-loop budget with
.Fn xtc_loop_set_stall_budget
(nanoseconds; 0 disables) or arm every loop of an executor at once with
.Fn xtc_exec_set_stall_budget .
Install a report sink with
.Fn xtc_loop_set_stall_cb ;
with no callback the runtime logs a warning and emits a backtrace of
the loop to stderr.
.Fn xtc_loop_stall_count
returns the number of over-budget reports (telemetry).
.Pp
The check is a cheap wall-clock comparison at the run-end boundary in
the loop step — no watcher thread, no signal — so it is a single branch
on a disabled flag when off (zero overhead).  This is INSPIRED BY
Glommio's stall detector
.Pq executor/stall.rs .
.Sh CROSS-THREAD WAKERS
A
.Vt xtc_waker_t
captured on one loop may be fired from any thread; cross-thread
fires are routed through the target loop's inbox plus an
.Fn xtc_io_wakeup
ping that breaks the target out of
.Xr xtc_io 3
.Sy poll .
The wake remains idempotent: many concurrent wakes collapse to at
most one re-queue per yielded run.
.Sh THREAD AFFINITY
.Fn xtc_exec_loop_id
returns the executor index of the loop the calling task is running
on, or -1 from a thread that is not a worker.  Tests use this to
verify spawn placement and steal behaviour.
It is also the migration-detection idiom: because it re-reads the
CURRENT resume's carrier every call (like
.Xr xtc_self 3 ) ,
a proc that captures it before a yield and compares after learns
whether it was work-stolen to a different loop in between -- see
.Xr xtc_proc 3
MIGRATION for the pattern a consumer with carrier-affine cached state
(e.g.\& a per-loop wait-set registration) should use.
.Pp
.Fn xtc_shard_id
and
.Fn xtc_shard_count
are the Seastar-style per-shard API: the 0-based index of the loop
the caller runs on, and the number of shards.  Unlike
.Fn xtc_exec_loop_id
they treat a standalone loop
.Pq not under an executor
as shard 0 of 1, so a shared-nothing consumer can always index
per-core state with
.Fn xtc_shard_id
regardless of whether it runs under the executor or a single loop.
Both return the off-loop sentinels (-1 and 0) only when the caller is
not on any loop.
.Pp
.Fn xtc_exec_loop_stats
fills
.Fa out
with loop
.Fa idx Ns 's
work counters:
.Va tasks_run
(task steps executed on that loop) and
.Va steals
(tasks that loop stole from a peer).
The values are lock-free snapshots, for load-balance and
tail-latency diagnosis.
Note that explicitly placed work
.Pq Fn xtc_exec_spawn_on
and processes are pinned to their loop and are never stolen; only
the unpinned general task pool
.Pq Fn xtc_exec_spawn
migrates.
.Sh RETURN VALUES
.Dv XTC_OK
on success;
.Dv XTC_E_INVAL
on a bad argument;
.Dv XTC_E_NOMEM
on allocation failure;
.Dv XTC_E_INTERNAL
on platform failure.
.Sh THREAD SAFETY
All public functions are safe to call from any thread.
A given task handle (out_task) returned from a spawn must be used only
in ways consistent with the M3/M4 task contracts; in particular, a
.Vt xtc_waker_t
captured from one task may travel anywhere.
.Sh EXAMPLES
.Bd -literal -offset indent
static intptr_t hello(void *arg) {
        printf("hello from loop %d\\n", xtc_exec_loop_id());
        return 0;
}

int main(void) {
        xtc_exec_t *e;
        xtc_exec_init(&e, 4);
        for (int i = 0; i < 4; i++)
                xtc_exec_async(e, hello, NULL, NULL);
        xtc_exec_run(e);
        xtc_exec_fini(e);
        return 0;
}
.Ed
.Sh SEE ALSO
.Xr xtc_loop 3 ,
.Xr xtc_async 3 ,
.Xr xtc_io 3 ,
.Xr xtc 7
.Sh HISTORY
First appeared in xtc 0.0.1 (M5).  M5.5 will add NUMA-aware steal
ordering and configure-time auto-detect of CPU count via
.Sy __os_ncpus .
