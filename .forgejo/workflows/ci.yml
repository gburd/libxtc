name: CI

# Build libxtc and run the full test suite (make check: munit + PBT +
# shell tests) on every push and pull request.  This is the workflow
# that backs the README's "CI-tested every commit" statement; until
# it existed that claim was aspirational.
#
# The autotools path is the canonical build; meson is a secondary
# stub and is not exercised here.  hegel-c property tests need a local
# hegel server and are configured out in CI (the PBT sources still
# compile; the generative runs are a developer-local step).
#
# I/O backend: these jobs force --with-io-backend=epoll because the
# Codeberg hosted runners execute in containers whose seccomp profile
# blocks the io_uring_* syscalls, so the auto-selected uring backend
# would fail io_uring_queue_init at runtime (the backend is chosen at
# configure time, with no runtime fallback by design).  The GitHub
# mirror runs on VMs that permit io_uring and exercises the uring
# backend, so between the two CIs both Linux backends are covered.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: codeberg-small
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, clang]
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ${{ matrix.compiler }} make autoconf automake \
            pkg-config libssl-dev liburing-dev gawk openssl

      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4

      - name: Autoreconf
        working-directory: dist
        run: autoreconf -i

      - name: Configure
        run: |
          mkdir -p build_ci
          cd build_ci
          CC=${{ matrix.compiler }} ../dist/configure --with-tls=auto --with-io-backend=epoll

      - name: Build
        run: make -C build_ci -j"$(nproc)"

      - name: Test
        run: make -C build_ci check

  coro-fctx:
    # Force the fcontext coroutine substrate (the musl path) on a
    # glibc runner so it is exercised on every commit without needing
    # a musl toolchain.
    runs-on: codeberg-small
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc make autoconf automake pkg-config libssl-dev liburing-dev gawk openssl
      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4
      - name: Autoreconf
        working-directory: dist
        run: autoreconf -i
      - name: Configure (force fcontext)
        run: |
          mkdir -p build_fctx
          cd build_fctx
          CPPFLAGS=-DXTC_CORO_FORCE_FCTX ../dist/configure --with-tls=auto --with-io-backend=epoll
      - name: Build
        run: make -C build_fctx -j"$(nproc)"
      - name: Test
        run: make -C build_fctx check

  sanitizers:
    runs-on: codeberg-small
    strategy:
      fail-fast: false
      matrix:
        sanitizer: [address, undefined]
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc make autoconf automake pkg-config libssl-dev liburing-dev gawk openssl

      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4

      - name: Autoreconf
        working-directory: dist
        run: autoreconf -i

      - name: Configure with ${{ matrix.sanitizer }} sanitizer
        run: |
          mkdir -p build_san
          cd build_san
          CFLAGS="-fsanitize=${{ matrix.sanitizer }} -fno-omit-frame-pointer -g -O1" \
          LDFLAGS="-fsanitize=${{ matrix.sanitizer }}" \
            ../dist/configure --with-tls=auto --with-io-backend=epoll

      - name: Build
        run: make -C build_san -j"$(nproc)"

      - name: Test under sanitizer
        env:
          ASAN_OPTIONS: detect_leaks=1:abort_on_error=1:strict_string_checks=1
          UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1
        run: make -C build_san check

  examples:
    # Build the examples and run their in-process tests (no daemons,
    # so nothing hangs CI).  The examples exercise the public API
    # broadly -- including the single-file amalgamation drop-in -- and
    # would otherwise rot unnoticed since the library make check does
    # not build them.
    runs-on: codeberg-small
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc make autoconf automake pkg-config libssl-dev liburing-dev gawk openssl python3
      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4
      - name: Autoreconf
        working-directory: dist
        run: autoreconf -i
      - name: Build libxtc
        run: |
          mkdir -p build_ci
          cd build_ci
          ../dist/configure --with-tls=auto --with-io-backend=epoll
          make -j"$(nproc)"
      - name: kaka in-process tests
        working-directory: examples/07_kaka
        run: |
          make XTC_BUILD=../../build_ci
          ./test_frame
          ./test_partition
          ./test_broker
          ./test_group
      - name: sqlxtc in-process tests + amalgamation
        working-directory: examples/06_sqlxtc
        run: |
          make XTC_BUILD=../../build_ci test-quack test-sql-parse test-sql-ast test-sqlrec test-vexec test-vexec-par test-vexec-ord test-vexec-join test-vexec-run test-db-vexec test-vfs test-vfs-loop test-pcache test-concurrency test-admin test-mem test-wal test-wal-recover test-torn-smo test-wal-compact test-shard test-mvcc test-xstore test-xstore-scan test-xlog test-recover-undo test-parallel test-bufmgr test-redo-page test-inplace-redo test-bufmgr-mt test-scan-resist test-persist test-cursor-resume test-server-storage test-clean-restart test-steal test-btnode test-btree test-btree-loop test-btree-mt test-btree-delete-merge test-savepoint test-isolation test-native-txn test-native-driver
          make XTC_BUILD=../../build_ci amalg
          make XTC_BUILD=../../build_ci test-vfs-amalg test-pcache-amalg
      - name: sqlxtc multi-thread load (one loop per CPU; cross-loop regression)
        working-directory: examples/06_sqlxtc
        run: |
          make XTC_BUILD=../../build_ci sqlxtc-server
          bash ../../test/sqlxtc/test_sqlxtc_mt.sh

  tls-backends:
    # Build against each TLS backend that works with the apt packages
    # on the small Codeberg runner and run the m18 TLS suite.  BoringSSL
    # (from-source: cmake + Go) and mbedTLS (the backend needs the 3.x
    # API, but apt ships 2.x) are impractical here, so they run on the
    # GitHub mirror's tls-backends job; openssl, gnutls, and wolfssl are
    # exercised on both CIs.
    runs-on: codeberg-small
    strategy:
      fail-fast: false
      matrix:
        backend: [openssl, gnutls, wolfssl]
    steps:
      - name: Install base toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc make autoconf automake pkg-config \
            liburing-dev gawk openssl
      - name: Install backend ${{ matrix.backend }}
        run: |
          case "${{ matrix.backend }}" in
            openssl)   sudo apt-get install -y libssl-dev ;;
            gnutls)    sudo apt-get install -y libgnutls28-dev ;;
            wolfssl)   sudo apt-get install -y libwolfssl-dev ;;
          esac
      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4
      - name: Autoreconf
        working-directory: dist
        run: autoreconf -i
      - name: Configure --with-tls=${{ matrix.backend }}
        run: |
          mkdir -p build_tls && cd build_tls
          ../dist/configure --with-tls=${{ matrix.backend }} --with-io-backend=epoll
      - name: Build + run the m18 TLS suite
        run: |
          make -C build_tls -j"$(nproc)" test_tls_server test_tls_client
          ( cd build_tls && ./test_tls_server && ./test_tls_client )
