/* This file is automatically generated by Lemon from input grammar
** source file "sql_grammar.lime".
*/
/*
** 2000-05-29
**
** The author disclaims copyright to this source code.  In place of
** a legal notice, here is a blessing:
**
**    May you do good and not evil.
**    May you find forgiveness for yourself and forgive others.
**    May you share freely, never taking more than you give.
**
*************************************************************************
** Driver template for the lime parser generator.
**
** The "lemon" program processes an LALR(1) input grammar file, then uses
** this template to construct a parser.  The "lemon" program inserts text
** at each "%%" line.  Also, any "P-a-r-s-e" identifier prefix (without the
** interstitial "-" characters) contained in this template is changed into
** the value of the %name directive from the grammar.  Otherwise, the content
** of this template is copied straight through into the generated parser
** source file.
**
** The following is the concatenation of all %include directives from the
** input grammar file:
*/
/************ Begin %include sections from the grammar ************************/
#line 19 "sql_grammar.lime"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include "sql_parse.h"
#include "sql_ast.h"
#include "sql_parse_lime.h"

/* Allocate an AST node of the given type from the parse arena. */
#define AST(ps, T)  ((T *)sql_arena_alloc((ps)->arena, sizeof(T)))

/* Build a sql_str_t from a token (a slice into the source SQL). */
static sql_str_t tok_str(sql_token_t t) {
    sql_str_t s; s.p = t.p; s.len = (uint32_t)(t.len < 0 ? 0 : t.len); return s;
}

/* A string-literal token '...' -- strip the surrounding quotes. */
static sql_str_t tok_str_unquote(sql_token_t t) {
    sql_str_t s = tok_str(t);
    if (s.len >= 2 && (s.p[0] == '\'' || s.p[0] == '"')) { s.p++; s.len -= 2; }
    return s;
}

/* Record an expression node's verbatim source span from a token (the
 * slice covering this leaf in the original SQL).  The whole-expression
 * span is unioned over the tree by sql_expr_span(). */
static void span_tok(sql_expr_t *e, sql_token_t t) {
    if (e && t.p && t.len > 0) { e->src = t.p; e->srclen = (uint32_t)t.len; }
}

static sql_expr_t *mk_expr(sql_parse_state_t *ps, sql_expr_op_t op) {
    sql_expr_t *e = AST(ps, sql_expr_t);
    if (e) e->op = op;
    return e;
}

static sql_expr_t *mk_binary(sql_parse_state_t *ps, int tok,
                             sql_expr_t *a, sql_expr_t *b) {
    sql_expr_t *e = mk_expr(ps, SX_E_BINARY);
    if (e) { e->op2 = tok; e->a = a; e->b = b; }
    return e;
}

static sql_expr_t *mk_unary(sql_parse_state_t *ps, int tok, sql_expr_t *a) {
    sql_expr_t *e = mk_expr(ps, SX_E_UNARY);
    if (e) { e->op2 = tok; e->a = a; }
    return e;
}

static sql_exprlist_t *mk_list(sql_parse_state_t *ps) {
    return AST(ps, sql_exprlist_t);
}

static sql_exprlist_t *list_add(sql_parse_state_t *ps, sql_exprlist_t *l,
                                sql_expr_t *e) {
    sql_exprlist_item_t *it;
    if (l == NULL) l = mk_list(ps);
    if (l == NULL) return NULL;
    it = AST(ps, sql_exprlist_item_t);
    if (it == NULL) return l;
    it->expr = e;
    if (l->tail) l->tail->next = it; else l->head = it;
    l->tail = it;
    l->n++;
    return l;
}

/* Carries a WITH clause up to the select it prefixes. */
struct with_clause_v { sql_cte_t *list; int rec; };
#line 101 "sql_grammar.c"
/**************** End of %include directives **********************************/
/* These constants specify the various numeric values for terminal symbols.
***************** Begin token definitions *************************************/
#ifndef TK_SELECT
#define TK_SELECT                       1
#define TK_FROM                         2
#define TK_WHERE                        3
#define TK_GROUP                        4
#define TK_BY                           5
#define TK_HAVING                       6
#define TK_ORDER                        7
#define TK_LIMIT                        8
#define TK_OFFSET                       9
#define TK_ASC                         10
#define TK_DESC                        11
#define TK_DISTINCT                    12
#define TK_ALL                         13
#define TK_UNION                       14
#define TK_INTERSECT                   15
#define TK_EXCEPT                      16
#define TK_INSERT                      17
#define TK_INTO                        18
#define TK_VALUES                      19
#define TK_DEFAULT                     20
#define TK_REPLACE                     21
#define TK_UPDATE                      22
#define TK_SET                         23
#define TK_DELETE                      24
#define TK_CREATE                      25
#define TK_TABLE                       26
#define TK_INDEX                       27
#define TK_VIEW                        28
#define TK_TRIGGER                     29
#define TK_TEMP                        30
#define TK_TEMPORARY                   31
#define TK_ALTER                       32
#define TK_RENAME                      33
#define TK_DROP                        34
#define TK_IF                          35
#define TK_NOT                         36
#define TK_EXISTS                      37
#define TK_PRAGMA                      38
#define TK_BEGIN                       39
#define TK_COMMIT                      40
#define TK_END                         41
#define TK_ROLLBACK                    42
#define TK_TRANSACTION                 43
#define TK_SAVEPOINT                   44
#define TK_RELEASE                     45
#define TK_ATTACH                      46
#define TK_DETACH                      47
#define TK_DATABASE                    48
#define TK_AS                          49
#define TK_AND                         50
#define TK_OR                          51
#define TK_NULL                        52
#define TK_TRUE                        53
#define TK_FALSE                       54
#define TK_PRIMARY                     55
#define TK_KEY                         56
#define TK_UNIQUE                      57
#define TK_FOREIGN                     58
#define TK_REFERENCES                  59
#define TK_CHECK                       60
#define TK_AUTOINCR                    61
#define TK_ON                          62
#define TK_USING                       63
#define TK_IN                          64
#define TK_LIKE                        65
#define TK_BETWEEN                     66
#define TK_IS                          67
#define TK_CASE                        68
#define TK_WHEN                        69
#define TK_THEN                        70
#define TK_ELSE                        71
#define TK_EXPLAIN                     72
#define TK_QUERY                       73
#define TK_PLAN                        74
#define TK_WITH                        75
#define TK_RECURSIVE                   76
#define TK_TO                          77
#define TK_JOIN                        78
#define TK_INNER                       79
#define TK_OUTER                       80
#define TK_LEFT                        81
#define TK_RIGHT                       82
#define TK_FULL                        83
#define TK_CROSS                       84
#define TK_NATURAL                     85
#define TK_LP                          86
#define TK_RP                          87
#define TK_COMMA                       88
#define TK_SEMI                        89
#define TK_DOT                         90
#define TK_STAR                        91
#define TK_PLUS                        92
#define TK_MINUS                       93
#define TK_SLASH                       94
#define TK_PERCENT                     95
#define TK_EQ                          96
#define TK_NE                          97
#define TK_LT                          98
#define TK_LE                          99
#define TK_GT                          100
#define TK_GE                          101
#define TK_CONCAT                      102
#define TK_AMP                         103
#define TK_PIPE                        104
#define TK_QMARK                       105
#define TK_TILDE                       106
#define TK_ID                          107
#define TK_NUMBER                      108
#define TK_STRING                      109
#define TK_BLOB                        110
#define TK_UMINUS                      111
#endif
/**************** End token definitions ***************************************/

/* The next sections is a series of control #defines.
** various aspects of the generated parser.
**    YYCODETYPE         is the data type used to store the integer codes
**                       that represent terminal and non-terminal symbols.
**                       "unsigned char" is used if there are fewer than
**                       256 symbols.  Larger types otherwise.
**    YYNOCODE           is a number of type YYCODETYPE that is not used for
**                       any terminal or nonterminal symbol.
**    YYFALLBACK         If defined, this indicates that one or more tokens
**                       (also known as: "terminal symbols") have fall-back
**                       values which should be used if the original symbol
**                       would not parse.  This permits keywords to sometimes
**                       be used as identifiers, for example.
**    YYACTIONTYPE       is the data type used for "action codes" - numbers
**                       that indicate what to do in response to the next
**                       token.
**    SqlParseTOKENTYPE     is the data type used for minor type for terminal
**                       symbols.  Background: A "minor type" is a semantic
**                       value associated with a terminal or non-terminal
**                       symbols.  For example, for an "ID" terminal symbol,
**                       the minor type might be the name of the identifier.
**                       Each non-terminal can have a different minor type.
**                       Terminal symbols all have the same minor type, though.
**                       This macros defines the minor type for terminal 
**                       symbols.
**    YYMINORTYPE        is the data type used for all minor types.
**                       This is typically a union of many types, one of
**                       which is SqlParseTOKENTYPE.  The entry in the union
**                       for terminal symbols is called "yy0".
**    YYSTACKDEPTH       is the maximum depth of the parser's stack.  If
**                       zero the stack is dynamically sized using realloc()
**    SqlParseARG_SDECL     A static variable declaration for the %extra_argument
**    SqlParseARG_PDECL     A parameter declaration for the %extra_argument
**    SqlParseARG_PARAM     Code to pass %extra_argument as a subroutine parameter
**    SqlParseARG_STORE     Code to store %extra_argument into yypParser
**    SqlParseARG_FETCH     Code to extract %extra_argument from yypParser
**    SqlParseCTX_*         As SqlParseARG_ except for %extra_context
**    YYREALLOC          Name of the realloc() function to use
**    YYFREE             Name of the free() function to use
**    YYDYNSTACK         True if stack space should be extended on heap
**    YYERRORSYMBOL      is the code number of the error symbol.  If not
**                       defined, then do no error processing.
**    YYNSTATE           the combined number of states.
**    YYNRULE            the number of rules in the grammar
**    YYNTOKEN           Number of terminal symbols
**    YY_MAX_SHIFT       Maximum value for shift actions
**    YY_MIN_SHIFTREDUCE Minimum value for shift-reduce actions
**    YY_MAX_SHIFTREDUCE Maximum value for shift-reduce actions
**    YY_ERROR_ACTION    The yy_action[] code for syntax error
**    YY_ACCEPT_ACTION   The yy_action[] code for accept
**    YY_NO_ACTION       The yy_action[] code for no-op
**    YY_MIN_REDUCE      Minimum value for reduce actions
**    YY_MAX_REDUCE      Maximum value for reduce actions
**    YY_MIN_DSTRCTR     Minimum symbol value that has a destructor
**    YY_MAX_DSTRCTR     Maximum symbol value that has a destructor
*/
#ifndef INTERFACE
# define INTERFACE 1
#endif
/************* Begin control #defines *****************************************/
#define YYCODETYPE unsigned char
#define YYNOCODE 174
#define YYACTIONTYPE unsigned short int
#define YYFIRSTTOKEN 0
#define YYNRHS_MAX 10
#define SqlParseTOKENTYPE sql_token_t
typedef union {
  int yyinit;
  SqlParseTOKENTYPE yy0;
  sql_case_arm_t * yy76;
  sql_str_t yy77;
  sql_stmt_t * yy97;
  sql_exprlist_t * yy116;
  sql_src_t * yy157;
  sql_coldef_t * yy182;
  sql_expr_t * yy192;
  int yy220;
  sql_select_t * yy261;
  sql_assign_t * yy262;
  sql_cte_t * yy277;
  sql_insert_t * yy278;
  sql_qname_t yy289;
  sql_exprlist_item_t * yy298;
  struct with_clause_v yy339;
} YYMINORTYPE;
#ifndef YYSTACKDEPTH
#define YYSTACKDEPTH 100
#endif
#define SqlParseARG_SDECL sql_parse_state_t *pstate;
#define SqlParseARG_PDECL ,sql_parse_state_t *pstate
#define SqlParseARG_PARAM ,pstate
#define SqlParseARG_FETCH sql_parse_state_t *pstate=yypParser->pstate; (void)pstate;
#define SqlParseARG_STORE yypParser->pstate=pstate;
#undef YYREALLOC
#define YYREALLOC realloc
#undef YYFREE
#define YYFREE free
#undef YYDYNSTACK
#define YYDYNSTACK 0
#undef YYSIZELIMIT
#define SqlParseCTX(P) 0
#define SqlParseCTX_SDECL
#define SqlParseCTX_PDECL
#define SqlParseCTX_PARAM
#define SqlParseCTX_FETCH
#define SqlParseCTX_STORE
#undef YYERRORSYMBOL
#undef YYERRSYMDT
#undef YYFALLBACK
#define YYNSTATE             303
#define YYNRULE              228
#define YYNRULE_WITH_ACTION  221
#define YYNTOKEN             112
#define YY_MAX_SHIFT         302
#define YY_MIN_SHIFTREDUCE   457
#define YY_MAX_SHIFTREDUCE   684
#define YY_ERROR_ACTION      685
#define YY_ACCEPT_ACTION     686
#define YY_NO_ACTION         687
#define YY_MIN_REDUCE        688
#define YY_MAX_REDUCE        915
#define YY_MIN_DSTRCTR       174
#define YY_MAX_DSTRCTR       0
/************* End control #defines *******************************************/
#define YY_NLOOKAHEAD ((int)(sizeof(yy_lookahead)/sizeof(yy_lookahead[0])))

/* Define the yytestcase() macro to be a no-op if is not already defined
** otherwise.
**
** Applications can choose to define yytestcase() in the %include section
** to a macro that can assist in verifying code coverage.  For production
** code the yytestcase() macro should be turned off.  But it is useful
** for testing.
*/
#ifndef yytestcase
# define yytestcase(X)
#endif

/* Branch-prediction hints.  These are no-ops on compilers without
** __builtin_expect support; the runtime is correct either way.
** Used in the hot find_shift / find_reduce / dispatch paths to
** mark statistically-rare branches (collisions, accept, error).
** Per .agent/notes/c-perf-audit.md item #5: limpar.c was the
** runtime template every user binary embeds, and had zero hints. */
#ifndef LIME_LIKELY
# if defined(__GNUC__) || defined(__clang__)
#  define LIME_LIKELY(x)   __builtin_expect(!!(x), 1)
#  define LIME_UNLIKELY(x) __builtin_expect(!!(x), 0)
# else
#  define LIME_LIKELY(x)   (x)
#  define LIME_UNLIKELY(x) (x)
# endif
#endif

/* Macro to determine if stack space has the ability to grow using
** heap memory.
*/
#if YYSTACKDEPTH<=0 || YYDYNSTACK
# define YYGROWABLESTACK 1
#else
# define YYGROWABLESTACK 0
#endif

/* Guarantee a minimum number of initial stack slots.
*/
#if YYSTACKDEPTH<=0
# undef YYSTACKDEPTH
# define YYSTACKDEPTH 2  /* Need a minimum stack size */
#endif


/* Next are the tables used to determine what action to take based on the
** current state and lookahead token.  These tables are used to implement
** functions that take a state number and lookahead value and return an
** action integer.  
**
** Suppose the action integer is N.  Then the action is determined as
** follows
**
**   0 <= N <= YY_MAX_SHIFT             Shift N.  That is, push the lookahead
**                                      token onto the stack and goto state N.
**
**   N between YY_MIN_SHIFTREDUCE       Shift to an arbitrary state then
**     and YY_MAX_SHIFTREDUCE           reduce by rule N-YY_MIN_SHIFTREDUCE.
**
**   N == YY_ERROR_ACTION               A syntax error has occurred.
**
**   N == YY_ACCEPT_ACTION              The parser accepts its input.
**
**   N == YY_NO_ACTION                  No such action.  Denotes unused
**                                      slots in the yy_action[] table.
**
**   N between YY_MIN_REDUCE            Reduce by rule N-YY_MIN_REDUCE
**     and YY_MAX_REDUCE
**
** The action table is constructed as a single large table named yy_action[].
** Given state S and lookahead X, the action is computed as either:
**
**    (A)   N = yy_action[ yy_shift_ofst[S] + X ]
**    (B)   N = yy_default[S]
**
** The (A) formula is preferred.  The B formula is used instead if
** yy_lookahead[yy_shift_ofst[S]+X] is not equal to X.
**
** The formulas above are for computing the action when the lookahead is
** a terminal symbol.  If the lookahead is a non-terminal (as occurs after
** a reduce action) then the yy_reduce_ofst[] array is used in place of
** the yy_shift_ofst[] array.
**
** The following are the tables generated in this section:
**
**  yy_action[]        A single table containing all actions.
**  yy_lookahead[]     A table containing the lookahead for each entry in
**                     yy_action.  Used to detect hash collisions.
**  yy_shift_ofst[]    For each state, the offset into yy_action for
**                     shifting terminals.
**  yy_reduce_ofst[]   For each state, the offset into yy_action for
**                     shifting non-terminals after a reduce.
**  yy_default[]       Default action for each state.
**
*********** Begin parsing tables **********************************************/
#define YY_ACTTAB_COUNT (958)
static const YYACTIONTYPE yy_action[] = {
 /*     0 */   689,  136,  691,  176,  692,  693,  694,  695,  696,  697,
 /*    10 */   698,  699,  700,  701,  702,  703,  704,  705,  706,   80,
 /*    20 */   726,   79,  139,  856,  216,  286,  176,  138,  856,  192,
 /*    30 */    89,  717,  233,  280,  757,  214,  208,  251,  101,   99,
 /*    40 */    97,   95,  156,  856,  207,  686,   71,   28,  225,  277,
 /*    50 */    66,   67,  140,  856,  208,  828,  252,  128,  584,  816,
 /*    60 */   113,  250,   84,  801,  291,   47,   42,   46,   66,   67,
 /*    70 */    28,  671,  672,  673,  190,   86,  799,  802,  156,  856,
 /*    80 */   205,  809,  291,   47,   42,   46,  138,  856,   38,  783,
 /*    90 */   718,   53,   55,   54,   52,   51,   64,   62,   60,   58,
 /*   100 */    57,   56,   50,   49,   48,  529,  530,  492,  728,   53,
 /*   110 */    55,   54,   52,   51,   64,   62,   60,   58,   57,   56,
 /*   120 */    50,   49,   48,   44,  674,   39,  283,  668,  669,  670,
 /*   130 */   254,  208,   12,  498,  270,  210,  199,  198,  197,  266,
 /*   140 */   196,  141,  856,   85,  212,   66,   67,   90,  244,  208,
 /*   150 */   242,  241,  795,  237,   83,  562,  563,  285,  176,  291,
 /*   160 */    47,   42,   46,   41,   67,  260,  261,   76,  156,  856,
 /*   170 */   194,  671,  672,  673,  156,  856,  203,  291,   47,   42,
 /*   180 */    46,  275,  565,  156,  856,  195,   53,   55,   54,   52,
 /*   190 */    51,   64,   62,   60,   58,   57,   56,   50,   49,   48,
 /*   200 */   114,  727,   28,  617,   53,   55,   54,   52,   51,   64,
 /*   210 */    62,   60,   58,   57,   56,   50,   49,   48,  156,  856,
 /*   220 */   278,   72,  276,  208,  674,   90,  283,  668,  669,  670,
 /*   230 */   284,   53,   55,   54,   52,   51,   24,   66,   67,  515,
 /*   240 */   293,  264,   50,   49,   48,  257,  120,  713,  253,   70,
 /*   250 */   217,  291,   47,   42,   46,   90,  228,  175,  173,  671,
 /*   260 */   672,  673,   91,  181,  223,  222,  180,   27,  218,  178,
 /*   270 */    75,  177,  145,  856,  684,  156,  856,  204,   53,   55,
 /*   280 */    54,   52,   51,   64,   62,   60,   58,   57,   56,   50,
 /*   290 */    49,   48,    7,  208,  139,  856,    1,  211,  135,   72,
 /*   300 */   146,  856,  282,  176,  147,  856,  758,   66,   19,   23,
 /*   310 */    22,  208,  674,  613,  283,  668,  669,  670,   90,  144,
 /*   320 */   856,  291,   47,   42,   46,   66,   67,  148,  856,   72,
 /*   330 */   259,  580,  581,   53,  149,  856,   52,   51,   33,  291,
 /*   340 */    47,   42,   46,  775,  176,   49,   48,   28,   53,   55,
 /*   350 */    54,   52,   51,   64,   62,   60,   58,   57,   56,   50,
 /*   360 */    49,   48,  586,  709,  176,   21,   53,   55,   54,   52,
 /*   370 */    51,   64,   62,   60,   58,   57,   56,   50,   49,   48,
 /*   380 */   801,  263,  582,  150,  856,  600,  791,  176,   28,   74,
 /*   390 */   789,  176,   72,  800,  802,  151,  856,  142,  856,  208,
 /*   400 */   182,   90,    9,  712,  176,  711,  176,  824,   28,  152,
 /*   410 */   856,  153,  856,   66,   67,  165,  856,  264,  154,  856,
 /*   420 */   210,  257,  120,  119,  253,   70,  227,  291,   47,   42,
 /*   430 */    46,   28,  228,   37,  173,   28,  274,  176,   91,  181,
 /*   440 */   223,  222,  180,  300,  218,  178,   75,  177,   28,  224,
 /*   450 */    28,  296,  176,  823,   53,   55,   54,   52,   51,   64,
 /*   460 */    62,   60,   58,   57,   56,   50,   49,   48,  299,  176,
 /*   470 */   208,  229,    1,  710,  176,   72,  155,  856,  707,  176,
 /*   480 */   129,   28,  143,  856,   66,   67,  160,  856,  164,  856,
 /*   490 */   221,  208,  166,  856,  301,  769,   28,   94,  291,   47,
 /*   500 */    42,   46,  878,  856,   40,   66,   67,  877,  856,  170,
 /*   510 */   856,  875,  856,   28,  874,  856,  873,  856,   28,  291,
 /*   520 */    47,   42,   46,   28,  179,   53,   55,   54,   52,   51,
 /*   530 */    64,   62,   60,   58,   57,   56,   50,   49,   48,   96,
 /*   540 */   171,  856,  626,  172,  856,  822,   53,   55,   54,   52,
 /*   550 */    51,   64,   62,   60,   58,   57,   56,   50,   49,   48,
 /*   560 */   850,  821,  691,  176,  692,  693,  694,  695,  696,  697,
 /*   570 */   698,  699,  700,  701,  702,  703,  704,  705,  706,  851,
 /*   580 */    98,  691,  176,  692,  693,  694,  695,  696,  697,  698,
 /*   590 */   699,  700,  701,  702,  703,  704,  705,  706,   68,  208,
 /*   600 */   167,  856,  168,  856,  169,  856,  820,   28,  862,  856,
 /*   610 */   163,  856,  100,  861,  856,  162,  856,  860,  856,  161,
 /*   620 */   856,  159,  856,  158,  856,  621,   28,  291,   47,   42,
 /*   630 */    46,  219,  690,  183,  691,  176,  692,  693,  694,  695,
 /*   640 */   696,  697,  698,  699,  700,  701,  702,  703,  704,  705,
 /*   650 */   706,   17,  157,  856,   53,   55,   54,   52,   51,   64,
 /*   660 */    62,   60,   58,   57,   56,   50,   49,   48,   90,   82,
 /*   670 */   784,  294,  769,   32,  271,   65,   77,   36,  232,   28,
 /*   680 */   484,  485,  559,  295,  683,  295,   78,  239,  295,  681,
 /*   690 */   295,  671,  672,  673,  609,  680,  295,  575,  246,  588,
 /*   700 */   295,  557,   18,   65,  116,  236,  106,   69,  549,  295,
 /*   710 */   550,   43,  551,   43,  507,  103,  504,  265,  267,  671,
 /*   720 */   672,  673,  502,  489,  268,    6,  660,  500,  234,  269,
 /*   730 */   290,   61,   63,  511,  295,   69,   11,   11,  105,  288,
 /*   740 */   201,  289,   72,  651,  674,   59,  206,  668,  669,  670,
 /*   750 */   654,   43,  184,    6,  238,  292,  663,   43,  185,   61,
 /*   760 */    63,  249,   65,  662,   43,  186,  187,  653,   43,  298,
 /*   770 */   295,  189,  674,   59,  206,  668,  669,  670,  671,  672,
 /*   780 */   673,  248,  191,  137,   33,  115,  787,  117,  785,  255,
 /*   790 */    65,  260,  256,  124,   69,  193,  122,  121,   10,   38,
 /*   800 */   125,  200,   87,   35,   88,  770,  671,  672,  673,  209,
 /*   810 */   281,   34,    6,  131,  279,  132,  133,  488,   61,   63,
 /*   820 */     3,  134,   69,    2,  302,   81,  618,  616,  213,  615,
 /*   830 */   614,  674,   59,  202,  668,  669,  670,  215,  220,  612,
 /*   840 */     6,  611,  610,  606,  604,  602,   61,   63,   92,   65,
 /*   850 */   598,  226,   93,  595,  102,  230,  231,  104,   20,  674,
 /*   860 */    59,  206,  668,  669,  670,  650,  672,  673,  567,  235,
 /*   870 */    29,  107,  109,  108,  576,  240,  110,   45,  111,  243,
 /*   880 */   583,   69,  245,  188,  247,  112,  118,   31,  174,   73,
 /*   890 */    30,  123,   13,  649,  672,  673,  262,  258,  543,    6,
 /*   900 */   547,   14,  508,  126,  517,   61,   63,  506,  127,   69,
 /*   910 */   272,   15,  505,  503,  501,  499,    8,  289,  674,   59,
 /*   920 */   206,  668,  669,  670,  514,  273,  627,    6,  513,  659,
 /*   930 */    43,   16,  491,   61,   63,  655,  656,  287,  661,    4,
 /*   940 */     5,  658,   25,  130,  541,  297,  674,   59,  206,  668,
 /*   950 */   669,  670,  677,  288,  540,   26,  688,  622,
};
static const YYCODETYPE yy_lookahead[] = {
 /*     0 */   112,  113,  114,  115,  116,  117,  118,  119,  120,  121,
 /*    10 */   122,  123,  124,  125,  126,  127,  128,  129,  130,  147,
 /*    20 */   148,   20,  131,  132,  132,  114,  115,  131,  132,  164,
 /*    30 */   134,  135,  167,  142,  143,  132,   36,   36,   26,   27,
 /*    40 */    28,   29,  131,  132,  133,  157,    2,  159,  132,   49,
 /*    50 */    50,   51,  131,  132,   36,  132,   55,  132,   57,  132,
 /*    60 */    59,   60,  149,  155,   64,   65,   66,   67,   50,   51,
 /*    70 */   159,   52,   53,   54,  166,  150,  168,  169,  131,  132,
 /*    80 */   133,  172,   64,   65,   66,   67,  131,  132,   69,  138,
 /*    90 */   135,   91,   92,   93,   94,   95,   96,   97,   98,   99,
 /*   100 */   100,  101,  102,  103,  104,   10,   11,  107,  160,   91,
 /*   110 */    92,   93,   94,   95,   96,   97,   98,   99,  100,  101,
 /*   120 */   102,  103,  104,   69,  105,   71,  107,  108,  109,  110,
 /*   130 */   107,   36,   88,   78,   79,  107,   81,   82,   83,   84,
 /*   140 */    85,  131,  132,   88,   48,   50,   51,    1,   55,   36,
 /*   150 */    57,   58,   27,   60,  148,   30,   31,  114,  115,   64,
 /*   160 */    65,   66,   67,   50,   51,   19,   20,   48,  131,  132,
 /*   170 */   133,   52,   53,   54,  131,  132,  133,   64,   65,   66,
 /*   180 */    67,   49,   57,  131,  132,  133,   91,   92,   93,   94,
 /*   190 */    95,   96,   97,   98,   99,  100,  101,  102,  103,  104,
 /*   200 */   107,  148,  159,  107,   91,   92,   93,   94,   95,   96,
 /*   210 */    97,   98,   99,  100,  101,  102,  103,  104,  131,  132,
 /*   220 */   133,   75,   90,   36,  105,    1,  107,  108,  109,  110,
 /*   230 */   151,   91,   92,   93,   94,   95,   86,   50,   51,  107,
 /*   240 */   151,   17,  102,  103,  104,   21,   22,  145,   24,   25,
 /*   250 */    44,   64,   65,   66,   67,    1,   32,  107,   34,   52,
 /*   260 */    53,   54,   38,   39,   40,   41,   42,   13,   44,   45,
 /*   270 */    46,   47,  131,  132,   87,  131,  132,  133,   91,   92,
 /*   280 */    93,   94,   95,   96,   97,   98,   99,  100,  101,  102,
 /*   290 */   103,  104,  137,   36,  131,  132,   72,   73,  146,   75,
 /*   300 */   131,  132,  114,  115,  131,  132,  143,   50,   14,   15,
 /*   310 */    16,   36,  105,  107,  107,  108,  109,  110,    1,  131,
 /*   320 */   132,   64,   65,   66,   67,   50,   51,  131,  132,   75,
 /*   330 */   107,   10,   11,   91,  131,  132,   94,   95,    3,   64,
 /*   340 */    65,   66,   67,  114,  115,  103,  104,  159,   91,   92,
 /*   350 */    93,   94,   95,   96,   97,   98,   99,  100,  101,  102,
 /*   360 */   103,  104,   87,  114,  115,   49,   91,   92,   93,   94,
 /*   370 */    95,   96,   97,   98,   99,  100,  101,  102,  103,  104,
 /*   380 */   155,  152,   61,  131,  132,   43,  114,  115,  159,   76,
 /*   390 */   114,  115,   75,  168,  169,  131,  132,  131,  132,   36,
 /*   400 */   163,    1,   86,  114,  115,  114,  115,  163,  159,  131,
 /*   410 */   132,  131,  132,   50,   51,  131,  132,   17,  131,  132,
 /*   420 */   107,   21,   22,   88,   24,   25,  163,   64,   65,   66,
 /*   430 */    67,  159,   32,   70,   34,  159,  114,  115,   38,   39,
 /*   440 */    40,   41,   42,   49,   44,   45,   46,   47,  159,  107,
 /*   450 */   159,  114,  115,  163,   91,   92,   93,   94,   95,   96,
 /*   460 */    97,   98,   99,  100,  101,  102,  103,  104,  114,  115,
 /*   470 */    36,   35,   72,  114,  115,   75,  131,  132,  114,  115,
 /*   480 */    86,  159,  131,  132,   50,   51,  131,  132,  131,  132,
 /*   490 */    43,   36,  131,  132,  161,  162,  159,  173,   64,   65,
 /*   500 */    66,   67,  131,  132,   70,   50,   51,  131,  132,  131,
 /*   510 */   132,  131,  132,  159,  131,  132,  131,  132,  159,   64,
 /*   520 */    65,   66,   67,  159,   77,   91,   92,   93,   94,   95,
 /*   530 */    96,   97,   98,   99,  100,  101,  102,  103,  104,  173,
 /*   540 */   131,  132,   87,  131,  132,  163,   91,   92,   93,   94,
 /*   550 */    95,   96,   97,   98,   99,  100,  101,  102,  103,  104,
 /*   560 */   112,  163,  114,  115,  116,  117,  118,  119,  120,  121,
 /*   570 */   122,  123,  124,  125,  126,  127,  128,  129,  130,  112,
 /*   580 */   173,  114,  115,  116,  117,  118,  119,  120,  121,  122,
 /*   590 */   123,  124,  125,  126,  127,  128,  129,  130,    9,   36,
 /*   600 */   131,  132,  131,  132,  131,  132,  163,  159,  131,  132,
 /*   610 */   131,  132,  173,  131,  132,  131,  132,  131,  132,  131,
 /*   620 */   132,  131,  132,  131,  132,  107,  159,   64,   65,   66,
 /*   630 */    67,   44,  112,  136,  114,  115,  116,  117,  118,  119,
 /*   640 */   120,  121,  122,  123,  124,  125,  126,  127,  128,  129,
 /*   650 */   130,   12,  131,  132,   91,   92,   93,   94,   95,   96,
 /*   660 */    97,   98,   99,  100,  101,  102,  103,  104,    1,  153,
 /*   670 */   154,  161,  162,   62,   63,   36,   86,   88,  163,  159,
 /*   680 */    12,   13,   87,   88,   87,   88,   96,   87,   88,   87,
 /*   690 */    88,   52,   53,   54,  107,   87,   88,   87,   88,   87,
 /*   700 */    88,   87,   88,   36,   26,   35,   28,   68,   87,   88,
 /*   710 */    87,   88,   87,   88,   78,  165,   78,   81,   80,   52,
 /*   720 */    53,   54,   78,   91,   80,   86,   87,   78,  163,   80,
 /*   730 */    91,   92,   93,   87,   88,   68,   86,   86,  165,  107,
 /*   740 */    90,   90,   75,   52,  105,  106,  107,  108,  109,  110,
 /*   750 */    87,   88,  136,   86,  163,   64,   87,   88,  136,   92,
 /*   760 */    93,  163,   36,   87,   88,  136,  136,   87,   88,   87,
 /*   770 */    88,  136,  105,  106,  107,  108,  109,  110,   52,   53,
 /*   780 */    54,  107,  163,  170,    3,  165,  138,  163,  154,  163,
 /*   790 */    36,   19,  152,   86,   68,  136,  163,  156,  156,   69,
 /*   800 */   163,  136,  150,    8,  144,  162,   52,   53,   54,  136,
 /*   810 */     7,    6,   86,  141,    4,  140,  139,   91,   92,   93,
 /*   820 */    89,  138,   68,   74,  158,  171,  107,  107,   49,  107,
 /*   830 */   107,  105,  106,  107,  108,  109,  110,   49,   77,  107,
 /*   840 */    86,  107,  107,   43,   43,   43,   92,   93,   77,   36,
 /*   850 */    87,   33,   26,   37,   86,  107,   62,   27,   49,  105,
 /*   860 */   106,  107,  108,  109,  110,   52,   53,   54,   37,   36,
 /*   870 */    86,   86,   86,   59,   87,   56,   86,   36,   86,   56,
 /*   880 */    52,   68,  108,  108,   86,   86,    2,   96,   56,   23,
 /*   890 */    86,   18,   86,   52,   53,   54,   88,   90,   19,   86,
 /*   900 */   107,   86,   78,   18,  107,   92,   93,   78,   86,   68,
 /*   910 */    49,    5,   78,   78,   78,   78,    5,   90,  105,  106,
 /*   920 */   107,  108,  109,  110,  107,   87,   87,   86,  107,   41,
 /*   930 */    88,   88,  107,   92,   93,   87,   87,   90,   87,   86,
 /*   940 */    86,   41,   86,   88,   87,   49,  105,  106,  107,  108,
 /*   950 */   109,  110,  107,  107,   87,   86,    0,  107,  174,  174,
 /*   960 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*   970 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*   980 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*   990 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*  1000 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*  1010 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*  1020 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*  1030 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*  1040 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*  1050 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
 /*  1060 */   174,  174,  174,  174,  174,  174,  174,  174,  174,  174,
};
#define YY_SHIFT_COUNT    (302)
#define YY_SHIFT_MIN      (0)
#define YY_SHIFT_MAX      (956)
static const unsigned short int yy_shift_ofst[] = {
 /*     0 */   400,  224,  400,  400,  667,  667,  667,  726,  754,   93,
 /*    10 */   146,  639,  726,  754,  754,  754,  754,  754,   93,  254,
 /*    20 */   317,  317,  317,  317,  317,  317,  317,  317,  317,  754,
 /*    30 */   754,  754,  754,  754,  754,  754,  754,  754,  754,  754,
 /*    40 */   754,  754,  754,  754,  754,  813,  841,  754,  754,  754,
 /*    50 */   754,  754,  754,  754,  754,  754,  754,  754,  754,  754,
 /*    60 */   754,  754,  754,  754,  754,  754,  754,  754,  754,   19,
 /*    70 */   125,  150,  313,   23,   28,  119,  207,  207,  207,  207,
 /*    80 */    55,    1,  335,  611,  150,  150,   54,   54,  589,   44,
 /*    90 */   668,  223,  223,  223,  223,  436,  223,  436,  223,  436,
 /*   100 */   223,  436,  518,  223,  670,  223,  670,  518,  223,  518,
 /*   110 */   518,  518,  518,  223,  674,  223,  670,  781,  223,   23,
 /*   120 */   223,  772,  707,  223,  518,  707,  223,  518,  730,  518,
 /*   130 */    28,  795,  803,  805,  810,  781,  731,  958,    0,   95,
 /*   140 */   187,  275,  363,  434,  455,   18,   18,   18,   18,   18,
 /*   150 */    18,   18,   18,   18,  113,   18,   18,   18,  257,  563,
 /*   160 */   563,  140,  140,  140,  140,  140,  140,  140,  140,  140,
 /*   170 */   242,  242,  242,   12,  321,  132,  294,   96,  206,  587,
 /*   180 */   447,  342,  590,  595,  597,  600,  602,  608,  610,  612,
 /*   190 */   614,  316,  678,  621,  623,  625,  636,  638,  644,  649,
 /*   200 */   646,  632,  650,  663,  669,  676,  651,  680,  691,  682,
 /*   210 */   394,  749,  719,  720,  779,  722,  788,  723,  732,  734,
 /*   220 */   735,  761,  800,  801,  802,  763,  771,  818,  826,  816,
 /*   230 */   768,  748,  794,  830,  809,  831,  833,  784,  785,  814,
 /*   240 */   786,  819,  790,  792,  823,  787,  774,  775,  798,  799,
 /*   250 */   804,  828,  832,  884,  791,  866,  808,  873,  793,  807,
 /*   260 */   806,  879,  815,  808,  885,  824,  829,  834,  835,  836,
 /*   270 */   837,  822,  797,  861,  838,  817,  821,  825,  842,  906,
 /*   280 */   843,  911,  839,  827,  888,  848,  849,  845,  847,  846,
 /*   290 */   851,  853,  854,  900,  855,  850,  857,  856,  896,  867,
 /*   300 */   869,  855,  956,
};
#define YY_REDUCE_COUNT (137)
#define YY_REDUCE_MIN   (-135)
#define YY_REDUCE_MAX   (683)
static const short yy_reduce_ofst[] = {
 /*     0 */  -112,  448,  467,  520,  -89,   43,  188, -104, -109,  -92,
 /*    10 */   229,  -53,  -45,   37,   52,   87,  163,  144,  225,  249,
 /*    20 */   272,  276,  289,  291,  322,  337,  354,  359,  364,  -79,
 /*    30 */    10,  141,  169,  173,  196,  203,  252,  264,  266,  278,
 /*    40 */   280,  284,  287,  345,  351,  355,  357,  361,  371,  376,
 /*    50 */   378,  380,  383,  385,  409,  412,  469,  471,  473,  477,
 /*    60 */   479,  482,  484,  486,  488,  355,  490,  492,  521,  -75,
 /*    70 */  -135, -128,  333,  516,  510, -108,  -97,  -84,  -77,  -73,
 /*    80 */   -87,  -91,  -49,  -52,    6,   53,   79,   89,  102,  152,
 /*    90 */   155,  237,  244,  263,  290,  324,  382,  366,  398,  407,
 /*   100 */   443,  439,  497,  515,  550,  565,  573,  616,  591,  622,
 /*   110 */   629,  630,  635,  598,  613,  619,  620,  648,  624,  634,
 /*   120 */   626,  640,  641,  633,  659,  642,  637,  665,  652,  673,
 /*   130 */   643,  660,  672,  675,  677,  683,  666,  654,
};
static const YYACTIONTYPE yy_default[] = {
 /*     0 */   685,  685,  685,  910,  685,  685,  685,  685,  685,  685,
 /*    10 */   685,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*    20 */   685,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*    30 */   685,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*    40 */   685,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*    50 */   685,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*    60 */   685,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*    70 */   792,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*    80 */   725,  803,  749,  740,  685,  685,  897,  897,  764,  724,
 /*    90 */   714,  685,  685,  685,  685,  825,  685,  825,  685,  825,
 /*   100 */   685,  825,  685,  685,  797,  685,  797,  685,  685,  685,
 /*   110 */   685,  685,  685,  685,  804,  685,  797,  749,  685,  685,
 /*   120 */   685,  685,  779,  685,  685,  779,  685,  685,  685,  685,
 /*   130 */   685,  762,  755,  753,  751,  749,  909,  808,  721,  759,
 /*   140 */   685,  685,  685,  685,  685,  786,  741,  750,  754,  763,
 /*   150 */   766,  895,  898,  896,  685,  855,  854,  765,  863,  864,
 /*   160 */   859,  865,  866,  867,  883,  888,  879,  870,  869,  868,
 /*   170 */   876,  872,  871,  685,  810,  743,  708,  685,  685,  685,
 /*   180 */   838,  830,  827,  685,  685,  685,  685,  685,  685,  685,
 /*   190 */   685,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*   200 */   685,  685,  906,  685,  685,  685,  906,  685,  685,  685,
 /*   210 */   685,  685,  685,  685,  685,  685,  685,  685,  685,  685,
 /*   220 */   685,  839,  836,  834,  832,  685,  685,  685,  685,  685,
 /*   230 */   685,  685,  685,  685,  685,  685,  685,  685,  913,  685,
 /*   240 */   685,  685,  685,  685,  685,  685,  685,  685,  805,  818,
 /*   250 */   685,  685,  685,  685,  685,  685,  776,  685,  685,  777,
 /*   260 */   685,  685,  685,  773,  685,  685,  685,  685,  685,  685,
 /*   270 */   685,  685,  685,  747,  685,  685,  685,  685,  752,  685,
 /*   280 */   756,  685,  685,  906,  685,  685,  685,  685,  907,  685,
 /*   290 */   685,  685,  685,  685,  768,  685,  685,  685,  685,  685,
 /*   300 */   685,  767,  685,
};
/********** End of lemon-generated parsing tables *****************************/

/* The next table maps tokens (terminal symbols) into fallback tokens.  
** If a construct like the following:
** 
**      %fallback ID X Y Z.
**
** appears in the grammar, then ID becomes a fallback token for X, Y,
** and Z.  Whenever one of the tokens X, Y, or Z is input to the parser
** but it does not parse, the type of the token is changed to ID and
** the parse is retried before an error is thrown.
**
** This feature can be used, for example, to cause some keywords in a language
** to revert to identifiers if they keyword does not apply in the context where
** it appears.
*/
#ifdef YYFALLBACK
static const YYCODETYPE yyFallback[] = {
};
#endif /* YYFALLBACK */

/* The following structure represents a single element of the
** parser's stack.  Information stored includes:
**
**   +  The state number for the parser at this level of the stack.
**
**   +  The value of the token stored at this level of the stack.
**      (In other words, the "major" token.)
**
**   +  The semantic value stored at this level of the stack.  This is
**      the information used by the action routines in the grammar.
**      It is sometimes called the "minor" token.
**
** After the "shift" half of a SHIFTREDUCE action, the stateno field
** actually contains the reduce action for the second half of the
** SHIFTREDUCE.
*/
struct yyStackEntry {
  YYACTIONTYPE stateno;  /* The state-number, or reduce action in SHIFTREDUCE */
  YYCODETYPE major;      /* The major token value.  This is the code
                         ** number for the token at this stack level */
  YYMINORTYPE minor;     /* The user-supplied minor token value.  This
                         ** is the value of the token  */
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc;  /* Source location for this stack entry.
                         ** Only present when %locations is active. */
#endif
};
typedef struct yyStackEntry yyStackEntry;

/* The state of the parser is completely contained in an instance of
** the following structure */
struct yyParser {
  yyStackEntry *yytos;          /* Pointer to top element of the stack */
#ifdef YYTRACKMAXSTACKDEPTH
  int yyhwm;                    /* High-water mark of the stack */
#endif
#ifndef YYNOERRORRECOVERY
  int yyerrcnt;                 /* Shifts left before out of the error */
#endif
#ifdef YYLOCATIONTYPE
  /* Source location of the token currently being processed.  Updated
  ** at each SqlParseLoc() entry so that %syntax_error sees the location
  ** of the offending lookahead, not the location of the stack-top
  ** symbol (which would be the previously-shifted token, off by one). */
  YYLOCATIONTYPE yyLookaheadLoc;
#endif
  /* Action-time lookahead access (P0-NEW-5).  While SqlParse() runs a
  ** sequence of reduces before shifting the just-pushed token, the
  ** token's external code and value are stashed here so that user
  ** action code can read them via SqlParse_get_lookahead() and
  ** optionally signal consumption via SqlParse_clear_lookahead().  This
  ** is the equivalent of bison's yychar/yyclearin and exists to
  ** support grammars (notably PostgreSQL's plpgsql pl_gram.y) where
  ** an empty production needs to consume the lookahead and reach
  ** into the lexer for further tokens.
  **
  ** yyLookaheadMajor is YYEMPTY (-2) when no SqlParse() call is in
  ** progress.  yyLookaheadCleared is set by SqlParse_clear_lookahead()
  ** to tell the dispatch loop to skip the trailing shift and return,
  ** treating the lookahead as consumed by the action body. */
  int yyLookaheadMajor;
  SqlParseTOKENTYPE yyLookaheadMinor;
  char yyLookaheadCleared;
  SqlParseARG_SDECL                /* A place to hold %extra_argument */
  SqlParseCTX_SDECL                /* A place to hold %extra_context */
  yyStackEntry *yystackEnd;           /* Last entry in the stack */
  yyStackEntry *yystack;              /* The parser stack */
  yyStackEntry yystk0[YYSTACKDEPTH];  /* Initial stack space */
};
typedef struct yyParser yyParser;

#include <assert.h>
#include <stdlib.h>
#include <string.h>
#ifndef NDEBUG
#include <stdio.h>
static FILE *yyTraceFILE = 0;
static char *yyTracePrompt = 0;
#endif /* NDEBUG */

#ifndef NDEBUG
/* 
** Turn parser tracing on by giving a stream to which to write the trace
** and a prompt to preface each trace message.  Tracing is turned off
** by making either argument NULL 
**
** Inputs:
** <ul>
** <li> A FILE* to which trace output should be written.
**      If NULL, then tracing is turned off.
** <li> A prefix string written at the beginning of every
**      line of trace output.  If NULL, then tracing is
**      turned off.
** </ul>
**
** Outputs:
** None.
*/
void SqlParseTrace(FILE*,char*);
void SqlParseTrace(FILE *TraceFILE, char *zTracePrompt){
  yyTraceFILE = TraceFILE;
  yyTracePrompt = zTracePrompt;
  if( yyTraceFILE==0 ) yyTracePrompt = 0;
  else if( yyTracePrompt==0 ) yyTraceFILE = 0;
}
#endif /* NDEBUG */

/* Terminal and nonterminal names.  Always emitted (required by
** SqlParseTokenName, SqlParseExpectedTokens, SqlParseExpectedTokensString,
** and optionally by trace/coverage code). */
static const char *const yyTokenName[] = { 
  /*    0 */ "$",
  /*    1 */ "TK_SELECT",
  /*    2 */ "TK_FROM",
  /*    3 */ "TK_WHERE",
  /*    4 */ "TK_GROUP",
  /*    5 */ "TK_BY",
  /*    6 */ "TK_HAVING",
  /*    7 */ "TK_ORDER",
  /*    8 */ "TK_LIMIT",
  /*    9 */ "TK_OFFSET",
  /*   10 */ "TK_ASC",
  /*   11 */ "TK_DESC",
  /*   12 */ "TK_DISTINCT",
  /*   13 */ "TK_ALL",
  /*   14 */ "TK_UNION",
  /*   15 */ "TK_INTERSECT",
  /*   16 */ "TK_EXCEPT",
  /*   17 */ "TK_INSERT",
  /*   18 */ "TK_INTO",
  /*   19 */ "TK_VALUES",
  /*   20 */ "TK_DEFAULT",
  /*   21 */ "TK_REPLACE",
  /*   22 */ "TK_UPDATE",
  /*   23 */ "TK_SET",
  /*   24 */ "TK_DELETE",
  /*   25 */ "TK_CREATE",
  /*   26 */ "TK_TABLE",
  /*   27 */ "TK_INDEX",
  /*   28 */ "TK_VIEW",
  /*   29 */ "TK_TRIGGER",
  /*   30 */ "TK_TEMP",
  /*   31 */ "TK_TEMPORARY",
  /*   32 */ "TK_ALTER",
  /*   33 */ "TK_RENAME",
  /*   34 */ "TK_DROP",
  /*   35 */ "TK_IF",
  /*   36 */ "TK_NOT",
  /*   37 */ "TK_EXISTS",
  /*   38 */ "TK_PRAGMA",
  /*   39 */ "TK_BEGIN",
  /*   40 */ "TK_COMMIT",
  /*   41 */ "TK_END",
  /*   42 */ "TK_ROLLBACK",
  /*   43 */ "TK_TRANSACTION",
  /*   44 */ "TK_SAVEPOINT",
  /*   45 */ "TK_RELEASE",
  /*   46 */ "TK_ATTACH",
  /*   47 */ "TK_DETACH",
  /*   48 */ "TK_DATABASE",
  /*   49 */ "TK_AS",
  /*   50 */ "TK_AND",
  /*   51 */ "TK_OR",
  /*   52 */ "TK_NULL",
  /*   53 */ "TK_TRUE",
  /*   54 */ "TK_FALSE",
  /*   55 */ "TK_PRIMARY",
  /*   56 */ "TK_KEY",
  /*   57 */ "TK_UNIQUE",
  /*   58 */ "TK_FOREIGN",
  /*   59 */ "TK_REFERENCES",
  /*   60 */ "TK_CHECK",
  /*   61 */ "TK_AUTOINCR",
  /*   62 */ "TK_ON",
  /*   63 */ "TK_USING",
  /*   64 */ "TK_IN",
  /*   65 */ "TK_LIKE",
  /*   66 */ "TK_BETWEEN",
  /*   67 */ "TK_IS",
  /*   68 */ "TK_CASE",
  /*   69 */ "TK_WHEN",
  /*   70 */ "TK_THEN",
  /*   71 */ "TK_ELSE",
  /*   72 */ "TK_EXPLAIN",
  /*   73 */ "TK_QUERY",
  /*   74 */ "TK_PLAN",
  /*   75 */ "TK_WITH",
  /*   76 */ "TK_RECURSIVE",
  /*   77 */ "TK_TO",
  /*   78 */ "TK_JOIN",
  /*   79 */ "TK_INNER",
  /*   80 */ "TK_OUTER",
  /*   81 */ "TK_LEFT",
  /*   82 */ "TK_RIGHT",
  /*   83 */ "TK_FULL",
  /*   84 */ "TK_CROSS",
  /*   85 */ "TK_NATURAL",
  /*   86 */ "TK_LP",
  /*   87 */ "TK_RP",
  /*   88 */ "TK_COMMA",
  /*   89 */ "TK_SEMI",
  /*   90 */ "TK_DOT",
  /*   91 */ "TK_STAR",
  /*   92 */ "TK_PLUS",
  /*   93 */ "TK_MINUS",
  /*   94 */ "TK_SLASH",
  /*   95 */ "TK_PERCENT",
  /*   96 */ "TK_EQ",
  /*   97 */ "TK_NE",
  /*   98 */ "TK_LT",
  /*   99 */ "TK_LE",
  /*  100 */ "TK_GT",
  /*  101 */ "TK_GE",
  /*  102 */ "TK_CONCAT",
  /*  103 */ "TK_AMP",
  /*  104 */ "TK_PIPE",
  /*  105 */ "TK_QMARK",
  /*  106 */ "TK_TILDE",
  /*  107 */ "TK_ID",
  /*  108 */ "TK_NUMBER",
  /*  109 */ "TK_STRING",
  /*  110 */ "TK_BLOB",
  /*  111 */ "TK_UMINUS",
  /*  112 */ "stmt",
  /*  113 */ "stmt_list",
  /*  114 */ "select_stmt",
  /*  115 */ "select_core",
  /*  116 */ "insert_stmt",
  /*  117 */ "update_stmt",
  /*  118 */ "delete_stmt",
  /*  119 */ "create_stmt",
  /*  120 */ "drop_stmt",
  /*  121 */ "alter_stmt",
  /*  122 */ "pragma_stmt",
  /*  123 */ "begin_stmt",
  /*  124 */ "commit_stmt",
  /*  125 */ "rollback_stmt",
  /*  126 */ "savepoint_stmt",
  /*  127 */ "release_stmt",
  /*  128 */ "attach_stmt",
  /*  129 */ "detach_stmt",
  /*  130 */ "explain_stmt",
  /*  131 */ "expr",
  /*  132 */ "expr_atom",
  /*  133 */ "expr_list",
  /*  134 */ "sel_list",
  /*  135 */ "sel_item",
  /*  136 */ "id_list",
  /*  137 */ "distinct_opt",
  /*  138 */ "where_opt",
  /*  139 */ "group_opt",
  /*  140 */ "having_opt",
  /*  141 */ "order_opt",
  /*  142 */ "order_list",
  /*  143 */ "order_item",
  /*  144 */ "limit_opt",
  /*  145 */ "offset_part",
  /*  146 */ "from_opt",
  /*  147 */ "table_list",
  /*  148 */ "table_ref",
  /*  149 */ "join_op",
  /*  150 */ "case_arms",
  /*  151 */ "case_else",
  /*  152 */ "values_list",
  /*  153 */ "assign_list",
  /*  154 */ "assign",
  /*  155 */ "coldef",
  /*  156 */ "insert_cols",
  /*  157 */ "input",
  /*  158 */ "maybe_semi",
  /*  159 */ "with_clause",
  /*  160 */ "join_constraint",
  /*  161 */ "cte_list",
  /*  162 */ "cte",
  /*  163 */ "qname",
  /*  164 */ "temp_opt",
  /*  165 */ "if_not_exists",
  /*  166 */ "table_body",
  /*  167 */ "unique_opt",
  /*  168 */ "table_elem",
  /*  169 */ "table_constraint",
  /*  170 */ "type_opt",
  /*  171 */ "col_constraints",
  /*  172 */ "col_constraint",
  /*  173 */ "if_exists",
};

#ifndef NDEBUG
/* For tracing reduce actions, the names of all rules are required.
*/
static const char *const yyRuleName[] = {
 /*   0 */ "input ::= stmt_list maybe_semi",
 /*   1 */ "stmt_list ::= stmt",
 /*   2 */ "stmt_list ::= stmt_list TK_SEMI stmt",
 /*   3 */ "stmt ::= select_stmt",
 /*   4 */ "stmt ::= insert_stmt",
 /*   5 */ "stmt ::= update_stmt",
 /*   6 */ "stmt ::= delete_stmt",
 /*   7 */ "stmt ::= create_stmt",
 /*   8 */ "stmt ::= drop_stmt",
 /*   9 */ "stmt ::= alter_stmt",
 /*  10 */ "stmt ::= pragma_stmt",
 /*  11 */ "stmt ::= begin_stmt",
 /*  12 */ "stmt ::= commit_stmt",
 /*  13 */ "stmt ::= rollback_stmt",
 /*  14 */ "stmt ::= savepoint_stmt",
 /*  15 */ "stmt ::= release_stmt",
 /*  16 */ "stmt ::= attach_stmt",
 /*  17 */ "stmt ::= detach_stmt",
 /*  18 */ "stmt ::= explain_stmt",
 /*  19 */ "select_stmt ::= with_clause select_stmt",
 /*  20 */ "select_stmt ::= select_core",
 /*  21 */ "select_stmt ::= select_core TK_UNION select_stmt",
 /*  22 */ "select_stmt ::= select_core TK_UNION TK_ALL select_stmt",
 /*  23 */ "select_stmt ::= select_core TK_INTERSECT select_stmt",
 /*  24 */ "select_stmt ::= select_core TK_EXCEPT select_stmt",
 /*  25 */ "select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part",
 /*  26 */ "distinct_opt ::=",
 /*  27 */ "distinct_opt ::= TK_DISTINCT",
 /*  28 */ "distinct_opt ::= TK_ALL",
 /*  29 */ "sel_list ::= sel_item",
 /*  30 */ "sel_list ::= sel_list TK_COMMA sel_item",
 /*  31 */ "sel_item ::= TK_STAR",
 /*  32 */ "sel_item ::= TK_ID TK_DOT TK_STAR",
 /*  33 */ "sel_item ::= expr",
 /*  34 */ "sel_item ::= expr TK_AS TK_ID",
 /*  35 */ "sel_item ::= expr TK_ID",
 /*  36 */ "from_opt ::=",
 /*  37 */ "from_opt ::= TK_FROM table_list",
 /*  38 */ "table_list ::= table_ref",
 /*  39 */ "table_list ::= table_list TK_COMMA table_ref",
 /*  40 */ "table_list ::= table_list join_op table_ref join_constraint",
 /*  41 */ "join_op ::= TK_JOIN",
 /*  42 */ "join_op ::= TK_INNER TK_JOIN",
 /*  43 */ "join_op ::= TK_LEFT TK_JOIN",
 /*  44 */ "join_op ::= TK_LEFT TK_OUTER TK_JOIN",
 /*  45 */ "join_op ::= TK_RIGHT TK_JOIN",
 /*  46 */ "join_op ::= TK_RIGHT TK_OUTER TK_JOIN",
 /*  47 */ "join_op ::= TK_FULL TK_JOIN",
 /*  48 */ "join_op ::= TK_FULL TK_OUTER TK_JOIN",
 /*  49 */ "join_op ::= TK_CROSS TK_JOIN",
 /*  50 */ "join_op ::= TK_NATURAL TK_JOIN",
 /*  51 */ "join_op ::= TK_NATURAL TK_LEFT TK_JOIN",
 /*  52 */ "join_constraint ::=",
 /*  53 */ "join_constraint ::= TK_ON expr",
 /*  54 */ "join_constraint ::= TK_USING TK_LP id_list TK_RP",
 /*  55 */ "table_ref ::= TK_ID",
 /*  56 */ "table_ref ::= TK_ID TK_DOT TK_ID",
 /*  57 */ "table_ref ::= TK_ID TK_AS TK_ID",
 /*  58 */ "table_ref ::= TK_ID TK_ID",
 /*  59 */ "table_ref ::= TK_LP select_stmt TK_RP",
 /*  60 */ "table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID",
 /*  61 */ "where_opt ::=",
 /*  62 */ "where_opt ::= TK_WHERE expr",
 /*  63 */ "group_opt ::=",
 /*  64 */ "group_opt ::= TK_GROUP TK_BY expr_list",
 /*  65 */ "having_opt ::=",
 /*  66 */ "having_opt ::= TK_HAVING expr",
 /*  67 */ "order_opt ::=",
 /*  68 */ "order_opt ::= TK_ORDER TK_BY order_list",
 /*  69 */ "order_list ::= order_item",
 /*  70 */ "order_list ::= order_list TK_COMMA order_item",
 /*  71 */ "order_item ::= expr",
 /*  72 */ "order_item ::= expr TK_ASC",
 /*  73 */ "order_item ::= expr TK_DESC",
 /*  74 */ "limit_opt ::=",
 /*  75 */ "limit_opt ::= TK_LIMIT expr",
 /*  76 */ "offset_part ::=",
 /*  77 */ "offset_part ::= TK_OFFSET expr",
 /*  78 */ "offset_part ::= TK_COMMA expr",
 /*  79 */ "with_clause ::= TK_WITH cte_list",
 /*  80 */ "with_clause ::= TK_WITH TK_RECURSIVE cte_list",
 /*  81 */ "cte_list ::= cte",
 /*  82 */ "cte_list ::= cte_list TK_COMMA cte",
 /*  83 */ "cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP",
 /*  84 */ "cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP",
 /*  85 */ "insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list",
 /*  86 */ "insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES",
 /*  87 */ "insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt",
 /*  88 */ "insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list",
 /*  89 */ "qname ::= TK_ID",
 /*  90 */ "qname ::= TK_ID TK_DOT TK_ID",
 /*  91 */ "insert_cols ::=",
 /*  92 */ "insert_cols ::= TK_LP id_list TK_RP",
 /*  93 */ "values_list ::= TK_VALUES TK_LP expr_list TK_RP",
 /*  94 */ "values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP",
 /*  95 */ "update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt",
 /*  96 */ "assign_list ::= assign",
 /*  97 */ "assign_list ::= assign_list TK_COMMA assign",
 /*  98 */ "assign ::= TK_ID TK_EQ expr",
 /*  99 */ "delete_stmt ::= TK_DELETE TK_FROM qname where_opt",
 /* 100 */ "create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP",
 /* 101 */ "create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt",
 /* 102 */ "create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP",
 /* 103 */ "create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt",
 /* 104 */ "temp_opt ::=",
 /* 105 */ "temp_opt ::= TK_TEMP",
 /* 106 */ "temp_opt ::= TK_TEMPORARY",
 /* 107 */ "unique_opt ::=",
 /* 108 */ "unique_opt ::= TK_UNIQUE",
 /* 109 */ "if_not_exists ::=",
 /* 110 */ "if_not_exists ::= TK_IF TK_NOT TK_EXISTS",
 /* 111 */ "table_body ::= table_elem",
 /* 112 */ "table_body ::= table_body TK_COMMA table_elem",
 /* 113 */ "table_elem ::= coldef",
 /* 114 */ "table_elem ::= table_constraint",
 /* 115 */ "coldef ::= TK_ID type_opt col_constraints",
 /* 116 */ "type_opt ::=",
 /* 117 */ "type_opt ::= TK_ID",
 /* 118 */ "type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP",
 /* 119 */ "type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP",
 /* 120 */ "col_constraints ::=",
 /* 121 */ "col_constraints ::= col_constraints col_constraint",
 /* 122 */ "col_constraint ::= TK_PRIMARY TK_KEY",
 /* 123 */ "col_constraint ::= TK_PRIMARY TK_KEY TK_ASC",
 /* 124 */ "col_constraint ::= TK_PRIMARY TK_KEY TK_DESC",
 /* 125 */ "col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR",
 /* 126 */ "col_constraint ::= TK_NOT TK_NULL",
 /* 127 */ "col_constraint ::= TK_UNIQUE",
 /* 128 */ "col_constraint ::= TK_DEFAULT expr_atom",
 /* 129 */ "col_constraint ::= TK_CHECK TK_LP expr TK_RP",
 /* 130 */ "col_constraint ::= TK_REFERENCES qname",
 /* 131 */ "col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP",
 /* 132 */ "drop_stmt ::= TK_DROP TK_TABLE if_exists qname",
 /* 133 */ "drop_stmt ::= TK_DROP TK_INDEX if_exists qname",
 /* 134 */ "drop_stmt ::= TK_DROP TK_VIEW if_exists qname",
 /* 135 */ "drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname",
 /* 136 */ "alter_stmt ::= TK_ALTER TK_TABLE qname TK_RENAME TK_TO qname",
 /* 137 */ "if_exists ::=",
 /* 138 */ "if_exists ::= TK_IF TK_EXISTS",
 /* 139 */ "pragma_stmt ::= TK_PRAGMA qname",
 /* 140 */ "pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom",
 /* 141 */ "pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP",
 /* 142 */ "begin_stmt ::= TK_BEGIN",
 /* 143 */ "begin_stmt ::= TK_BEGIN TK_TRANSACTION",
 /* 144 */ "begin_stmt ::= TK_BEGIN TK_ID",
 /* 145 */ "begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION",
 /* 146 */ "commit_stmt ::= TK_COMMIT",
 /* 147 */ "commit_stmt ::= TK_COMMIT TK_TRANSACTION",
 /* 148 */ "commit_stmt ::= TK_END",
 /* 149 */ "commit_stmt ::= TK_END TK_TRANSACTION",
 /* 150 */ "rollback_stmt ::= TK_ROLLBACK",
 /* 151 */ "rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION",
 /* 152 */ "rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID",
 /* 153 */ "rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID",
 /* 154 */ "rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID",
 /* 155 */ "savepoint_stmt ::= TK_SAVEPOINT TK_ID",
 /* 156 */ "release_stmt ::= TK_RELEASE TK_ID",
 /* 157 */ "release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID",
 /* 158 */ "attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID",
 /* 159 */ "attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID",
 /* 160 */ "detach_stmt ::= TK_DETACH TK_ID",
 /* 161 */ "detach_stmt ::= TK_DETACH TK_DATABASE TK_ID",
 /* 162 */ "explain_stmt ::= TK_EXPLAIN stmt",
 /* 163 */ "explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt",
 /* 164 */ "id_list ::= TK_ID",
 /* 165 */ "id_list ::= id_list TK_COMMA TK_ID",
 /* 166 */ "expr_list ::= expr",
 /* 167 */ "expr_list ::= expr_list TK_COMMA expr",
 /* 168 */ "expr ::= expr_atom",
 /* 169 */ "expr ::= TK_LP expr TK_RP",
 /* 170 */ "expr ::= TK_LP select_stmt TK_RP",
 /* 171 */ "expr ::= TK_NOT expr",
 /* 172 */ "expr ::= TK_MINUS expr",
 /* 173 */ "expr ::= TK_PLUS expr",
 /* 174 */ "expr ::= TK_TILDE expr",
 /* 175 */ "expr ::= expr TK_OR expr",
 /* 176 */ "expr ::= expr TK_AND expr",
 /* 177 */ "expr ::= expr TK_EQ expr",
 /* 178 */ "expr ::= expr TK_NE expr",
 /* 179 */ "expr ::= expr TK_LT expr",
 /* 180 */ "expr ::= expr TK_LE expr",
 /* 181 */ "expr ::= expr TK_GT expr",
 /* 182 */ "expr ::= expr TK_GE expr",
 /* 183 */ "expr ::= expr TK_PLUS expr",
 /* 184 */ "expr ::= expr TK_MINUS expr",
 /* 185 */ "expr ::= expr TK_STAR expr",
 /* 186 */ "expr ::= expr TK_SLASH expr",
 /* 187 */ "expr ::= expr TK_PERCENT expr",
 /* 188 */ "expr ::= expr TK_CONCAT expr",
 /* 189 */ "expr ::= expr TK_AMP expr",
 /* 190 */ "expr ::= expr TK_PIPE expr",
 /* 191 */ "expr ::= expr TK_LIKE expr",
 /* 192 */ "expr ::= expr TK_IS TK_NULL",
 /* 193 */ "expr ::= expr TK_IS TK_NOT TK_NULL",
 /* 194 */ "expr ::= expr TK_NOT TK_NULL",
 /* 195 */ "expr ::= expr TK_IS expr",
 /* 196 */ "expr ::= expr TK_IN TK_LP expr_list TK_RP",
 /* 197 */ "expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP",
 /* 198 */ "expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP",
 /* 199 */ "expr ::= expr TK_IN TK_LP select_stmt TK_RP",
 /* 200 */ "expr ::= expr TK_BETWEEN expr TK_AND expr",
 /* 201 */ "expr ::= TK_CASE expr_atom case_arms case_else TK_END",
 /* 202 */ "expr ::= TK_CASE case_arms case_else TK_END",
 /* 203 */ "expr ::= TK_ID TK_LP TK_RP",
 /* 204 */ "expr ::= TK_ID TK_LP TK_STAR TK_RP",
 /* 205 */ "expr ::= TK_ID TK_LP expr_list TK_RP",
 /* 206 */ "expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP",
 /* 207 */ "case_arms ::= TK_WHEN expr TK_THEN expr",
 /* 208 */ "case_arms ::= case_arms TK_WHEN expr TK_THEN expr",
 /* 209 */ "case_else ::=",
 /* 210 */ "case_else ::= TK_ELSE expr",
 /* 211 */ "expr_atom ::= TK_NUMBER",
 /* 212 */ "expr_atom ::= TK_STRING",
 /* 213 */ "expr_atom ::= TK_BLOB",
 /* 214 */ "expr_atom ::= TK_NULL",
 /* 215 */ "expr_atom ::= TK_TRUE",
 /* 216 */ "expr_atom ::= TK_FALSE",
 /* 217 */ "expr_atom ::= TK_QMARK",
 /* 218 */ "expr_atom ::= TK_ID",
 /* 219 */ "expr_atom ::= TK_ID TK_DOT TK_ID",
 /* 220 */ "expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID",
 /* 221 */ "maybe_semi ::=",
 /* 222 */ "maybe_semi ::= TK_SEMI",
 /* 223 */ "table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP",
 /* 224 */ "table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP",
 /* 225 */ "table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname",
 /* 226 */ "table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP",
 /* 227 */ "table_constraint ::= TK_CHECK TK_LP expr TK_RP",
};
#endif /* NDEBUG */


#if YYGROWABLESTACK
/*
** Try to increase the size of the parser stack.  Return the number
** of errors.  Return 0 on success.
*/
static int yyGrowStack(yyParser *p){
  int oldSize = 1 + (int)(p->yystackEnd - p->yystack);
  int newSize;
  int idx;
  yyStackEntry *pNew;
#ifdef YYSIZELIMIT
  int nLimit = YYSIZELIMIT(SqlParseCTX(p));
#endif

  newSize = oldSize*2 + 100;
#ifdef YYSIZELIMIT
  if( newSize>nLimit ){
    newSize = nLimit;
    if( newSize<=oldSize ) return 1;
  }
#endif
  idx = (int)(p->yytos - p->yystack);
  if( p->yystack==p->yystk0 ){
    pNew = YYREALLOC(0, newSize*sizeof(pNew[0]), SqlParseCTX(p));
    if( pNew==0 ) return 1;
    memcpy(pNew, p->yystack, oldSize*sizeof(pNew[0]));
  }else{
    pNew = YYREALLOC(p->yystack, newSize*sizeof(pNew[0]), SqlParseCTX(p));
    if( pNew==0 ) return 1;
  }
  p->yystack = pNew;
  p->yytos = &p->yystack[idx];
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE,"%sStack grows from %d to %d entries.\n",
            yyTracePrompt, oldSize, newSize);
  }
#endif
  p->yystackEnd = &p->yystack[newSize-1];
  return 0;
}
#endif /* YYGROWABLESTACK */

#if !YYGROWABLESTACK
/* For builds that do no have a growable stack, yyGrowStack always
** returns an error.
*/
# define yyGrowStack(X) 1
#endif

/* Datatype of the argument to the memory allocated passed as the
** second argument to SqlParseAlloc() below.  This can be changed by
** putting an appropriate #define in the %include section of the input
** grammar.
*/
#ifndef YYMALLOCARGTYPE
# define YYMALLOCARGTYPE size_t
#endif

/* Initialize a new parser that has already been allocated.
*/
void SqlParseInit(void* SqlParseCTX_PDECL);
void SqlParseInit(void *yypRawParser SqlParseCTX_PDECL){
  yyParser *yypParser = (yyParser*)yypRawParser;
  SqlParseCTX_STORE
#ifdef YYTRACKMAXSTACKDEPTH
  yypParser->yyhwm = 0;
#endif
  yypParser->yystack = yypParser->yystk0;
  yypParser->yystackEnd = &yypParser->yystack[YYSTACKDEPTH-1];
#ifndef YYNOERRORRECOVERY
  yypParser->yyerrcnt = -1;
#endif
#ifdef YYLOCATIONTYPE
  /* Zero-initialise the lookahead-location stash so %syntax_error
  ** sees a defined value if it fires before the first SqlParseLoc() call,
  ** or in pull-parser mode (SqlParse() without SqlParseLoc()). */
  memset(&yypParser->yyLookaheadLoc, 0, sizeof(yypParser->yyLookaheadLoc));
  /* P0-NEW-6: zero-init the sentinel slot's yyloc.  When yy_reduce
  ** invokes a user-defined YYLLOC_DEFAULT it reads yymsp[-1].yyloc
  ** (the location of the slot below the rule, Bison's Rhs[0]).  For
  ** the very first reduce -- particularly an empty rule -- yymsp[-1]
  ** is yystack[0], the parser's stack sentinel.  Without this init,
  ** that read returns whatever was in heap memory at allocation. */
  memset(&yypParser->yystack[0].yyloc, 0, sizeof(yypParser->yystack[0].yyloc));
#endif
  /* Initialise action-time lookahead state (P0-NEW-5).  YYEMPTY is
  ** -2 by convention (matching bison's yychar empty value); use it
  ** as a sentinel for "no SqlParse() call in progress". */
  yypParser->yyLookaheadMajor = -2;
  memset(&yypParser->yyLookaheadMinor, 0, sizeof(yypParser->yyLookaheadMinor));
  yypParser->yyLookaheadCleared = 0;
  yypParser->yytos = yypParser->yystack;
  yypParser->yystack[0].stateno = 0;
  yypParser->yystack[0].major = 0;
}

#ifndef SqlParse_ENGINEALWAYSONSTACK
/* 
** This function allocates a new parser.
** The only argument is a pointer to a function which works like
** malloc.
**
** Inputs:
** A pointer to the function used to allocate memory.
**
** Outputs:
** A pointer to a parser.  This pointer is used in subsequent calls
** to SqlParse and SqlParseFree.
*/
void *SqlParseAlloc(void *(*)(YYMALLOCARGTYPE) SqlParseCTX_PDECL);
void *SqlParseAlloc(void *(*mallocProc)(YYMALLOCARGTYPE) SqlParseCTX_PDECL){
  yyParser *yypParser;
  yypParser = (yyParser*)(*mallocProc)( (YYMALLOCARGTYPE)sizeof(yyParser) );
  if( yypParser ){
    SqlParseCTX_STORE
    SqlParseInit(yypParser SqlParseCTX_PARAM);
  }
  return (void*)yypParser;
}
#endif /* SqlParse_ENGINEALWAYSONSTACK */


/* The following function deletes the "minor type" or semantic value
** associated with a symbol.  The symbol can be either a terminal
** or nonterminal. "yymajor" is the symbol code, and "yypminor" is
** a pointer to the value to be deleted.  The code used to do the 
** deletions is derived from the %destructor and/or %token_destructor
** directives of the input grammar.
*/
static void yy_destructor(
  yyParser *yypParser,    /* The parser */
  YYCODETYPE yymajor,     /* Type code for object to destroy */
  YYMINORTYPE *yypminor   /* The object to be destroyed */
){
  SqlParseARG_FETCH
  SqlParseCTX_FETCH
  /* Suppress unused-variable warnings when no %destructor is defined */
  SqlParseARG_STORE
  SqlParseCTX_STORE
  (void)yypminor;
  switch( yymajor ){
    /* Here is inserted the actions which take place when a
    ** terminal or non-terminal is destroyed.  This can happen
    ** when the symbol is popped from the stack during a
    ** reduce or during error processing or when a parser is 
    ** being destroyed before it is finished parsing.
    **
    ** Note: during a reduce, the only symbols destroyed are those
    ** which appear on the RHS of the rule, but which are *not* used
    ** inside the C code.
    */
/********* Begin destructor definitions ***************************************/
/********* End destructor definitions *****************************************/
    default:  break;   /* If no destructor action specified: do nothing */
  }
}

/*
** Pop the parser's stack once.
**
** If there is a destructor routine associated with the token which
** is popped from the stack, then call it.
*/
static void yy_pop_parser_stack(yyParser *pParser){
  yyStackEntry *yytos;
  assert( pParser->yytos!=0 );
  assert( pParser->yytos > pParser->yystack );
  yytos = pParser->yytos--;
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE,"%sPopping %s\n",
      yyTracePrompt,
      yyTokenName[yytos->major]);
  }
#endif
  yy_destructor(pParser, yytos->major, &yytos->minor);
}

/*
** Clear all secondary memory allocations from the parser
*/
void SqlParseFinalize(void*);
void SqlParseFinalize(void *p){
  yyParser *pParser = (yyParser*)p;

  /* In-lined version of calling yy_pop_parser_stack() for each
  ** element left in the stack */
  yyStackEntry *yytos = pParser->yytos;
  while( yytos>pParser->yystack ){
#ifndef NDEBUG
    if( yyTraceFILE ){
      fprintf(yyTraceFILE,"%sPopping %s\n",
        yyTracePrompt,
        yyTokenName[yytos->major]);
    }
#endif
    if( yytos->major>=YY_MIN_DSTRCTR ){
      yy_destructor(pParser, yytos->major, &yytos->minor);
    }
    yytos--;
  }

#if YYGROWABLESTACK
  if( pParser->yystack!=pParser->yystk0 ){
    YYFREE(pParser->yystack, SqlParseCTX(pParser));
  }
#endif
}

#ifndef SqlParse_ENGINEALWAYSONSTACK
/* 
** Deallocate and destroy a parser.  Destructors are called for
** all stack elements before shutting the parser down.
**
** If the YYPARSEFREENEVERNULL macro exists (for example because it
** is defined in a %include section of the input grammar) then it is
** assumed that the input pointer is never NULL.
*/
void SqlParseFree(void*,void(*)(void*));
void SqlParseFree(
  void *p,                    /* The parser to be deleted */
  void (*freeProc)(void*)     /* Function used to reclaim memory */
){
#ifndef YYPARSEFREENEVERNULL
  if( p==0 ) return;
#endif
  SqlParseFinalize(p);
  (*freeProc)(p);
}
#endif /* SqlParse_ENGINEALWAYSONSTACK */

/**
 * Reset a parser to its initial state without freeing the parser
 * struct or stack memory.  Pops everything off the stack
 * (running %destructor), resets state-0 markers + error count
 * + high-water mark.  user-arg is preserved.  Available v0.6.1.
 */
void SqlParseReset(void*);
void SqlParseReset(void *yyp){
  yyParser *pParser = (yyParser*)yyp;
#ifndef YYPARSEFREENEVERNULL
  if( pParser==0 ) return;
#endif
  /* Pop everything off the stack, running destructors */
  while( pParser->yytos > pParser->yystack ){
#ifndef NDEBUG
    if( yyTraceFILE ){
      fprintf(yyTraceFILE,"%sPopping %s\n",
        yyTracePrompt,
        yyTokenName[pParser->yytos->major]);
    }
#endif
    if( pParser->yytos->major >= YY_MIN_DSTRCTR ){
      yy_destructor(pParser, pParser->yytos->major, &pParser->yytos->minor);
    }
    pParser->yytos--;
  }
  /* Reset to initial state (preserve yystack, yystackEnd, and ARG/CTX) */
  pParser->yytos = pParser->yystack;
  pParser->yystack[0].stateno = 0;
  pParser->yystack[0].major = 0;
#ifdef YYTRACKMAXSTACKDEPTH
  pParser->yyhwm = 0;
#endif
#ifndef YYNOERRORRECOVERY
  pParser->yyerrcnt = -1;
#endif
#ifdef YYLOCATIONTYPE
  /* Zero-init lookahead and sentinel location */
  memset(&pParser->yyLookaheadLoc, 0, sizeof(pParser->yyLookaheadLoc));
  memset(&pParser->yystack[0].yyloc, 0, sizeof(pParser->yystack[0].yyloc));
#endif
  /* Reset action-time lookahead state */
  pParser->yyLookaheadMajor = -2;
  memset(&pParser->yyLookaheadMinor, 0, sizeof(pParser->yyLookaheadMinor));
  pParser->yyLookaheadCleared = 0;
}

/*
** Return the peak depth of the stack for a parser.
*/
#ifdef YYTRACKMAXSTACKDEPTH
int SqlParseStackPeak(void*);
int SqlParseStackPeak(void *p){
  yyParser *pParser = (yyParser*)p;
  return pParser->yyhwm;
}
#endif

/* This array of booleans keeps track of the parser statement
** coverage.  The element yycoverage[X][Y] is set when the parser
** is in state X and has a lookahead token Y.  In a well-tested
** systems, every element of this matrix should end up being set.
*/
#if defined(YYCOVERAGE)
static unsigned char yycoverage[YYNSTATE][YYNTOKEN];
#endif

/*
** Write into out a description of every state/lookahead combination that
**
**   (1)  has not been used by the parser, and
**   (2)  is not a syntax error.
**
** Return the number of missed state/lookahead combinations.
*/
#if defined(YYCOVERAGE)
int SqlParseCoverage(FILE*);
int SqlParseCoverage(FILE *out){
  int stateno, iLookAhead, i;
  int nMissed = 0;
  for(stateno=0; stateno<YYNSTATE; stateno++){
    i = yy_shift_ofst[stateno];
    for(iLookAhead=0; iLookAhead<YYNTOKEN; iLookAhead++){
      if( yy_lookahead[i+iLookAhead]!=iLookAhead ) continue;
      if( yycoverage[stateno][iLookAhead]==0 ) nMissed++;
      if( out ){
        fprintf(out,"State %d lookahead %s %s\n", stateno,
                yyTokenName[iLookAhead],
                yycoverage[stateno][iLookAhead] ? "ok" : "missed");
      }
    }
  }
  return nMissed;
}
#endif

/*
** Find the appropriate action for a parser given the terminal
** look-ahead token iLookAhead.
*/
/* Forward declaration for AOT-compiled action lookup */
#ifdef YYAOT
extern YYACTIONTYPE yy_find_shift_action_aot(YYACTIONTYPE stateno,
                                              unsigned short iLookAhead);
#endif

static YYACTIONTYPE yy_find_shift_action(
  YYCODETYPE iLookAhead,    /* The look-ahead token */
  YYACTIONTYPE stateno      /* Current state number */
){
  int i;

  if( LIME_UNLIKELY(stateno>YY_MAX_SHIFT) ) return stateno;
#ifdef YYAOT
  /* Use the AOT-compiled switch-based lookup instead of table-driven */
  return yy_find_shift_action_aot(stateno, (unsigned short)iLookAhead);
#endif
  assert( stateno <= YY_SHIFT_COUNT );
#if defined(YYCOVERAGE)
  yycoverage[stateno][iLookAhead] = 1;
#endif
  do{
    i = yy_shift_ofst[stateno];
    assert( i>=0 );
    assert( i<=YY_ACTTAB_COUNT );
    assert( i+YYNTOKEN<=(int)YY_NLOOKAHEAD );
    assert( iLookAhead!=YYNOCODE );
    assert( iLookAhead < YYNTOKEN );
    i += iLookAhead;
    assert( i<(int)YY_NLOOKAHEAD );
    if( LIME_UNLIKELY(yy_lookahead[i]!=iLookAhead) ){
#ifdef YYFALLBACK
      YYCODETYPE iFallback;            /* Fallback token */
      assert( iLookAhead<sizeof(yyFallback)/sizeof(yyFallback[0]) );
      iFallback = yyFallback[iLookAhead];
      if( iFallback!=0 ){
#ifndef NDEBUG
        if( yyTraceFILE ){
          fprintf(yyTraceFILE, "%sFALLBACK %s => %s\n",
             yyTracePrompt, yyTokenName[iLookAhead], yyTokenName[iFallback]);
        }
#endif
        assert( yyFallback[iFallback]==0 ); /* Fallback loop must terminate */
        iLookAhead = iFallback;
        continue;
      }
#endif
#ifdef YYWILDCARD
      {
        int j = i - iLookAhead + YYWILDCARD;
        assert( j<(int)(sizeof(yy_lookahead)/sizeof(yy_lookahead[0])) );
        if( yy_lookahead[j]==YYWILDCARD && iLookAhead>0 ){
#ifndef NDEBUG
          if( yyTraceFILE ){
            fprintf(yyTraceFILE, "%sWILDCARD %s => %s\n",
               yyTracePrompt, yyTokenName[iLookAhead],
               yyTokenName[YYWILDCARD]);
          }
#endif /* NDEBUG */
          return yy_action[j];
        }
      }
#endif /* YYWILDCARD */
      return yy_default[stateno];
    }else{
      assert( i>=0 && i<(int)(sizeof(yy_action)/sizeof(yy_action[0])) );
      return yy_action[i];
    }
  }while(1);
}

/*
** Find the appropriate action for a parser given the non-terminal
** look-ahead token iLookAhead.
*/
static YYACTIONTYPE yy_find_reduce_action(
  YYACTIONTYPE stateno,     /* Current state number */
  YYCODETYPE iLookAhead     /* The look-ahead token */
){
  int i;
#ifdef YYERRORSYMBOL
  if( stateno>YY_REDUCE_COUNT ){
    return yy_default[stateno];
  }
#else
  assert( stateno<=YY_REDUCE_COUNT );
#endif
  i = yy_reduce_ofst[stateno];
  assert( iLookAhead!=YYNOCODE );
  i += iLookAhead;
#ifdef YYERRORSYMBOL
  if( i<0 || i>=YY_ACTTAB_COUNT || yy_lookahead[i]!=iLookAhead ){
    return yy_default[stateno];
  }
#else
  assert( i>=0 && i<YY_ACTTAB_COUNT );
  assert( yy_lookahead[i]==iLookAhead );
#endif
  return yy_action[i];
}

/*
** The following routine is called if the stack overflows.
*/
static void yyStackOverflow(yyParser *yypParser){
   SqlParseARG_FETCH
   SqlParseCTX_FETCH
#ifndef NDEBUG
   if( yyTraceFILE ){
     fprintf(yyTraceFILE,"%sStack Overflow!\n",yyTracePrompt);
   }
#endif
   while( yypParser->yytos>yypParser->yystack ) yy_pop_parser_stack(yypParser);
   /* Here code is inserted which will execute if the parser
   ** stack every overflows */
/******** Begin %stack_overflow code ******************************************/
/******** End %stack_overflow code ********************************************/
   SqlParseARG_STORE /* Suppress warning about unused %extra_argument var */
   SqlParseCTX_STORE
}

/*
** Print tracing information for a SHIFT action
*/
#ifndef NDEBUG
static void yyTraceShift(yyParser *yypParser, int yyNewState, const char *zTag){
  if( yyTraceFILE ){
    if( yyNewState<YYNSTATE ){
      fprintf(yyTraceFILE,"%s%s '%s', go to state %d\n",
         yyTracePrompt, zTag, yyTokenName[yypParser->yytos->major],
         yyNewState);
    }else{
      fprintf(yyTraceFILE,"%s%s '%s', pending reduce %d\n",
         yyTracePrompt, zTag, yyTokenName[yypParser->yytos->major],
         yyNewState - YY_MIN_REDUCE);
    }
  }
}
#else
# define yyTraceShift(X,Y,Z)
#endif

/*
** Perform a shift action.
*/
static void yy_shift(
  yyParser *yypParser,          /* The parser to be shifted */
  YYACTIONTYPE yyNewState,      /* The new state to shift in */
  YYCODETYPE yyMajor,           /* The major token to shift in */
  SqlParseTOKENTYPE yyMinor        /* The minor token to shift in */
){
  yyStackEntry *yytos;
  yypParser->yytos++;
#ifdef YYTRACKMAXSTACKDEPTH
  if( (int)(yypParser->yytos - yypParser->yystack)>yypParser->yyhwm ){
    yypParser->yyhwm++;
    assert( yypParser->yyhwm == (int)(yypParser->yytos - yypParser->yystack) );
  }
#endif
  yytos = yypParser->yytos;
  if( yytos>yypParser->yystackEnd ){
    if( yyGrowStack(yypParser) ){
      yypParser->yytos--;
      yyStackOverflow(yypParser);
      return;
    }
    yytos = yypParser->yytos;
    assert( yytos <= yypParser->yystackEnd );
  }
  if( yyNewState > YY_MAX_SHIFT ){
    yyNewState += YY_MIN_REDUCE - YY_MIN_SHIFTREDUCE;
  }
  yytos->stateno = yyNewState;
  yytos->major = yyMajor;
  yytos->minor.yy0 = yyMinor;
  yyTraceShift(yypParser, yyNewState, "Shift");
}

/* For rule J, yyRuleInfoLhs[J] contains the symbol on the left-hand side
** of that rule */
static const YYCODETYPE yyRuleInfoLhs[] = {
   157,  /* (0) input ::= stmt_list maybe_semi */
   113,  /* (1) stmt_list ::= stmt */
   113,  /* (2) stmt_list ::= stmt_list TK_SEMI stmt */
   112,  /* (3) stmt ::= select_stmt */
   112,  /* (4) stmt ::= insert_stmt */
   112,  /* (5) stmt ::= update_stmt */
   112,  /* (6) stmt ::= delete_stmt */
   112,  /* (7) stmt ::= create_stmt */
   112,  /* (8) stmt ::= drop_stmt */
   112,  /* (9) stmt ::= alter_stmt */
   112,  /* (10) stmt ::= pragma_stmt */
   112,  /* (11) stmt ::= begin_stmt */
   112,  /* (12) stmt ::= commit_stmt */
   112,  /* (13) stmt ::= rollback_stmt */
   112,  /* (14) stmt ::= savepoint_stmt */
   112,  /* (15) stmt ::= release_stmt */
   112,  /* (16) stmt ::= attach_stmt */
   112,  /* (17) stmt ::= detach_stmt */
   112,  /* (18) stmt ::= explain_stmt */
   114,  /* (19) select_stmt ::= with_clause select_stmt */
   114,  /* (20) select_stmt ::= select_core */
   114,  /* (21) select_stmt ::= select_core TK_UNION select_stmt */
   114,  /* (22) select_stmt ::= select_core TK_UNION TK_ALL select_stmt */
   114,  /* (23) select_stmt ::= select_core TK_INTERSECT select_stmt */
   114,  /* (24) select_stmt ::= select_core TK_EXCEPT select_stmt */
   115,  /* (25) select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part */
   137,  /* (26) distinct_opt ::= */
   137,  /* (27) distinct_opt ::= TK_DISTINCT */
   137,  /* (28) distinct_opt ::= TK_ALL */
   134,  /* (29) sel_list ::= sel_item */
   134,  /* (30) sel_list ::= sel_list TK_COMMA sel_item */
   135,  /* (31) sel_item ::= TK_STAR */
   135,  /* (32) sel_item ::= TK_ID TK_DOT TK_STAR */
   135,  /* (33) sel_item ::= expr */
   135,  /* (34) sel_item ::= expr TK_AS TK_ID */
   135,  /* (35) sel_item ::= expr TK_ID */
   146,  /* (36) from_opt ::= */
   146,  /* (37) from_opt ::= TK_FROM table_list */
   147,  /* (38) table_list ::= table_ref */
   147,  /* (39) table_list ::= table_list TK_COMMA table_ref */
   147,  /* (40) table_list ::= table_list join_op table_ref join_constraint */
   149,  /* (41) join_op ::= TK_JOIN */
   149,  /* (42) join_op ::= TK_INNER TK_JOIN */
   149,  /* (43) join_op ::= TK_LEFT TK_JOIN */
   149,  /* (44) join_op ::= TK_LEFT TK_OUTER TK_JOIN */
   149,  /* (45) join_op ::= TK_RIGHT TK_JOIN */
   149,  /* (46) join_op ::= TK_RIGHT TK_OUTER TK_JOIN */
   149,  /* (47) join_op ::= TK_FULL TK_JOIN */
   149,  /* (48) join_op ::= TK_FULL TK_OUTER TK_JOIN */
   149,  /* (49) join_op ::= TK_CROSS TK_JOIN */
   149,  /* (50) join_op ::= TK_NATURAL TK_JOIN */
   149,  /* (51) join_op ::= TK_NATURAL TK_LEFT TK_JOIN */
   160,  /* (52) join_constraint ::= */
   160,  /* (53) join_constraint ::= TK_ON expr */
   160,  /* (54) join_constraint ::= TK_USING TK_LP id_list TK_RP */
   148,  /* (55) table_ref ::= TK_ID */
   148,  /* (56) table_ref ::= TK_ID TK_DOT TK_ID */
   148,  /* (57) table_ref ::= TK_ID TK_AS TK_ID */
   148,  /* (58) table_ref ::= TK_ID TK_ID */
   148,  /* (59) table_ref ::= TK_LP select_stmt TK_RP */
   148,  /* (60) table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID */
   138,  /* (61) where_opt ::= */
   138,  /* (62) where_opt ::= TK_WHERE expr */
   139,  /* (63) group_opt ::= */
   139,  /* (64) group_opt ::= TK_GROUP TK_BY expr_list */
   140,  /* (65) having_opt ::= */
   140,  /* (66) having_opt ::= TK_HAVING expr */
   141,  /* (67) order_opt ::= */
   141,  /* (68) order_opt ::= TK_ORDER TK_BY order_list */
   142,  /* (69) order_list ::= order_item */
   142,  /* (70) order_list ::= order_list TK_COMMA order_item */
   143,  /* (71) order_item ::= expr */
   143,  /* (72) order_item ::= expr TK_ASC */
   143,  /* (73) order_item ::= expr TK_DESC */
   144,  /* (74) limit_opt ::= */
   144,  /* (75) limit_opt ::= TK_LIMIT expr */
   145,  /* (76) offset_part ::= */
   145,  /* (77) offset_part ::= TK_OFFSET expr */
   145,  /* (78) offset_part ::= TK_COMMA expr */
   159,  /* (79) with_clause ::= TK_WITH cte_list */
   159,  /* (80) with_clause ::= TK_WITH TK_RECURSIVE cte_list */
   161,  /* (81) cte_list ::= cte */
   161,  /* (82) cte_list ::= cte_list TK_COMMA cte */
   162,  /* (83) cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP */
   162,  /* (84) cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP */
   116,  /* (85) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list */
   116,  /* (86) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES */
   116,  /* (87) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt */
   116,  /* (88) insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list */
   163,  /* (89) qname ::= TK_ID */
   163,  /* (90) qname ::= TK_ID TK_DOT TK_ID */
   156,  /* (91) insert_cols ::= */
   156,  /* (92) insert_cols ::= TK_LP id_list TK_RP */
   152,  /* (93) values_list ::= TK_VALUES TK_LP expr_list TK_RP */
   152,  /* (94) values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP */
   117,  /* (95) update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt */
   153,  /* (96) assign_list ::= assign */
   153,  /* (97) assign_list ::= assign_list TK_COMMA assign */
   154,  /* (98) assign ::= TK_ID TK_EQ expr */
   118,  /* (99) delete_stmt ::= TK_DELETE TK_FROM qname where_opt */
   119,  /* (100) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP */
   119,  /* (101) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt */
   119,  /* (102) create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP */
   119,  /* (103) create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt */
   164,  /* (104) temp_opt ::= */
   164,  /* (105) temp_opt ::= TK_TEMP */
   164,  /* (106) temp_opt ::= TK_TEMPORARY */
   167,  /* (107) unique_opt ::= */
   167,  /* (108) unique_opt ::= TK_UNIQUE */
   165,  /* (109) if_not_exists ::= */
   165,  /* (110) if_not_exists ::= TK_IF TK_NOT TK_EXISTS */
   166,  /* (111) table_body ::= table_elem */
   166,  /* (112) table_body ::= table_body TK_COMMA table_elem */
   168,  /* (113) table_elem ::= coldef */
   168,  /* (114) table_elem ::= table_constraint */
   155,  /* (115) coldef ::= TK_ID type_opt col_constraints */
   170,  /* (116) type_opt ::= */
   170,  /* (117) type_opt ::= TK_ID */
   170,  /* (118) type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP */
   170,  /* (119) type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP */
   171,  /* (120) col_constraints ::= */
   171,  /* (121) col_constraints ::= col_constraints col_constraint */
   172,  /* (122) col_constraint ::= TK_PRIMARY TK_KEY */
   172,  /* (123) col_constraint ::= TK_PRIMARY TK_KEY TK_ASC */
   172,  /* (124) col_constraint ::= TK_PRIMARY TK_KEY TK_DESC */
   172,  /* (125) col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR */
   172,  /* (126) col_constraint ::= TK_NOT TK_NULL */
   172,  /* (127) col_constraint ::= TK_UNIQUE */
   172,  /* (128) col_constraint ::= TK_DEFAULT expr_atom */
   172,  /* (129) col_constraint ::= TK_CHECK TK_LP expr TK_RP */
   172,  /* (130) col_constraint ::= TK_REFERENCES qname */
   172,  /* (131) col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP */
   120,  /* (132) drop_stmt ::= TK_DROP TK_TABLE if_exists qname */
   120,  /* (133) drop_stmt ::= TK_DROP TK_INDEX if_exists qname */
   120,  /* (134) drop_stmt ::= TK_DROP TK_VIEW if_exists qname */
   120,  /* (135) drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname */
   121,  /* (136) alter_stmt ::= TK_ALTER TK_TABLE qname TK_RENAME TK_TO qname */
   173,  /* (137) if_exists ::= */
   173,  /* (138) if_exists ::= TK_IF TK_EXISTS */
   122,  /* (139) pragma_stmt ::= TK_PRAGMA qname */
   122,  /* (140) pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom */
   122,  /* (141) pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP */
   123,  /* (142) begin_stmt ::= TK_BEGIN */
   123,  /* (143) begin_stmt ::= TK_BEGIN TK_TRANSACTION */
   123,  /* (144) begin_stmt ::= TK_BEGIN TK_ID */
   123,  /* (145) begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION */
   124,  /* (146) commit_stmt ::= TK_COMMIT */
   124,  /* (147) commit_stmt ::= TK_COMMIT TK_TRANSACTION */
   124,  /* (148) commit_stmt ::= TK_END */
   124,  /* (149) commit_stmt ::= TK_END TK_TRANSACTION */
   125,  /* (150) rollback_stmt ::= TK_ROLLBACK */
   125,  /* (151) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION */
   125,  /* (152) rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID */
   125,  /* (153) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID */
   125,  /* (154) rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID */
   126,  /* (155) savepoint_stmt ::= TK_SAVEPOINT TK_ID */
   127,  /* (156) release_stmt ::= TK_RELEASE TK_ID */
   127,  /* (157) release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID */
   128,  /* (158) attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID */
   128,  /* (159) attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID */
   129,  /* (160) detach_stmt ::= TK_DETACH TK_ID */
   129,  /* (161) detach_stmt ::= TK_DETACH TK_DATABASE TK_ID */
   130,  /* (162) explain_stmt ::= TK_EXPLAIN stmt */
   130,  /* (163) explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt */
   136,  /* (164) id_list ::= TK_ID */
   136,  /* (165) id_list ::= id_list TK_COMMA TK_ID */
   133,  /* (166) expr_list ::= expr */
   133,  /* (167) expr_list ::= expr_list TK_COMMA expr */
   131,  /* (168) expr ::= expr_atom */
   131,  /* (169) expr ::= TK_LP expr TK_RP */
   131,  /* (170) expr ::= TK_LP select_stmt TK_RP */
   131,  /* (171) expr ::= TK_NOT expr */
   131,  /* (172) expr ::= TK_MINUS expr */
   131,  /* (173) expr ::= TK_PLUS expr */
   131,  /* (174) expr ::= TK_TILDE expr */
   131,  /* (175) expr ::= expr TK_OR expr */
   131,  /* (176) expr ::= expr TK_AND expr */
   131,  /* (177) expr ::= expr TK_EQ expr */
   131,  /* (178) expr ::= expr TK_NE expr */
   131,  /* (179) expr ::= expr TK_LT expr */
   131,  /* (180) expr ::= expr TK_LE expr */
   131,  /* (181) expr ::= expr TK_GT expr */
   131,  /* (182) expr ::= expr TK_GE expr */
   131,  /* (183) expr ::= expr TK_PLUS expr */
   131,  /* (184) expr ::= expr TK_MINUS expr */
   131,  /* (185) expr ::= expr TK_STAR expr */
   131,  /* (186) expr ::= expr TK_SLASH expr */
   131,  /* (187) expr ::= expr TK_PERCENT expr */
   131,  /* (188) expr ::= expr TK_CONCAT expr */
   131,  /* (189) expr ::= expr TK_AMP expr */
   131,  /* (190) expr ::= expr TK_PIPE expr */
   131,  /* (191) expr ::= expr TK_LIKE expr */
   131,  /* (192) expr ::= expr TK_IS TK_NULL */
   131,  /* (193) expr ::= expr TK_IS TK_NOT TK_NULL */
   131,  /* (194) expr ::= expr TK_NOT TK_NULL */
   131,  /* (195) expr ::= expr TK_IS expr */
   131,  /* (196) expr ::= expr TK_IN TK_LP expr_list TK_RP */
   131,  /* (197) expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP */
   131,  /* (198) expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP */
   131,  /* (199) expr ::= expr TK_IN TK_LP select_stmt TK_RP */
   131,  /* (200) expr ::= expr TK_BETWEEN expr TK_AND expr */
   131,  /* (201) expr ::= TK_CASE expr_atom case_arms case_else TK_END */
   131,  /* (202) expr ::= TK_CASE case_arms case_else TK_END */
   131,  /* (203) expr ::= TK_ID TK_LP TK_RP */
   131,  /* (204) expr ::= TK_ID TK_LP TK_STAR TK_RP */
   131,  /* (205) expr ::= TK_ID TK_LP expr_list TK_RP */
   131,  /* (206) expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP */
   150,  /* (207) case_arms ::= TK_WHEN expr TK_THEN expr */
   150,  /* (208) case_arms ::= case_arms TK_WHEN expr TK_THEN expr */
   151,  /* (209) case_else ::= */
   151,  /* (210) case_else ::= TK_ELSE expr */
   132,  /* (211) expr_atom ::= TK_NUMBER */
   132,  /* (212) expr_atom ::= TK_STRING */
   132,  /* (213) expr_atom ::= TK_BLOB */
   132,  /* (214) expr_atom ::= TK_NULL */
   132,  /* (215) expr_atom ::= TK_TRUE */
   132,  /* (216) expr_atom ::= TK_FALSE */
   132,  /* (217) expr_atom ::= TK_QMARK */
   132,  /* (218) expr_atom ::= TK_ID */
   132,  /* (219) expr_atom ::= TK_ID TK_DOT TK_ID */
   132,  /* (220) expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID */
   158,  /* (221) maybe_semi ::= */
   158,  /* (222) maybe_semi ::= TK_SEMI */
   169,  /* (223) table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP */
   169,  /* (224) table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP */
   169,  /* (225) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname */
   169,  /* (226) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP */
   169,  /* (227) table_constraint ::= TK_CHECK TK_LP expr TK_RP */
};

/* For rule J, yyRuleInfoNRhs[J] contains the negative of the number
** of symbols on the right-hand side of that rule. */
static const signed char yyRuleInfoNRhs[] = {
   -2,  /* (0) input ::= stmt_list maybe_semi */
   -1,  /* (1) stmt_list ::= stmt */
   -3,  /* (2) stmt_list ::= stmt_list TK_SEMI stmt */
   -1,  /* (3) stmt ::= select_stmt */
   -1,  /* (4) stmt ::= insert_stmt */
   -1,  /* (5) stmt ::= update_stmt */
   -1,  /* (6) stmt ::= delete_stmt */
   -1,  /* (7) stmt ::= create_stmt */
   -1,  /* (8) stmt ::= drop_stmt */
   -1,  /* (9) stmt ::= alter_stmt */
   -1,  /* (10) stmt ::= pragma_stmt */
   -1,  /* (11) stmt ::= begin_stmt */
   -1,  /* (12) stmt ::= commit_stmt */
   -1,  /* (13) stmt ::= rollback_stmt */
   -1,  /* (14) stmt ::= savepoint_stmt */
   -1,  /* (15) stmt ::= release_stmt */
   -1,  /* (16) stmt ::= attach_stmt */
   -1,  /* (17) stmt ::= detach_stmt */
   -1,  /* (18) stmt ::= explain_stmt */
   -2,  /* (19) select_stmt ::= with_clause select_stmt */
   -1,  /* (20) select_stmt ::= select_core */
   -3,  /* (21) select_stmt ::= select_core TK_UNION select_stmt */
   -4,  /* (22) select_stmt ::= select_core TK_UNION TK_ALL select_stmt */
   -3,  /* (23) select_stmt ::= select_core TK_INTERSECT select_stmt */
   -3,  /* (24) select_stmt ::= select_core TK_EXCEPT select_stmt */
  -10,  /* (25) select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part */
    0,  /* (26) distinct_opt ::= */
   -1,  /* (27) distinct_opt ::= TK_DISTINCT */
   -1,  /* (28) distinct_opt ::= TK_ALL */
   -1,  /* (29) sel_list ::= sel_item */
   -3,  /* (30) sel_list ::= sel_list TK_COMMA sel_item */
   -1,  /* (31) sel_item ::= TK_STAR */
   -3,  /* (32) sel_item ::= TK_ID TK_DOT TK_STAR */
   -1,  /* (33) sel_item ::= expr */
   -3,  /* (34) sel_item ::= expr TK_AS TK_ID */
   -2,  /* (35) sel_item ::= expr TK_ID */
    0,  /* (36) from_opt ::= */
   -2,  /* (37) from_opt ::= TK_FROM table_list */
   -1,  /* (38) table_list ::= table_ref */
   -3,  /* (39) table_list ::= table_list TK_COMMA table_ref */
   -4,  /* (40) table_list ::= table_list join_op table_ref join_constraint */
   -1,  /* (41) join_op ::= TK_JOIN */
   -2,  /* (42) join_op ::= TK_INNER TK_JOIN */
   -2,  /* (43) join_op ::= TK_LEFT TK_JOIN */
   -3,  /* (44) join_op ::= TK_LEFT TK_OUTER TK_JOIN */
   -2,  /* (45) join_op ::= TK_RIGHT TK_JOIN */
   -3,  /* (46) join_op ::= TK_RIGHT TK_OUTER TK_JOIN */
   -2,  /* (47) join_op ::= TK_FULL TK_JOIN */
   -3,  /* (48) join_op ::= TK_FULL TK_OUTER TK_JOIN */
   -2,  /* (49) join_op ::= TK_CROSS TK_JOIN */
   -2,  /* (50) join_op ::= TK_NATURAL TK_JOIN */
   -3,  /* (51) join_op ::= TK_NATURAL TK_LEFT TK_JOIN */
    0,  /* (52) join_constraint ::= */
   -2,  /* (53) join_constraint ::= TK_ON expr */
   -4,  /* (54) join_constraint ::= TK_USING TK_LP id_list TK_RP */
   -1,  /* (55) table_ref ::= TK_ID */
   -3,  /* (56) table_ref ::= TK_ID TK_DOT TK_ID */
   -3,  /* (57) table_ref ::= TK_ID TK_AS TK_ID */
   -2,  /* (58) table_ref ::= TK_ID TK_ID */
   -3,  /* (59) table_ref ::= TK_LP select_stmt TK_RP */
   -5,  /* (60) table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID */
    0,  /* (61) where_opt ::= */
   -2,  /* (62) where_opt ::= TK_WHERE expr */
    0,  /* (63) group_opt ::= */
   -3,  /* (64) group_opt ::= TK_GROUP TK_BY expr_list */
    0,  /* (65) having_opt ::= */
   -2,  /* (66) having_opt ::= TK_HAVING expr */
    0,  /* (67) order_opt ::= */
   -3,  /* (68) order_opt ::= TK_ORDER TK_BY order_list */
   -1,  /* (69) order_list ::= order_item */
   -3,  /* (70) order_list ::= order_list TK_COMMA order_item */
   -1,  /* (71) order_item ::= expr */
   -2,  /* (72) order_item ::= expr TK_ASC */
   -2,  /* (73) order_item ::= expr TK_DESC */
    0,  /* (74) limit_opt ::= */
   -2,  /* (75) limit_opt ::= TK_LIMIT expr */
    0,  /* (76) offset_part ::= */
   -2,  /* (77) offset_part ::= TK_OFFSET expr */
   -2,  /* (78) offset_part ::= TK_COMMA expr */
   -2,  /* (79) with_clause ::= TK_WITH cte_list */
   -3,  /* (80) with_clause ::= TK_WITH TK_RECURSIVE cte_list */
   -1,  /* (81) cte_list ::= cte */
   -3,  /* (82) cte_list ::= cte_list TK_COMMA cte */
   -5,  /* (83) cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP */
   -8,  /* (84) cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP */
   -5,  /* (85) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list */
   -6,  /* (86) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES */
   -5,  /* (87) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt */
   -5,  /* (88) insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list */
   -1,  /* (89) qname ::= TK_ID */
   -3,  /* (90) qname ::= TK_ID TK_DOT TK_ID */
    0,  /* (91) insert_cols ::= */
   -3,  /* (92) insert_cols ::= TK_LP id_list TK_RP */
   -4,  /* (93) values_list ::= TK_VALUES TK_LP expr_list TK_RP */
   -5,  /* (94) values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP */
   -5,  /* (95) update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt */
   -1,  /* (96) assign_list ::= assign */
   -3,  /* (97) assign_list ::= assign_list TK_COMMA assign */
   -3,  /* (98) assign ::= TK_ID TK_EQ expr */
   -4,  /* (99) delete_stmt ::= TK_DELETE TK_FROM qname where_opt */
   -8,  /* (100) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP */
   -7,  /* (101) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt */
  -10,  /* (102) create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP */
   -7,  /* (103) create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt */
    0,  /* (104) temp_opt ::= */
   -1,  /* (105) temp_opt ::= TK_TEMP */
   -1,  /* (106) temp_opt ::= TK_TEMPORARY */
    0,  /* (107) unique_opt ::= */
   -1,  /* (108) unique_opt ::= TK_UNIQUE */
    0,  /* (109) if_not_exists ::= */
   -3,  /* (110) if_not_exists ::= TK_IF TK_NOT TK_EXISTS */
   -1,  /* (111) table_body ::= table_elem */
   -3,  /* (112) table_body ::= table_body TK_COMMA table_elem */
   -1,  /* (113) table_elem ::= coldef */
   -1,  /* (114) table_elem ::= table_constraint */
   -3,  /* (115) coldef ::= TK_ID type_opt col_constraints */
    0,  /* (116) type_opt ::= */
   -1,  /* (117) type_opt ::= TK_ID */
   -4,  /* (118) type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP */
   -6,  /* (119) type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP */
    0,  /* (120) col_constraints ::= */
   -2,  /* (121) col_constraints ::= col_constraints col_constraint */
   -2,  /* (122) col_constraint ::= TK_PRIMARY TK_KEY */
   -3,  /* (123) col_constraint ::= TK_PRIMARY TK_KEY TK_ASC */
   -3,  /* (124) col_constraint ::= TK_PRIMARY TK_KEY TK_DESC */
   -3,  /* (125) col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR */
   -2,  /* (126) col_constraint ::= TK_NOT TK_NULL */
   -1,  /* (127) col_constraint ::= TK_UNIQUE */
   -2,  /* (128) col_constraint ::= TK_DEFAULT expr_atom */
   -4,  /* (129) col_constraint ::= TK_CHECK TK_LP expr TK_RP */
   -2,  /* (130) col_constraint ::= TK_REFERENCES qname */
   -5,  /* (131) col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP */
   -4,  /* (132) drop_stmt ::= TK_DROP TK_TABLE if_exists qname */
   -4,  /* (133) drop_stmt ::= TK_DROP TK_INDEX if_exists qname */
   -4,  /* (134) drop_stmt ::= TK_DROP TK_VIEW if_exists qname */
   -4,  /* (135) drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname */
   -6,  /* (136) alter_stmt ::= TK_ALTER TK_TABLE qname TK_RENAME TK_TO qname */
    0,  /* (137) if_exists ::= */
   -2,  /* (138) if_exists ::= TK_IF TK_EXISTS */
   -2,  /* (139) pragma_stmt ::= TK_PRAGMA qname */
   -4,  /* (140) pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom */
   -5,  /* (141) pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP */
   -1,  /* (142) begin_stmt ::= TK_BEGIN */
   -2,  /* (143) begin_stmt ::= TK_BEGIN TK_TRANSACTION */
   -2,  /* (144) begin_stmt ::= TK_BEGIN TK_ID */
   -3,  /* (145) begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION */
   -1,  /* (146) commit_stmt ::= TK_COMMIT */
   -2,  /* (147) commit_stmt ::= TK_COMMIT TK_TRANSACTION */
   -1,  /* (148) commit_stmt ::= TK_END */
   -2,  /* (149) commit_stmt ::= TK_END TK_TRANSACTION */
   -1,  /* (150) rollback_stmt ::= TK_ROLLBACK */
   -2,  /* (151) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION */
   -3,  /* (152) rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID */
   -4,  /* (153) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID */
   -4,  /* (154) rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID */
   -2,  /* (155) savepoint_stmt ::= TK_SAVEPOINT TK_ID */
   -2,  /* (156) release_stmt ::= TK_RELEASE TK_ID */
   -3,  /* (157) release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID */
   -4,  /* (158) attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID */
   -5,  /* (159) attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID */
   -2,  /* (160) detach_stmt ::= TK_DETACH TK_ID */
   -3,  /* (161) detach_stmt ::= TK_DETACH TK_DATABASE TK_ID */
   -2,  /* (162) explain_stmt ::= TK_EXPLAIN stmt */
   -4,  /* (163) explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt */
   -1,  /* (164) id_list ::= TK_ID */
   -3,  /* (165) id_list ::= id_list TK_COMMA TK_ID */
   -1,  /* (166) expr_list ::= expr */
   -3,  /* (167) expr_list ::= expr_list TK_COMMA expr */
   -1,  /* (168) expr ::= expr_atom */
   -3,  /* (169) expr ::= TK_LP expr TK_RP */
   -3,  /* (170) expr ::= TK_LP select_stmt TK_RP */
   -2,  /* (171) expr ::= TK_NOT expr */
   -2,  /* (172) expr ::= TK_MINUS expr */
   -2,  /* (173) expr ::= TK_PLUS expr */
   -2,  /* (174) expr ::= TK_TILDE expr */
   -3,  /* (175) expr ::= expr TK_OR expr */
   -3,  /* (176) expr ::= expr TK_AND expr */
   -3,  /* (177) expr ::= expr TK_EQ expr */
   -3,  /* (178) expr ::= expr TK_NE expr */
   -3,  /* (179) expr ::= expr TK_LT expr */
   -3,  /* (180) expr ::= expr TK_LE expr */
   -3,  /* (181) expr ::= expr TK_GT expr */
   -3,  /* (182) expr ::= expr TK_GE expr */
   -3,  /* (183) expr ::= expr TK_PLUS expr */
   -3,  /* (184) expr ::= expr TK_MINUS expr */
   -3,  /* (185) expr ::= expr TK_STAR expr */
   -3,  /* (186) expr ::= expr TK_SLASH expr */
   -3,  /* (187) expr ::= expr TK_PERCENT expr */
   -3,  /* (188) expr ::= expr TK_CONCAT expr */
   -3,  /* (189) expr ::= expr TK_AMP expr */
   -3,  /* (190) expr ::= expr TK_PIPE expr */
   -3,  /* (191) expr ::= expr TK_LIKE expr */
   -3,  /* (192) expr ::= expr TK_IS TK_NULL */
   -4,  /* (193) expr ::= expr TK_IS TK_NOT TK_NULL */
   -3,  /* (194) expr ::= expr TK_NOT TK_NULL */
   -3,  /* (195) expr ::= expr TK_IS expr */
   -5,  /* (196) expr ::= expr TK_IN TK_LP expr_list TK_RP */
   -6,  /* (197) expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP */
   -6,  /* (198) expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP */
   -5,  /* (199) expr ::= expr TK_IN TK_LP select_stmt TK_RP */
   -5,  /* (200) expr ::= expr TK_BETWEEN expr TK_AND expr */
   -5,  /* (201) expr ::= TK_CASE expr_atom case_arms case_else TK_END */
   -4,  /* (202) expr ::= TK_CASE case_arms case_else TK_END */
   -3,  /* (203) expr ::= TK_ID TK_LP TK_RP */
   -4,  /* (204) expr ::= TK_ID TK_LP TK_STAR TK_RP */
   -4,  /* (205) expr ::= TK_ID TK_LP expr_list TK_RP */
   -5,  /* (206) expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP */
   -4,  /* (207) case_arms ::= TK_WHEN expr TK_THEN expr */
   -5,  /* (208) case_arms ::= case_arms TK_WHEN expr TK_THEN expr */
    0,  /* (209) case_else ::= */
   -2,  /* (210) case_else ::= TK_ELSE expr */
   -1,  /* (211) expr_atom ::= TK_NUMBER */
   -1,  /* (212) expr_atom ::= TK_STRING */
   -1,  /* (213) expr_atom ::= TK_BLOB */
   -1,  /* (214) expr_atom ::= TK_NULL */
   -1,  /* (215) expr_atom ::= TK_TRUE */
   -1,  /* (216) expr_atom ::= TK_FALSE */
   -1,  /* (217) expr_atom ::= TK_QMARK */
   -1,  /* (218) expr_atom ::= TK_ID */
   -3,  /* (219) expr_atom ::= TK_ID TK_DOT TK_ID */
   -5,  /* (220) expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID */
    0,  /* (221) maybe_semi ::= */
   -1,  /* (222) maybe_semi ::= TK_SEMI */
   -5,  /* (223) table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP */
   -4,  /* (224) table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP */
   -7,  /* (225) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname */
  -10,  /* (226) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP */
   -4,  /* (227) table_constraint ::= TK_CHECK TK_LP expr TK_RP */
};

static void yy_accept(yyParser*);  /* Forward Declaration */

/* Forward declarations for the action-time lookahead API
** (SqlParse_get_lookahead / SqlParse_clear_lookahead).  The definitions
** live just before void SqlParse() further down; these forward
** declarations let user action bodies inside yy_reduce reference
** the API without an out-of-order-symbol error.  P0-NEW-5. */
#ifndef YYEMPTY
# define YYEMPTY (-2)
#endif
int  SqlParse_get_lookahead(
  void *yyp,
  SqlParseTOKENTYPE *yyminor_out
#ifdef YYLOCATIONTYPE
  , YYLOCATIONTYPE *yyloc_out
#endif
);
void SqlParse_clear_lookahead(void *yyp);

/* P0-NEW-8: opt-in eager default-reduce drain.  After a SqlParse()
** call returns the parser may be in a state whose only available
** action is a reduce (independent of the next lookahead).  Bison's
** pull-mode fires those reduces eagerly between yylex calls; Lime's
** push-mode normally waits for the next push to confirm the
** reduction.  SqlParse_drain forces the eager-fire behavior on demand,
** letting drivers like ecpg's preproc -- whose action bodies have
** side effects (fprintf to base_yyout) that must precede the
** lexer's next echo of whitespace -- match Bison's timing.
**
** Loops while the top-of-stack state's default action is
** (a) a reduce and (b) unconditional (no token-specific entries
** would override it).  Stops on a shift state, an unconditional
** non-reduce default, or YY_ACCEPT.  Action bodies fired during
** drain see no lookahead (SqlParse_get_lookahead returns YYEMPTY).
** Safe to call between SqlParse() invocations; safe to call multiple
** times in a row (the second call is a no-op once the parser is
** quiescent). */
void SqlParse_drain(void *yyp SqlParseCTX_PDECL);

/*
** v0.6.0: per-rule reduce action callbacks.
**
** Each grammar rule's reduction code is emitted as its own
** static function `yy_rule_<N>(yy_reduce_ctx *ctx)` and
** dispatched through `yy_rule_reduce_fn[ruleno]` below.  This
** replaces Lemon's classical 30-year switch-on-yyruleno.
**
** Why the change:
**   1. JIT specialisation -- per-rule callbacks let the JIT
**      inline reduce code into the JIT-compiled trace,
**      eliminating the indirect call through yy_reduce.
**   2. Composition + extension -- merging two grammars now
**      concatenates `yy_rule_reduce_fn[]` arrays trivially
**      instead of doing source-level switch surgery.
**   3. Hot-rule annotation -- `__attribute__((hot))` on the
**      handful of rules a profile says are hottest is now
**      possible per-function (impossible per switch case).
**   4. Plugin overrides -- a future feature lets an extension
**      replace one rule's reduce action by overwriting one
**      slot in the dispatch array, no whole-grammar rebuild.
**
** ABI break vs v0.5.x: the generated .c file's reduce path
** changes shape but the runtime ABI (SqlParserSnapshot, the
** push-parser entry, the JIT) stays put.  See ROADMAP for
** the v0.6.0 charter.
*/
typedef struct yy_reduce_ctx {
  yyParser *yypParser;             /* The parser */
  yyStackEntry *yymsp;             /* Top of the parser's stack */
  int yyLookahead;                 /* Lookahead token or YYNOCODE */
  SqlParseTOKENTYPE yyLookaheadToken; /* Value of the lookahead token */
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE *yyloc_lhs_ptr;   /* &yyloc_lhs in caller; per-rule
                                   ** functions read AND write through
                                   ** this pointer so @$ assignments
                                   ** propagate to the LHS slot. */
#endif
} yy_reduce_ctx;

/********** Begin per-rule reduce action functions ****************************/
/* (0) input ::= stmt_list maybe_semi */
static void yy_rule_0(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 226 "sql_grammar.lime"
{
    pstate->stmts = yymsp[-1].minor.yy97;
    /* Route on the FIRST statement.  Set here (not in the per-statement
     * actions) because reductions happen inner-first: a nested SELECT
     * inside INSERT/CREATE VIEW/EXPLAIN would otherwise claim the kind
     * before the outer statement reduces. */
    if (yymsp[-1].minor.yy97) { pstate->kind = yymsp[-1].minor.yy97->kind; pstate->readonly = yymsp[-1].minor.yy97->readonly; }
}
#line 2363 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (1) stmt_list ::= stmt */
static void yy_rule_1(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 238 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2390 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (2) stmt_list ::= stmt_list TK_SEMI stmt */
static void yy_rule_2(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 239 "sql_grammar.lime"
{
    /* append S to the tail of L */
    if (yymsp[-2].minor.yy97 == NULL) { yylhsminor.yy97 = yymsp[0].minor.yy97; }
    else { sql_stmt_t *t = yymsp[-2].minor.yy97; while (t->next) t = t->next; t->next = yymsp[0].minor.yy97; yylhsminor.yy97 = yymsp[-2].minor.yy97; }
}
#line 2422 "sql_grammar.c"
  yymsp[-2].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (3) stmt ::= select_stmt */
static void yy_rule_3(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 245 "sql_grammar.lime"
{ yylhsminor.yy97 = AST(pstate, sql_stmt_t); if (yylhsminor.yy97) { yylhsminor.yy97->kind = SQL_KIND_SELECT; yylhsminor.yy97->readonly = 1; yylhsminor.yy97->u.select = yymsp[0].minor.yy261; } }
#line 2450 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (4) stmt ::= insert_stmt */
static void yy_rule_4(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 246 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2478 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (5) stmt ::= update_stmt */
static void yy_rule_5(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 247 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2506 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (6) stmt ::= delete_stmt */
static void yy_rule_6(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 248 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2534 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (7) stmt ::= create_stmt */
static void yy_rule_7(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 249 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2562 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (8) stmt ::= drop_stmt */
static void yy_rule_8(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 250 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2590 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (9) stmt ::= alter_stmt */
static void yy_rule_9(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 251 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2618 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (10) stmt ::= pragma_stmt */
static void yy_rule_10(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 252 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2646 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (11) stmt ::= begin_stmt */
static void yy_rule_11(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 253 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2674 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (12) stmt ::= commit_stmt */
static void yy_rule_12(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 254 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2702 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (13) stmt ::= rollback_stmt */
static void yy_rule_13(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 255 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2730 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (14) stmt ::= savepoint_stmt */
static void yy_rule_14(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 256 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2758 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (15) stmt ::= release_stmt */
static void yy_rule_15(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 257 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2786 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (16) stmt ::= attach_stmt */
static void yy_rule_16(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 258 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2814 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (17) stmt ::= detach_stmt */
static void yy_rule_17(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 259 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2842 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (18) stmt ::= explain_stmt */
static void yy_rule_18(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 260 "sql_grammar.lime"
{ yylhsminor.yy97 = yymsp[0].minor.yy97; }
#line 2870 "sql_grammar.c"
  yymsp[0].minor.yy97 = yylhsminor.yy97;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (19) select_stmt ::= with_clause select_stmt */
static void yy_rule_19(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 268 "sql_grammar.lime"
{
    if (yymsp[0].minor.yy261) { yymsp[0].minor.yy261->with = yymsp[-1].minor.yy339.list; yymsp[0].minor.yy261->with_recursive = yymsp[-1].minor.yy339.rec; }
    yylhsminor.yy261 = yymsp[0].minor.yy261;
}
#line 2901 "sql_grammar.c"
  yymsp[-1].minor.yy261 = yylhsminor.yy261;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (20) select_stmt ::= select_core */
static void yy_rule_20(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 272 "sql_grammar.lime"
{ yylhsminor.yy261 = yymsp[0].minor.yy261; }
#line 2929 "sql_grammar.c"
  yymsp[0].minor.yy261 = yylhsminor.yy261;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (21) select_stmt ::= select_core TK_UNION select_stmt */
static void yy_rule_21(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 273 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy261) { yymsp[-2].minor.yy261->setop = SX_SET_UNION; yymsp[-2].minor.yy261->rhs = yymsp[0].minor.yy261; } yylhsminor.yy261 = yymsp[-2].minor.yy261;
}
#line 2959 "sql_grammar.c"
  yymsp[-2].minor.yy261 = yylhsminor.yy261;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (22) select_stmt ::= select_core TK_UNION TK_ALL select_stmt */
static void yy_rule_22(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 276 "sql_grammar.lime"
{
    if (yymsp[-3].minor.yy261) { yymsp[-3].minor.yy261->setop = SX_SET_UNION_ALL; yymsp[-3].minor.yy261->rhs = yymsp[0].minor.yy261; } yylhsminor.yy261 = yymsp[-3].minor.yy261;
}
#line 2989 "sql_grammar.c"
  yymsp[-3].minor.yy261 = yylhsminor.yy261;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (23) select_stmt ::= select_core TK_INTERSECT select_stmt */
static void yy_rule_23(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 279 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy261) { yymsp[-2].minor.yy261->setop = SX_SET_INTERSECT; yymsp[-2].minor.yy261->rhs = yymsp[0].minor.yy261; } yylhsminor.yy261 = yymsp[-2].minor.yy261;
}
#line 3019 "sql_grammar.c"
  yymsp[-2].minor.yy261 = yylhsminor.yy261;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (24) select_stmt ::= select_core TK_EXCEPT select_stmt */
static void yy_rule_24(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 282 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy261) { yymsp[-2].minor.yy261->setop = SX_SET_EXCEPT; yymsp[-2].minor.yy261->rhs = yymsp[0].minor.yy261; } yylhsminor.yy261 = yymsp[-2].minor.yy261;
}
#line 3049 "sql_grammar.c"
  yymsp[-2].minor.yy261 = yylhsminor.yy261;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (25) select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part */
static void yy_rule_25(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 288 "sql_grammar.lime"
{
    yylhsminor.yy261 = AST(pstate, sql_select_t);
    if (yylhsminor.yy261) {
        yylhsminor.yy261->distinct = yymsp[-8].minor.yy220; yylhsminor.yy261->cols = yymsp[-7].minor.yy116; yylhsminor.yy261->from = yymsp[-6].minor.yy157; yylhsminor.yy261->where = yymsp[-5].minor.yy192;
        yylhsminor.yy261->group = yymsp[-4].minor.yy116; yylhsminor.yy261->having = yymsp[-3].minor.yy192; yylhsminor.yy261->order = yymsp[-2].minor.yy116; yylhsminor.yy261->limit = yymsp[-1].minor.yy192;
        yylhsminor.yy261->offset = yymsp[0].minor.yy192;
        yylhsminor.yy261->src = yymsp[-9].minor.yy0.p;   /* SELECT keyword; length resolved by the consumer */
    }
}
#line 3085 "sql_grammar.c"
  yymsp[-9].minor.yy261 = yylhsminor.yy261;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (26) distinct_opt ::= */
static void yy_rule_26(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 298 "sql_grammar.lime"
{ yymsp[1].minor.yy220 = 0; }
#line 3113 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (27) distinct_opt ::= TK_DISTINCT */
static void yy_rule_27(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 299 "sql_grammar.lime"
{ yymsp[0].minor.yy220 = 1; }
#line 3140 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (28) distinct_opt ::= TK_ALL */
static void yy_rule_28(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 300 "sql_grammar.lime"
{ yymsp[0].minor.yy220 = 0; }
#line 3167 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (29) sel_list ::= sel_item */
static void yy_rule_29(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 302 "sql_grammar.lime"
{ yylhsminor.yy116 = mk_list(pstate); if (yylhsminor.yy116 && yymsp[0].minor.yy298) { yylhsminor.yy116->head = yylhsminor.yy116->tail = yymsp[0].minor.yy298; yylhsminor.yy116->n = 1; } }
#line 3194 "sql_grammar.c"
  yymsp[0].minor.yy116 = yylhsminor.yy116;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (30) sel_list ::= sel_list TK_COMMA sel_item */
static void yy_rule_30(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 303 "sql_grammar.lime"
{
    yylhsminor.yy116 = yymsp[-2].minor.yy116; if (yylhsminor.yy116 && yymsp[0].minor.yy298) { if (yylhsminor.yy116->tail) yylhsminor.yy116->tail->next = yymsp[0].minor.yy298; else yylhsminor.yy116->head = yymsp[0].minor.yy298; yylhsminor.yy116->tail = yymsp[0].minor.yy298; yylhsminor.yy116->n++; }
}
#line 3224 "sql_grammar.c"
  yymsp[-2].minor.yy116 = yylhsminor.yy116;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (31) sel_item ::= TK_STAR */
static void yy_rule_31(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 307 "sql_grammar.lime"
{
    yymsp[0].minor.yy298 = AST(pstate, sql_exprlist_item_t);
    if (yymsp[0].minor.yy298) { yymsp[0].minor.yy298->expr = mk_expr(pstate, SX_E_STAR); }
}
#line 3255 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (32) sel_item ::= TK_ID TK_DOT TK_STAR */
static void yy_rule_32(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 311 "sql_grammar.lime"
{
    yylhsminor.yy298 = AST(pstate, sql_exprlist_item_t);
    if (yylhsminor.yy298) { yylhsminor.yy298->expr = mk_expr(pstate, SX_E_STAR);
             if (yylhsminor.yy298->expr) { yylhsminor.yy298->expr->name[0] = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy298->expr->nname = 1; } }
}
#line 3286 "sql_grammar.c"
  yymsp[-2].minor.yy298 = yylhsminor.yy298;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (33) sel_item ::= expr */
static void yy_rule_33(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 316 "sql_grammar.lime"
{ yylhsminor.yy298 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy298) yylhsminor.yy298->expr = yymsp[0].minor.yy192; }
#line 3314 "sql_grammar.c"
  yymsp[0].minor.yy298 = yylhsminor.yy298;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (34) sel_item ::= expr TK_AS TK_ID */
static void yy_rule_34(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 317 "sql_grammar.lime"
{
    yylhsminor.yy298 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy298) { yylhsminor.yy298->expr = yymsp[-2].minor.yy192; yylhsminor.yy298->alias = tok_str(yymsp[0].minor.yy0); }
}
#line 3344 "sql_grammar.c"
  yymsp[-2].minor.yy298 = yylhsminor.yy298;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (35) sel_item ::= expr TK_ID */
static void yy_rule_35(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 320 "sql_grammar.lime"
{
    yylhsminor.yy298 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy298) { yylhsminor.yy298->expr = yymsp[-1].minor.yy192; yylhsminor.yy298->alias = tok_str(yymsp[0].minor.yy0); }
}
#line 3374 "sql_grammar.c"
  yymsp[-1].minor.yy298 = yylhsminor.yy298;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (36) from_opt ::= */
static void yy_rule_36(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 324 "sql_grammar.lime"
{ yymsp[1].minor.yy157 = NULL; }
#line 3402 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (37) from_opt ::= TK_FROM table_list */
static void yy_rule_37(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 325 "sql_grammar.lime"
{ yymsp[-1].minor.yy157 = yymsp[0].minor.yy157; }
#line 3429 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (38) table_list ::= table_ref */
static void yy_rule_38(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 327 "sql_grammar.lime"
{ yylhsminor.yy157 = yymsp[0].minor.yy157; }
#line 3456 "sql_grammar.c"
  yymsp[0].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (39) table_list ::= table_list TK_COMMA table_ref */
static void yy_rule_39(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 328 "sql_grammar.lime"
{
    if (yymsp[0].minor.yy157) yymsp[0].minor.yy157->join = SX_J_NONE;
    if (yymsp[-2].minor.yy157 == NULL) { yylhsminor.yy157 = yymsp[0].minor.yy157; }
    else { sql_src_t *s = yymsp[-2].minor.yy157; while (s->next) s = s->next; s->next = yymsp[0].minor.yy157; yylhsminor.yy157 = yymsp[-2].minor.yy157; }
}
#line 3488 "sql_grammar.c"
  yymsp[-2].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (40) table_list ::= table_list join_op table_ref join_constraint */
static void yy_rule_40(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 333 "sql_grammar.lime"
{
    if (yymsp[-1].minor.yy157) { yymsp[-1].minor.yy157->join = (sql_join_type_t)yymsp[-2].minor.yy220; yymsp[-1].minor.yy157->on = yymsp[0].minor.yy192; }
    if (yymsp[-3].minor.yy157 == NULL) { yylhsminor.yy157 = yymsp[-1].minor.yy157; }
    else { sql_src_t *s = yymsp[-3].minor.yy157; while (s->next) s = s->next; s->next = yymsp[-1].minor.yy157; yylhsminor.yy157 = yymsp[-3].minor.yy157; }
}
#line 3520 "sql_grammar.c"
  yymsp[-3].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (41) join_op ::= TK_JOIN */
static void yy_rule_41(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 339 "sql_grammar.lime"
{ yymsp[0].minor.yy220 = SX_J_INNER; }
#line 3548 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (42) join_op ::= TK_INNER TK_JOIN */
static void yy_rule_42(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 340 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = SX_J_INNER; }
#line 3575 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (43) join_op ::= TK_LEFT TK_JOIN */
static void yy_rule_43(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 341 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = SX_J_LEFT; }
#line 3602 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (44) join_op ::= TK_LEFT TK_OUTER TK_JOIN */
static void yy_rule_44(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 342 "sql_grammar.lime"
{ yymsp[-2].minor.yy220 = SX_J_LEFT; }
#line 3629 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (45) join_op ::= TK_RIGHT TK_JOIN */
static void yy_rule_45(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 343 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = SX_J_RIGHT; }
#line 3656 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (46) join_op ::= TK_RIGHT TK_OUTER TK_JOIN */
static void yy_rule_46(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 344 "sql_grammar.lime"
{ yymsp[-2].minor.yy220 = SX_J_RIGHT; }
#line 3683 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (47) join_op ::= TK_FULL TK_JOIN */
static void yy_rule_47(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 345 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = SX_J_FULL; }
#line 3710 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (48) join_op ::= TK_FULL TK_OUTER TK_JOIN */
static void yy_rule_48(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 346 "sql_grammar.lime"
{ yymsp[-2].minor.yy220 = SX_J_FULL; }
#line 3737 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (49) join_op ::= TK_CROSS TK_JOIN */
static void yy_rule_49(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 347 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = SX_J_CROSS; }
#line 3764 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (50) join_op ::= TK_NATURAL TK_JOIN */
static void yy_rule_50(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 348 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = SX_J_NATURAL; }
#line 3791 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (51) join_op ::= TK_NATURAL TK_LEFT TK_JOIN */
static void yy_rule_51(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 349 "sql_grammar.lime"
{ yymsp[-2].minor.yy220 = SX_J_NATURAL; }
#line 3818 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (52) join_constraint ::= */
static void yy_rule_52(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 352 "sql_grammar.lime"
{ yymsp[1].minor.yy192 = NULL; }
#line 3845 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (53) join_constraint ::= TK_ON expr */
static void yy_rule_53(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 353 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = yymsp[0].minor.yy192; }
#line 3872 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (54) join_constraint ::= TK_USING TK_LP id_list TK_RP */
static void yy_rule_54(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 354 "sql_grammar.lime"
{ yymsp[-3].minor.yy192 = NULL; }
#line 3899 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (55) table_ref ::= TK_ID */
static void yy_rule_55(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 356 "sql_grammar.lime"
{
    yylhsminor.yy157 = AST(pstate, sql_src_t); if (yylhsminor.yy157) yylhsminor.yy157->table = tok_str(yymsp[0].minor.yy0);
}
#line 3928 "sql_grammar.c"
  yymsp[0].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (56) table_ref ::= TK_ID TK_DOT TK_ID */
static void yy_rule_56(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 359 "sql_grammar.lime"
{
    yylhsminor.yy157 = AST(pstate, sql_src_t); if (yylhsminor.yy157) { yylhsminor.yy157->schema = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy157->table = tok_str(yymsp[0].minor.yy0); }
}
#line 3958 "sql_grammar.c"
  yymsp[-2].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (57) table_ref ::= TK_ID TK_AS TK_ID */
static void yy_rule_57(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 362 "sql_grammar.lime"
{
    yylhsminor.yy157 = AST(pstate, sql_src_t); if (yylhsminor.yy157) { yylhsminor.yy157->table = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy157->alias = tok_str(yymsp[0].minor.yy0); }
}
#line 3988 "sql_grammar.c"
  yymsp[-2].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (58) table_ref ::= TK_ID TK_ID */
static void yy_rule_58(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 365 "sql_grammar.lime"
{
    yylhsminor.yy157 = AST(pstate, sql_src_t); if (yylhsminor.yy157) { yylhsminor.yy157->table = tok_str(yymsp[-1].minor.yy0); yylhsminor.yy157->alias = tok_str(yymsp[0].minor.yy0); }
}
#line 4018 "sql_grammar.c"
  yymsp[-1].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (59) table_ref ::= TK_LP select_stmt TK_RP */
static void yy_rule_59(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 368 "sql_grammar.lime"
{
    yylhsminor.yy157 = AST(pstate, sql_src_t);
    if (yylhsminor.yy157) { yylhsminor.yy157->subquery = yymsp[-1].minor.yy261; yylhsminor.yy157->subsrc = yymsp[-2].minor.yy0.p; yylhsminor.yy157->subsrclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 4049 "sql_grammar.c"
  yymsp[-2].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (60) table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID */
static void yy_rule_60(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 372 "sql_grammar.lime"
{
    yylhsminor.yy157 = AST(pstate, sql_src_t);
    if (yylhsminor.yy157) { yylhsminor.yy157->subquery = yymsp[-3].minor.yy261; yylhsminor.yy157->alias = tok_str(yymsp[0].minor.yy0);
             yylhsminor.yy157->subsrc = yymsp[-4].minor.yy0.p; yylhsminor.yy157->subsrclen = (uint32_t)((yymsp[-2].minor.yy0.p + yymsp[-2].minor.yy0.len) - yymsp[-4].minor.yy0.p); }
}
#line 4081 "sql_grammar.c"
  yymsp[-4].minor.yy157 = yylhsminor.yy157;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (61) where_opt ::= */
static void yy_rule_61(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 378 "sql_grammar.lime"
{ yymsp[1].minor.yy192 = NULL; }
#line 4109 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (62) where_opt ::= TK_WHERE expr */
static void yy_rule_62(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 379 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = yymsp[0].minor.yy192; }
#line 4136 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (63) group_opt ::= */
static void yy_rule_63(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 381 "sql_grammar.lime"
{ yymsp[1].minor.yy116 = NULL; }
#line 4163 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (64) group_opt ::= TK_GROUP TK_BY expr_list */
static void yy_rule_64(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 382 "sql_grammar.lime"
{ yymsp[-2].minor.yy116 = yymsp[0].minor.yy116; }
#line 4190 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (65) having_opt ::= */
static void yy_rule_65(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 384 "sql_grammar.lime"
{ yymsp[1].minor.yy192 = NULL; }
#line 4217 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (66) having_opt ::= TK_HAVING expr */
static void yy_rule_66(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 385 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = yymsp[0].minor.yy192; }
#line 4244 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (67) order_opt ::= */
static void yy_rule_67(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 387 "sql_grammar.lime"
{ yymsp[1].minor.yy116 = NULL; }
#line 4271 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (68) order_opt ::= TK_ORDER TK_BY order_list */
static void yy_rule_68(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 388 "sql_grammar.lime"
{ yymsp[-2].minor.yy116 = yymsp[0].minor.yy116; }
#line 4298 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (69) order_list ::= order_item */
static void yy_rule_69(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 390 "sql_grammar.lime"
{ yylhsminor.yy116 = mk_list(pstate); if (yylhsminor.yy116 && yymsp[0].minor.yy298) { yylhsminor.yy116->head = yylhsminor.yy116->tail = yymsp[0].minor.yy298; yylhsminor.yy116->n = 1; } }
#line 4325 "sql_grammar.c"
  yymsp[0].minor.yy116 = yylhsminor.yy116;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (70) order_list ::= order_list TK_COMMA order_item */
static void yy_rule_70(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 391 "sql_grammar.lime"
{
    yylhsminor.yy116 = yymsp[-2].minor.yy116; if (yylhsminor.yy116 && yymsp[0].minor.yy298) { if (yylhsminor.yy116->tail) yylhsminor.yy116->tail->next = yymsp[0].minor.yy298; else yylhsminor.yy116->head = yymsp[0].minor.yy298; yylhsminor.yy116->tail = yymsp[0].minor.yy298; yylhsminor.yy116->n++; }
}
#line 4355 "sql_grammar.c"
  yymsp[-2].minor.yy116 = yylhsminor.yy116;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (71) order_item ::= expr */
static void yy_rule_71(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 395 "sql_grammar.lime"
{ yylhsminor.yy298 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy298) { yylhsminor.yy298->expr = yymsp[0].minor.yy192; yylhsminor.yy298->sort = 1; } }
#line 4383 "sql_grammar.c"
  yymsp[0].minor.yy298 = yylhsminor.yy298;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (72) order_item ::= expr TK_ASC */
static void yy_rule_72(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 396 "sql_grammar.lime"
{ yylhsminor.yy298 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy298) { yylhsminor.yy298->expr = yymsp[-1].minor.yy192; yylhsminor.yy298->sort = 1; } }
#line 4411 "sql_grammar.c"
  yymsp[-1].minor.yy298 = yylhsminor.yy298;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (73) order_item ::= expr TK_DESC */
static void yy_rule_73(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 397 "sql_grammar.lime"
{ yylhsminor.yy298 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy298) { yylhsminor.yy298->expr = yymsp[-1].minor.yy192; yylhsminor.yy298->sort = 2; } }
#line 4439 "sql_grammar.c"
  yymsp[-1].minor.yy298 = yylhsminor.yy298;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (74) limit_opt ::= */
static void yy_rule_74(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 399 "sql_grammar.lime"
{ yymsp[1].minor.yy192 = NULL; }
#line 4467 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (75) limit_opt ::= TK_LIMIT expr */
static void yy_rule_75(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 400 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = yymsp[0].minor.yy192; }
#line 4494 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (76) offset_part ::= */
static void yy_rule_76(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 402 "sql_grammar.lime"
{ yymsp[1].minor.yy192 = NULL; }
#line 4521 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (77) offset_part ::= TK_OFFSET expr */
static void yy_rule_77(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 403 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = yymsp[0].minor.yy192; }
#line 4548 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (78) offset_part ::= TK_COMMA expr */
static void yy_rule_78(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 404 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = yymsp[0].minor.yy192; }
#line 4575 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (79) with_clause ::= TK_WITH cte_list */
static void yy_rule_79(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 412 "sql_grammar.lime"
{ yymsp[-1].minor.yy339.list = yymsp[0].minor.yy277; yymsp[-1].minor.yy339.rec = 0; }
#line 4602 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (80) with_clause ::= TK_WITH TK_RECURSIVE cte_list */
static void yy_rule_80(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 413 "sql_grammar.lime"
{ yymsp[-2].minor.yy339.list = yymsp[0].minor.yy277; yymsp[-2].minor.yy339.rec = 1; }
#line 4629 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (81) cte_list ::= cte */
static void yy_rule_81(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 415 "sql_grammar.lime"
{ yylhsminor.yy277 = yymsp[0].minor.yy277; }
#line 4656 "sql_grammar.c"
  yymsp[0].minor.yy277 = yylhsminor.yy277;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (82) cte_list ::= cte_list TK_COMMA cte */
static void yy_rule_82(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 416 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy277 == NULL) { yylhsminor.yy277 = yymsp[0].minor.yy277; }
    else { sql_cte_t *s = yymsp[-2].minor.yy277; while (s->next) s = s->next; s->next = yymsp[0].minor.yy277; yylhsminor.yy277 = yymsp[-2].minor.yy277; }
}
#line 4687 "sql_grammar.c"
  yymsp[-2].minor.yy277 = yylhsminor.yy277;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (83) cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP */
static void yy_rule_83(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 421 "sql_grammar.lime"
{
    yylhsminor.yy277 = AST(pstate, sql_cte_t); if (yylhsminor.yy277) { yylhsminor.yy277->name = tok_str(yymsp[-4].minor.yy0); yylhsminor.yy277->select = yymsp[-1].minor.yy261; }
}
#line 4717 "sql_grammar.c"
  yymsp[-4].minor.yy277 = yylhsminor.yy277;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (84) cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP */
static void yy_rule_84(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 424 "sql_grammar.lime"
{
    yylhsminor.yy277 = AST(pstate, sql_cte_t); if (yylhsminor.yy277) { yylhsminor.yy277->name = tok_str(yymsp[-7].minor.yy0); yylhsminor.yy277->cols = yymsp[-5].minor.yy116; yylhsminor.yy277->select = yymsp[-1].minor.yy261; }
}
#line 4747 "sql_grammar.c"
  yymsp[-7].minor.yy277 = yylhsminor.yy277;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (85) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list */
static void yy_rule_85(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 430 "sql_grammar.lime"
{
    sql_insert_t *ins = yymsp[0].minor.yy278 ? yymsp[0].minor.yy278 : AST(pstate, sql_insert_t);
    yymsp[-4].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy97 && ins) {
        ins->schema = yymsp[-2].minor.yy289.schema; ins->table = yymsp[-2].minor.yy289.table; ins->cols = yymsp[-1].minor.yy116;
        yymsp[-4].minor.yy97->kind = SQL_KIND_INSERT; yymsp[-4].minor.yy97->u.insert = ins;
    }
}
#line 4782 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (86) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES */
static void yy_rule_86(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 438 "sql_grammar.lime"
{
    sql_insert_t *ins = AST(pstate, sql_insert_t);
    yymsp[-5].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-5].minor.yy97 && ins) {
        ins->schema = yymsp[-3].minor.yy289.schema; ins->table = yymsp[-3].minor.yy289.table; ins->cols = yymsp[-2].minor.yy116; ins->def_values = 1;
        yymsp[-5].minor.yy97->kind = SQL_KIND_INSERT; yymsp[-5].minor.yy97->u.insert = ins;
    }
}
#line 4816 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (87) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt */
static void yy_rule_87(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 446 "sql_grammar.lime"
{
    sql_insert_t *ins = AST(pstate, sql_insert_t);
    yymsp[-4].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy97 && ins) {
        ins->schema = yymsp[-2].minor.yy289.schema; ins->table = yymsp[-2].minor.yy289.table; ins->cols = yymsp[-1].minor.yy116; ins->select = yymsp[0].minor.yy261;
        yymsp[-4].minor.yy97->kind = SQL_KIND_INSERT; yymsp[-4].minor.yy97->u.insert = ins;
    }
}
#line 4850 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (88) insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list */
static void yy_rule_88(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 454 "sql_grammar.lime"
{
    sql_insert_t *ins = yymsp[0].minor.yy278 ? yymsp[0].minor.yy278 : AST(pstate, sql_insert_t);
    yymsp[-4].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy97 && ins) {
        ins->schema = yymsp[-2].minor.yy289.schema; ins->table = yymsp[-2].minor.yy289.table; ins->cols = yymsp[-1].minor.yy116; ins->replace = 1;
        yymsp[-4].minor.yy97->kind = SQL_KIND_INSERT; yymsp[-4].minor.yy97->u.insert = ins;
    }
}
#line 4884 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (89) qname ::= TK_ID */
static void yy_rule_89(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 464 "sql_grammar.lime"
{ yylhsminor.yy289.schema.p = NULL; yylhsminor.yy289.schema.len = 0; yylhsminor.yy289.table = tok_str(yymsp[0].minor.yy0); }
#line 4911 "sql_grammar.c"
  yymsp[0].minor.yy289 = yylhsminor.yy289;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (90) qname ::= TK_ID TK_DOT TK_ID */
static void yy_rule_90(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 465 "sql_grammar.lime"
{ yylhsminor.yy289.schema = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy289.table = tok_str(yymsp[0].minor.yy0); }
#line 4939 "sql_grammar.c"
  yymsp[-2].minor.yy289 = yylhsminor.yy289;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (91) insert_cols ::= */
static void yy_rule_91(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 467 "sql_grammar.lime"
{ yymsp[1].minor.yy116 = NULL; }
#line 4967 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (92) insert_cols ::= TK_LP id_list TK_RP */
static void yy_rule_92(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 468 "sql_grammar.lime"
{ yymsp[-2].minor.yy116 = yymsp[-1].minor.yy116; }
#line 4994 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (93) values_list ::= TK_VALUES TK_LP expr_list TK_RP */
static void yy_rule_93(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 470 "sql_grammar.lime"
{
    yymsp[-3].minor.yy278 = AST(pstate, sql_insert_t);
    if (yymsp[-3].minor.yy278) {
        yymsp[-3].minor.yy278->rows = (sql_exprlist_t **)sql_arena_alloc(pstate->arena, sizeof(sql_exprlist_t *));
        if (yymsp[-3].minor.yy278->rows) { yymsp[-3].minor.yy278->rows[0] = yymsp[-1].minor.yy116; yymsp[-3].minor.yy278->n_rows = 1; }
    }
}
#line 5027 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (94) values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP */
static void yy_rule_94(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 477 "sql_grammar.lime"
{
    yylhsminor.yy278 = yymsp[-4].minor.yy278;
    if (yylhsminor.yy278) {
        sql_exprlist_t **nr = (sql_exprlist_t **)sql_arena_alloc(pstate->arena,
            sizeof(sql_exprlist_t *) * (size_t)(yylhsminor.yy278->n_rows + 1));
        if (nr) {
            int i; for (i = 0; i < yylhsminor.yy278->n_rows; i++) nr[i] = yylhsminor.yy278->rows[i];
            nr[yylhsminor.yy278->n_rows] = yymsp[-1].minor.yy116; yylhsminor.yy278->rows = nr; yylhsminor.yy278->n_rows++;
        }
    }
}
#line 5064 "sql_grammar.c"
  yymsp[-4].minor.yy278 = yylhsminor.yy278;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (95) update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt */
static void yy_rule_95(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 491 "sql_grammar.lime"
{
    sql_update_t *up = AST(pstate, sql_update_t);
    yymsp[-4].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy97 && up) {
        up->schema = yymsp[-3].minor.yy289.schema; up->table = yymsp[-3].minor.yy289.table; up->sets = yymsp[-1].minor.yy262; up->where = yymsp[0].minor.yy192;
        yymsp[-4].minor.yy97->kind = SQL_KIND_UPDATE; yymsp[-4].minor.yy97->u.update = up;
    }
}
#line 5099 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (96) assign_list ::= assign */
static void yy_rule_96(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 500 "sql_grammar.lime"
{ yylhsminor.yy262 = yymsp[0].minor.yy262; }
#line 5126 "sql_grammar.c"
  yymsp[0].minor.yy262 = yylhsminor.yy262;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (97) assign_list ::= assign_list TK_COMMA assign */
static void yy_rule_97(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 501 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy262 == NULL) { yylhsminor.yy262 = yymsp[0].minor.yy262; }
    else { sql_assign_t *s = yymsp[-2].minor.yy262; while (s->next) s = s->next; s->next = yymsp[0].minor.yy262; yylhsminor.yy262 = yymsp[-2].minor.yy262; }
}
#line 5157 "sql_grammar.c"
  yymsp[-2].minor.yy262 = yylhsminor.yy262;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (98) assign ::= TK_ID TK_EQ expr */
static void yy_rule_98(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 506 "sql_grammar.lime"
{
    yylhsminor.yy262 = AST(pstate, sql_assign_t); if (yylhsminor.yy262) { yylhsminor.yy262->col = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy262->val = yymsp[0].minor.yy192; }
}
#line 5187 "sql_grammar.c"
  yymsp[-2].minor.yy262 = yylhsminor.yy262;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (99) delete_stmt ::= TK_DELETE TK_FROM qname where_opt */
static void yy_rule_99(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 512 "sql_grammar.lime"
{
    sql_delete_t *d = AST(pstate, sql_delete_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97 && d) {
        d->schema = yymsp[-1].minor.yy289.schema; d->table = yymsp[-1].minor.yy289.table; d->where = yymsp[0].minor.yy192;
        yymsp[-3].minor.yy97->kind = SQL_KIND_DELETE; yymsp[-3].minor.yy97->u.del = d;
    }
}
#line 5222 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (100) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP */
static void yy_rule_100(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 524 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-7].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-7].minor.yy97 && cr) {
        cr->kind = SX_CR_TABLE; cr->temp = yymsp[-6].minor.yy220; cr->if_not_exists = yymsp[-4].minor.yy220;
        cr->schema = yymsp[-3].minor.yy289.schema; cr->name = yymsp[-3].minor.yy289.table; cr->cols = yymsp[-1].minor.yy182;
        yymsp[-7].minor.yy97->kind = SQL_KIND_CREATE; yymsp[-7].minor.yy97->u.create = cr;
    }
}
#line 5257 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (101) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt */
static void yy_rule_101(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 534 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-6].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-6].minor.yy97 && cr) {
        cr->kind = SX_CR_TABLE_AS; cr->temp = yymsp[-5].minor.yy220; cr->if_not_exists = yymsp[-3].minor.yy220;
        cr->schema = yymsp[-2].minor.yy289.schema; cr->name = yymsp[-2].minor.yy289.table; cr->select = yymsp[0].minor.yy261;
        yymsp[-6].minor.yy97->kind = SQL_KIND_CREATE; yymsp[-6].minor.yy97->u.create = cr;
    }
}
#line 5292 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (102) create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP */
static void yy_rule_102(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 544 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-9].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-9].minor.yy97 && cr) {
        cr->kind = SX_CR_INDEX; cr->unique = yymsp[-8].minor.yy220; cr->if_not_exists = yymsp[-6].minor.yy220;
        cr->schema = yymsp[-5].minor.yy289.schema; cr->name = yymsp[-5].minor.yy289.table; cr->on_table = tok_str(yymsp[-3].minor.yy0);
        cr->index_cols = yymsp[-1].minor.yy116;
        yymsp[-9].minor.yy97->kind = SQL_KIND_CREATE; yymsp[-9].minor.yy97->u.create = cr;
    }
}
#line 5328 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (103) create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt */
static void yy_rule_103(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 555 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-6].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-6].minor.yy97 && cr) {
        cr->kind = SX_CR_VIEW; cr->temp = yymsp[-5].minor.yy220; cr->if_not_exists = yymsp[-3].minor.yy220;
        cr->schema = yymsp[-2].minor.yy289.schema; cr->name = yymsp[-2].minor.yy289.table; cr->select = yymsp[0].minor.yy261;
        yymsp[-6].minor.yy97->kind = SQL_KIND_CREATE; yymsp[-6].minor.yy97->u.create = cr;
    }
}
#line 5363 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (104) temp_opt ::= */
static void yy_rule_104(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 568 "sql_grammar.lime"
{ yymsp[1].minor.yy220 = 0; }
#line 5390 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (105) temp_opt ::= TK_TEMP */
static void yy_rule_105(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 569 "sql_grammar.lime"
{ yymsp[0].minor.yy220 = 1; }
#line 5417 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (106) temp_opt ::= TK_TEMPORARY */
static void yy_rule_106(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 570 "sql_grammar.lime"
{ yymsp[0].minor.yy220 = 1; }
#line 5444 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (107) unique_opt ::= */
static void yy_rule_107(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 572 "sql_grammar.lime"
{ yymsp[1].minor.yy220 = 0; }
#line 5471 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (108) unique_opt ::= TK_UNIQUE */
static void yy_rule_108(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 573 "sql_grammar.lime"
{ yymsp[0].minor.yy220 = 1; }
#line 5498 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (109) if_not_exists ::= */
static void yy_rule_109(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 575 "sql_grammar.lime"
{ yymsp[1].minor.yy220 = 0; }
#line 5525 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (110) if_not_exists ::= TK_IF TK_NOT TK_EXISTS */
static void yy_rule_110(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 576 "sql_grammar.lime"
{ yymsp[-2].minor.yy220 = 1; }
#line 5552 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (111) table_body ::= table_elem */
static void yy_rule_111(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 588 "sql_grammar.lime"
{ yylhsminor.yy182 = yymsp[0].minor.yy182; }
#line 5579 "sql_grammar.c"
  yymsp[0].minor.yy182 = yylhsminor.yy182;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (112) table_body ::= table_body TK_COMMA table_elem */
static void yy_rule_112(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 589 "sql_grammar.lime"
{
    if (yymsp[0].minor.yy182 == NULL) { yylhsminor.yy182 = yymsp[-2].minor.yy182; }                    /* a constraint: no column */
    else if (yymsp[-2].minor.yy182 == NULL) { yylhsminor.yy182 = yymsp[0].minor.yy182; }
    else { sql_coldef_t *s = yymsp[-2].minor.yy182; while (s->next) s = s->next; s->next = yymsp[0].minor.yy182; yylhsminor.yy182 = yymsp[-2].minor.yy182; }
}
#line 5611 "sql_grammar.c"
  yymsp[-2].minor.yy182 = yylhsminor.yy182;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (113) table_elem ::= coldef */
static void yy_rule_113(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 595 "sql_grammar.lime"
{ yylhsminor.yy182 = yymsp[0].minor.yy182; }
#line 5639 "sql_grammar.c"
  yymsp[0].minor.yy182 = yylhsminor.yy182;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (114) table_elem ::= table_constraint */
static void yy_rule_114(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 596 "sql_grammar.lime"
{ yymsp[0].minor.yy182 = NULL; }
#line 5667 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (115) coldef ::= TK_ID type_opt col_constraints */
static void yy_rule_115(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 598 "sql_grammar.lime"
{
    yylhsminor.yy182 = AST(pstate, sql_coldef_t);
    if (yylhsminor.yy182) { yylhsminor.yy182->name = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy182->type = yymsp[-1].minor.yy77;
             yylhsminor.yy182->primary = (yymsp[0].minor.yy220 & 1) != 0; yylhsminor.yy182->notnull = (yymsp[0].minor.yy220 & 2) != 0;
             yylhsminor.yy182->unique = (yymsp[0].minor.yy220 & 4) != 0; }
}
#line 5699 "sql_grammar.c"
  yymsp[-2].minor.yy182 = yylhsminor.yy182;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (116) type_opt ::= */
static void yy_rule_116(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 606 "sql_grammar.lime"
{ yymsp[1].minor.yy77.p = NULL; yymsp[1].minor.yy77.len = 0; }
#line 5727 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (117) type_opt ::= TK_ID */
static void yy_rule_117(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 607 "sql_grammar.lime"
{ yylhsminor.yy77 = tok_str(yymsp[0].minor.yy0); }
#line 5754 "sql_grammar.c"
  yymsp[0].minor.yy77 = yylhsminor.yy77;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (118) type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP */
static void yy_rule_118(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 608 "sql_grammar.lime"
{ yylhsminor.yy77 = tok_str(yymsp[-3].minor.yy0); }
#line 5782 "sql_grammar.c"
  yymsp[-3].minor.yy77 = yylhsminor.yy77;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (119) type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP */
static void yy_rule_119(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 609 "sql_grammar.lime"
{ yylhsminor.yy77 = tok_str(yymsp[-5].minor.yy0); }
#line 5810 "sql_grammar.c"
  yymsp[-5].minor.yy77 = yylhsminor.yy77;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (120) col_constraints ::= */
static void yy_rule_120(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 613 "sql_grammar.lime"
{ yymsp[1].minor.yy220 = 0; }
#line 5838 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (121) col_constraints ::= col_constraints col_constraint */
static void yy_rule_121(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 614 "sql_grammar.lime"
{ yylhsminor.yy220 = yymsp[-1].minor.yy220 | yymsp[0].minor.yy220; }
#line 5865 "sql_grammar.c"
  yymsp[-1].minor.yy220 = yylhsminor.yy220;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (122) col_constraint ::= TK_PRIMARY TK_KEY */
static void yy_rule_122(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 616 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = 1; }
#line 5893 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (123) col_constraint ::= TK_PRIMARY TK_KEY TK_ASC */
static void yy_rule_123(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 617 "sql_grammar.lime"
{ yymsp[-2].minor.yy220 = 1; }
#line 5920 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (124) col_constraint ::= TK_PRIMARY TK_KEY TK_DESC */
static void yy_rule_124(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 618 "sql_grammar.lime"
{ yymsp[-2].minor.yy220 = 1; }
#line 5947 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (125) col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR */
static void yy_rule_125(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 619 "sql_grammar.lime"
{ yymsp[-2].minor.yy220 = 1; }
#line 5974 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (126) col_constraint ::= TK_NOT TK_NULL */
static void yy_rule_126(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 620 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = 2; }
#line 6001 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (127) col_constraint ::= TK_UNIQUE */
static void yy_rule_127(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 621 "sql_grammar.lime"
{ yymsp[0].minor.yy220 = 4; }
#line 6028 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (128) col_constraint ::= TK_DEFAULT expr_atom */
static void yy_rule_128(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 622 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = 0; }
#line 6055 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (129) col_constraint ::= TK_CHECK TK_LP expr TK_RP */
static void yy_rule_129(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 623 "sql_grammar.lime"
{ yymsp[-3].minor.yy220 = 0; }
#line 6082 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (130) col_constraint ::= TK_REFERENCES qname */
static void yy_rule_130(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 624 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = 0; }
#line 6109 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (131) col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP */
static void yy_rule_131(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 625 "sql_grammar.lime"
{ yymsp[-4].minor.yy220 = 0; }
#line 6136 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (132) drop_stmt ::= TK_DROP TK_TABLE if_exists qname */
static void yy_rule_132(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 635 "sql_grammar.lime"
{
    sql_drop_t *d = AST(pstate, sql_drop_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97 && d) { d->kind = SX_DR_TABLE; d->if_exists = yymsp[-1].minor.yy220; d->schema = yymsp[0].minor.yy289.schema; d->name = yymsp[0].minor.yy289.table;
                  yymsp[-3].minor.yy97->kind = SQL_KIND_DROP; yymsp[-3].minor.yy97->u.drop = d; }
}
#line 6168 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (133) drop_stmt ::= TK_DROP TK_INDEX if_exists qname */
static void yy_rule_133(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 641 "sql_grammar.lime"
{
    sql_drop_t *d = AST(pstate, sql_drop_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97 && d) { d->kind = SX_DR_INDEX; d->if_exists = yymsp[-1].minor.yy220; d->schema = yymsp[0].minor.yy289.schema; d->name = yymsp[0].minor.yy289.table;
                  yymsp[-3].minor.yy97->kind = SQL_KIND_DROP; yymsp[-3].minor.yy97->u.drop = d; }
}
#line 6200 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (134) drop_stmt ::= TK_DROP TK_VIEW if_exists qname */
static void yy_rule_134(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 647 "sql_grammar.lime"
{
    sql_drop_t *d = AST(pstate, sql_drop_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97 && d) { d->kind = SX_DR_VIEW; d->if_exists = yymsp[-1].minor.yy220; d->schema = yymsp[0].minor.yy289.schema; d->name = yymsp[0].minor.yy289.table;
                  yymsp[-3].minor.yy97->kind = SQL_KIND_DROP; yymsp[-3].minor.yy97->u.drop = d; }
}
#line 6232 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (135) drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname */
static void yy_rule_135(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 653 "sql_grammar.lime"
{
    sql_drop_t *d = AST(pstate, sql_drop_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97 && d) { d->kind = SX_DR_TRIGGER; d->if_exists = yymsp[-1].minor.yy220; d->schema = yymsp[0].minor.yy289.schema; d->name = yymsp[0].minor.yy289.table;
                  yymsp[-3].minor.yy97->kind = SQL_KIND_DROP; yymsp[-3].minor.yy97->u.drop = d; }
}
#line 6264 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (136) alter_stmt ::= TK_ALTER TK_TABLE qname TK_RENAME TK_TO qname */
static void yy_rule_136(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 662 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-5].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-5].minor.yy97 && cr) { cr->name = yymsp[-3].minor.yy289.table; cr->on_table = yymsp[0].minor.yy289.table;
                   yymsp[-5].minor.yy97->kind = SQL_KIND_ALTER; yymsp[-5].minor.yy97->u.create = cr; }
}
#line 6296 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (137) if_exists ::= */
static void yy_rule_137(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 670 "sql_grammar.lime"
{ yymsp[1].minor.yy220 = 0; }
#line 6323 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (138) if_exists ::= TK_IF TK_EXISTS */
static void yy_rule_138(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 671 "sql_grammar.lime"
{ yymsp[-1].minor.yy220 = 1; }
#line 6350 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (139) pragma_stmt ::= TK_PRAGMA qname */
static void yy_rule_139(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 675 "sql_grammar.lime"
{
    sql_pragma_t *p = AST(pstate, sql_pragma_t);
    yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-1].minor.yy97 && p) { p->schema = yymsp[0].minor.yy289.schema; p->name = yymsp[0].minor.yy289.table; yymsp[-1].minor.yy97->kind = SQL_KIND_PRAGMA; yymsp[-1].minor.yy97->readonly = 1; yymsp[-1].minor.yy97->u.pragma = p; }
}
#line 6381 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (140) pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom */
static void yy_rule_140(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 680 "sql_grammar.lime"
{
    sql_pragma_t *p = AST(pstate, sql_pragma_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97 && p) { p->schema = yymsp[-2].minor.yy289.schema; p->name = yymsp[-2].minor.yy289.table; p->value = yymsp[0].minor.yy192; yymsp[-3].minor.yy97->kind = SQL_KIND_PRAGMA; yymsp[-3].minor.yy97->u.pragma = p; }
}
#line 6412 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (141) pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP */
static void yy_rule_141(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 685 "sql_grammar.lime"
{
    sql_pragma_t *p = AST(pstate, sql_pragma_t);
    yymsp[-4].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy97 && p) { p->schema = yymsp[-3].minor.yy289.schema; p->name = yymsp[-3].minor.yy289.table; p->value = yymsp[-1].minor.yy192; yymsp[-4].minor.yy97->kind = SQL_KIND_PRAGMA; yymsp[-4].minor.yy97->u.pragma = p; }
}
#line 6443 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (142) begin_stmt ::= TK_BEGIN */
static void yy_rule_142(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 693 "sql_grammar.lime"
{ yymsp[0].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[0].minor.yy97) yymsp[0].minor.yy97->kind = SQL_KIND_BEGIN; }
#line 6470 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (143) begin_stmt ::= TK_BEGIN TK_TRANSACTION */
static void yy_rule_143(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 694 "sql_grammar.lime"
{ yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy97) yymsp[-1].minor.yy97->kind = SQL_KIND_BEGIN; }
#line 6497 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (144) begin_stmt ::= TK_BEGIN TK_ID */
static void yy_rule_144(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 695 "sql_grammar.lime"
{ yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy97) yymsp[-1].minor.yy97->kind = SQL_KIND_BEGIN; }
#line 6524 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (145) begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION */
static void yy_rule_145(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 696 "sql_grammar.lime"
{ yymsp[-2].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[-2].minor.yy97) yymsp[-2].minor.yy97->kind = SQL_KIND_BEGIN; }
#line 6551 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (146) commit_stmt ::= TK_COMMIT */
static void yy_rule_146(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 698 "sql_grammar.lime"
{ yymsp[0].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[0].minor.yy97) yymsp[0].minor.yy97->kind = SQL_KIND_COMMIT; }
#line 6578 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (147) commit_stmt ::= TK_COMMIT TK_TRANSACTION */
static void yy_rule_147(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 699 "sql_grammar.lime"
{ yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy97) yymsp[-1].minor.yy97->kind = SQL_KIND_COMMIT; }
#line 6605 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (148) commit_stmt ::= TK_END */
static void yy_rule_148(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 700 "sql_grammar.lime"
{ yymsp[0].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[0].minor.yy97) yymsp[0].minor.yy97->kind = SQL_KIND_COMMIT; }
#line 6632 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (149) commit_stmt ::= TK_END TK_TRANSACTION */
static void yy_rule_149(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 701 "sql_grammar.lime"
{ yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy97) yymsp[-1].minor.yy97->kind = SQL_KIND_COMMIT; }
#line 6659 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (150) rollback_stmt ::= TK_ROLLBACK */
static void yy_rule_150(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 703 "sql_grammar.lime"
{ yymsp[0].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[0].minor.yy97) yymsp[0].minor.yy97->kind = SQL_KIND_ROLLBACK; }
#line 6686 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (151) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION */
static void yy_rule_151(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 704 "sql_grammar.lime"
{ yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy97) yymsp[-1].minor.yy97->kind = SQL_KIND_ROLLBACK; }
#line 6713 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (152) rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID */
static void yy_rule_152(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 705 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-2].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-2].minor.yy97) { yymsp[-2].minor.yy97->kind = SQL_KIND_ROLLBACK; yymsp[-2].minor.yy97->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6743 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (153) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID */
static void yy_rule_153(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 709 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97) { yymsp[-3].minor.yy97->kind = SQL_KIND_ROLLBACK; yymsp[-3].minor.yy97->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6773 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (154) rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID */
static void yy_rule_154(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 713 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97) { yymsp[-3].minor.yy97->kind = SQL_KIND_ROLLBACK; yymsp[-3].minor.yy97->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6803 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (155) savepoint_stmt ::= TK_SAVEPOINT TK_ID */
static void yy_rule_155(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 718 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-1].minor.yy97) { yymsp[-1].minor.yy97->kind = SQL_KIND_SAVEPOINT; yymsp[-1].minor.yy97->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6833 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (156) release_stmt ::= TK_RELEASE TK_ID */
static void yy_rule_156(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 723 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-1].minor.yy97) { yymsp[-1].minor.yy97->kind = SQL_KIND_RELEASE; yymsp[-1].minor.yy97->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6863 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (157) release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID */
static void yy_rule_157(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 727 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-2].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-2].minor.yy97) { yymsp[-2].minor.yy97->kind = SQL_KIND_RELEASE; yymsp[-2].minor.yy97->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6893 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (158) attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID */
static void yy_rule_158(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 732 "sql_grammar.lime"
{
    sql_attach_t *at = AST(pstate, sql_attach_t);
    yymsp[-3].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy97 && at) { at->target = yymsp[-2].minor.yy192; at->alias = tok_str(yymsp[0].minor.yy0); yymsp[-3].minor.yy97->kind = SQL_KIND_ATTACH; yymsp[-3].minor.yy97->u.attach = at; }
}
#line 6924 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (159) attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID */
static void yy_rule_159(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 737 "sql_grammar.lime"
{
    sql_attach_t *at = AST(pstate, sql_attach_t);
    yymsp[-4].minor.yy97 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy97 && at) { at->target = yymsp[-2].minor.yy192; at->alias = tok_str(yymsp[0].minor.yy0); yymsp[-4].minor.yy97->kind = SQL_KIND_ATTACH; yymsp[-4].minor.yy97->u.attach = at; }
}
#line 6955 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (160) detach_stmt ::= TK_DETACH TK_ID */
static void yy_rule_160(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 743 "sql_grammar.lime"
{
    yymsp[-1].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy97) { yymsp[-1].minor.yy97->kind = SQL_KIND_DETACH; yymsp[-1].minor.yy97->u.detach = tok_str(yymsp[0].minor.yy0); }
}
#line 6984 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (161) detach_stmt ::= TK_DETACH TK_DATABASE TK_ID */
static void yy_rule_161(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 746 "sql_grammar.lime"
{
    yymsp[-2].minor.yy97 = AST(pstate, sql_stmt_t); if (yymsp[-2].minor.yy97) { yymsp[-2].minor.yy97->kind = SQL_KIND_DETACH; yymsp[-2].minor.yy97->u.detach = tok_str(yymsp[0].minor.yy0); }
}
#line 7013 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (162) explain_stmt ::= TK_EXPLAIN stmt */
static void yy_rule_162(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 750 "sql_grammar.lime"
{
    yymsp[-1].minor.yy97 = yymsp[0].minor.yy97; if (yymsp[-1].minor.yy97) { yymsp[-1].minor.yy97->explain = 1; yymsp[-1].minor.yy97->kind = SQL_KIND_EXPLAIN; yymsp[-1].minor.yy97->readonly = 1; }
}
#line 7042 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (163) explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt */
static void yy_rule_163(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 753 "sql_grammar.lime"
{
    yymsp[-3].minor.yy97 = yymsp[0].minor.yy97; if (yymsp[-3].minor.yy97) { yymsp[-3].minor.yy97->explain = 2; yymsp[-3].minor.yy97->kind = SQL_KIND_EXPLAIN; yymsp[-3].minor.yy97->readonly = 1; }
}
#line 7071 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (164) id_list ::= TK_ID */
static void yy_rule_164(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 759 "sql_grammar.lime"
{
    sql_expr_t *c = mk_expr(pstate, SX_E_COLUMN);
    if (c) { c->name[0] = tok_str(yymsp[0].minor.yy0); c->nname = 1; }
    yylhsminor.yy116 = list_add(pstate, NULL, c);
}
#line 7102 "sql_grammar.c"
  yymsp[0].minor.yy116 = yylhsminor.yy116;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (165) id_list ::= id_list TK_COMMA TK_ID */
static void yy_rule_165(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 764 "sql_grammar.lime"
{
    sql_expr_t *c = mk_expr(pstate, SX_E_COLUMN);
    if (c) { c->name[0] = tok_str(yymsp[0].minor.yy0); c->nname = 1; }
    yylhsminor.yy116 = list_add(pstate, yymsp[-2].minor.yy116, c);
}
#line 7134 "sql_grammar.c"
  yymsp[-2].minor.yy116 = yylhsminor.yy116;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (166) expr_list ::= expr */
static void yy_rule_166(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 770 "sql_grammar.lime"
{ yylhsminor.yy116 = list_add(pstate, NULL, yymsp[0].minor.yy192); }
#line 7162 "sql_grammar.c"
  yymsp[0].minor.yy116 = yylhsminor.yy116;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (167) expr_list ::= expr_list TK_COMMA expr */
static void yy_rule_167(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 771 "sql_grammar.lime"
{ yylhsminor.yy116 = list_add(pstate, yymsp[-2].minor.yy116, yymsp[0].minor.yy192); }
#line 7190 "sql_grammar.c"
  yymsp[-2].minor.yy116 = yylhsminor.yy116;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (168) expr ::= expr_atom */
static void yy_rule_168(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 773 "sql_grammar.lime"
{ yylhsminor.yy192 = yymsp[0].minor.yy192; }
#line 7218 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (169) expr ::= TK_LP expr TK_RP */
static void yy_rule_169(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 774 "sql_grammar.lime"
{
    yylhsminor.yy192 = yymsp[-1].minor.yy192;
    /* Grow the span to include the parentheses so a parenthesized
     * select item is named with them, as SQLite does ((a+1)*2). */
    if (yylhsminor.yy192) { yylhsminor.yy192->src = yymsp[-2].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 7251 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (170) expr ::= TK_LP select_stmt TK_RP */
static void yy_rule_170(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 780 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_SUBQUERY);
    if (yylhsminor.yy192) { yylhsminor.yy192->sel = yymsp[-1].minor.yy261; yylhsminor.yy192->src = yymsp[-2].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 7282 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (171) expr ::= TK_NOT expr */
static void yy_rule_171(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 784 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = mk_unary(pstate, TK_NOT, yymsp[0].minor.yy192); }
#line 7310 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (172) expr ::= TK_MINUS expr */
static void yy_rule_172(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 785 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = mk_unary(pstate, TK_MINUS, yymsp[0].minor.yy192); }
#line 7337 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (173) expr ::= TK_PLUS expr */
static void yy_rule_173(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 786 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = mk_unary(pstate, TK_PLUS, yymsp[0].minor.yy192); }
#line 7364 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (174) expr ::= TK_TILDE expr */
static void yy_rule_174(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 787 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = mk_unary(pstate, TK_TILDE, yymsp[0].minor.yy192); }
#line 7391 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (175) expr ::= expr TK_OR expr */
static void yy_rule_175(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 788 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_OR, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7418 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (176) expr ::= expr TK_AND expr */
static void yy_rule_176(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 789 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_AND, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7446 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (177) expr ::= expr TK_EQ expr */
static void yy_rule_177(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 790 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_EQ, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7474 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (178) expr ::= expr TK_NE expr */
static void yy_rule_178(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 791 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_NE, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7502 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (179) expr ::= expr TK_LT expr */
static void yy_rule_179(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 792 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_LT, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7530 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (180) expr ::= expr TK_LE expr */
static void yy_rule_180(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 793 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_LE, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7558 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (181) expr ::= expr TK_GT expr */
static void yy_rule_181(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 794 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_GT, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7586 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (182) expr ::= expr TK_GE expr */
static void yy_rule_182(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 795 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_GE, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7614 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (183) expr ::= expr TK_PLUS expr */
static void yy_rule_183(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 796 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_PLUS, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7642 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (184) expr ::= expr TK_MINUS expr */
static void yy_rule_184(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 797 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_MINUS, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7670 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (185) expr ::= expr TK_STAR expr */
static void yy_rule_185(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 798 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_STAR, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7698 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (186) expr ::= expr TK_SLASH expr */
static void yy_rule_186(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 799 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_SLASH, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7726 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (187) expr ::= expr TK_PERCENT expr */
static void yy_rule_187(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 800 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_PERCENT, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7754 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (188) expr ::= expr TK_CONCAT expr */
static void yy_rule_188(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 801 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_CONCAT, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7782 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (189) expr ::= expr TK_AMP expr */
static void yy_rule_189(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 802 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_AMP, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7810 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (190) expr ::= expr TK_PIPE expr */
static void yy_rule_190(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 803 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_PIPE, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7838 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (191) expr ::= expr TK_LIKE expr */
static void yy_rule_191(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 804 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_LIKE, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7866 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (192) expr ::= expr TK_IS TK_NULL */
static void yy_rule_192(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 805 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_IS_NULL); if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-2].minor.yy192; yylhsminor.yy192->ival = 0; } }
#line 7894 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (193) expr ::= expr TK_IS TK_NOT TK_NULL */
static void yy_rule_193(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 806 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_IS_NULL); if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-3].minor.yy192; yylhsminor.yy192->ival = 1; } }
#line 7922 "sql_grammar.c"
  yymsp[-3].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (194) expr ::= expr TK_NOT TK_NULL */
static void yy_rule_194(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 807 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_IS_NULL); if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-2].minor.yy192; yylhsminor.yy192->ival = 1; } }
#line 7950 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (195) expr ::= expr TK_IS expr */
static void yy_rule_195(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 808 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_binary(pstate, TK_IS, yymsp[-2].minor.yy192, yymsp[0].minor.yy192); }
#line 7978 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (196) expr ::= expr TK_IN TK_LP expr_list TK_RP */
static void yy_rule_196(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 809 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_IN_LIST); if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-4].minor.yy192; yylhsminor.yy192->list = yymsp[-1].minor.yy116; }
}
#line 8008 "sql_grammar.c"
  yymsp[-4].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (197) expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP */
static void yy_rule_197(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 812 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_IN_LIST); if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-5].minor.yy192; yylhsminor.yy192->list = yymsp[-1].minor.yy116; yylhsminor.yy192->ival = 1; }
}
#line 8038 "sql_grammar.c"
  yymsp[-5].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (198) expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP */
static void yy_rule_198(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 815 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_IN_SELECT);
    if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-5].minor.yy192; yylhsminor.yy192->sel = yymsp[-1].minor.yy261; yylhsminor.yy192->ival = 1;
             yylhsminor.yy192->src = yymsp[-2].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 8070 "sql_grammar.c"
  yymsp[-5].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (199) expr ::= expr TK_IN TK_LP select_stmt TK_RP */
static void yy_rule_199(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 820 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_IN_SELECT);
    if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-4].minor.yy192; yylhsminor.yy192->sel = yymsp[-1].minor.yy261; yylhsminor.yy192->src = yymsp[-2].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 8101 "sql_grammar.c"
  yymsp[-4].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (200) expr ::= expr TK_BETWEEN expr TK_AND expr */
static void yy_rule_200(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 824 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_BETWEEN); if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-4].minor.yy192; yylhsminor.yy192->b = yymsp[-2].minor.yy192; yylhsminor.yy192->c = yymsp[0].minor.yy192; }
}
#line 8131 "sql_grammar.c"
  yymsp[-4].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (201) expr ::= TK_CASE expr_atom case_arms case_else TK_END */
static void yy_rule_201(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 827 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_CASE);
    if (yylhsminor.yy192) { yylhsminor.yy192->a = yymsp[-3].minor.yy192; yylhsminor.yy192->arms = yymsp[-2].minor.yy76; yylhsminor.yy192->els = yymsp[-1].minor.yy192;
             yylhsminor.yy192->src = yymsp[-4].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-4].minor.yy0.p); }
}
#line 8163 "sql_grammar.c"
  yymsp[-4].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (202) expr ::= TK_CASE case_arms case_else TK_END */
static void yy_rule_202(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 832 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_CASE);
    if (yylhsminor.yy192) { yylhsminor.yy192->arms = yymsp[-2].minor.yy76; yylhsminor.yy192->els = yymsp[-1].minor.yy192;
             yylhsminor.yy192->src = yymsp[-3].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-3].minor.yy0.p); }
}
#line 8195 "sql_grammar.c"
  yymsp[-3].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (203) expr ::= TK_ID TK_LP TK_RP */
static void yy_rule_203(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 837 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_FUNC); if (yylhsminor.yy192) { yylhsminor.yy192->name[0] = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy192->nname = 1; }
    if (yylhsminor.yy192) { yylhsminor.yy192->src = yymsp[-2].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 8226 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (204) expr ::= TK_ID TK_LP TK_STAR TK_RP */
static void yy_rule_204(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 841 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_FUNC); if (yylhsminor.yy192) { yylhsminor.yy192->name[0] = tok_str(yymsp[-3].minor.yy0); yylhsminor.yy192->nname = 1; yylhsminor.yy192->ival = 2; }
    if (yylhsminor.yy192) { yylhsminor.yy192->src = yymsp[-3].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-3].minor.yy0.p); }
}
#line 8257 "sql_grammar.c"
  yymsp[-3].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (205) expr ::= TK_ID TK_LP expr_list TK_RP */
static void yy_rule_205(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 845 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_FUNC); if (yylhsminor.yy192) { yylhsminor.yy192->name[0] = tok_str(yymsp[-3].minor.yy0); yylhsminor.yy192->nname = 1; yylhsminor.yy192->list = yymsp[-1].minor.yy116; }
    if (yylhsminor.yy192) { yylhsminor.yy192->src = yymsp[-3].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-3].minor.yy0.p); }
}
#line 8288 "sql_grammar.c"
  yymsp[-3].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (206) expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP */
static void yy_rule_206(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 849 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_FUNC); if (yylhsminor.yy192) { yylhsminor.yy192->name[0] = tok_str(yymsp[-4].minor.yy0); yylhsminor.yy192->nname = 1; yylhsminor.yy192->list = yymsp[-1].minor.yy116; yylhsminor.yy192->ival = 1; }
    if (yylhsminor.yy192) { yylhsminor.yy192->src = yymsp[-4].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-4].minor.yy0.p); }
}
#line 8319 "sql_grammar.c"
  yymsp[-4].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (207) case_arms ::= TK_WHEN expr TK_THEN expr */
static void yy_rule_207(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 854 "sql_grammar.lime"
{
    yymsp[-3].minor.yy76 = AST(pstate, sql_case_arm_t); if (yymsp[-3].minor.yy76) { yymsp[-3].minor.yy76->when = yymsp[-2].minor.yy192; yymsp[-3].minor.yy76->then = yymsp[0].minor.yy192; }
}
#line 8349 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (208) case_arms ::= case_arms TK_WHEN expr TK_THEN expr */
static void yy_rule_208(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 857 "sql_grammar.lime"
{
    sql_case_arm_t *arm = AST(pstate, sql_case_arm_t);
    if (arm) { arm->when = yymsp[-2].minor.yy192; arm->then = yymsp[0].minor.yy192; }
    if (yymsp[-4].minor.yy76 == NULL) { yylhsminor.yy76 = arm; }
    else { sql_case_arm_t *s = yymsp[-4].minor.yy76; while (s->next) s = s->next; s->next = arm; yylhsminor.yy76 = yymsp[-4].minor.yy76; }
}
#line 8381 "sql_grammar.c"
  yymsp[-4].minor.yy76 = yylhsminor.yy76;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (209) case_else ::= */
static void yy_rule_209(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 864 "sql_grammar.lime"
{ yymsp[1].minor.yy192 = NULL; }
#line 8409 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (210) case_else ::= TK_ELSE expr */
static void yy_rule_210(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 865 "sql_grammar.lime"
{ yymsp[-1].minor.yy192 = yymsp[0].minor.yy192; }
#line 8436 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (211) expr_atom ::= TK_NUMBER */
static void yy_rule_211(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 867 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_NUMBER); if (yylhsminor.yy192) { yylhsminor.yy192->lit = tok_str(yymsp[0].minor.yy0); span_tok(yylhsminor.yy192, yymsp[0].minor.yy0); } }
#line 8463 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (212) expr_atom ::= TK_STRING */
static void yy_rule_212(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 868 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_STRING); if (yylhsminor.yy192) { yylhsminor.yy192->lit = tok_str_unquote(yymsp[0].minor.yy0); span_tok(yylhsminor.yy192, yymsp[0].minor.yy0); } }
#line 8491 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (213) expr_atom ::= TK_BLOB */
static void yy_rule_213(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 869 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_BLOB);   if (yylhsminor.yy192) { yylhsminor.yy192->lit = tok_str(yymsp[0].minor.yy0); span_tok(yylhsminor.yy192, yymsp[0].minor.yy0); } }
#line 8519 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (214) expr_atom ::= TK_NULL */
static void yy_rule_214(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 870 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_NULL); span_tok(yylhsminor.yy192, yymsp[0].minor.yy0); }
#line 8547 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (215) expr_atom ::= TK_TRUE */
static void yy_rule_215(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 871 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_BOOL); if (yylhsminor.yy192) { yylhsminor.yy192->ival = 1; span_tok(yylhsminor.yy192, yymsp[0].minor.yy0); } }
#line 8575 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (216) expr_atom ::= TK_FALSE */
static void yy_rule_216(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 872 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_BOOL); if (yylhsminor.yy192) { yylhsminor.yy192->ival = 0; span_tok(yylhsminor.yy192, yymsp[0].minor.yy0); } }
#line 8603 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (217) expr_atom ::= TK_QMARK */
static void yy_rule_217(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 873 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_PARAM); span_tok(yylhsminor.yy192, yymsp[0].minor.yy0); }
#line 8631 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (218) expr_atom ::= TK_ID */
static void yy_rule_218(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 874 "sql_grammar.lime"
{ yylhsminor.yy192 = mk_expr(pstate, SX_E_COLUMN); if (yylhsminor.yy192) { yylhsminor.yy192->name[0] = tok_str(yymsp[0].minor.yy0); yylhsminor.yy192->nname = 1; span_tok(yylhsminor.yy192, yymsp[0].minor.yy0); } }
#line 8659 "sql_grammar.c"
  yymsp[0].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (219) expr_atom ::= TK_ID TK_DOT TK_ID */
static void yy_rule_219(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 875 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_COLUMN); if (yylhsminor.yy192) { yylhsminor.yy192->name[0] = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy192->name[1] = tok_str(yymsp[0].minor.yy0); yylhsminor.yy192->nname = 2;
                                              yylhsminor.yy192->src = yymsp[-2].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 8690 "sql_grammar.c"
  yymsp[-2].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (220) expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID */
static void yy_rule_220(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 879 "sql_grammar.lime"
{
    yylhsminor.yy192 = mk_expr(pstate, SX_E_COLUMN); if (yylhsminor.yy192) { yylhsminor.yy192->name[0] = tok_str(yymsp[-4].minor.yy0); yylhsminor.yy192->name[1] = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy192->name[2] = tok_str(yymsp[0].minor.yy0); yylhsminor.yy192->nname = 3;
                                              yylhsminor.yy192->src = yymsp[-4].minor.yy0.p; yylhsminor.yy192->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-4].minor.yy0.p); }
}
#line 8721 "sql_grammar.c"
  yymsp[-4].minor.yy192 = yylhsminor.yy192;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (221) maybe_semi ::= */
static void yy_rule_221(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (222) maybe_semi ::= TK_SEMI */
static void yy_rule_222(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (223) table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP */
static void yy_rule_223(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (224) table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP */
static void yy_rule_224(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (225) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname */
static void yy_rule_225(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (226) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP */
static void yy_rule_226(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (227) table_constraint ::= TK_CHECK TK_LP expr TK_RP */
static void yy_rule_227(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* Dispatch table -- one entry per rule, parallel to
** yyRuleInfoLhs[] / yyRuleInfoNRhs[].  Composition merges
** grammars by concatenating these arrays. */
static void (*const yy_rule_reduce_fn[])(yy_reduce_ctx *) = {
  yy_rule_0,  /* (0) input ::= stmt_list maybe_semi */
  yy_rule_1,  /* (1) stmt_list ::= stmt */
  yy_rule_2,  /* (2) stmt_list ::= stmt_list TK_SEMI stmt */
  yy_rule_3,  /* (3) stmt ::= select_stmt */
  yy_rule_4,  /* (4) stmt ::= insert_stmt */
  yy_rule_5,  /* (5) stmt ::= update_stmt */
  yy_rule_6,  /* (6) stmt ::= delete_stmt */
  yy_rule_7,  /* (7) stmt ::= create_stmt */
  yy_rule_8,  /* (8) stmt ::= drop_stmt */
  yy_rule_9,  /* (9) stmt ::= alter_stmt */
  yy_rule_10,  /* (10) stmt ::= pragma_stmt */
  yy_rule_11,  /* (11) stmt ::= begin_stmt */
  yy_rule_12,  /* (12) stmt ::= commit_stmt */
  yy_rule_13,  /* (13) stmt ::= rollback_stmt */
  yy_rule_14,  /* (14) stmt ::= savepoint_stmt */
  yy_rule_15,  /* (15) stmt ::= release_stmt */
  yy_rule_16,  /* (16) stmt ::= attach_stmt */
  yy_rule_17,  /* (17) stmt ::= detach_stmt */
  yy_rule_18,  /* (18) stmt ::= explain_stmt */
  yy_rule_19,  /* (19) select_stmt ::= with_clause select_stmt */
  yy_rule_20,  /* (20) select_stmt ::= select_core */
  yy_rule_21,  /* (21) select_stmt ::= select_core TK_UNION select_stmt */
  yy_rule_22,  /* (22) select_stmt ::= select_core TK_UNION TK_ALL select_stmt */
  yy_rule_23,  /* (23) select_stmt ::= select_core TK_INTERSECT select_stmt */
  yy_rule_24,  /* (24) select_stmt ::= select_core TK_EXCEPT select_stmt */
  yy_rule_25,  /* (25) select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part */
  yy_rule_26,  /* (26) distinct_opt ::= */
  yy_rule_27,  /* (27) distinct_opt ::= TK_DISTINCT */
  yy_rule_28,  /* (28) distinct_opt ::= TK_ALL */
  yy_rule_29,  /* (29) sel_list ::= sel_item */
  yy_rule_30,  /* (30) sel_list ::= sel_list TK_COMMA sel_item */
  yy_rule_31,  /* (31) sel_item ::= TK_STAR */
  yy_rule_32,  /* (32) sel_item ::= TK_ID TK_DOT TK_STAR */
  yy_rule_33,  /* (33) sel_item ::= expr */
  yy_rule_34,  /* (34) sel_item ::= expr TK_AS TK_ID */
  yy_rule_35,  /* (35) sel_item ::= expr TK_ID */
  yy_rule_36,  /* (36) from_opt ::= */
  yy_rule_37,  /* (37) from_opt ::= TK_FROM table_list */
  yy_rule_38,  /* (38) table_list ::= table_ref */
  yy_rule_39,  /* (39) table_list ::= table_list TK_COMMA table_ref */
  yy_rule_40,  /* (40) table_list ::= table_list join_op table_ref join_constraint */
  yy_rule_41,  /* (41) join_op ::= TK_JOIN */
  yy_rule_42,  /* (42) join_op ::= TK_INNER TK_JOIN */
  yy_rule_43,  /* (43) join_op ::= TK_LEFT TK_JOIN */
  yy_rule_44,  /* (44) join_op ::= TK_LEFT TK_OUTER TK_JOIN */
  yy_rule_45,  /* (45) join_op ::= TK_RIGHT TK_JOIN */
  yy_rule_46,  /* (46) join_op ::= TK_RIGHT TK_OUTER TK_JOIN */
  yy_rule_47,  /* (47) join_op ::= TK_FULL TK_JOIN */
  yy_rule_48,  /* (48) join_op ::= TK_FULL TK_OUTER TK_JOIN */
  yy_rule_49,  /* (49) join_op ::= TK_CROSS TK_JOIN */
  yy_rule_50,  /* (50) join_op ::= TK_NATURAL TK_JOIN */
  yy_rule_51,  /* (51) join_op ::= TK_NATURAL TK_LEFT TK_JOIN */
  yy_rule_52,  /* (52) join_constraint ::= */
  yy_rule_53,  /* (53) join_constraint ::= TK_ON expr */
  yy_rule_54,  /* (54) join_constraint ::= TK_USING TK_LP id_list TK_RP */
  yy_rule_55,  /* (55) table_ref ::= TK_ID */
  yy_rule_56,  /* (56) table_ref ::= TK_ID TK_DOT TK_ID */
  yy_rule_57,  /* (57) table_ref ::= TK_ID TK_AS TK_ID */
  yy_rule_58,  /* (58) table_ref ::= TK_ID TK_ID */
  yy_rule_59,  /* (59) table_ref ::= TK_LP select_stmt TK_RP */
  yy_rule_60,  /* (60) table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID */
  yy_rule_61,  /* (61) where_opt ::= */
  yy_rule_62,  /* (62) where_opt ::= TK_WHERE expr */
  yy_rule_63,  /* (63) group_opt ::= */
  yy_rule_64,  /* (64) group_opt ::= TK_GROUP TK_BY expr_list */
  yy_rule_65,  /* (65) having_opt ::= */
  yy_rule_66,  /* (66) having_opt ::= TK_HAVING expr */
  yy_rule_67,  /* (67) order_opt ::= */
  yy_rule_68,  /* (68) order_opt ::= TK_ORDER TK_BY order_list */
  yy_rule_69,  /* (69) order_list ::= order_item */
  yy_rule_70,  /* (70) order_list ::= order_list TK_COMMA order_item */
  yy_rule_71,  /* (71) order_item ::= expr */
  yy_rule_72,  /* (72) order_item ::= expr TK_ASC */
  yy_rule_73,  /* (73) order_item ::= expr TK_DESC */
  yy_rule_74,  /* (74) limit_opt ::= */
  yy_rule_75,  /* (75) limit_opt ::= TK_LIMIT expr */
  yy_rule_76,  /* (76) offset_part ::= */
  yy_rule_77,  /* (77) offset_part ::= TK_OFFSET expr */
  yy_rule_78,  /* (78) offset_part ::= TK_COMMA expr */
  yy_rule_79,  /* (79) with_clause ::= TK_WITH cte_list */
  yy_rule_80,  /* (80) with_clause ::= TK_WITH TK_RECURSIVE cte_list */
  yy_rule_81,  /* (81) cte_list ::= cte */
  yy_rule_82,  /* (82) cte_list ::= cte_list TK_COMMA cte */
  yy_rule_83,  /* (83) cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP */
  yy_rule_84,  /* (84) cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP */
  yy_rule_85,  /* (85) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list */
  yy_rule_86,  /* (86) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES */
  yy_rule_87,  /* (87) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt */
  yy_rule_88,  /* (88) insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list */
  yy_rule_89,  /* (89) qname ::= TK_ID */
  yy_rule_90,  /* (90) qname ::= TK_ID TK_DOT TK_ID */
  yy_rule_91,  /* (91) insert_cols ::= */
  yy_rule_92,  /* (92) insert_cols ::= TK_LP id_list TK_RP */
  yy_rule_93,  /* (93) values_list ::= TK_VALUES TK_LP expr_list TK_RP */
  yy_rule_94,  /* (94) values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP */
  yy_rule_95,  /* (95) update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt */
  yy_rule_96,  /* (96) assign_list ::= assign */
  yy_rule_97,  /* (97) assign_list ::= assign_list TK_COMMA assign */
  yy_rule_98,  /* (98) assign ::= TK_ID TK_EQ expr */
  yy_rule_99,  /* (99) delete_stmt ::= TK_DELETE TK_FROM qname where_opt */
  yy_rule_100,  /* (100) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP */
  yy_rule_101,  /* (101) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt */
  yy_rule_102,  /* (102) create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP */
  yy_rule_103,  /* (103) create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt */
  yy_rule_104,  /* (104) temp_opt ::= */
  yy_rule_105,  /* (105) temp_opt ::= TK_TEMP */
  yy_rule_106,  /* (106) temp_opt ::= TK_TEMPORARY */
  yy_rule_107,  /* (107) unique_opt ::= */
  yy_rule_108,  /* (108) unique_opt ::= TK_UNIQUE */
  yy_rule_109,  /* (109) if_not_exists ::= */
  yy_rule_110,  /* (110) if_not_exists ::= TK_IF TK_NOT TK_EXISTS */
  yy_rule_111,  /* (111) table_body ::= table_elem */
  yy_rule_112,  /* (112) table_body ::= table_body TK_COMMA table_elem */
  yy_rule_113,  /* (113) table_elem ::= coldef */
  yy_rule_114,  /* (114) table_elem ::= table_constraint */
  yy_rule_115,  /* (115) coldef ::= TK_ID type_opt col_constraints */
  yy_rule_116,  /* (116) type_opt ::= */
  yy_rule_117,  /* (117) type_opt ::= TK_ID */
  yy_rule_118,  /* (118) type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP */
  yy_rule_119,  /* (119) type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP */
  yy_rule_120,  /* (120) col_constraints ::= */
  yy_rule_121,  /* (121) col_constraints ::= col_constraints col_constraint */
  yy_rule_122,  /* (122) col_constraint ::= TK_PRIMARY TK_KEY */
  yy_rule_123,  /* (123) col_constraint ::= TK_PRIMARY TK_KEY TK_ASC */
  yy_rule_124,  /* (124) col_constraint ::= TK_PRIMARY TK_KEY TK_DESC */
  yy_rule_125,  /* (125) col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR */
  yy_rule_126,  /* (126) col_constraint ::= TK_NOT TK_NULL */
  yy_rule_127,  /* (127) col_constraint ::= TK_UNIQUE */
  yy_rule_128,  /* (128) col_constraint ::= TK_DEFAULT expr_atom */
  yy_rule_129,  /* (129) col_constraint ::= TK_CHECK TK_LP expr TK_RP */
  yy_rule_130,  /* (130) col_constraint ::= TK_REFERENCES qname */
  yy_rule_131,  /* (131) col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP */
  yy_rule_132,  /* (132) drop_stmt ::= TK_DROP TK_TABLE if_exists qname */
  yy_rule_133,  /* (133) drop_stmt ::= TK_DROP TK_INDEX if_exists qname */
  yy_rule_134,  /* (134) drop_stmt ::= TK_DROP TK_VIEW if_exists qname */
  yy_rule_135,  /* (135) drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname */
  yy_rule_136,  /* (136) alter_stmt ::= TK_ALTER TK_TABLE qname TK_RENAME TK_TO qname */
  yy_rule_137,  /* (137) if_exists ::= */
  yy_rule_138,  /* (138) if_exists ::= TK_IF TK_EXISTS */
  yy_rule_139,  /* (139) pragma_stmt ::= TK_PRAGMA qname */
  yy_rule_140,  /* (140) pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom */
  yy_rule_141,  /* (141) pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP */
  yy_rule_142,  /* (142) begin_stmt ::= TK_BEGIN */
  yy_rule_143,  /* (143) begin_stmt ::= TK_BEGIN TK_TRANSACTION */
  yy_rule_144,  /* (144) begin_stmt ::= TK_BEGIN TK_ID */
  yy_rule_145,  /* (145) begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION */
  yy_rule_146,  /* (146) commit_stmt ::= TK_COMMIT */
  yy_rule_147,  /* (147) commit_stmt ::= TK_COMMIT TK_TRANSACTION */
  yy_rule_148,  /* (148) commit_stmt ::= TK_END */
  yy_rule_149,  /* (149) commit_stmt ::= TK_END TK_TRANSACTION */
  yy_rule_150,  /* (150) rollback_stmt ::= TK_ROLLBACK */
  yy_rule_151,  /* (151) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION */
  yy_rule_152,  /* (152) rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID */
  yy_rule_153,  /* (153) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID */
  yy_rule_154,  /* (154) rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID */
  yy_rule_155,  /* (155) savepoint_stmt ::= TK_SAVEPOINT TK_ID */
  yy_rule_156,  /* (156) release_stmt ::= TK_RELEASE TK_ID */
  yy_rule_157,  /* (157) release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID */
  yy_rule_158,  /* (158) attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID */
  yy_rule_159,  /* (159) attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID */
  yy_rule_160,  /* (160) detach_stmt ::= TK_DETACH TK_ID */
  yy_rule_161,  /* (161) detach_stmt ::= TK_DETACH TK_DATABASE TK_ID */
  yy_rule_162,  /* (162) explain_stmt ::= TK_EXPLAIN stmt */
  yy_rule_163,  /* (163) explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt */
  yy_rule_164,  /* (164) id_list ::= TK_ID */
  yy_rule_165,  /* (165) id_list ::= id_list TK_COMMA TK_ID */
  yy_rule_166,  /* (166) expr_list ::= expr */
  yy_rule_167,  /* (167) expr_list ::= expr_list TK_COMMA expr */
  yy_rule_168,  /* (168) expr ::= expr_atom */
  yy_rule_169,  /* (169) expr ::= TK_LP expr TK_RP */
  yy_rule_170,  /* (170) expr ::= TK_LP select_stmt TK_RP */
  yy_rule_171,  /* (171) expr ::= TK_NOT expr */
  yy_rule_172,  /* (172) expr ::= TK_MINUS expr */
  yy_rule_173,  /* (173) expr ::= TK_PLUS expr */
  yy_rule_174,  /* (174) expr ::= TK_TILDE expr */
  yy_rule_175,  /* (175) expr ::= expr TK_OR expr */
  yy_rule_176,  /* (176) expr ::= expr TK_AND expr */
  yy_rule_177,  /* (177) expr ::= expr TK_EQ expr */
  yy_rule_178,  /* (178) expr ::= expr TK_NE expr */
  yy_rule_179,  /* (179) expr ::= expr TK_LT expr */
  yy_rule_180,  /* (180) expr ::= expr TK_LE expr */
  yy_rule_181,  /* (181) expr ::= expr TK_GT expr */
  yy_rule_182,  /* (182) expr ::= expr TK_GE expr */
  yy_rule_183,  /* (183) expr ::= expr TK_PLUS expr */
  yy_rule_184,  /* (184) expr ::= expr TK_MINUS expr */
  yy_rule_185,  /* (185) expr ::= expr TK_STAR expr */
  yy_rule_186,  /* (186) expr ::= expr TK_SLASH expr */
  yy_rule_187,  /* (187) expr ::= expr TK_PERCENT expr */
  yy_rule_188,  /* (188) expr ::= expr TK_CONCAT expr */
  yy_rule_189,  /* (189) expr ::= expr TK_AMP expr */
  yy_rule_190,  /* (190) expr ::= expr TK_PIPE expr */
  yy_rule_191,  /* (191) expr ::= expr TK_LIKE expr */
  yy_rule_192,  /* (192) expr ::= expr TK_IS TK_NULL */
  yy_rule_193,  /* (193) expr ::= expr TK_IS TK_NOT TK_NULL */
  yy_rule_194,  /* (194) expr ::= expr TK_NOT TK_NULL */
  yy_rule_195,  /* (195) expr ::= expr TK_IS expr */
  yy_rule_196,  /* (196) expr ::= expr TK_IN TK_LP expr_list TK_RP */
  yy_rule_197,  /* (197) expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP */
  yy_rule_198,  /* (198) expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP */
  yy_rule_199,  /* (199) expr ::= expr TK_IN TK_LP select_stmt TK_RP */
  yy_rule_200,  /* (200) expr ::= expr TK_BETWEEN expr TK_AND expr */
  yy_rule_201,  /* (201) expr ::= TK_CASE expr_atom case_arms case_else TK_END */
  yy_rule_202,  /* (202) expr ::= TK_CASE case_arms case_else TK_END */
  yy_rule_203,  /* (203) expr ::= TK_ID TK_LP TK_RP */
  yy_rule_204,  /* (204) expr ::= TK_ID TK_LP TK_STAR TK_RP */
  yy_rule_205,  /* (205) expr ::= TK_ID TK_LP expr_list TK_RP */
  yy_rule_206,  /* (206) expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP */
  yy_rule_207,  /* (207) case_arms ::= TK_WHEN expr TK_THEN expr */
  yy_rule_208,  /* (208) case_arms ::= case_arms TK_WHEN expr TK_THEN expr */
  yy_rule_209,  /* (209) case_else ::= */
  yy_rule_210,  /* (210) case_else ::= TK_ELSE expr */
  yy_rule_211,  /* (211) expr_atom ::= TK_NUMBER */
  yy_rule_212,  /* (212) expr_atom ::= TK_STRING */
  yy_rule_213,  /* (213) expr_atom ::= TK_BLOB */
  yy_rule_214,  /* (214) expr_atom ::= TK_NULL */
  yy_rule_215,  /* (215) expr_atom ::= TK_TRUE */
  yy_rule_216,  /* (216) expr_atom ::= TK_FALSE */
  yy_rule_217,  /* (217) expr_atom ::= TK_QMARK */
  yy_rule_218,  /* (218) expr_atom ::= TK_ID */
  yy_rule_219,  /* (219) expr_atom ::= TK_ID TK_DOT TK_ID */
  yy_rule_220,  /* (220) expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID */
  yy_rule_221,  /* (221) maybe_semi ::= */
  yy_rule_222,  /* (222) maybe_semi ::= TK_SEMI */
  yy_rule_223,  /* (223) table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP */
  yy_rule_224,  /* (224) table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP */
  yy_rule_225,  /* (225) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname */
  yy_rule_226,  /* (226) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP */
  yy_rule_227,  /* (227) table_constraint ::= TK_CHECK TK_LP expr TK_RP */
};
/********** End per-rule reduce action functions ******************************/

/*
** Perform a reduce action and the shift that must immediately
** follow the reduce.
**
** The yyLookahead and yyLookaheadToken parameters provide reduce actions
** access to the lookahead token (if any).  The yyLookahead will be YYNOCODE
** if the lookahead token has already been consumed.  As this procedure is
** only called from one place, optimizing compilers will in-line it, which
** means that the extra parameters have no performance impact.
*/
static YYACTIONTYPE yy_reduce(
  yyParser *yypParser,         /* The parser */
  unsigned int yyruleno,       /* Number of the rule by which to reduce */
  int yyLookahead,             /* Lookahead token, or YYNOCODE if none */
  SqlParseTOKENTYPE yyLookaheadToken  /* Value of the lookahead token */
  SqlParseCTX_PDECL                   /* %extra_context */
){
  int yygoto;                     /* The next state */
  YYACTIONTYPE yyact;             /* The next action */
  yyStackEntry *yymsp;            /* The top of the parser's stack */
  int yysize;                     /* Amount to pop the stack */
#ifdef YYLOCATIONTYPE
  /* P0-NEW-7: per-reduce LHS-location local.  Bound to @$ /
  ** @<lhsalias> in user action bodies, so the action sees
  ** Bison's documented pre-action default and may overwrite
  ** it.  Computed below before the action switch; committed
  ** to the LHS slot's yyloc after the stack adjustment. */
  YYLOCATIONTYPE yyloc_lhs;
# ifndef YYRHSLOC
#  define YYRHSLOC(Rhs, K) ((Rhs)[K])
# endif
#endif
  (void)yyLookahead;
  (void)yyLookaheadToken;
  yymsp = yypParser->yytos;

#ifdef YYLOCATIONTYPE
  /* Compute the LHS location BEFORE the action body, matching
  ** Bison's documented YYLLOC_DEFAULT ordering (P0-NEW-7).  The
  ** action body's @$ writes go to yyloc_lhs and are preserved;
  ** see P0-NEW-6 for why this matters (ecpg's preproc.y has 70+
  ** non-trivial @$ assignments that build source-text
  ** accumulation across each reduce). */
  {
    int yyN = -yyRuleInfoNRhs[yyruleno];   /* nrhs >= 0 */
# ifdef YYLLOC_DEFAULT
    /* User-defined override.  Build a 0-indexed YYLOCATIONTYPE
    ** array per Bison's signature: Rhs[i] for i in 1..N is the
    ** i-th RHS's location, Rhs[0] is the slot below the rule
    ** (used for empty-rule fallback). */
    YYLOCATIONTYPE yyloc_rhs[YYNRHS_MAX + 1];
    int yi;
    yyloc_rhs[0] = yymsp[-yyN].yyloc;
    for( yi=1; yi<=yyN; yi++ ){
      yyloc_rhs[yi] = yymsp[yi-yyN].yyloc;
    }
    YYLLOC_DEFAULT(yyloc_lhs, yyloc_rhs, yyN);
# else
    /* Built-in default: Rhs[1] for non-empty, lookahead for
    ** empty.  Same observable result as the prior post-action
    ** slot-reuse logic. */
    if( yyN == 0 ){
      yyloc_lhs = yypParser->yyLookaheadLoc;
    }else{
      yyloc_lhs = yymsp[1-yyN].yyloc;
    }
# endif
  }
#endif

  /* v0.6.0: dispatch the rule's reduction via the per-rule
  ** function pointer table.  The bookkeeping below (LHS lookup,
  ** stack pop+push, location commit) is unchanged. */
  assert( yyruleno<sizeof(yyRuleInfoLhs)/sizeof(yyRuleInfoLhs[0]) );
  assert( yyruleno<(sizeof(yy_rule_reduce_fn)/sizeof(yy_rule_reduce_fn[0])) );
  {
    yy_reduce_ctx yy_ctx;
    yy_ctx.yypParser = yypParser;
    yy_ctx.yymsp = yymsp;
    yy_ctx.yyLookahead = yyLookahead;
    yy_ctx.yyLookaheadToken = yyLookaheadToken;
#ifdef YYLOCATIONTYPE
    yy_ctx.yyloc_lhs_ptr = &yyloc_lhs;
#endif
    yy_rule_reduce_fn[yyruleno](&yy_ctx);
  }

  yygoto = yyRuleInfoLhs[yyruleno];
  yysize = yyRuleInfoNRhs[yyruleno];
  yyact = yy_find_reduce_action(yymsp[yysize].stateno,(YYCODETYPE)yygoto);

  /* There are no SHIFTREDUCE actions on nonterminals because the table
  ** generator has simplified them to pure REDUCE actions. */
  assert( !(yyact>YY_MAX_SHIFT && yyact<=YY_MAX_SHIFTREDUCE) );

  /* It is not possible for a REDUCE to be followed by an error */
  assert( yyact!=YY_ERROR_ACTION );

  yymsp += yysize+1;
  yypParser->yytos = yymsp;
  yymsp->stateno = (YYACTIONTYPE)yyact;
  yymsp->major = (YYCODETYPE)yygoto;
#ifdef YYLOCATIONTYPE
  /* P0-NEW-7: commit the LHS location computed before the
  ** action body (and possibly overwritten by the action via
  ** @$ / @<lhsalias>) to the LHS slot's yyloc field. */
  yymsp->yyloc = yyloc_lhs;
#endif
  yyTraceShift(yypParser, yyact, "... then shift");
  return yyact;
}

/*
** The following code executes when the parse fails
*/
#ifndef YYNOERRORRECOVERY
static void yy_parse_failed(
  yyParser *yypParser           /* The parser */
){
  SqlParseARG_FETCH
  SqlParseCTX_FETCH
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE,"%sFail!\n",yyTracePrompt);
  }
#endif
  while( yypParser->yytos>yypParser->yystack ) yy_pop_parser_stack(yypParser);
  /* Here code is inserted which will be executed whenever the
  ** parser fails */
/************ Begin %parse_failure code ***************************************/
#line 102 "sql_grammar.lime"

    pstate->error = 1;
    if (!pstate->err_msg) pstate->err_msg = "parse failure";
#line 9134 "sql_grammar.c"
/************ End %parse_failure code *****************************************/
  SqlParseARG_STORE /* Suppress warning about unused %extra_argument variable */
  SqlParseCTX_STORE
}
#endif /* YYNOERRORRECOVERY */

/*
** The following code executes when a syntax error first occurs.
**
** Inside the user-supplied %syntax_error block these locals are bound:
**
**   yymajor   -- token code of the offending lookahead.  0 for EOF.
**   yyminor   -- token value (SqlParseTOKENTYPE) of the offending
**                lookahead.  Also available as the macro TOKEN.
**   yyloc     -- source location of the offending lookahead, when the
**                grammar declares %locations and parsing is driven by
**                SqlParseLoc().  Also available as the macro TOKEN_LOC.
**                Zero-initialised when locations are not threaded.
**   yypParser -- the parser handle (yyParser *).
**
** These bindings let %syntax_error implementations distinguish, for
** example, "error at end of input" (yymajor==0) from "error at or
** near <token>" (yymajor==some_terminal) the way bison's yyerror
** does via *yychar / *yylloc.
*/
static void yy_syntax_error(
  yyParser *yypParser,           /* The parser */
  int yymajor,                   /* The major type of the error token */
  SqlParseTOKENTYPE yyminor         /* The minor type of the error token */
#ifdef YYLOCATIONTYPE
  ,YYLOCATIONTYPE yyloc          /* Source location of the error token */
#endif
){
  SqlParseARG_FETCH
  SqlParseCTX_FETCH
  /* When the user does not supply a %syntax_error block, yymajor /
  ** yyminor are unused.  Reference them via a (void) cast so generated
  ** parsers compile cleanly under -Wunused-parameter without forcing
  ** an explicit pragma in every emitted file. */
  (void)yymajor;
  (void)yyminor;
#define TOKEN yyminor
#ifdef YYLOCATIONTYPE
#define TOKEN_LOC yyloc
  (void)yyloc;
#endif
/************ Begin %syntax_error code ****************************************/
#line 95 "sql_grammar.lime"

    (void)yymajor;
    (void)yyminor;
    pstate->error = 1;
    if (!pstate->err_msg) pstate->err_msg = "syntax error";
#line 9188 "sql_grammar.c"
/************ End %syntax_error code ******************************************/
  SqlParseARG_STORE /* Suppress warning about unused %extra_argument variable */
  SqlParseCTX_STORE
}

/*
** The following is executed when the parser accepts
*/
static void yy_accept(
  yyParser *yypParser           /* The parser */
){
  SqlParseARG_FETCH
  SqlParseCTX_FETCH
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE,"%sAccept!\n",yyTracePrompt);
  }
#endif
#ifndef YYNOERRORRECOVERY
  yypParser->yyerrcnt = -1;
#endif
  assert( yypParser->yytos==yypParser->yystack );
  /* Here code is inserted which will be executed whenever the
  ** parser accepts */
/*********** Begin %parse_accept code *****************************************/
/*********** End %parse_accept code *******************************************/
  SqlParseARG_STORE /* Suppress warning about unused %extra_argument variable */
  SqlParseCTX_STORE
}

/* The main parser program.
** The first argument is a pointer to a structure obtained from
** "SqlParseAlloc" which describes the current state of the parser.
** The second argument is the major token number.  The third is
** the minor token.  The fourth optional argument is whatever the
** user wants (and specified in the grammar) and is available for
** use by the action routines.
**
** Inputs:
** <ul>
** <li> A pointer to the parser (an opaque structure.)
** <li> The major token number.
** <li> The minor token number.
** <li> An option argument of a grammar-specified type.
** </ul>
**
** Outputs:
** None.
*/

/* Helper macro for calling yy_syntax_error with optional location argument.
**
** The yyloc argument is the source location of the *offending lookahead*,
** not of the stack-top symbol.  SqlParseLoc() stashes the lookahead's
** location in yypParser->yyLookaheadLoc on each call; the macro reads it
** from there.  This matches Bison's *yylloc semantics in yyerror() --
** the location of the token that the parser couldn't accept -- and is
** what callers writing PostgreSQL-compatible error messages need.
**
** When the pull-parser entry point SqlParse() is used (no location
** threading), or when a syntax error fires before the first SqlParseLoc()
** call, yyLookaheadLoc is zero-initialized -- callers should treat
** that as "location unknown". */
#ifdef YYLOCATIONTYPE
# define YY_SYNTAX_ERROR(P,M,m) \
    yy_syntax_error(P,M,m, (P)->yyLookaheadLoc)
#else
# define YY_SYNTAX_ERROR(P,M,m) yy_syntax_error(P,M,m)
#endif

/*
** Action-time lookahead access (P0-NEW-5).  Equivalent to bison's
** yychar / yyclearin from inside an action body.
**
** Use case: empty productions whose action wants to consume the
** parser's pending lookahead and reach into the lexer for further
** tokens.  PostgreSQL's plpgsql pl_gram.y has a dozen rules of the
** form
**     decl_datatype : empty { $$ = read_datatype(yychar, ...); yyclearin; }
** that all rely on this idiom.
**
** SqlParse_get_lookahead() returns the externally-visible token code
** the parser is about to shift -- the same value the caller passed
** to SqlParse() / SqlParseLoc() and the same value the user-emitted
** #defines compare against.  Returns YYEMPTY (-2) if no SqlParse() call
** is currently in progress.  yyminor_out and yyloc_out are optional;
** pass NULL to skip.  yyloc_out is only written when the parser was
** built with %locations (YYLOCATIONTYPE defined); otherwise it is
** ignored.
**
** SqlParse_clear_lookahead() tells the dispatch loop the action body
** consumed the lookahead via SqlParse_get_lookahead() and pulled
** further tokens from the lexer itself.  The current SqlParse() call
** returns without shifting.  The driver's next SqlParse() call provides
** whatever the lexer has advanced to, treated as a fresh lookahead.
*/
int SqlParse_get_lookahead(
  void *yyp,                          /* The parser */
  SqlParseTOKENTYPE *yyminor_out         /* (out) Token value, or NULL */
#ifdef YYLOCATIONTYPE
  , YYLOCATIONTYPE *yyloc_out         /* (out) Token location, or NULL */
#endif
){
  yyParser *yypParser = (yyParser*)yyp;
  if( yyminor_out!=0 ){
    *yyminor_out = yypParser->yyLookaheadMinor;
  }
#ifdef YYLOCATIONTYPE
  if( yyloc_out!=0 ){
    *yyloc_out = yypParser->yyLookaheadLoc;
  }
#endif
  return yypParser->yyLookaheadMajor;
}

void SqlParse_clear_lookahead(void *yyp){
  yyParser *yypParser = (yyParser*)yyp;
  yypParser->yyLookaheadCleared = 1;
}

/* P0-NEW-8: opt-in eager default-reduce drain.  See forward-decl
** comment above for the semantics.  Implementation: at each step,
** check if the top state's yy_default[] entry is an unconditional
** reduce (no token-specific entries would override it for any
** lookahead).  If so, fire it via yy_reduce; loop.  Otherwise stop.
**
** Detecting "unconditional" requires scanning the state's region of
** yy_lookahead[] for any entry that matches its position-relative
** token code -- O(YYNTOKEN) per drain step.  This is acceptable for
** the use case (one drain per token in the driver loop); a future
** optimization could emit a per-state bitmap at parser-build time. */
void SqlParse_drain(void *yyp SqlParseCTX_PDECL){
  yyParser *yypParser = (yyParser*)yyp;
  SqlParseCTX_FETCH
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE, "%sDrain entry, top=%d\n",
            yyTracePrompt, yypParser->yytos->stateno);
  }
#endif
  for(;;){
    YYACTIONTYPE state = yypParser->yytos->stateno;
    YYACTIONTYPE def;
    int base, i, unconditional;
    SqlParseTOKENTYPE dummy_tok;
    unsigned int ruleno;

    /* Case 1: state is a deferred reduce from a prior
    ** SHIFTREDUCE action.  In that case yy_shift stashed
    ** stateno = (real_state) + (YY_MIN_REDUCE - YY_MIN_SHIFTREDUCE)
    ** so the next SqlParse() call would re-enter the dispatch loop
    ** and fire the reduce on the new lookahead.  Fire it here
    ** instead -- with no lookahead in hand, exactly the
    ** semantics the caller asked for. */
    if( state >= YY_MIN_REDUCE && state <= YY_MAX_REDUCE ){
      ruleno = (unsigned int)(state - YY_MIN_REDUCE);
#ifndef NDEBUG
      if( yyTraceFILE ){
        fprintf(yyTraceFILE,
            "%sDrain firing deferred SHIFTREDUCE rule %u\n",
            yyTracePrompt, ruleno);
      }
#endif
      memset(&dummy_tok, 0, sizeof(dummy_tok));
      yy_reduce(yypParser, ruleno, YYNOCODE, dummy_tok SqlParseCTX_PARAM);
      continue;
    }

    /* Case 2: state is a normal shift state.  If its default
    ** action is an unconditional reduce (no token-specific
    ** entries would override it for any lookahead), fire that
    ** reduce now.  Otherwise stop -- a real lookahead is
    ** required to disambiguate. */
    if( state > YY_MAX_SHIFT ) break;       /* error/accept etc. */

    def = yy_default[state];
    if( def < YY_MIN_REDUCE || def > YY_MAX_REDUCE ) break;

    if( state > YY_SHIFT_COUNT ){
      /* Out of yy_shift_ofst[] range -- no specific actions
      ** possible, so the default is unconditional. */
      unconditional = 1;
    }else{
      base = yy_shift_ofst[state];
      unconditional = 1;
      for( i=0; i<YYNTOKEN; i++ ){
        int j = base + i;
        if( j>=0 && j<YY_ACTTAB_COUNT && yy_lookahead[j]==(YYCODETYPE)i ){
          unconditional = 0;
          break;
        }
      }
    }
    if( !unconditional ) break;

    ruleno = (unsigned int)(def - YY_MIN_REDUCE);
#ifndef NDEBUG
    if( yyTraceFILE ){
      fprintf(yyTraceFILE,
          "%sDrain firing unconditional default reduce by rule %u\n",
          yyTracePrompt, ruleno);
    }
#endif
    memset(&dummy_tok, 0, sizeof(dummy_tok));
    yy_reduce(yypParser, ruleno, YYNOCODE, dummy_tok SqlParseCTX_PARAM);
    /* Loop. */
  }
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE, "%sDrain exit, top=%d\n",
            yyTracePrompt, yypParser->yytos->stateno);
  }
#endif
}

void SqlParse(void*,int,SqlParseTOKENTYPE SqlParseARG_PDECL);
void SqlParse(
  void *yyp,                   /* The parser */
  int yymajor,                 /* The major token code number */
  SqlParseTOKENTYPE yyminor       /* The value for the token */
  SqlParseARG_PDECL               /* Optional %extra_argument parameter */
){
  YYMINORTYPE yyminorunion;
  YYACTIONTYPE yyact;   /* The parser action. */
#if !defined(YYERRORSYMBOL) && !defined(YYNOERRORRECOVERY)
  int yyendofinput;     /* True if we are at the end of input */
#endif
#ifdef YYERRORSYMBOL
  int yyerrorhit = 0;   /* True if yymajor has invoked an error */
#endif
  yyParser *yypParser = (yyParser*)yyp;  /* The parser */
  int yymajorExternal;
  SqlParseCTX_FETCH
  SqlParseARG_STORE

  /* Stash the external lookahead first thing so action code that
  ** runs in any reduce sequence can read it via
  ** SqlParse_get_lookahead().  Done before the %first_token range
  ** check so that even %syntax_error fired on an out-of-range
  ** code can see the offending token via the same API.  P0-NEW-5. */
  yypParser->yyLookaheadMajor = yymajor;
  yypParser->yyLookaheadMinor = yyminor;
  yypParser->yyLookaheadCleared = 0;

  /* %first_token N -- the user passes the externally-visible token
  ** code (with the offset applied per the emitted #defines).  Convert
  ** to the internal action-table index by subtracting the offset.
  ** EOF (0) is preserved.  When YYFIRSTTOKEN is 0 (the default), this
  ** entire block is a no-op that the optimiser deletes.
  **
  ** If the external code is out of range -- e.g. an ASCII '+' (43)
  ** sneaking through to a parser declared with %first_token 258 --
  ** fire %syntax_error directly and return.  Indexing the action
  ** table at a negative or out-of-range slot would be undefined
  ** behaviour. */
  yymajorExternal = yymajor;
#if YYFIRSTTOKEN > 0
  if( yymajor != 0 ){
    yymajor -= YYFIRSTTOKEN;
    if( yymajor < 0 || yymajor >= YYNTOKEN ){
      YYMINORTYPE yyminorunion_local;
      memset(&yyminorunion_local, 0, sizeof(yyminorunion_local));
      yyminorunion_local.yy0 = yyminor;
#ifndef NDEBUG
      if( yyTraceFILE ){
        fprintf(yyTraceFILE, "%sExternal token code %d outside "
                "[YYFIRSTTOKEN..YYFIRSTTOKEN+YYNTOKEN); rejecting.\n",
                yyTracePrompt, yymajorExternal);
      }
#endif
      YY_SYNTAX_ERROR(yypParser, yymajorExternal, yyminor);
      (void)yyminorunion_local;
      yypParser->yyLookaheadMajor = -2;
      yypParser->yyLookaheadCleared = 0;
      SqlParseARG_STORE;
      return;
    }
  }
#endif

  assert( yypParser->yytos!=0 );
#if !defined(YYERRORSYMBOL) && !defined(YYNOERRORRECOVERY)
  yyendofinput = (yymajor==0);
#endif

  yyact = yypParser->yytos->stateno;
#ifndef NDEBUG
  if( yyTraceFILE ){
    if( yyact < YY_MIN_REDUCE ){
      fprintf(yyTraceFILE,"%sInput '%s' in state %d\n",
              yyTracePrompt,yyTokenName[yymajor],yyact);
    }else{
      fprintf(yyTraceFILE,"%sInput '%s' with pending reduce %d\n",
              yyTracePrompt,yyTokenName[yymajor],yyact-YY_MIN_REDUCE);
    }
  }
#endif

  while(1){ /* Exit by "break" */
    assert( yypParser->yytos>=yypParser->yystack );
    assert( yyact==yypParser->yytos->stateno );
    yyact = yy_find_shift_action((YYCODETYPE)yymajor,yyact);
    if( yyact >= YY_MIN_REDUCE ){
      unsigned int yyruleno = yyact - YY_MIN_REDUCE; /* Reduce by this rule */
#ifndef NDEBUG
      assert( yyruleno<(int)(sizeof(yyRuleName)/sizeof(yyRuleName[0])) );
      if( yyTraceFILE ){
        int yysize = yyRuleInfoNRhs[yyruleno];
        if( yysize ){
          fprintf(yyTraceFILE, "%sReduce %d [%s]%s, pop back to state %d.\n",
            yyTracePrompt,
            yyruleno, yyRuleName[yyruleno],
            yyruleno<YYNRULE_WITH_ACTION ? "" : " without external action",
            yypParser->yytos[yysize].stateno);
        }else{
          fprintf(yyTraceFILE, "%sReduce %d [%s]%s.\n",
            yyTracePrompt, yyruleno, yyRuleName[yyruleno],
            yyruleno<YYNRULE_WITH_ACTION ? "" : " without external action");
        }
      }
#endif /* NDEBUG */

      /* Check that the stack is large enough to grow by a single entry
      ** if the RHS of the rule is empty.  This ensures that there is room
      ** enough on the stack to push the LHS value */
      if( yyRuleInfoNRhs[yyruleno]==0 ){
#ifdef YYTRACKMAXSTACKDEPTH
        if( (int)(yypParser->yytos - yypParser->yystack)>yypParser->yyhwm ){
          yypParser->yyhwm++;
          assert( yypParser->yyhwm ==
                  (int)(yypParser->yytos - yypParser->yystack));
        }
#endif
        if( yypParser->yytos>=yypParser->yystackEnd ){
          if( yyGrowStack(yypParser) ){
            yyStackOverflow(yypParser);
            break;
          }
        }
      }
      yyact = yy_reduce(yypParser,yyruleno,yymajor,yyminor SqlParseCTX_PARAM);
      /* If the reduce action called SqlParse_clear_lookahead() the user
      ** action consumed the token via SqlParse_get_lookahead() and read
      ** further tokens from the lexer themselves.  Skip the rest of
      ** the dispatch (no shift) and return.  The driver's next
      ** SqlParse() call provides whatever the lexer has advanced to.
      ** P0-NEW-5. */
      if( yypParser->yyLookaheadCleared ){
#ifndef NDEBUG
        if( yyTraceFILE ){
          fprintf(yyTraceFILE,
            "%sLookahead consumed by action; returning without shift.\n",
            yyTracePrompt);
        }
#endif
        yypParser->yyLookaheadMajor = -2;
        yypParser->yyLookaheadCleared = 0;
        SqlParseARG_STORE;
        return;
      }
    }else if( yyact <= YY_MAX_SHIFTREDUCE ){
      yy_shift(yypParser,yyact,(YYCODETYPE)yymajor,yyminor);
#ifndef YYNOERRORRECOVERY
      yypParser->yyerrcnt--;
#endif
      break;
    }else if( LIME_UNLIKELY(yyact==YY_ACCEPT_ACTION) ){
      yypParser->yytos--;
      yy_accept(yypParser);
      return;
    }else{
      assert( yyact == YY_ERROR_ACTION );
      yyminorunion.yy0 = yyminor;
#ifdef YYERRORSYMBOL
      int yymx;
#endif
#ifndef NDEBUG
      if( yyTraceFILE ){
        fprintf(yyTraceFILE,"%sSyntax Error!\n",yyTracePrompt);
      }
#endif
#ifdef YYERRORSYMBOL
      /* A syntax error has occurred.
      ** The response to an error depends upon whether or not the
      ** grammar defines an error token "ERROR".  
      **
      ** This is what we do if the grammar does define ERROR:
      **
      **  * Call the %syntax_error function.
      **
      **  * Begin popping the stack until we enter a state where
      **    it is legal to shift the error symbol, then shift
      **    the error symbol.
      **
      **  * Set the error count to three.
      **
      **  * Begin accepting and shifting new tokens.  No new error
      **    processing will occur until three tokens have been
      **    shifted successfully.
      **
      */
      if( yypParser->yyerrcnt<0 ){
        /* yymajorExternal is the offset-corrected token code that the
        ** caller passed to SqlParse(); user %syntax_error code expects to
        ** see this rather than the internal action-table index. */
        YY_SYNTAX_ERROR(yypParser,yymajorExternal,yyminor);
      }
      yymx = yypParser->yytos->major;
      if( yymx==YYERRORSYMBOL || yyerrorhit ){
#ifndef NDEBUG
        if( yyTraceFILE ){
          fprintf(yyTraceFILE,"%sDiscard input token %s\n",
             yyTracePrompt,yyTokenName[yymajor]);
        }
#endif
        yy_destructor(yypParser, (YYCODETYPE)yymajor, &yyminorunion);
        yymajor = YYNOCODE;
      }else{
        while( yypParser->yytos > yypParser->yystack ){
          yyact = yy_find_reduce_action(yypParser->yytos->stateno,
                                        YYERRORSYMBOL);
          if( yyact<=YY_MAX_SHIFTREDUCE ) break;
          yy_pop_parser_stack(yypParser);
        }
        if( yypParser->yytos <= yypParser->yystack || yymajor==0 ){
          yy_destructor(yypParser,(YYCODETYPE)yymajor,&yyminorunion);
          yy_parse_failed(yypParser);
#ifndef YYNOERRORRECOVERY
          yypParser->yyerrcnt = -1;
#endif
          yymajor = YYNOCODE;
        }else if( yymx!=YYERRORSYMBOL ){
          yy_shift(yypParser,yyact,YYERRORSYMBOL,yyminor);
        }
      }
      yypParser->yyerrcnt = 3;
      yyerrorhit = 1;
      if( yymajor==YYNOCODE ) break;
      yyact = yypParser->yytos->stateno;
#elif defined(YYNOERRORRECOVERY)
      /* If the YYNOERRORRECOVERY macro is defined, then do not attempt to
      ** do any kind of error recovery.  Instead, simply invoke the syntax
      ** error routine and continue going as if nothing had happened.
      **
      ** Applications can set this macro (for example inside %include) if
      ** they intend to abandon the parse upon the first syntax error seen.
      */
      YY_SYNTAX_ERROR(yypParser,yymajorExternal,yyminor);
      yy_destructor(yypParser,(YYCODETYPE)yymajor,&yyminorunion);
      break;
#else  /* YYERRORSYMBOL is not defined */
      /* This is what we do if the grammar does not define ERROR:
      **
      **  * Report an error message, and throw away the input token.
      **
      **  * If the input token is $, then fail the parse.
      **
      ** As before, subsequent error messages are suppressed until
      ** three input tokens have been successfully shifted.
      **
      ** If YYERRORSYNC is defined, use panic-mode recovery:
      ** discard tokens until a synchronization token is found,
      ** then resume parsing from the current state.
      */
      if( yypParser->yyerrcnt<=0 ){
        YY_SYNTAX_ERROR(yypParser,yymajorExternal,yyminor);
      }
      yypParser->yyerrcnt = 3;
#ifdef YYERRORSYNC
      /* Panic-mode recovery: discard tokens until we find a sync token */
      if( yymajor!=0 && (YYCODETYPE)yymajor<(YYCODETYPE)(sizeof(yy_is_sync_token)/sizeof(yy_is_sync_token[0]))
          && yy_is_sync_token[(YYCODETYPE)yymajor] ){
        /* Current token IS a sync token -- resume parsing here */
        break;
      }
      /* Not a sync token -- discard and continue */
      yy_destructor(yypParser,(YYCODETYPE)yymajor,&yyminorunion);
      yymajor = YYNOCODE;
      break;
#else /* !YYERRORSYNC */
      yy_destructor(yypParser,(YYCODETYPE)yymajor,&yyminorunion);
      if( yyendofinput ){
        yy_parse_failed(yypParser);
#ifndef YYNOERRORRECOVERY
        yypParser->yyerrcnt = -1;
#endif
      }
      break;
#endif /* YYERRORSYNC */
#endif /* YYERRORSYMBOL */
    }
  }
#ifndef NDEBUG
  if( yyTraceFILE ){
    yyStackEntry *i;
    char cDiv = '[';
    fprintf(yyTraceFILE,"%sReturn. Stack=",yyTracePrompt);
    for(i=&yypParser->yystack[1]; i<=yypParser->yytos; i++){
      fprintf(yyTraceFILE,"%c%s", cDiv, yyTokenName[i->major]);
      cDiv = ' ';
    }
    fprintf(yyTraceFILE,"]\n");
  }
#endif
  /* Clear action-time lookahead state on SqlParse() exit (P0-NEW-5).
  ** Calls to SqlParse_get_lookahead() outside an active SqlParse() are
  ** defined to return YYEMPTY. */
  yypParser->yyLookaheadMajor = -2;
  yypParser->yyLookaheadCleared = 0;
  return;
}

#ifdef YYLOCATIONTYPE
/*
** Location-aware version of SqlParse().
** Same as SqlParse() but also stores the source location for the shifted token.
** Use this entry point when the %locations directive is active.
**
** The location is stored on the parse stack alongside the token value.
** During reductions, a YYLOC(N) macro can access the location of the
** Nth RHS symbol, and the result location is computed by merging the
** locations of the first and last RHS symbols.
*/
void SqlParseLoc(void*,int,SqlParseTOKENTYPE,YYLOCATIONTYPE SqlParseARG_PDECL);
void SqlParseLoc(
  void *yyp,                   /* The parser */
  int yymajor,                 /* The major token code number */
  SqlParseTOKENTYPE yyminor,      /* The value for the token */
  YYLOCATIONTYPE yyloc         /* Source location of the token */
  SqlParseARG_PDECL               /* Optional %extra_argument parameter */
){
  yyParser *yypParser = (yyParser*)yyp;
  /* Stash the lookahead's location so %syntax_error can see it via
  ** yyLookaheadLoc / TOKEN_LOC even when the parser fails on this
  ** token (no shift happens, so yytos->yyloc would still be the
  ** previously-shifted token's location). */
  yypParser->yyLookaheadLoc = yyloc;
  SqlParse(yyp, yymajor, yyminor SqlParseARG_PARAM);
  /* After SqlParse shifts the token, patch the location on top of stack */
  if( yypParser->yytos > yypParser->yystack ){
    yypParser->yytos->yyloc = yyloc;
  }
}
#endif /* YYLOCATIONTYPE */

/*
** Return the fallback token corresponding to canonical token iToken, or
** 0 if iToken has no fallback.
*/
int SqlParseFallback(int);
int SqlParseFallback(int iToken){
#ifdef YYFALLBACK
  assert( iToken<(int)(sizeof(yyFallback)/sizeof(yyFallback[0])) );
  return yyFallback[iToken];
#else
  (void)iToken;
  return 0;
#endif
}

/*
** Return a heap-allocated string listing the tokens that would be
** valid in the current parser state.  The caller must free() the
** returned string.  Returns NULL if the parser is NULL or on
** allocation failure.
**
** The output is a comma-separated list like: "SELECT, INSERT, UPDATE".
** This is useful for producing human-readable error messages.
**
** See also SqlParseExpectedTokens() for a structured (array) form and
** SqlParseTokenName() for looking up individual token names.
*/
char *SqlParseExpectedTokensString(void*);
char *SqlParseExpectedTokensString(void *yyp){
  yyParser *yypParser = (yyParser*)yyp;
  YYACTIONTYPE stateno;
  char *buf = 0;
  size_t buf_len = 0;
  size_t buf_cap = 0;
  int first = 1;
  int i;
  if( yypParser==0 || yypParser->yytos==0 ) return 0;
  stateno = yypParser->yytos->stateno;

  for(i=0; i<YYNTOKEN; i++){
    YYACTIONTYPE act;
#if YY_SHIFT_COUNT >= 0
    int j = i + yy_shift_ofst[stateno];
    if( j>=0 && j<YY_ACTTAB_COUNT && yy_lookahead[j]==(YYCODETYPE)i ){
      act = yy_action[j];
    }else{
      act = yy_default[stateno];
    }
#else
    act = yy_default[stateno];
#endif
    if( act!=YY_ERROR_ACTION && act!=yy_default[stateno] ){
      const char *name = yyTokenName[i];
      size_t nlen = 0;
      const char *p = name;
      size_t needed;
      while( *p ) { nlen++; p++; }

      /* Need space for ", " + name + NUL */
      needed = buf_len + (first ? 0 : 2) + nlen + 1;
      if( needed > buf_cap ){
        size_t new_cap = (buf_cap == 0) ? 128 : buf_cap * 2;
        char *tmp;
        if( new_cap < needed ) new_cap = needed;
        tmp = (char*)realloc(buf, new_cap);
        if( tmp==0 ){
          free(buf);
          return 0;
        }
        buf = tmp;
        buf_cap = new_cap;
      }
      if( !first ){
        buf[buf_len++] = ',';
        buf[buf_len++] = ' ';
      }
      memcpy(buf + buf_len, name, nlen);
      buf_len += nlen;
      buf[buf_len] = 0;
      first = 0;
    }
  }
  return buf;
}


/* ====================================================================
**  RFC 0059 Diagnostics API
**  --------------------------------------------------------------------
**  These three functions let a host produce rich error messages:
**
**    SqlParseTokenName(code)      -> string name for a token code
**    SqlParseState(parser)        -> current parser state number
**    SqlParseExpectedTokens(...)  -> array of token codes valid at a state
**
**  Together with Token.length from the tokenizer, this is enough to
**  build Rust-compiler-style diagnostics with caret underlines and
**  "expected one of ..." hints.
** ================================================================== */

/*
** Return the string name of a terminal token code, or NULL if the
** code is out of range.  Names come from the yyTokenName[] table.
*/
const char *SqlParseTokenName(int);
const char *SqlParseTokenName(int tokenCode){
  if( tokenCode < 0 || tokenCode >= (int)YYNTOKEN ) return 0;
  return yyTokenName[tokenCode];
}

/*
** Return the current parser state number, or -1 if the parser handle
** is invalid.  For a freshly-initialized parser, returns 0 (the
** initial/start state).
**
** The returned value is meaningful only as an input to
** SqlParseExpectedTokens().  Host code should not attach semantic meaning
** to the number itself -- it is an internal identifier and may change
** whenever the grammar is regenerated.
*/
int SqlParseState(void*);
int SqlParseState(void *yyp){
  yyParser *yypParser = (yyParser*)yyp;
  if( yypParser==0 || yypParser->yytos==0 ) return -1;
  return (int)yypParser->yytos->stateno;
}

/*
** Fill `out` with the token codes that would be valid at state
** `stateno` and return the count written (up to `max`).  If `out` is
** NULL or `max` is 0, returns the count that would have been written,
** which lets the caller size a buffer correctly on a second call.
**
** The returned codes can be passed to SqlParseTokenName() to obtain
** human-readable names.  Codes are returned in ascending numeric
** order.
**
** Typical usage from a %syntax_error callback:
**
**   int s = SqlParseState(yypParser);
**   int n = SqlParseExpectedTokens(s, NULL, 0);
**   int *codes = malloc(n * sizeof(int));
**   SqlParseExpectedTokens(s, codes, n);
**   for( int i = 0; i < n; i++ ){
**       fprintf(stderr, "  expected: %s\n", SqlParseTokenName(codes[i]));
**   }
**   free(codes);
*/
int SqlParseExpectedTokens(int,int*,int);
int SqlParseExpectedTokens(int stateno, int *out, int max){
  int count = 0;
  int i;

  if( stateno < 0 ) return 0;
#if YY_SHIFT_COUNT >= 0
  if( stateno > YY_SHIFT_COUNT ) return 0;
#endif

  for(i = 0; i < YYNTOKEN; i++){
    YYACTIONTYPE act;
#if YY_SHIFT_COUNT >= 0
    int j = i + yy_shift_ofst[stateno];
    if( j >= 0 && j < YY_ACTTAB_COUNT && yy_lookahead[j] == (YYCODETYPE)i ){
      act = yy_action[j];
    }else{
      act = yy_default[stateno];
    }
#else
    act = yy_default[stateno];
#endif
    if( act != YY_ERROR_ACTION && act != yy_default[stateno] ){
      if( out && count < max ){
        out[count] = i;
      }
      count++;
    }
  }
  return count;
}
