/* This file is automatically generated by Lemon from input grammar
** source file "sql_grammar.lime".
*/
/*
** 2000-05-29
**
** The author disclaims copyright to this source code.  In place of
** a legal notice, here is a blessing:
**
**    May you do good and not evil.
**    May you find forgiveness for yourself and forgive others.
**    May you share freely, never taking more than you give.
**
*************************************************************************
** Driver template for the lime parser generator.
**
** The "lemon" program processes an LALR(1) input grammar file, then uses
** this template to construct a parser.  The "lemon" program inserts text
** at each "%%" line.  Also, any "P-a-r-s-e" identifier prefix (without the
** interstitial "-" characters) contained in this template is changed into
** the value of the %name directive from the grammar.  Otherwise, the content
** of this template is copied straight through into the generated parser
** source file.
**
** The following is the concatenation of all %include directives from the
** input grammar file:
*/
/************ Begin %include sections from the grammar ************************/
#line 19 "sql_grammar.lime"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include "sql_parse.h"
#include "sql_ast.h"
#include "sql_parse_lime.h"

/* Allocate an AST node of the given type from the parse arena. */
#define AST(ps, T)  ((T *)sql_arena_alloc((ps)->arena, sizeof(T)))

/* Build a sql_str_t from a token (a slice into the source SQL). */
static sql_str_t tok_str(sql_token_t t) {
    sql_str_t s; s.p = t.p; s.len = (uint32_t)(t.len < 0 ? 0 : t.len); return s;
}

/* A string-literal token '...' -- strip the surrounding quotes. */
static sql_str_t tok_str_unquote(sql_token_t t) {
    sql_str_t s = tok_str(t);
    if (s.len >= 2 && (s.p[0] == '\'' || s.p[0] == '"')) { s.p++; s.len -= 2; }
    return s;
}

/* Record an expression node's verbatim source span from a token (the
 * slice covering this leaf in the original SQL).  The whole-expression
 * span is unioned over the tree by sql_expr_span(). */
static void span_tok(sql_expr_t *e, sql_token_t t) {
    if (e && t.p && t.len > 0) { e->src = t.p; e->srclen = (uint32_t)t.len; }
}

static sql_expr_t *mk_expr(sql_parse_state_t *ps, sql_expr_op_t op) {
    sql_expr_t *e = AST(ps, sql_expr_t);
    if (e) e->op = op;
    return e;
}

static sql_expr_t *mk_binary(sql_parse_state_t *ps, int tok,
                             sql_expr_t *a, sql_expr_t *b) {
    sql_expr_t *e = mk_expr(ps, SX_E_BINARY);
    if (e) { e->op2 = tok; e->a = a; e->b = b; }
    return e;
}

static sql_expr_t *mk_unary(sql_parse_state_t *ps, int tok, sql_expr_t *a) {
    sql_expr_t *e = mk_expr(ps, SX_E_UNARY);
    if (e) { e->op2 = tok; e->a = a; }
    return e;
}

static sql_exprlist_t *mk_list(sql_parse_state_t *ps) {
    return AST(ps, sql_exprlist_t);
}

static sql_exprlist_t *list_add(sql_parse_state_t *ps, sql_exprlist_t *l,
                                sql_expr_t *e) {
    sql_exprlist_item_t *it;
    if (l == NULL) l = mk_list(ps);
    if (l == NULL) return NULL;
    it = AST(ps, sql_exprlist_item_t);
    if (it == NULL) return l;
    it->expr = e;
    if (l->tail) l->tail->next = it; else l->head = it;
    l->tail = it;
    l->n++;
    return l;
}

/* Carries a WITH clause up to the select it prefixes. */
struct with_clause_v { sql_cte_t *list; int rec; };
#line 101 "sql_grammar.c"
/**************** End of %include directives **********************************/
/* These constants specify the various numeric values for terminal symbols.
***************** Begin token definitions *************************************/
#ifndef TK_SELECT
#define TK_SELECT                       1
#define TK_FROM                         2
#define TK_WHERE                        3
#define TK_GROUP                        4
#define TK_BY                           5
#define TK_HAVING                       6
#define TK_ORDER                        7
#define TK_LIMIT                        8
#define TK_OFFSET                       9
#define TK_ASC                         10
#define TK_DESC                        11
#define TK_DISTINCT                    12
#define TK_ALL                         13
#define TK_UNION                       14
#define TK_INTERSECT                   15
#define TK_EXCEPT                      16
#define TK_INSERT                      17
#define TK_INTO                        18
#define TK_VALUES                      19
#define TK_DEFAULT                     20
#define TK_REPLACE                     21
#define TK_UPDATE                      22
#define TK_SET                         23
#define TK_DELETE                      24
#define TK_CREATE                      25
#define TK_TABLE                       26
#define TK_INDEX                       27
#define TK_VIEW                        28
#define TK_TRIGGER                     29
#define TK_TEMP                        30
#define TK_TEMPORARY                   31
#define TK_DROP                        32
#define TK_IF                          33
#define TK_NOT                         34
#define TK_EXISTS                      35
#define TK_PRAGMA                      36
#define TK_BEGIN                       37
#define TK_COMMIT                      38
#define TK_END                         39
#define TK_ROLLBACK                    40
#define TK_TRANSACTION                 41
#define TK_SAVEPOINT                   42
#define TK_RELEASE                     43
#define TK_ATTACH                      44
#define TK_DETACH                      45
#define TK_DATABASE                    46
#define TK_AS                          47
#define TK_AND                         48
#define TK_OR                          49
#define TK_NULL                        50
#define TK_TRUE                        51
#define TK_FALSE                       52
#define TK_PRIMARY                     53
#define TK_KEY                         54
#define TK_UNIQUE                      55
#define TK_FOREIGN                     56
#define TK_REFERENCES                  57
#define TK_CHECK                       58
#define TK_AUTOINCR                    59
#define TK_ON                          60
#define TK_USING                       61
#define TK_IN                          62
#define TK_LIKE                        63
#define TK_BETWEEN                     64
#define TK_IS                          65
#define TK_CASE                        66
#define TK_WHEN                        67
#define TK_THEN                        68
#define TK_ELSE                        69
#define TK_EXPLAIN                     70
#define TK_QUERY                       71
#define TK_PLAN                        72
#define TK_WITH                        73
#define TK_RECURSIVE                   74
#define TK_TO                          75
#define TK_JOIN                        76
#define TK_INNER                       77
#define TK_OUTER                       78
#define TK_LEFT                        79
#define TK_RIGHT                       80
#define TK_FULL                        81
#define TK_CROSS                       82
#define TK_NATURAL                     83
#define TK_LP                          84
#define TK_RP                          85
#define TK_COMMA                       86
#define TK_SEMI                        87
#define TK_DOT                         88
#define TK_STAR                        89
#define TK_PLUS                        90
#define TK_MINUS                       91
#define TK_SLASH                       92
#define TK_PERCENT                     93
#define TK_EQ                          94
#define TK_NE                          95
#define TK_LT                          96
#define TK_LE                          97
#define TK_GT                          98
#define TK_GE                          99
#define TK_CONCAT                      100
#define TK_AMP                         101
#define TK_PIPE                        102
#define TK_QMARK                       103
#define TK_TILDE                       104
#define TK_ID                          105
#define TK_NUMBER                      106
#define TK_STRING                      107
#define TK_BLOB                        108
#define TK_UMINUS                      109
#endif
/**************** End token definitions ***************************************/

/* The next sections is a series of control #defines.
** various aspects of the generated parser.
**    YYCODETYPE         is the data type used to store the integer codes
**                       that represent terminal and non-terminal symbols.
**                       "unsigned char" is used if there are fewer than
**                       256 symbols.  Larger types otherwise.
**    YYNOCODE           is a number of type YYCODETYPE that is not used for
**                       any terminal or nonterminal symbol.
**    YYFALLBACK         If defined, this indicates that one or more tokens
**                       (also known as: "terminal symbols") have fall-back
**                       values which should be used if the original symbol
**                       would not parse.  This permits keywords to sometimes
**                       be used as identifiers, for example.
**    YYACTIONTYPE       is the data type used for "action codes" - numbers
**                       that indicate what to do in response to the next
**                       token.
**    SqlParseTOKENTYPE     is the data type used for minor type for terminal
**                       symbols.  Background: A "minor type" is a semantic
**                       value associated with a terminal or non-terminal
**                       symbols.  For example, for an "ID" terminal symbol,
**                       the minor type might be the name of the identifier.
**                       Each non-terminal can have a different minor type.
**                       Terminal symbols all have the same minor type, though.
**                       This macros defines the minor type for terminal 
**                       symbols.
**    YYMINORTYPE        is the data type used for all minor types.
**                       This is typically a union of many types, one of
**                       which is SqlParseTOKENTYPE.  The entry in the union
**                       for terminal symbols is called "yy0".
**    YYSTACKDEPTH       is the maximum depth of the parser's stack.  If
**                       zero the stack is dynamically sized using realloc()
**    SqlParseARG_SDECL     A static variable declaration for the %extra_argument
**    SqlParseARG_PDECL     A parameter declaration for the %extra_argument
**    SqlParseARG_PARAM     Code to pass %extra_argument as a subroutine parameter
**    SqlParseARG_STORE     Code to store %extra_argument into yypParser
**    SqlParseARG_FETCH     Code to extract %extra_argument from yypParser
**    SqlParseCTX_*         As SqlParseARG_ except for %extra_context
**    YYREALLOC          Name of the realloc() function to use
**    YYFREE             Name of the free() function to use
**    YYDYNSTACK         True if stack space should be extended on heap
**    YYERRORSYMBOL      is the code number of the error symbol.  If not
**                       defined, then do no error processing.
**    YYNSTATE           the combined number of states.
**    YYNRULE            the number of rules in the grammar
**    YYNTOKEN           Number of terminal symbols
**    YY_MAX_SHIFT       Maximum value for shift actions
**    YY_MIN_SHIFTREDUCE Minimum value for shift-reduce actions
**    YY_MAX_SHIFTREDUCE Maximum value for shift-reduce actions
**    YY_ERROR_ACTION    The yy_action[] code for syntax error
**    YY_ACCEPT_ACTION   The yy_action[] code for accept
**    YY_NO_ACTION       The yy_action[] code for no-op
**    YY_MIN_REDUCE      Minimum value for reduce actions
**    YY_MAX_REDUCE      Maximum value for reduce actions
**    YY_MIN_DSTRCTR     Minimum symbol value that has a destructor
**    YY_MAX_DSTRCTR     Maximum symbol value that has a destructor
*/
#ifndef INTERFACE
# define INTERFACE 1
#endif
/************* Begin control #defines *****************************************/
#define YYCODETYPE unsigned char
#define YYNOCODE 171
#define YYACTIONTYPE unsigned short int
#define YYFIRSTTOKEN 0
#define YYNRHS_MAX 10
#define SqlParseTOKENTYPE sql_token_t
typedef union {
  int yyinit;
  SqlParseTOKENTYPE yy0;
  sql_select_t * yy21;
  sql_case_arm_t * yy28;
  sql_stmt_t * yy31;
  sql_qname_t yy32;
  sql_str_t yy52;
  sql_assign_t * yy88;
  sql_src_t * yy133;
  sql_coldef_t * yy134;
  sql_expr_t * yy138;
  struct with_clause_v yy201;
  int yy274;
  sql_exprlist_item_t * yy280;
  sql_insert_t * yy284;
  sql_exprlist_t * yy314;
  sql_cte_t * yy319;
} YYMINORTYPE;
#ifndef YYSTACKDEPTH
#define YYSTACKDEPTH 100
#endif
#define SqlParseARG_SDECL sql_parse_state_t *pstate;
#define SqlParseARG_PDECL ,sql_parse_state_t *pstate
#define SqlParseARG_PARAM ,pstate
#define SqlParseARG_FETCH sql_parse_state_t *pstate=yypParser->pstate; (void)pstate;
#define SqlParseARG_STORE yypParser->pstate=pstate;
#undef YYREALLOC
#define YYREALLOC realloc
#undef YYFREE
#define YYFREE free
#undef YYDYNSTACK
#define YYDYNSTACK 0
#undef YYSIZELIMIT
#define SqlParseCTX(P) 0
#define SqlParseCTX_SDECL
#define SqlParseCTX_PDECL
#define SqlParseCTX_PARAM
#define SqlParseCTX_FETCH
#define SqlParseCTX_STORE
#undef YYERRORSYMBOL
#undef YYERRSYMDT
#undef YYFALLBACK
#define YYNSTATE             298
#define YYNRULE              226
#define YYNRULE_WITH_ACTION  219
#define YYNTOKEN             110
#define YY_MAX_SHIFT         297
#define YY_MIN_SHIFTREDUCE   450
#define YY_MAX_SHIFTREDUCE   675
#define YY_ERROR_ACTION      676
#define YY_ACCEPT_ACTION     677
#define YY_NO_ACTION         678
#define YY_MIN_REDUCE        679
#define YY_MAX_REDUCE        904
#define YY_MIN_DSTRCTR       171
#define YY_MAX_DSTRCTR       0
/************* End control #defines *******************************************/
#define YY_NLOOKAHEAD ((int)(sizeof(yy_lookahead)/sizeof(yy_lookahead[0])))

/* Define the yytestcase() macro to be a no-op if is not already defined
** otherwise.
**
** Applications can choose to define yytestcase() in the %include section
** to a macro that can assist in verifying code coverage.  For production
** code the yytestcase() macro should be turned off.  But it is useful
** for testing.
*/
#ifndef yytestcase
# define yytestcase(X)
#endif

/* Branch-prediction hints.  These are no-ops on compilers without
** __builtin_expect support; the runtime is correct either way.
** Used in the hot find_shift / find_reduce / dispatch paths to
** mark statistically-rare branches (collisions, accept, error).
** Per .agent/notes/c-perf-audit.md item #5: limpar.c was the
** runtime template every user binary embeds, and had zero hints. */
#ifndef LIME_LIKELY
# if defined(__GNUC__) || defined(__clang__)
#  define LIME_LIKELY(x)   __builtin_expect(!!(x), 1)
#  define LIME_UNLIKELY(x) __builtin_expect(!!(x), 0)
# else
#  define LIME_LIKELY(x)   (x)
#  define LIME_UNLIKELY(x) (x)
# endif
#endif

/* Macro to determine if stack space has the ability to grow using
** heap memory.
*/
#if YYSTACKDEPTH<=0 || YYDYNSTACK
# define YYGROWABLESTACK 1
#else
# define YYGROWABLESTACK 0
#endif

/* Guarantee a minimum number of initial stack slots.
*/
#if YYSTACKDEPTH<=0
# undef YYSTACKDEPTH
# define YYSTACKDEPTH 2  /* Need a minimum stack size */
#endif


/* Next are the tables used to determine what action to take based on the
** current state and lookahead token.  These tables are used to implement
** functions that take a state number and lookahead value and return an
** action integer.  
**
** Suppose the action integer is N.  Then the action is determined as
** follows
**
**   0 <= N <= YY_MAX_SHIFT             Shift N.  That is, push the lookahead
**                                      token onto the stack and goto state N.
**
**   N between YY_MIN_SHIFTREDUCE       Shift to an arbitrary state then
**     and YY_MAX_SHIFTREDUCE           reduce by rule N-YY_MIN_SHIFTREDUCE.
**
**   N == YY_ERROR_ACTION               A syntax error has occurred.
**
**   N == YY_ACCEPT_ACTION              The parser accepts its input.
**
**   N == YY_NO_ACTION                  No such action.  Denotes unused
**                                      slots in the yy_action[] table.
**
**   N between YY_MIN_REDUCE            Reduce by rule N-YY_MIN_REDUCE
**     and YY_MAX_REDUCE
**
** The action table is constructed as a single large table named yy_action[].
** Given state S and lookahead X, the action is computed as either:
**
**    (A)   N = yy_action[ yy_shift_ofst[S] + X ]
**    (B)   N = yy_default[S]
**
** The (A) formula is preferred.  The B formula is used instead if
** yy_lookahead[yy_shift_ofst[S]+X] is not equal to X.
**
** The formulas above are for computing the action when the lookahead is
** a terminal symbol.  If the lookahead is a non-terminal (as occurs after
** a reduce action) then the yy_reduce_ofst[] array is used in place of
** the yy_shift_ofst[] array.
**
** The following are the tables generated in this section:
**
**  yy_action[]        A single table containing all actions.
**  yy_lookahead[]     A table containing the lookahead for each entry in
**                     yy_action.  Used to detect hash collisions.
**  yy_shift_ofst[]    For each state, the offset into yy_action for
**                     shifting terminals.
**  yy_reduce_ofst[]   For each state, the offset into yy_action for
**                     shifting non-terminals after a reduce.
**  yy_default[]       Default action for each state.
**
*********** Begin parsing tables **********************************************/
#define YY_ACTTAB_COUNT (960)
static const YYACTIONTYPE yy_action[] = {
 /*     0 */   680,  134,  682,  174,  683,  684,  685,  686,  687,  688,
 /*    10 */   689,  690,  691,  692,  693,  694,  695,  696,  214,  137,
 /*    20 */   845,  137,  845,  281,  174,  136,  845,  126,   89,  707,
 /*    30 */   275,  747,  190,  748,  206,  228,   79,  138,  845,  154,
 /*    40 */   845,  205,  280,  174,  677,   86,   28,  272,   66,   67,
 /*    50 */   246,  785,  206,  212,  554,  555,  139,  845,  154,  845,
 /*    60 */   201,   90,  286,   47,   42,   46,   66,   28,   44,  247,
 /*    70 */    39,  576,  223,  111,  245,   99,   97,   95,   93,  557,
 /*    80 */   286,   47,   42,   46,  765,  174,   28,  143,  845,   53,
 /*    90 */    55,   54,   52,   51,   64,   62,   60,   58,   57,   56,
 /*   100 */    50,   49,   48,  521,  522,  484,   74,   53,   55,   54,
 /*   110 */    52,   51,   64,   62,   60,   58,   57,   56,   50,   49,
 /*   120 */    48,  258,  154,  845,  203,  791,  249,  206,   28,   90,
 /*   130 */    24,   53,   71,   72,   52,   51,  188,  208,  789,  792,
 /*   140 */   219,   66,   67,   49,   48,  259,  208,  206,  791,  252,
 /*   150 */   118,  173,  248,   70,  817,  286,   47,   42,   46,   77,
 /*   160 */   171,  790,  792,  210,   91,  179,  221,  220,  178,   78,
 /*   170 */   216,  176,   75,  175,  177,  286,   47,   42,   46,  806,
 /*   180 */   277,  174,   53,   55,   54,   52,   51,   64,   62,   60,
 /*   190 */    58,   57,   56,   50,   49,   48,  142,  845,    1,  209,
 /*   200 */    84,   72,   53,   55,   54,   52,   51,   64,   62,   60,
 /*   210 */    58,   57,   56,   50,   49,   48,   12,  206,  239,   90,
 /*   220 */   237,  236,  608,  232,   28,  136,  845,  699,  174,  708,
 /*   230 */   799,   66,   67,   90,   68,  259,  154,  845,  192,  252,
 /*   240 */   118,  215,  248,   70,  773,  286,   47,   42,   46,  718,
 /*   250 */   171,  255,  256,  217,   91,  179,  221,  220,  178,   83,
 /*   260 */   216,  176,   75,  175,  154,  845,  193,  717,  675,   21,
 /*   270 */   112,   28,   53,   55,   54,   52,   51,   64,   62,   60,
 /*   280 */    58,   57,   56,   50,   49,   48,  572,  573,    1,  144,
 /*   290 */   845,   72,  279,  662,  663,  664,   53,   55,   54,   52,
 /*   300 */    51,   17,  288,  206,  604,   72,    9,   50,   49,   48,
 /*   310 */    38,   36,  154,  845,  273,   90,  600,   66,   67,  154,
 /*   320 */   845,  202,  481,   65,   76,  145,  845,   27,  662,  663,
 /*   330 */   664,  286,   47,   42,   46,  574,  146,  845,  283,  662,
 /*   340 */   663,  664,  147,  845,  148,  845,  665,  703,  278,  659,
 /*   350 */   660,  661,  149,  845,  578,   69,  781,  174,   53,   55,
 /*   360 */    54,   52,   51,   64,   62,   60,   58,   57,   56,   50,
 /*   370 */    49,   48,  133,    6,  651,  662,  663,  664,  285,   61,
 /*   380 */    63,  665,    7,  278,  659,  660,  661,   72,  254,  206,
 /*   390 */   140,  845,  665,   59,  204,  659,  660,  661,  779,  174,
 /*   400 */    28,  150,  845,   66,   67,  490,  265,  180,  197,  196,
 /*   410 */   195,  261,  194,  151,  845,   85,  813,  286,   47,   42,
 /*   420 */    46,  702,  174,   37,  224,  701,  174,  270,  665,   92,
 /*   430 */   278,  659,  660,  661,  269,  174,  291,  174,  163,  845,
 /*   440 */   294,  174,   28,  231,   53,   55,   54,   52,   51,   64,
 /*   450 */    62,   60,   58,   57,   56,   50,   49,   48,  206,  700,
 /*   460 */   174,  697,  174,  152,  845,   28,  153,  845,  271,   28,
 /*   470 */   141,  845,   66,   67,   19,   23,   22,  812,   28,  206,
 /*   480 */    28,  158,  845,  811,   28,  507,  286,   47,   42,   46,
 /*   490 */   162,  845,   40,   66,   67,  164,  845,  867,  845,  866,
 /*   500 */   845,   80,  716,   28,   94,   28,   96,  286,   47,   42,
 /*   510 */    46,   82,  774,   53,   55,   54,   52,   51,   64,   62,
 /*   520 */    60,   58,   57,   56,   50,   49,   48,  168,  845,   98,
 /*   530 */   617,  864,  845,  810,   53,   55,   54,   52,   51,   64,
 /*   540 */    62,   60,   58,   57,   56,   50,   49,   48,  206,  863,
 /*   550 */   845,  862,  845,  612,  169,  845,  170,  845,  591,  165,
 /*   560 */   845,  227,   66,   67,  166,  845,  167,  845,  181,  206,
 /*   570 */   851,  845,  296,  759,  161,  845,  286,   47,   42,   46,
 /*   580 */   850,  845,  101,   41,   67,  160,  845,  849,  845,  159,
 /*   590 */   845,  157,  845,  156,  845,  155,  845,  286,   47,   42,
 /*   600 */    46,  289,  759,   53,   55,   54,   52,   51,   64,   62,
 /*   610 */    60,   58,   57,   56,   50,   49,   48,   33,   32,  266,
 /*   620 */   476,  477,  222,  295,   53,   55,   54,   52,   51,   64,
 /*   630 */    62,   60,   58,   57,   56,   50,   49,   48,  551,  290,
 /*   640 */   496,  839,  262,  682,  174,  683,  684,  685,  686,  687,
 /*   650 */   688,  689,  690,  691,  692,  693,  694,  695,  696,  840,
 /*   660 */   127,  682,  174,  683,  684,  685,  686,  687,  688,  689,
 /*   670 */   690,  691,  692,  693,  694,  695,  696,  674,  290,  234,
 /*   680 */   290,  672,  290,  229,  671,  290,  103,   28,  567,  241,
 /*   690 */   580,  290,  549,   18,  114,   11,  104,  541,  290,  199,
 /*   700 */   117,  542,   43,  543,   43,   28,  499,   90,  681,  260,
 /*   710 */   682,  174,  683,  684,  685,  686,  687,  688,  689,  690,
 /*   720 */   691,  692,  693,  694,  695,  696,  494,  492,  263,  264,
 /*   730 */   503,  290,  645,   43,  654,   43,  243,  653,   43,   11,
 /*   740 */    65,  644,   43,  284,  293,  290,  182,  642,   65,  233,
 /*   750 */   183,  244,  184,  185,   28,  187,  662,  663,  664,  287,
 /*   760 */   135,  189,  113,   33,  662,  663,  664,  777,  122,  115,
 /*   770 */   250,  255,   69,  251,  775,   65,  191,  198,  119,   72,
 /*   780 */    69,  120,   10,   65,   38,   87,  123,  207,   35,   88,
 /*   790 */     6,  662,  663,  664,  760,  276,   61,   63,    6,  641,
 /*   800 */   663,  664,  129,  480,   61,   63,   34,   69,  130,  665,
 /*   810 */    59,  204,  659,  660,  661,   69,  274,  665,   59,  200,
 /*   820 */   659,  660,  661,  131,  132,    6,    3,  297,   81,    2,
 /*   830 */   609,   61,   63,    6,  607,  606,  211,  213,  605,   61,
 /*   840 */    63,  603,   45,  602,  665,   59,  204,  659,  660,  661,
 /*   850 */   601,  218,  665,   59,  204,  659,  660,  661,  640,  663,
 /*   860 */   664,  597,  595,  593,  589,  586,  100,  225,  102,  226,
 /*   870 */    20,  559,  230,  106,   69,   29,  105,  107,  235,  108,
 /*   880 */   109,  238,  568,  240,  186,  242,  110,   30,  172,  575,
 /*   890 */   116,   73,    6,   31,  121,  253,  257,   13,   61,   63,
 /*   900 */   535,   14,  124,  539,  500,  498,  267,  497,  495,  493,
 /*   910 */   125,  665,   59,  204,  659,  660,  661,   15,  491,  509,
 /*   920 */   268,    8,  618,  506,  505,  650,   43,  292,  649,   16,
 /*   930 */   483,  646,  284,  647,    4,  282,  652,    5,  128,  679,
 /*   940 */   533,   25,  678,  532,   26,  678,  678,  678,  678,  678,
 /*   950 */   668,  678,  678,  678,  283,  678,  678,  678,  678,  613,
};
static const YYCODETYPE yy_lookahead[] = {
 /*     0 */   110,  111,  112,  113,  114,  115,  116,  117,  118,  119,
 /*    10 */   120,  121,  122,  123,  124,  125,  126,  127,  129,  128,
 /*    20 */   129,  128,  129,  112,  113,  128,  129,  129,  131,  132,
 /*    30 */   139,  140,  161,  140,   34,  164,   20,  128,  129,  128,
 /*    40 */   129,  130,  112,  113,  154,  147,  156,   47,   48,   49,
 /*    50 */    34,   27,   34,  129,   30,   31,  128,  129,  128,  129,
 /*    60 */   130,    1,   62,   63,   64,   65,   48,  156,   67,   53,
 /*    70 */    69,   55,  129,   57,   58,   26,   27,   28,   29,   55,
 /*    80 */    62,   63,   64,   65,  112,  113,  156,  128,  129,   89,
 /*    90 */    90,   91,   92,   93,   94,   95,   96,   97,   98,   99,
 /*   100 */   100,  101,  102,   10,   11,  105,   74,   89,   90,   91,
 /*   110 */    92,   93,   94,   95,   96,   97,   98,   99,  100,  101,
 /*   120 */   102,  149,  128,  129,  130,  152,  105,   34,  156,    1,
 /*   130 */    84,   89,    2,   73,   92,   93,  163,  105,  165,  166,
 /*   140 */    41,   48,   49,  101,  102,   17,  105,   34,  152,   21,
 /*   150 */    22,  105,   24,   25,  129,   62,   63,   64,   65,   84,
 /*   160 */    32,  165,  166,   46,   36,   37,   38,   39,   40,   94,
 /*   170 */    42,   43,   44,   45,   75,   62,   63,   64,   65,  129,
 /*   180 */   112,  113,   89,   90,   91,   92,   93,   94,   95,   96,
 /*   190 */    97,   98,   99,  100,  101,  102,  128,  129,   70,   71,
 /*   200 */   146,   73,   89,   90,   91,   92,   93,   94,   95,   96,
 /*   210 */    97,   98,   99,  100,  101,  102,   86,   34,   53,    1,
 /*   220 */    55,   56,  105,   58,  156,  128,  129,  112,  113,  132,
 /*   230 */   169,   48,   49,    1,    9,   17,  128,  129,  130,   21,
 /*   240 */    22,   42,   24,   25,  135,   62,   63,   64,   65,  157,
 /*   250 */    32,   19,   20,   42,   36,   37,   38,   39,   40,  145,
 /*   260 */    42,   43,   44,   45,  128,  129,  130,  145,   85,   47,
 /*   270 */   105,  156,   89,   90,   91,   92,   93,   94,   95,   96,
 /*   280 */    97,   98,   99,  100,  101,  102,   10,   11,   70,  128,
 /*   290 */   129,   73,  148,   50,   51,   52,   89,   90,   91,   92,
 /*   300 */    93,   12,  148,   34,  105,   73,   84,  100,  101,  102,
 /*   310 */    67,   86,  128,  129,  130,    1,  105,   48,   49,  128,
 /*   320 */   129,  130,   89,   34,   46,  128,  129,   13,   50,   51,
 /*   330 */    52,   62,   63,   64,   65,   59,  128,  129,  105,   50,
 /*   340 */    51,   52,  128,  129,  128,  129,  103,  142,  105,  106,
 /*   350 */   107,  108,  128,  129,   85,   66,  112,  113,   89,   90,
 /*   360 */    91,   92,   93,   94,   95,   96,   97,   98,   99,  100,
 /*   370 */   101,  102,  143,   84,   85,   50,   51,   52,   89,   90,
 /*   380 */    91,  103,  134,  105,  106,  107,  108,   73,  105,   34,
 /*   390 */   128,  129,  103,  104,  105,  106,  107,  108,  112,  113,
 /*   400 */   156,  128,  129,   48,   49,   76,   77,  160,   79,   80,
 /*   410 */    81,   82,   83,  128,  129,   86,  160,   62,   63,   64,
 /*   420 */    65,  112,  113,   68,   33,  112,  113,   47,  103,  170,
 /*   430 */   105,  106,  107,  108,  112,  113,  112,  113,  128,  129,
 /*   440 */   112,  113,  156,   33,   89,   90,   91,   92,   93,   94,
 /*   450 */    95,   96,   97,   98,   99,  100,  101,  102,   34,  112,
 /*   460 */   113,  112,  113,  128,  129,  156,  128,  129,   88,  156,
 /*   470 */   128,  129,   48,   49,   14,   15,   16,  160,  156,   34,
 /*   480 */   156,  128,  129,  160,  156,  105,   62,   63,   64,   65,
 /*   490 */   128,  129,   68,   48,   49,  128,  129,  128,  129,  128,
 /*   500 */   129,  144,  145,  156,  170,  156,  170,   62,   63,   64,
 /*   510 */    65,  150,  151,   89,   90,   91,   92,   93,   94,   95,
 /*   520 */    96,   97,   98,   99,  100,  101,  102,  128,  129,  170,
 /*   530 */    85,  128,  129,  160,   89,   90,   91,   92,   93,   94,
 /*   540 */    95,   96,   97,   98,   99,  100,  101,  102,   34,  128,
 /*   550 */   129,  128,  129,  105,  128,  129,  128,  129,   41,  128,
 /*   560 */   129,  160,   48,   49,  128,  129,  128,  129,  133,   34,
 /*   570 */   128,  129,  158,  159,  128,  129,   62,   63,   64,   65,
 /*   580 */   128,  129,  162,   48,   49,  128,  129,  128,  129,  128,
 /*   590 */   129,  128,  129,  128,  129,  128,  129,   62,   63,   64,
 /*   600 */    65,  158,  159,   89,   90,   91,   92,   93,   94,   95,
 /*   610 */    96,   97,   98,   99,  100,  101,  102,    3,   60,   61,
 /*   620 */    12,   13,  105,   47,   89,   90,   91,   92,   93,   94,
 /*   630 */    95,   96,   97,   98,   99,  100,  101,  102,   85,   86,
 /*   640 */    76,  110,   78,  112,  113,  114,  115,  116,  117,  118,
 /*   650 */   119,  120,  121,  122,  123,  124,  125,  126,  127,  110,
 /*   660 */    84,  112,  113,  114,  115,  116,  117,  118,  119,  120,
 /*   670 */   121,  122,  123,  124,  125,  126,  127,   85,   86,   85,
 /*   680 */    86,   85,   86,  160,   85,   86,  162,  156,   85,   86,
 /*   690 */    85,   86,   85,   86,   26,   84,   28,   85,   86,   88,
 /*   700 */    86,   85,   86,   85,   86,  156,   76,    1,  110,   79,
 /*   710 */   112,  113,  114,  115,  116,  117,  118,  119,  120,  121,
 /*   720 */   122,  123,  124,  125,  126,  127,   76,   76,   78,   78,
 /*   730 */    85,   86,   85,   86,   85,   86,  105,   85,   86,   84,
 /*   740 */    34,   85,   86,   88,   85,   86,  133,   50,   34,  160,
 /*   750 */   133,  160,  133,  133,  156,  133,   50,   51,   52,   62,
 /*   760 */   167,  160,  162,    3,   50,   51,   52,  135,   84,  160,
 /*   770 */   160,   19,   66,  149,  151,   34,  133,  133,  153,   73,
 /*   780 */    66,  160,  153,   34,   67,  147,  160,  133,    8,  141,
 /*   790 */    84,   50,   51,   52,  159,    7,   90,   91,   84,   50,
 /*   800 */    51,   52,  138,   89,   90,   91,    6,   66,  137,  103,
 /*   810 */   104,  105,  106,  107,  108,   66,    4,  103,  104,  105,
 /*   820 */   106,  107,  108,  136,  135,   84,   87,  155,  168,   72,
 /*   830 */   105,   90,   91,   84,  105,  105,   47,   47,  105,   90,
 /*   840 */    91,  105,   34,  105,  103,  104,  105,  106,  107,  108,
 /*   850 */   105,   75,  103,  104,  105,  106,  107,  108,   50,   51,
 /*   860 */    52,   41,   41,   41,   85,   35,   84,  105,   27,   60,
 /*   870 */    47,   35,   34,   57,   66,   84,   84,   84,   54,   84,
 /*   880 */    84,   54,   85,  106,  106,   84,   84,   84,   54,   50,
 /*   890 */     2,   23,   84,   94,   18,   88,   86,   84,   90,   91,
 /*   900 */    19,   84,   18,  105,   76,   76,   47,   76,   76,   76,
 /*   910 */    84,  103,  104,  105,  106,  107,  108,    5,   76,  105,
 /*   920 */    85,    5,   85,  105,  105,   39,   86,   47,   39,   86,
 /*   930 */   105,   85,   88,   85,   84,   88,   85,   84,   86,    0,
 /*   940 */    85,   84,  171,   85,   84,  171,  171,  171,  171,  171,
 /*   950 */   105,  171,  171,  171,  105,  171,  171,  171,  171,  105,
 /*   960 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*   970 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*   980 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*   990 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*  1000 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*  1010 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*  1020 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*  1030 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*  1040 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*  1050 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
 /*  1060 */   171,  171,  171,  171,  171,  171,  171,  171,  171,  171,
};
#define YY_SHIFT_COUNT    (297)
#define YY_SHIFT_MIN      (0)
#define YY_SHIFT_MAX      (939)
static const unsigned short int yy_shift_ofst[] = {
 /*     0 */   218,  128,  218,  218,  706,  706,  706,  714,  741,  165,
 /*    10 */   232,  289,  714,  741,  741,  741,  741,  741,  165,  314,
 /*    20 */    60,   60,   60,   60,   60,   60,   60,   60,   60,  741,
 /*    30 */   741,  741,  741,  741,  741,  741,  741,  741,  741,  741,
 /*    40 */   741,  741,  741,  741,  741,  749,  808,  741,  741,  741,
 /*    50 */   741,  741,  741,  741,  741,  741,  741,  741,  741,  741,
 /*    60 */   741,  741,  741,  741,  741,  741,  741,  741,  741,  243,
 /*    70 */    24,   46,   32,   21,   41,  278,  325,  325,  325,  325,
 /*    80 */   329,   16,  614,  558,   46,   46,    1,    1,  225,  130,
 /*    90 */   608,  283,  283,  391,  283,  391,  283,  391,  283,  391,
 /*   100 */   448,  283,  410,  283,  410,  448,  283,  448,  448,  448,
 /*   110 */   448,  283,  631,  283,  410,  760,  283,   21,  283,  752,
 /*   120 */   684,  283,  448,  684,  283,  448,  717,  448,   41,  780,
 /*   130 */   788,  800,  812,  760,  739,  960,    0,   93,  183,  269,
 /*   140 */   355,  424,  445,  514,  514,  514,  514,  514,  514,  514,
 /*   150 */   514,  514,  535,  514,  514,  514,   18,  113,  113,  207,
 /*   160 */   207,  207,  207,  207,  207,  207,  207,  207,   42,   42,
 /*   170 */    42,   49,  276,  380,  460,  117,  199,  211,   99,  517,
 /*   180 */    75,  553,  592,  594,  596,  599,  603,  605,  607,  222,
 /*   190 */   668,  612,  616,  618,  630,  564,  650,  651,  645,  233,
 /*   200 */   611,  647,  649,  652,  655,  656,  697,  659,  576,  757,
 /*   210 */   725,  729,  789,  730,  790,  733,  736,  738,  745,  776,
 /*   220 */   820,  821,  822,  779,  830,  782,  762,  809,  841,  823,
 /*   230 */   836,  838,  791,  792,  816,  793,  824,  795,  796,  827,
 /*   240 */   797,  777,  778,  801,  802,  803,  839,  834,  888,  799,
 /*   250 */   868,  810,  876,  798,  807,  813,  881,  817,  810,  884,
 /*   260 */   828,  829,  831,  832,  833,  842,  826,  814,  859,  835,
 /*   270 */   818,  819,  825,  840,  912,  843,  916,  837,  844,  886,
 /*   280 */   846,  848,  845,  847,  849,  851,  850,  853,  889,  852,
 /*   290 */   854,  855,  857,  880,  858,  860,  852,  939,
};
#define YY_REDUCE_COUNT (135)
#define YY_REDUCE_MIN   (-129)
#define YY_REDUCE_MAX   (689)
static const short yy_reduce_ofst[] = {
 /*     0 */  -110,  531,  549,  598,  -89,  -70,   68, -103, -109,  -27,
 /*    10 */   -28,   -6,   97,  108,  136,  184, -107,  191,   -4,  115,
 /*    20 */   244,  286,  309,  313,  322,  324,  328,  347,  349,  -91,
 /*    30 */   -72,  -41,  161,  197,  208,  214,  216,  224,  262,  273,
 /*    40 */   285,  310,  335,  338,  342,  353,  362,  367,  369,  371,
 /*    50 */   399,  403,  421,  423,  426,  428,  431,  436,  438,  442,
 /*    60 */   446,  452,  457,  459,  461,  353,  463,  465,  467, -102,
 /*    70 */  -129,  357,  414,  361,  443, -111,  -76,  -57,   25,   50,
 /*    80 */    54,   61,  109,   92,  114,  122,  144,  154,  205,  229,
 /*    90 */   248,  247,  256,  259,  317,  334,  323,  336,  373,  359,
 /*   100 */   435,  401,  420,  523,  524,  613,  589,  617,  619,  620,
 /*   110 */   622,  591,  593,  601,  600,  632,  609,  623,  610,  624,
 /*   120 */   625,  621,  643,  629,  626,  644,  638,  654,  635,  648,
 /*   130 */   664,  671,  687,  689,  672,  660,
};
static const YYACTIONTYPE yy_default[] = {
 /*     0 */   676,  676,  676,  899,  676,  676,  676,  676,  676,  676,
 /*    10 */   676,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*    20 */   676,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*    30 */   676,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*    40 */   676,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*    50 */   676,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*    60 */   676,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*    70 */   782,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*    80 */   715,  793,  739,  730,  676,  676,  886,  886,  754,  714,
 /*    90 */   704,  676,  676,  814,  676,  814,  676,  814,  676,  814,
 /*   100 */   676,  676,  787,  676,  787,  676,  676,  676,  676,  676,
 /*   110 */   676,  676,  794,  676,  787,  739,  676,  676,  676,  676,
 /*   120 */   769,  676,  676,  769,  676,  676,  676,  676,  676,  752,
 /*   130 */   745,  743,  741,  739,  898,  798,  711,  749,  676,  676,
 /*   140 */   676,  676,  676,  776,  731,  740,  744,  753,  756,  884,
 /*   150 */   887,  885,  676,  844,  843,  755,  852,  853,  848,  854,
 /*   160 */   855,  856,  872,  877,  868,  859,  858,  857,  865,  861,
 /*   170 */   860,  676,  800,  733,  698,  676,  676,  676,  827,  819,
 /*   180 */   816,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*   190 */   676,  676,  676,  676,  676,  676,  676,  676,  676,  676,
 /*   200 */   895,  676,  676,  676,  895,  676,  676,  676,  676,  676,
 /*   210 */   676,  676,  676,  676,  676,  676,  676,  676,  676,  828,
 /*   220 */   825,  823,  821,  676,  676,  676,  676,  676,  676,  676,
 /*   230 */   676,  676,  676,  902,  676,  676,  676,  676,  676,  676,
 /*   240 */   676,  676,  676,  795,  808,  676,  676,  676,  676,  676,
 /*   250 */   676,  766,  676,  676,  767,  676,  676,  676,  763,  676,
 /*   260 */   676,  676,  676,  676,  676,  676,  676,  676,  737,  676,
 /*   270 */   676,  676,  676,  742,  676,  746,  676,  676,  895,  676,
 /*   280 */   676,  676,  676,  896,  676,  676,  676,  676,  676,  758,
 /*   290 */   676,  676,  676,  676,  676,  676,  757,  676,
};
/********** End of lemon-generated parsing tables *****************************/

/* The next table maps tokens (terminal symbols) into fallback tokens.  
** If a construct like the following:
** 
**      %fallback ID X Y Z.
**
** appears in the grammar, then ID becomes a fallback token for X, Y,
** and Z.  Whenever one of the tokens X, Y, or Z is input to the parser
** but it does not parse, the type of the token is changed to ID and
** the parse is retried before an error is thrown.
**
** This feature can be used, for example, to cause some keywords in a language
** to revert to identifiers if they keyword does not apply in the context where
** it appears.
*/
#ifdef YYFALLBACK
static const YYCODETYPE yyFallback[] = {
};
#endif /* YYFALLBACK */

/* The following structure represents a single element of the
** parser's stack.  Information stored includes:
**
**   +  The state number for the parser at this level of the stack.
**
**   +  The value of the token stored at this level of the stack.
**      (In other words, the "major" token.)
**
**   +  The semantic value stored at this level of the stack.  This is
**      the information used by the action routines in the grammar.
**      It is sometimes called the "minor" token.
**
** After the "shift" half of a SHIFTREDUCE action, the stateno field
** actually contains the reduce action for the second half of the
** SHIFTREDUCE.
*/
struct yyStackEntry {
  YYACTIONTYPE stateno;  /* The state-number, or reduce action in SHIFTREDUCE */
  YYCODETYPE major;      /* The major token value.  This is the code
                         ** number for the token at this stack level */
  YYMINORTYPE minor;     /* The user-supplied minor token value.  This
                         ** is the value of the token  */
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc;  /* Source location for this stack entry.
                         ** Only present when %locations is active. */
#endif
};
typedef struct yyStackEntry yyStackEntry;

/* The state of the parser is completely contained in an instance of
** the following structure */
struct yyParser {
  yyStackEntry *yytos;          /* Pointer to top element of the stack */
#ifdef YYTRACKMAXSTACKDEPTH
  int yyhwm;                    /* High-water mark of the stack */
#endif
#ifndef YYNOERRORRECOVERY
  int yyerrcnt;                 /* Shifts left before out of the error */
#endif
#ifdef YYLOCATIONTYPE
  /* Source location of the token currently being processed.  Updated
  ** at each SqlParseLoc() entry so that %syntax_error sees the location
  ** of the offending lookahead, not the location of the stack-top
  ** symbol (which would be the previously-shifted token, off by one). */
  YYLOCATIONTYPE yyLookaheadLoc;
#endif
  /* Action-time lookahead access (P0-NEW-5).  While SqlParse() runs a
  ** sequence of reduces before shifting the just-pushed token, the
  ** token's external code and value are stashed here so that user
  ** action code can read them via SqlParse_get_lookahead() and
  ** optionally signal consumption via SqlParse_clear_lookahead().  This
  ** is the equivalent of bison's yychar/yyclearin and exists to
  ** support grammars (notably PostgreSQL's plpgsql pl_gram.y) where
  ** an empty production needs to consume the lookahead and reach
  ** into the lexer for further tokens.
  **
  ** yyLookaheadMajor is YYEMPTY (-2) when no SqlParse() call is in
  ** progress.  yyLookaheadCleared is set by SqlParse_clear_lookahead()
  ** to tell the dispatch loop to skip the trailing shift and return,
  ** treating the lookahead as consumed by the action body. */
  int yyLookaheadMajor;
  SqlParseTOKENTYPE yyLookaheadMinor;
  char yyLookaheadCleared;
  SqlParseARG_SDECL                /* A place to hold %extra_argument */
  SqlParseCTX_SDECL                /* A place to hold %extra_context */
  yyStackEntry *yystackEnd;           /* Last entry in the stack */
  yyStackEntry *yystack;              /* The parser stack */
  yyStackEntry yystk0[YYSTACKDEPTH];  /* Initial stack space */
};
typedef struct yyParser yyParser;

#include <assert.h>
#include <stdlib.h>
#include <string.h>
#ifndef NDEBUG
#include <stdio.h>
static FILE *yyTraceFILE = 0;
static char *yyTracePrompt = 0;
#endif /* NDEBUG */

#ifndef NDEBUG
/* 
** Turn parser tracing on by giving a stream to which to write the trace
** and a prompt to preface each trace message.  Tracing is turned off
** by making either argument NULL 
**
** Inputs:
** <ul>
** <li> A FILE* to which trace output should be written.
**      If NULL, then tracing is turned off.
** <li> A prefix string written at the beginning of every
**      line of trace output.  If NULL, then tracing is
**      turned off.
** </ul>
**
** Outputs:
** None.
*/
void SqlParseTrace(FILE*,char*);
void SqlParseTrace(FILE *TraceFILE, char *zTracePrompt){
  yyTraceFILE = TraceFILE;
  yyTracePrompt = zTracePrompt;
  if( yyTraceFILE==0 ) yyTracePrompt = 0;
  else if( yyTracePrompt==0 ) yyTraceFILE = 0;
}
#endif /* NDEBUG */

/* Terminal and nonterminal names.  Always emitted (required by
** SqlParseTokenName, SqlParseExpectedTokens, SqlParseExpectedTokensString,
** and optionally by trace/coverage code). */
static const char *const yyTokenName[] = { 
  /*    0 */ "$",
  /*    1 */ "TK_SELECT",
  /*    2 */ "TK_FROM",
  /*    3 */ "TK_WHERE",
  /*    4 */ "TK_GROUP",
  /*    5 */ "TK_BY",
  /*    6 */ "TK_HAVING",
  /*    7 */ "TK_ORDER",
  /*    8 */ "TK_LIMIT",
  /*    9 */ "TK_OFFSET",
  /*   10 */ "TK_ASC",
  /*   11 */ "TK_DESC",
  /*   12 */ "TK_DISTINCT",
  /*   13 */ "TK_ALL",
  /*   14 */ "TK_UNION",
  /*   15 */ "TK_INTERSECT",
  /*   16 */ "TK_EXCEPT",
  /*   17 */ "TK_INSERT",
  /*   18 */ "TK_INTO",
  /*   19 */ "TK_VALUES",
  /*   20 */ "TK_DEFAULT",
  /*   21 */ "TK_REPLACE",
  /*   22 */ "TK_UPDATE",
  /*   23 */ "TK_SET",
  /*   24 */ "TK_DELETE",
  /*   25 */ "TK_CREATE",
  /*   26 */ "TK_TABLE",
  /*   27 */ "TK_INDEX",
  /*   28 */ "TK_VIEW",
  /*   29 */ "TK_TRIGGER",
  /*   30 */ "TK_TEMP",
  /*   31 */ "TK_TEMPORARY",
  /*   32 */ "TK_DROP",
  /*   33 */ "TK_IF",
  /*   34 */ "TK_NOT",
  /*   35 */ "TK_EXISTS",
  /*   36 */ "TK_PRAGMA",
  /*   37 */ "TK_BEGIN",
  /*   38 */ "TK_COMMIT",
  /*   39 */ "TK_END",
  /*   40 */ "TK_ROLLBACK",
  /*   41 */ "TK_TRANSACTION",
  /*   42 */ "TK_SAVEPOINT",
  /*   43 */ "TK_RELEASE",
  /*   44 */ "TK_ATTACH",
  /*   45 */ "TK_DETACH",
  /*   46 */ "TK_DATABASE",
  /*   47 */ "TK_AS",
  /*   48 */ "TK_AND",
  /*   49 */ "TK_OR",
  /*   50 */ "TK_NULL",
  /*   51 */ "TK_TRUE",
  /*   52 */ "TK_FALSE",
  /*   53 */ "TK_PRIMARY",
  /*   54 */ "TK_KEY",
  /*   55 */ "TK_UNIQUE",
  /*   56 */ "TK_FOREIGN",
  /*   57 */ "TK_REFERENCES",
  /*   58 */ "TK_CHECK",
  /*   59 */ "TK_AUTOINCR",
  /*   60 */ "TK_ON",
  /*   61 */ "TK_USING",
  /*   62 */ "TK_IN",
  /*   63 */ "TK_LIKE",
  /*   64 */ "TK_BETWEEN",
  /*   65 */ "TK_IS",
  /*   66 */ "TK_CASE",
  /*   67 */ "TK_WHEN",
  /*   68 */ "TK_THEN",
  /*   69 */ "TK_ELSE",
  /*   70 */ "TK_EXPLAIN",
  /*   71 */ "TK_QUERY",
  /*   72 */ "TK_PLAN",
  /*   73 */ "TK_WITH",
  /*   74 */ "TK_RECURSIVE",
  /*   75 */ "TK_TO",
  /*   76 */ "TK_JOIN",
  /*   77 */ "TK_INNER",
  /*   78 */ "TK_OUTER",
  /*   79 */ "TK_LEFT",
  /*   80 */ "TK_RIGHT",
  /*   81 */ "TK_FULL",
  /*   82 */ "TK_CROSS",
  /*   83 */ "TK_NATURAL",
  /*   84 */ "TK_LP",
  /*   85 */ "TK_RP",
  /*   86 */ "TK_COMMA",
  /*   87 */ "TK_SEMI",
  /*   88 */ "TK_DOT",
  /*   89 */ "TK_STAR",
  /*   90 */ "TK_PLUS",
  /*   91 */ "TK_MINUS",
  /*   92 */ "TK_SLASH",
  /*   93 */ "TK_PERCENT",
  /*   94 */ "TK_EQ",
  /*   95 */ "TK_NE",
  /*   96 */ "TK_LT",
  /*   97 */ "TK_LE",
  /*   98 */ "TK_GT",
  /*   99 */ "TK_GE",
  /*  100 */ "TK_CONCAT",
  /*  101 */ "TK_AMP",
  /*  102 */ "TK_PIPE",
  /*  103 */ "TK_QMARK",
  /*  104 */ "TK_TILDE",
  /*  105 */ "TK_ID",
  /*  106 */ "TK_NUMBER",
  /*  107 */ "TK_STRING",
  /*  108 */ "TK_BLOB",
  /*  109 */ "TK_UMINUS",
  /*  110 */ "stmt",
  /*  111 */ "stmt_list",
  /*  112 */ "select_stmt",
  /*  113 */ "select_core",
  /*  114 */ "insert_stmt",
  /*  115 */ "update_stmt",
  /*  116 */ "delete_stmt",
  /*  117 */ "create_stmt",
  /*  118 */ "drop_stmt",
  /*  119 */ "pragma_stmt",
  /*  120 */ "begin_stmt",
  /*  121 */ "commit_stmt",
  /*  122 */ "rollback_stmt",
  /*  123 */ "savepoint_stmt",
  /*  124 */ "release_stmt",
  /*  125 */ "attach_stmt",
  /*  126 */ "detach_stmt",
  /*  127 */ "explain_stmt",
  /*  128 */ "expr",
  /*  129 */ "expr_atom",
  /*  130 */ "expr_list",
  /*  131 */ "sel_list",
  /*  132 */ "sel_item",
  /*  133 */ "id_list",
  /*  134 */ "distinct_opt",
  /*  135 */ "where_opt",
  /*  136 */ "group_opt",
  /*  137 */ "having_opt",
  /*  138 */ "order_opt",
  /*  139 */ "order_list",
  /*  140 */ "order_item",
  /*  141 */ "limit_opt",
  /*  142 */ "offset_part",
  /*  143 */ "from_opt",
  /*  144 */ "table_list",
  /*  145 */ "table_ref",
  /*  146 */ "join_op",
  /*  147 */ "case_arms",
  /*  148 */ "case_else",
  /*  149 */ "values_list",
  /*  150 */ "assign_list",
  /*  151 */ "assign",
  /*  152 */ "coldef",
  /*  153 */ "insert_cols",
  /*  154 */ "input",
  /*  155 */ "maybe_semi",
  /*  156 */ "with_clause",
  /*  157 */ "join_constraint",
  /*  158 */ "cte_list",
  /*  159 */ "cte",
  /*  160 */ "qname",
  /*  161 */ "temp_opt",
  /*  162 */ "if_not_exists",
  /*  163 */ "table_body",
  /*  164 */ "unique_opt",
  /*  165 */ "table_elem",
  /*  166 */ "table_constraint",
  /*  167 */ "type_opt",
  /*  168 */ "col_constraints",
  /*  169 */ "col_constraint",
  /*  170 */ "if_exists",
};

#ifndef NDEBUG
/* For tracing reduce actions, the names of all rules are required.
*/
static const char *const yyRuleName[] = {
 /*   0 */ "input ::= stmt_list maybe_semi",
 /*   1 */ "stmt_list ::= stmt",
 /*   2 */ "stmt_list ::= stmt_list TK_SEMI stmt",
 /*   3 */ "stmt ::= select_stmt",
 /*   4 */ "stmt ::= insert_stmt",
 /*   5 */ "stmt ::= update_stmt",
 /*   6 */ "stmt ::= delete_stmt",
 /*   7 */ "stmt ::= create_stmt",
 /*   8 */ "stmt ::= drop_stmt",
 /*   9 */ "stmt ::= pragma_stmt",
 /*  10 */ "stmt ::= begin_stmt",
 /*  11 */ "stmt ::= commit_stmt",
 /*  12 */ "stmt ::= rollback_stmt",
 /*  13 */ "stmt ::= savepoint_stmt",
 /*  14 */ "stmt ::= release_stmt",
 /*  15 */ "stmt ::= attach_stmt",
 /*  16 */ "stmt ::= detach_stmt",
 /*  17 */ "stmt ::= explain_stmt",
 /*  18 */ "select_stmt ::= with_clause select_stmt",
 /*  19 */ "select_stmt ::= select_core",
 /*  20 */ "select_stmt ::= select_core TK_UNION select_stmt",
 /*  21 */ "select_stmt ::= select_core TK_UNION TK_ALL select_stmt",
 /*  22 */ "select_stmt ::= select_core TK_INTERSECT select_stmt",
 /*  23 */ "select_stmt ::= select_core TK_EXCEPT select_stmt",
 /*  24 */ "select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part",
 /*  25 */ "distinct_opt ::=",
 /*  26 */ "distinct_opt ::= TK_DISTINCT",
 /*  27 */ "distinct_opt ::= TK_ALL",
 /*  28 */ "sel_list ::= sel_item",
 /*  29 */ "sel_list ::= sel_list TK_COMMA sel_item",
 /*  30 */ "sel_item ::= TK_STAR",
 /*  31 */ "sel_item ::= TK_ID TK_DOT TK_STAR",
 /*  32 */ "sel_item ::= expr",
 /*  33 */ "sel_item ::= expr TK_AS TK_ID",
 /*  34 */ "sel_item ::= expr TK_ID",
 /*  35 */ "from_opt ::=",
 /*  36 */ "from_opt ::= TK_FROM table_list",
 /*  37 */ "table_list ::= table_ref",
 /*  38 */ "table_list ::= table_list TK_COMMA table_ref",
 /*  39 */ "table_list ::= table_list join_op table_ref join_constraint",
 /*  40 */ "join_op ::= TK_JOIN",
 /*  41 */ "join_op ::= TK_INNER TK_JOIN",
 /*  42 */ "join_op ::= TK_LEFT TK_JOIN",
 /*  43 */ "join_op ::= TK_LEFT TK_OUTER TK_JOIN",
 /*  44 */ "join_op ::= TK_RIGHT TK_JOIN",
 /*  45 */ "join_op ::= TK_RIGHT TK_OUTER TK_JOIN",
 /*  46 */ "join_op ::= TK_FULL TK_JOIN",
 /*  47 */ "join_op ::= TK_FULL TK_OUTER TK_JOIN",
 /*  48 */ "join_op ::= TK_CROSS TK_JOIN",
 /*  49 */ "join_op ::= TK_NATURAL TK_JOIN",
 /*  50 */ "join_op ::= TK_NATURAL TK_LEFT TK_JOIN",
 /*  51 */ "join_constraint ::=",
 /*  52 */ "join_constraint ::= TK_ON expr",
 /*  53 */ "join_constraint ::= TK_USING TK_LP id_list TK_RP",
 /*  54 */ "table_ref ::= TK_ID",
 /*  55 */ "table_ref ::= TK_ID TK_DOT TK_ID",
 /*  56 */ "table_ref ::= TK_ID TK_AS TK_ID",
 /*  57 */ "table_ref ::= TK_ID TK_ID",
 /*  58 */ "table_ref ::= TK_LP select_stmt TK_RP",
 /*  59 */ "table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID",
 /*  60 */ "where_opt ::=",
 /*  61 */ "where_opt ::= TK_WHERE expr",
 /*  62 */ "group_opt ::=",
 /*  63 */ "group_opt ::= TK_GROUP TK_BY expr_list",
 /*  64 */ "having_opt ::=",
 /*  65 */ "having_opt ::= TK_HAVING expr",
 /*  66 */ "order_opt ::=",
 /*  67 */ "order_opt ::= TK_ORDER TK_BY order_list",
 /*  68 */ "order_list ::= order_item",
 /*  69 */ "order_list ::= order_list TK_COMMA order_item",
 /*  70 */ "order_item ::= expr",
 /*  71 */ "order_item ::= expr TK_ASC",
 /*  72 */ "order_item ::= expr TK_DESC",
 /*  73 */ "limit_opt ::=",
 /*  74 */ "limit_opt ::= TK_LIMIT expr",
 /*  75 */ "offset_part ::=",
 /*  76 */ "offset_part ::= TK_OFFSET expr",
 /*  77 */ "offset_part ::= TK_COMMA expr",
 /*  78 */ "with_clause ::= TK_WITH cte_list",
 /*  79 */ "with_clause ::= TK_WITH TK_RECURSIVE cte_list",
 /*  80 */ "cte_list ::= cte",
 /*  81 */ "cte_list ::= cte_list TK_COMMA cte",
 /*  82 */ "cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP",
 /*  83 */ "cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP",
 /*  84 */ "insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list",
 /*  85 */ "insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES",
 /*  86 */ "insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt",
 /*  87 */ "insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list",
 /*  88 */ "qname ::= TK_ID",
 /*  89 */ "qname ::= TK_ID TK_DOT TK_ID",
 /*  90 */ "insert_cols ::=",
 /*  91 */ "insert_cols ::= TK_LP id_list TK_RP",
 /*  92 */ "values_list ::= TK_VALUES TK_LP expr_list TK_RP",
 /*  93 */ "values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP",
 /*  94 */ "update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt",
 /*  95 */ "assign_list ::= assign",
 /*  96 */ "assign_list ::= assign_list TK_COMMA assign",
 /*  97 */ "assign ::= TK_ID TK_EQ expr",
 /*  98 */ "delete_stmt ::= TK_DELETE TK_FROM qname where_opt",
 /*  99 */ "create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP",
 /* 100 */ "create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt",
 /* 101 */ "create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP",
 /* 102 */ "create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt",
 /* 103 */ "temp_opt ::=",
 /* 104 */ "temp_opt ::= TK_TEMP",
 /* 105 */ "temp_opt ::= TK_TEMPORARY",
 /* 106 */ "unique_opt ::=",
 /* 107 */ "unique_opt ::= TK_UNIQUE",
 /* 108 */ "if_not_exists ::=",
 /* 109 */ "if_not_exists ::= TK_IF TK_NOT TK_EXISTS",
 /* 110 */ "table_body ::= table_elem",
 /* 111 */ "table_body ::= table_body TK_COMMA table_elem",
 /* 112 */ "table_elem ::= coldef",
 /* 113 */ "table_elem ::= table_constraint",
 /* 114 */ "coldef ::= TK_ID type_opt col_constraints",
 /* 115 */ "type_opt ::=",
 /* 116 */ "type_opt ::= TK_ID",
 /* 117 */ "type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP",
 /* 118 */ "type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP",
 /* 119 */ "col_constraints ::=",
 /* 120 */ "col_constraints ::= col_constraints col_constraint",
 /* 121 */ "col_constraint ::= TK_PRIMARY TK_KEY",
 /* 122 */ "col_constraint ::= TK_PRIMARY TK_KEY TK_ASC",
 /* 123 */ "col_constraint ::= TK_PRIMARY TK_KEY TK_DESC",
 /* 124 */ "col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR",
 /* 125 */ "col_constraint ::= TK_NOT TK_NULL",
 /* 126 */ "col_constraint ::= TK_UNIQUE",
 /* 127 */ "col_constraint ::= TK_DEFAULT expr_atom",
 /* 128 */ "col_constraint ::= TK_CHECK TK_LP expr TK_RP",
 /* 129 */ "col_constraint ::= TK_REFERENCES qname",
 /* 130 */ "col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP",
 /* 131 */ "drop_stmt ::= TK_DROP TK_TABLE if_exists qname",
 /* 132 */ "drop_stmt ::= TK_DROP TK_INDEX if_exists qname",
 /* 133 */ "drop_stmt ::= TK_DROP TK_VIEW if_exists qname",
 /* 134 */ "drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname",
 /* 135 */ "if_exists ::=",
 /* 136 */ "if_exists ::= TK_IF TK_EXISTS",
 /* 137 */ "pragma_stmt ::= TK_PRAGMA qname",
 /* 138 */ "pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom",
 /* 139 */ "pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP",
 /* 140 */ "begin_stmt ::= TK_BEGIN",
 /* 141 */ "begin_stmt ::= TK_BEGIN TK_TRANSACTION",
 /* 142 */ "begin_stmt ::= TK_BEGIN TK_ID",
 /* 143 */ "begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION",
 /* 144 */ "commit_stmt ::= TK_COMMIT",
 /* 145 */ "commit_stmt ::= TK_COMMIT TK_TRANSACTION",
 /* 146 */ "commit_stmt ::= TK_END",
 /* 147 */ "commit_stmt ::= TK_END TK_TRANSACTION",
 /* 148 */ "rollback_stmt ::= TK_ROLLBACK",
 /* 149 */ "rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION",
 /* 150 */ "rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID",
 /* 151 */ "rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID",
 /* 152 */ "rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID",
 /* 153 */ "savepoint_stmt ::= TK_SAVEPOINT TK_ID",
 /* 154 */ "release_stmt ::= TK_RELEASE TK_ID",
 /* 155 */ "release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID",
 /* 156 */ "attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID",
 /* 157 */ "attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID",
 /* 158 */ "detach_stmt ::= TK_DETACH TK_ID",
 /* 159 */ "detach_stmt ::= TK_DETACH TK_DATABASE TK_ID",
 /* 160 */ "explain_stmt ::= TK_EXPLAIN stmt",
 /* 161 */ "explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt",
 /* 162 */ "id_list ::= TK_ID",
 /* 163 */ "id_list ::= id_list TK_COMMA TK_ID",
 /* 164 */ "expr_list ::= expr",
 /* 165 */ "expr_list ::= expr_list TK_COMMA expr",
 /* 166 */ "expr ::= expr_atom",
 /* 167 */ "expr ::= TK_LP expr TK_RP",
 /* 168 */ "expr ::= TK_LP select_stmt TK_RP",
 /* 169 */ "expr ::= TK_NOT expr",
 /* 170 */ "expr ::= TK_MINUS expr",
 /* 171 */ "expr ::= TK_PLUS expr",
 /* 172 */ "expr ::= TK_TILDE expr",
 /* 173 */ "expr ::= expr TK_OR expr",
 /* 174 */ "expr ::= expr TK_AND expr",
 /* 175 */ "expr ::= expr TK_EQ expr",
 /* 176 */ "expr ::= expr TK_NE expr",
 /* 177 */ "expr ::= expr TK_LT expr",
 /* 178 */ "expr ::= expr TK_LE expr",
 /* 179 */ "expr ::= expr TK_GT expr",
 /* 180 */ "expr ::= expr TK_GE expr",
 /* 181 */ "expr ::= expr TK_PLUS expr",
 /* 182 */ "expr ::= expr TK_MINUS expr",
 /* 183 */ "expr ::= expr TK_STAR expr",
 /* 184 */ "expr ::= expr TK_SLASH expr",
 /* 185 */ "expr ::= expr TK_PERCENT expr",
 /* 186 */ "expr ::= expr TK_CONCAT expr",
 /* 187 */ "expr ::= expr TK_AMP expr",
 /* 188 */ "expr ::= expr TK_PIPE expr",
 /* 189 */ "expr ::= expr TK_LIKE expr",
 /* 190 */ "expr ::= expr TK_IS TK_NULL",
 /* 191 */ "expr ::= expr TK_IS TK_NOT TK_NULL",
 /* 192 */ "expr ::= expr TK_NOT TK_NULL",
 /* 193 */ "expr ::= expr TK_IS expr",
 /* 194 */ "expr ::= expr TK_IN TK_LP expr_list TK_RP",
 /* 195 */ "expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP",
 /* 196 */ "expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP",
 /* 197 */ "expr ::= expr TK_IN TK_LP select_stmt TK_RP",
 /* 198 */ "expr ::= expr TK_BETWEEN expr TK_AND expr",
 /* 199 */ "expr ::= TK_CASE expr_atom case_arms case_else TK_END",
 /* 200 */ "expr ::= TK_CASE case_arms case_else TK_END",
 /* 201 */ "expr ::= TK_ID TK_LP TK_RP",
 /* 202 */ "expr ::= TK_ID TK_LP TK_STAR TK_RP",
 /* 203 */ "expr ::= TK_ID TK_LP expr_list TK_RP",
 /* 204 */ "expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP",
 /* 205 */ "case_arms ::= TK_WHEN expr TK_THEN expr",
 /* 206 */ "case_arms ::= case_arms TK_WHEN expr TK_THEN expr",
 /* 207 */ "case_else ::=",
 /* 208 */ "case_else ::= TK_ELSE expr",
 /* 209 */ "expr_atom ::= TK_NUMBER",
 /* 210 */ "expr_atom ::= TK_STRING",
 /* 211 */ "expr_atom ::= TK_BLOB",
 /* 212 */ "expr_atom ::= TK_NULL",
 /* 213 */ "expr_atom ::= TK_TRUE",
 /* 214 */ "expr_atom ::= TK_FALSE",
 /* 215 */ "expr_atom ::= TK_QMARK",
 /* 216 */ "expr_atom ::= TK_ID",
 /* 217 */ "expr_atom ::= TK_ID TK_DOT TK_ID",
 /* 218 */ "expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID",
 /* 219 */ "maybe_semi ::=",
 /* 220 */ "maybe_semi ::= TK_SEMI",
 /* 221 */ "table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP",
 /* 222 */ "table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP",
 /* 223 */ "table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname",
 /* 224 */ "table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP",
 /* 225 */ "table_constraint ::= TK_CHECK TK_LP expr TK_RP",
};
#endif /* NDEBUG */


#if YYGROWABLESTACK
/*
** Try to increase the size of the parser stack.  Return the number
** of errors.  Return 0 on success.
*/
static int yyGrowStack(yyParser *p){
  int oldSize = 1 + (int)(p->yystackEnd - p->yystack);
  int newSize;
  int idx;
  yyStackEntry *pNew;
#ifdef YYSIZELIMIT
  int nLimit = YYSIZELIMIT(SqlParseCTX(p));
#endif

  newSize = oldSize*2 + 100;
#ifdef YYSIZELIMIT
  if( newSize>nLimit ){
    newSize = nLimit;
    if( newSize<=oldSize ) return 1;
  }
#endif
  idx = (int)(p->yytos - p->yystack);
  if( p->yystack==p->yystk0 ){
    pNew = YYREALLOC(0, newSize*sizeof(pNew[0]), SqlParseCTX(p));
    if( pNew==0 ) return 1;
    memcpy(pNew, p->yystack, oldSize*sizeof(pNew[0]));
  }else{
    pNew = YYREALLOC(p->yystack, newSize*sizeof(pNew[0]), SqlParseCTX(p));
    if( pNew==0 ) return 1;
  }
  p->yystack = pNew;
  p->yytos = &p->yystack[idx];
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE,"%sStack grows from %d to %d entries.\n",
            yyTracePrompt, oldSize, newSize);
  }
#endif
  p->yystackEnd = &p->yystack[newSize-1];
  return 0;
}
#endif /* YYGROWABLESTACK */

#if !YYGROWABLESTACK
/* For builds that do no have a growable stack, yyGrowStack always
** returns an error.
*/
# define yyGrowStack(X) 1
#endif

/* Datatype of the argument to the memory allocated passed as the
** second argument to SqlParseAlloc() below.  This can be changed by
** putting an appropriate #define in the %include section of the input
** grammar.
*/
#ifndef YYMALLOCARGTYPE
# define YYMALLOCARGTYPE size_t
#endif

/* Initialize a new parser that has already been allocated.
*/
void SqlParseInit(void* SqlParseCTX_PDECL);
void SqlParseInit(void *yypRawParser SqlParseCTX_PDECL){
  yyParser *yypParser = (yyParser*)yypRawParser;
  SqlParseCTX_STORE
#ifdef YYTRACKMAXSTACKDEPTH
  yypParser->yyhwm = 0;
#endif
  yypParser->yystack = yypParser->yystk0;
  yypParser->yystackEnd = &yypParser->yystack[YYSTACKDEPTH-1];
#ifndef YYNOERRORRECOVERY
  yypParser->yyerrcnt = -1;
#endif
#ifdef YYLOCATIONTYPE
  /* Zero-initialise the lookahead-location stash so %syntax_error
  ** sees a defined value if it fires before the first SqlParseLoc() call,
  ** or in pull-parser mode (SqlParse() without SqlParseLoc()). */
  memset(&yypParser->yyLookaheadLoc, 0, sizeof(yypParser->yyLookaheadLoc));
  /* P0-NEW-6: zero-init the sentinel slot's yyloc.  When yy_reduce
  ** invokes a user-defined YYLLOC_DEFAULT it reads yymsp[-1].yyloc
  ** (the location of the slot below the rule, Bison's Rhs[0]).  For
  ** the very first reduce -- particularly an empty rule -- yymsp[-1]
  ** is yystack[0], the parser's stack sentinel.  Without this init,
  ** that read returns whatever was in heap memory at allocation. */
  memset(&yypParser->yystack[0].yyloc, 0, sizeof(yypParser->yystack[0].yyloc));
#endif
  /* Initialise action-time lookahead state (P0-NEW-5).  YYEMPTY is
  ** -2 by convention (matching bison's yychar empty value); use it
  ** as a sentinel for "no SqlParse() call in progress". */
  yypParser->yyLookaheadMajor = -2;
  memset(&yypParser->yyLookaheadMinor, 0, sizeof(yypParser->yyLookaheadMinor));
  yypParser->yyLookaheadCleared = 0;
  yypParser->yytos = yypParser->yystack;
  yypParser->yystack[0].stateno = 0;
  yypParser->yystack[0].major = 0;
}

#ifndef SqlParse_ENGINEALWAYSONSTACK
/* 
** This function allocates a new parser.
** The only argument is a pointer to a function which works like
** malloc.
**
** Inputs:
** A pointer to the function used to allocate memory.
**
** Outputs:
** A pointer to a parser.  This pointer is used in subsequent calls
** to SqlParse and SqlParseFree.
*/
void *SqlParseAlloc(void *(*)(YYMALLOCARGTYPE) SqlParseCTX_PDECL);
void *SqlParseAlloc(void *(*mallocProc)(YYMALLOCARGTYPE) SqlParseCTX_PDECL){
  yyParser *yypParser;
  yypParser = (yyParser*)(*mallocProc)( (YYMALLOCARGTYPE)sizeof(yyParser) );
  if( yypParser ){
    SqlParseCTX_STORE
    SqlParseInit(yypParser SqlParseCTX_PARAM);
  }
  return (void*)yypParser;
}
#endif /* SqlParse_ENGINEALWAYSONSTACK */


/* The following function deletes the "minor type" or semantic value
** associated with a symbol.  The symbol can be either a terminal
** or nonterminal. "yymajor" is the symbol code, and "yypminor" is
** a pointer to the value to be deleted.  The code used to do the 
** deletions is derived from the %destructor and/or %token_destructor
** directives of the input grammar.
*/
static void yy_destructor(
  yyParser *yypParser,    /* The parser */
  YYCODETYPE yymajor,     /* Type code for object to destroy */
  YYMINORTYPE *yypminor   /* The object to be destroyed */
){
  SqlParseARG_FETCH
  SqlParseCTX_FETCH
  /* Suppress unused-variable warnings when no %destructor is defined */
  SqlParseARG_STORE
  SqlParseCTX_STORE
  (void)yypminor;
  switch( yymajor ){
    /* Here is inserted the actions which take place when a
    ** terminal or non-terminal is destroyed.  This can happen
    ** when the symbol is popped from the stack during a
    ** reduce or during error processing or when a parser is 
    ** being destroyed before it is finished parsing.
    **
    ** Note: during a reduce, the only symbols destroyed are those
    ** which appear on the RHS of the rule, but which are *not* used
    ** inside the C code.
    */
/********* Begin destructor definitions ***************************************/
/********* End destructor definitions *****************************************/
    default:  break;   /* If no destructor action specified: do nothing */
  }
}

/*
** Pop the parser's stack once.
**
** If there is a destructor routine associated with the token which
** is popped from the stack, then call it.
*/
static void yy_pop_parser_stack(yyParser *pParser){
  yyStackEntry *yytos;
  assert( pParser->yytos!=0 );
  assert( pParser->yytos > pParser->yystack );
  yytos = pParser->yytos--;
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE,"%sPopping %s\n",
      yyTracePrompt,
      yyTokenName[yytos->major]);
  }
#endif
  yy_destructor(pParser, yytos->major, &yytos->minor);
}

/*
** Clear all secondary memory allocations from the parser
*/
void SqlParseFinalize(void*);
void SqlParseFinalize(void *p){
  yyParser *pParser = (yyParser*)p;

  /* In-lined version of calling yy_pop_parser_stack() for each
  ** element left in the stack */
  yyStackEntry *yytos = pParser->yytos;
  while( yytos>pParser->yystack ){
#ifndef NDEBUG
    if( yyTraceFILE ){
      fprintf(yyTraceFILE,"%sPopping %s\n",
        yyTracePrompt,
        yyTokenName[yytos->major]);
    }
#endif
    if( yytos->major>=YY_MIN_DSTRCTR ){
      yy_destructor(pParser, yytos->major, &yytos->minor);
    }
    yytos--;
  }

#if YYGROWABLESTACK
  if( pParser->yystack!=pParser->yystk0 ){
    YYFREE(pParser->yystack, SqlParseCTX(pParser));
  }
#endif
}

#ifndef SqlParse_ENGINEALWAYSONSTACK
/* 
** Deallocate and destroy a parser.  Destructors are called for
** all stack elements before shutting the parser down.
**
** If the YYPARSEFREENEVERNULL macro exists (for example because it
** is defined in a %include section of the input grammar) then it is
** assumed that the input pointer is never NULL.
*/
void SqlParseFree(void*,void(*)(void*));
void SqlParseFree(
  void *p,                    /* The parser to be deleted */
  void (*freeProc)(void*)     /* Function used to reclaim memory */
){
#ifndef YYPARSEFREENEVERNULL
  if( p==0 ) return;
#endif
  SqlParseFinalize(p);
  (*freeProc)(p);
}
#endif /* SqlParse_ENGINEALWAYSONSTACK */

/**
 * Reset a parser to its initial state without freeing the parser
 * struct or stack memory.  Pops everything off the stack
 * (running %destructor), resets state-0 markers + error count
 * + high-water mark.  user-arg is preserved.  Available v0.6.1.
 */
void SqlParseReset(void*);
void SqlParseReset(void *yyp){
  yyParser *pParser = (yyParser*)yyp;
#ifndef YYPARSEFREENEVERNULL
  if( pParser==0 ) return;
#endif
  /* Pop everything off the stack, running destructors */
  while( pParser->yytos > pParser->yystack ){
#ifndef NDEBUG
    if( yyTraceFILE ){
      fprintf(yyTraceFILE,"%sPopping %s\n",
        yyTracePrompt,
        yyTokenName[pParser->yytos->major]);
    }
#endif
    if( pParser->yytos->major >= YY_MIN_DSTRCTR ){
      yy_destructor(pParser, pParser->yytos->major, &pParser->yytos->minor);
    }
    pParser->yytos--;
  }
  /* Reset to initial state (preserve yystack, yystackEnd, and ARG/CTX) */
  pParser->yytos = pParser->yystack;
  pParser->yystack[0].stateno = 0;
  pParser->yystack[0].major = 0;
#ifdef YYTRACKMAXSTACKDEPTH
  pParser->yyhwm = 0;
#endif
#ifndef YYNOERRORRECOVERY
  pParser->yyerrcnt = -1;
#endif
#ifdef YYLOCATIONTYPE
  /* Zero-init lookahead and sentinel location */
  memset(&pParser->yyLookaheadLoc, 0, sizeof(pParser->yyLookaheadLoc));
  memset(&pParser->yystack[0].yyloc, 0, sizeof(pParser->yystack[0].yyloc));
#endif
  /* Reset action-time lookahead state */
  pParser->yyLookaheadMajor = -2;
  memset(&pParser->yyLookaheadMinor, 0, sizeof(pParser->yyLookaheadMinor));
  pParser->yyLookaheadCleared = 0;
}

/*
** Return the peak depth of the stack for a parser.
*/
#ifdef YYTRACKMAXSTACKDEPTH
int SqlParseStackPeak(void*);
int SqlParseStackPeak(void *p){
  yyParser *pParser = (yyParser*)p;
  return pParser->yyhwm;
}
#endif

/* This array of booleans keeps track of the parser statement
** coverage.  The element yycoverage[X][Y] is set when the parser
** is in state X and has a lookahead token Y.  In a well-tested
** systems, every element of this matrix should end up being set.
*/
#if defined(YYCOVERAGE)
static unsigned char yycoverage[YYNSTATE][YYNTOKEN];
#endif

/*
** Write into out a description of every state/lookahead combination that
**
**   (1)  has not been used by the parser, and
**   (2)  is not a syntax error.
**
** Return the number of missed state/lookahead combinations.
*/
#if defined(YYCOVERAGE)
int SqlParseCoverage(FILE*);
int SqlParseCoverage(FILE *out){
  int stateno, iLookAhead, i;
  int nMissed = 0;
  for(stateno=0; stateno<YYNSTATE; stateno++){
    i = yy_shift_ofst[stateno];
    for(iLookAhead=0; iLookAhead<YYNTOKEN; iLookAhead++){
      if( yy_lookahead[i+iLookAhead]!=iLookAhead ) continue;
      if( yycoverage[stateno][iLookAhead]==0 ) nMissed++;
      if( out ){
        fprintf(out,"State %d lookahead %s %s\n", stateno,
                yyTokenName[iLookAhead],
                yycoverage[stateno][iLookAhead] ? "ok" : "missed");
      }
    }
  }
  return nMissed;
}
#endif

/*
** Find the appropriate action for a parser given the terminal
** look-ahead token iLookAhead.
*/
/* Forward declaration for AOT-compiled action lookup */
#ifdef YYAOT
extern YYACTIONTYPE yy_find_shift_action_aot(YYACTIONTYPE stateno,
                                              unsigned short iLookAhead);
#endif

static YYACTIONTYPE yy_find_shift_action(
  YYCODETYPE iLookAhead,    /* The look-ahead token */
  YYACTIONTYPE stateno      /* Current state number */
){
  int i;

  if( LIME_UNLIKELY(stateno>YY_MAX_SHIFT) ) return stateno;
#ifdef YYAOT
  /* Use the AOT-compiled switch-based lookup instead of table-driven */
  return yy_find_shift_action_aot(stateno, (unsigned short)iLookAhead);
#endif
  assert( stateno <= YY_SHIFT_COUNT );
#if defined(YYCOVERAGE)
  yycoverage[stateno][iLookAhead] = 1;
#endif
  do{
    i = yy_shift_ofst[stateno];
    assert( i>=0 );
    assert( i<=YY_ACTTAB_COUNT );
    assert( i+YYNTOKEN<=(int)YY_NLOOKAHEAD );
    assert( iLookAhead!=YYNOCODE );
    assert( iLookAhead < YYNTOKEN );
    i += iLookAhead;
    assert( i<(int)YY_NLOOKAHEAD );
    if( LIME_UNLIKELY(yy_lookahead[i]!=iLookAhead) ){
#ifdef YYFALLBACK
      YYCODETYPE iFallback;            /* Fallback token */
      assert( iLookAhead<sizeof(yyFallback)/sizeof(yyFallback[0]) );
      iFallback = yyFallback[iLookAhead];
      if( iFallback!=0 ){
#ifndef NDEBUG
        if( yyTraceFILE ){
          fprintf(yyTraceFILE, "%sFALLBACK %s => %s\n",
             yyTracePrompt, yyTokenName[iLookAhead], yyTokenName[iFallback]);
        }
#endif
        assert( yyFallback[iFallback]==0 ); /* Fallback loop must terminate */
        iLookAhead = iFallback;
        continue;
      }
#endif
#ifdef YYWILDCARD
      {
        int j = i - iLookAhead + YYWILDCARD;
        assert( j<(int)(sizeof(yy_lookahead)/sizeof(yy_lookahead[0])) );
        if( yy_lookahead[j]==YYWILDCARD && iLookAhead>0 ){
#ifndef NDEBUG
          if( yyTraceFILE ){
            fprintf(yyTraceFILE, "%sWILDCARD %s => %s\n",
               yyTracePrompt, yyTokenName[iLookAhead],
               yyTokenName[YYWILDCARD]);
          }
#endif /* NDEBUG */
          return yy_action[j];
        }
      }
#endif /* YYWILDCARD */
      return yy_default[stateno];
    }else{
      assert( i>=0 && i<(int)(sizeof(yy_action)/sizeof(yy_action[0])) );
      return yy_action[i];
    }
  }while(1);
}

/*
** Find the appropriate action for a parser given the non-terminal
** look-ahead token iLookAhead.
*/
static YYACTIONTYPE yy_find_reduce_action(
  YYACTIONTYPE stateno,     /* Current state number */
  YYCODETYPE iLookAhead     /* The look-ahead token */
){
  int i;
#ifdef YYERRORSYMBOL
  if( stateno>YY_REDUCE_COUNT ){
    return yy_default[stateno];
  }
#else
  assert( stateno<=YY_REDUCE_COUNT );
#endif
  i = yy_reduce_ofst[stateno];
  assert( iLookAhead!=YYNOCODE );
  i += iLookAhead;
#ifdef YYERRORSYMBOL
  if( i<0 || i>=YY_ACTTAB_COUNT || yy_lookahead[i]!=iLookAhead ){
    return yy_default[stateno];
  }
#else
  assert( i>=0 && i<YY_ACTTAB_COUNT );
  assert( yy_lookahead[i]==iLookAhead );
#endif
  return yy_action[i];
}

/*
** The following routine is called if the stack overflows.
*/
static void yyStackOverflow(yyParser *yypParser){
   SqlParseARG_FETCH
   SqlParseCTX_FETCH
#ifndef NDEBUG
   if( yyTraceFILE ){
     fprintf(yyTraceFILE,"%sStack Overflow!\n",yyTracePrompt);
   }
#endif
   while( yypParser->yytos>yypParser->yystack ) yy_pop_parser_stack(yypParser);
   /* Here code is inserted which will execute if the parser
   ** stack every overflows */
/******** Begin %stack_overflow code ******************************************/
/******** End %stack_overflow code ********************************************/
   SqlParseARG_STORE /* Suppress warning about unused %extra_argument var */
   SqlParseCTX_STORE
}

/*
** Print tracing information for a SHIFT action
*/
#ifndef NDEBUG
static void yyTraceShift(yyParser *yypParser, int yyNewState, const char *zTag){
  if( yyTraceFILE ){
    if( yyNewState<YYNSTATE ){
      fprintf(yyTraceFILE,"%s%s '%s', go to state %d\n",
         yyTracePrompt, zTag, yyTokenName[yypParser->yytos->major],
         yyNewState);
    }else{
      fprintf(yyTraceFILE,"%s%s '%s', pending reduce %d\n",
         yyTracePrompt, zTag, yyTokenName[yypParser->yytos->major],
         yyNewState - YY_MIN_REDUCE);
    }
  }
}
#else
# define yyTraceShift(X,Y,Z)
#endif

/*
** Perform a shift action.
*/
static void yy_shift(
  yyParser *yypParser,          /* The parser to be shifted */
  YYACTIONTYPE yyNewState,      /* The new state to shift in */
  YYCODETYPE yyMajor,           /* The major token to shift in */
  SqlParseTOKENTYPE yyMinor        /* The minor token to shift in */
){
  yyStackEntry *yytos;
  yypParser->yytos++;
#ifdef YYTRACKMAXSTACKDEPTH
  if( (int)(yypParser->yytos - yypParser->yystack)>yypParser->yyhwm ){
    yypParser->yyhwm++;
    assert( yypParser->yyhwm == (int)(yypParser->yytos - yypParser->yystack) );
  }
#endif
  yytos = yypParser->yytos;
  if( yytos>yypParser->yystackEnd ){
    if( yyGrowStack(yypParser) ){
      yypParser->yytos--;
      yyStackOverflow(yypParser);
      return;
    }
    yytos = yypParser->yytos;
    assert( yytos <= yypParser->yystackEnd );
  }
  if( yyNewState > YY_MAX_SHIFT ){
    yyNewState += YY_MIN_REDUCE - YY_MIN_SHIFTREDUCE;
  }
  yytos->stateno = yyNewState;
  yytos->major = yyMajor;
  yytos->minor.yy0 = yyMinor;
  yyTraceShift(yypParser, yyNewState, "Shift");
}

/* For rule J, yyRuleInfoLhs[J] contains the symbol on the left-hand side
** of that rule */
static const YYCODETYPE yyRuleInfoLhs[] = {
   154,  /* (0) input ::= stmt_list maybe_semi */
   111,  /* (1) stmt_list ::= stmt */
   111,  /* (2) stmt_list ::= stmt_list TK_SEMI stmt */
   110,  /* (3) stmt ::= select_stmt */
   110,  /* (4) stmt ::= insert_stmt */
   110,  /* (5) stmt ::= update_stmt */
   110,  /* (6) stmt ::= delete_stmt */
   110,  /* (7) stmt ::= create_stmt */
   110,  /* (8) stmt ::= drop_stmt */
   110,  /* (9) stmt ::= pragma_stmt */
   110,  /* (10) stmt ::= begin_stmt */
   110,  /* (11) stmt ::= commit_stmt */
   110,  /* (12) stmt ::= rollback_stmt */
   110,  /* (13) stmt ::= savepoint_stmt */
   110,  /* (14) stmt ::= release_stmt */
   110,  /* (15) stmt ::= attach_stmt */
   110,  /* (16) stmt ::= detach_stmt */
   110,  /* (17) stmt ::= explain_stmt */
   112,  /* (18) select_stmt ::= with_clause select_stmt */
   112,  /* (19) select_stmt ::= select_core */
   112,  /* (20) select_stmt ::= select_core TK_UNION select_stmt */
   112,  /* (21) select_stmt ::= select_core TK_UNION TK_ALL select_stmt */
   112,  /* (22) select_stmt ::= select_core TK_INTERSECT select_stmt */
   112,  /* (23) select_stmt ::= select_core TK_EXCEPT select_stmt */
   113,  /* (24) select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part */
   134,  /* (25) distinct_opt ::= */
   134,  /* (26) distinct_opt ::= TK_DISTINCT */
   134,  /* (27) distinct_opt ::= TK_ALL */
   131,  /* (28) sel_list ::= sel_item */
   131,  /* (29) sel_list ::= sel_list TK_COMMA sel_item */
   132,  /* (30) sel_item ::= TK_STAR */
   132,  /* (31) sel_item ::= TK_ID TK_DOT TK_STAR */
   132,  /* (32) sel_item ::= expr */
   132,  /* (33) sel_item ::= expr TK_AS TK_ID */
   132,  /* (34) sel_item ::= expr TK_ID */
   143,  /* (35) from_opt ::= */
   143,  /* (36) from_opt ::= TK_FROM table_list */
   144,  /* (37) table_list ::= table_ref */
   144,  /* (38) table_list ::= table_list TK_COMMA table_ref */
   144,  /* (39) table_list ::= table_list join_op table_ref join_constraint */
   146,  /* (40) join_op ::= TK_JOIN */
   146,  /* (41) join_op ::= TK_INNER TK_JOIN */
   146,  /* (42) join_op ::= TK_LEFT TK_JOIN */
   146,  /* (43) join_op ::= TK_LEFT TK_OUTER TK_JOIN */
   146,  /* (44) join_op ::= TK_RIGHT TK_JOIN */
   146,  /* (45) join_op ::= TK_RIGHT TK_OUTER TK_JOIN */
   146,  /* (46) join_op ::= TK_FULL TK_JOIN */
   146,  /* (47) join_op ::= TK_FULL TK_OUTER TK_JOIN */
   146,  /* (48) join_op ::= TK_CROSS TK_JOIN */
   146,  /* (49) join_op ::= TK_NATURAL TK_JOIN */
   146,  /* (50) join_op ::= TK_NATURAL TK_LEFT TK_JOIN */
   157,  /* (51) join_constraint ::= */
   157,  /* (52) join_constraint ::= TK_ON expr */
   157,  /* (53) join_constraint ::= TK_USING TK_LP id_list TK_RP */
   145,  /* (54) table_ref ::= TK_ID */
   145,  /* (55) table_ref ::= TK_ID TK_DOT TK_ID */
   145,  /* (56) table_ref ::= TK_ID TK_AS TK_ID */
   145,  /* (57) table_ref ::= TK_ID TK_ID */
   145,  /* (58) table_ref ::= TK_LP select_stmt TK_RP */
   145,  /* (59) table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID */
   135,  /* (60) where_opt ::= */
   135,  /* (61) where_opt ::= TK_WHERE expr */
   136,  /* (62) group_opt ::= */
   136,  /* (63) group_opt ::= TK_GROUP TK_BY expr_list */
   137,  /* (64) having_opt ::= */
   137,  /* (65) having_opt ::= TK_HAVING expr */
   138,  /* (66) order_opt ::= */
   138,  /* (67) order_opt ::= TK_ORDER TK_BY order_list */
   139,  /* (68) order_list ::= order_item */
   139,  /* (69) order_list ::= order_list TK_COMMA order_item */
   140,  /* (70) order_item ::= expr */
   140,  /* (71) order_item ::= expr TK_ASC */
   140,  /* (72) order_item ::= expr TK_DESC */
   141,  /* (73) limit_opt ::= */
   141,  /* (74) limit_opt ::= TK_LIMIT expr */
   142,  /* (75) offset_part ::= */
   142,  /* (76) offset_part ::= TK_OFFSET expr */
   142,  /* (77) offset_part ::= TK_COMMA expr */
   156,  /* (78) with_clause ::= TK_WITH cte_list */
   156,  /* (79) with_clause ::= TK_WITH TK_RECURSIVE cte_list */
   158,  /* (80) cte_list ::= cte */
   158,  /* (81) cte_list ::= cte_list TK_COMMA cte */
   159,  /* (82) cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP */
   159,  /* (83) cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP */
   114,  /* (84) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list */
   114,  /* (85) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES */
   114,  /* (86) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt */
   114,  /* (87) insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list */
   160,  /* (88) qname ::= TK_ID */
   160,  /* (89) qname ::= TK_ID TK_DOT TK_ID */
   153,  /* (90) insert_cols ::= */
   153,  /* (91) insert_cols ::= TK_LP id_list TK_RP */
   149,  /* (92) values_list ::= TK_VALUES TK_LP expr_list TK_RP */
   149,  /* (93) values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP */
   115,  /* (94) update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt */
   150,  /* (95) assign_list ::= assign */
   150,  /* (96) assign_list ::= assign_list TK_COMMA assign */
   151,  /* (97) assign ::= TK_ID TK_EQ expr */
   116,  /* (98) delete_stmt ::= TK_DELETE TK_FROM qname where_opt */
   117,  /* (99) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP */
   117,  /* (100) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt */
   117,  /* (101) create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP */
   117,  /* (102) create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt */
   161,  /* (103) temp_opt ::= */
   161,  /* (104) temp_opt ::= TK_TEMP */
   161,  /* (105) temp_opt ::= TK_TEMPORARY */
   164,  /* (106) unique_opt ::= */
   164,  /* (107) unique_opt ::= TK_UNIQUE */
   162,  /* (108) if_not_exists ::= */
   162,  /* (109) if_not_exists ::= TK_IF TK_NOT TK_EXISTS */
   163,  /* (110) table_body ::= table_elem */
   163,  /* (111) table_body ::= table_body TK_COMMA table_elem */
   165,  /* (112) table_elem ::= coldef */
   165,  /* (113) table_elem ::= table_constraint */
   152,  /* (114) coldef ::= TK_ID type_opt col_constraints */
   167,  /* (115) type_opt ::= */
   167,  /* (116) type_opt ::= TK_ID */
   167,  /* (117) type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP */
   167,  /* (118) type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP */
   168,  /* (119) col_constraints ::= */
   168,  /* (120) col_constraints ::= col_constraints col_constraint */
   169,  /* (121) col_constraint ::= TK_PRIMARY TK_KEY */
   169,  /* (122) col_constraint ::= TK_PRIMARY TK_KEY TK_ASC */
   169,  /* (123) col_constraint ::= TK_PRIMARY TK_KEY TK_DESC */
   169,  /* (124) col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR */
   169,  /* (125) col_constraint ::= TK_NOT TK_NULL */
   169,  /* (126) col_constraint ::= TK_UNIQUE */
   169,  /* (127) col_constraint ::= TK_DEFAULT expr_atom */
   169,  /* (128) col_constraint ::= TK_CHECK TK_LP expr TK_RP */
   169,  /* (129) col_constraint ::= TK_REFERENCES qname */
   169,  /* (130) col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP */
   118,  /* (131) drop_stmt ::= TK_DROP TK_TABLE if_exists qname */
   118,  /* (132) drop_stmt ::= TK_DROP TK_INDEX if_exists qname */
   118,  /* (133) drop_stmt ::= TK_DROP TK_VIEW if_exists qname */
   118,  /* (134) drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname */
   170,  /* (135) if_exists ::= */
   170,  /* (136) if_exists ::= TK_IF TK_EXISTS */
   119,  /* (137) pragma_stmt ::= TK_PRAGMA qname */
   119,  /* (138) pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom */
   119,  /* (139) pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP */
   120,  /* (140) begin_stmt ::= TK_BEGIN */
   120,  /* (141) begin_stmt ::= TK_BEGIN TK_TRANSACTION */
   120,  /* (142) begin_stmt ::= TK_BEGIN TK_ID */
   120,  /* (143) begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION */
   121,  /* (144) commit_stmt ::= TK_COMMIT */
   121,  /* (145) commit_stmt ::= TK_COMMIT TK_TRANSACTION */
   121,  /* (146) commit_stmt ::= TK_END */
   121,  /* (147) commit_stmt ::= TK_END TK_TRANSACTION */
   122,  /* (148) rollback_stmt ::= TK_ROLLBACK */
   122,  /* (149) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION */
   122,  /* (150) rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID */
   122,  /* (151) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID */
   122,  /* (152) rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID */
   123,  /* (153) savepoint_stmt ::= TK_SAVEPOINT TK_ID */
   124,  /* (154) release_stmt ::= TK_RELEASE TK_ID */
   124,  /* (155) release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID */
   125,  /* (156) attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID */
   125,  /* (157) attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID */
   126,  /* (158) detach_stmt ::= TK_DETACH TK_ID */
   126,  /* (159) detach_stmt ::= TK_DETACH TK_DATABASE TK_ID */
   127,  /* (160) explain_stmt ::= TK_EXPLAIN stmt */
   127,  /* (161) explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt */
   133,  /* (162) id_list ::= TK_ID */
   133,  /* (163) id_list ::= id_list TK_COMMA TK_ID */
   130,  /* (164) expr_list ::= expr */
   130,  /* (165) expr_list ::= expr_list TK_COMMA expr */
   128,  /* (166) expr ::= expr_atom */
   128,  /* (167) expr ::= TK_LP expr TK_RP */
   128,  /* (168) expr ::= TK_LP select_stmt TK_RP */
   128,  /* (169) expr ::= TK_NOT expr */
   128,  /* (170) expr ::= TK_MINUS expr */
   128,  /* (171) expr ::= TK_PLUS expr */
   128,  /* (172) expr ::= TK_TILDE expr */
   128,  /* (173) expr ::= expr TK_OR expr */
   128,  /* (174) expr ::= expr TK_AND expr */
   128,  /* (175) expr ::= expr TK_EQ expr */
   128,  /* (176) expr ::= expr TK_NE expr */
   128,  /* (177) expr ::= expr TK_LT expr */
   128,  /* (178) expr ::= expr TK_LE expr */
   128,  /* (179) expr ::= expr TK_GT expr */
   128,  /* (180) expr ::= expr TK_GE expr */
   128,  /* (181) expr ::= expr TK_PLUS expr */
   128,  /* (182) expr ::= expr TK_MINUS expr */
   128,  /* (183) expr ::= expr TK_STAR expr */
   128,  /* (184) expr ::= expr TK_SLASH expr */
   128,  /* (185) expr ::= expr TK_PERCENT expr */
   128,  /* (186) expr ::= expr TK_CONCAT expr */
   128,  /* (187) expr ::= expr TK_AMP expr */
   128,  /* (188) expr ::= expr TK_PIPE expr */
   128,  /* (189) expr ::= expr TK_LIKE expr */
   128,  /* (190) expr ::= expr TK_IS TK_NULL */
   128,  /* (191) expr ::= expr TK_IS TK_NOT TK_NULL */
   128,  /* (192) expr ::= expr TK_NOT TK_NULL */
   128,  /* (193) expr ::= expr TK_IS expr */
   128,  /* (194) expr ::= expr TK_IN TK_LP expr_list TK_RP */
   128,  /* (195) expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP */
   128,  /* (196) expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP */
   128,  /* (197) expr ::= expr TK_IN TK_LP select_stmt TK_RP */
   128,  /* (198) expr ::= expr TK_BETWEEN expr TK_AND expr */
   128,  /* (199) expr ::= TK_CASE expr_atom case_arms case_else TK_END */
   128,  /* (200) expr ::= TK_CASE case_arms case_else TK_END */
   128,  /* (201) expr ::= TK_ID TK_LP TK_RP */
   128,  /* (202) expr ::= TK_ID TK_LP TK_STAR TK_RP */
   128,  /* (203) expr ::= TK_ID TK_LP expr_list TK_RP */
   128,  /* (204) expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP */
   147,  /* (205) case_arms ::= TK_WHEN expr TK_THEN expr */
   147,  /* (206) case_arms ::= case_arms TK_WHEN expr TK_THEN expr */
   148,  /* (207) case_else ::= */
   148,  /* (208) case_else ::= TK_ELSE expr */
   129,  /* (209) expr_atom ::= TK_NUMBER */
   129,  /* (210) expr_atom ::= TK_STRING */
   129,  /* (211) expr_atom ::= TK_BLOB */
   129,  /* (212) expr_atom ::= TK_NULL */
   129,  /* (213) expr_atom ::= TK_TRUE */
   129,  /* (214) expr_atom ::= TK_FALSE */
   129,  /* (215) expr_atom ::= TK_QMARK */
   129,  /* (216) expr_atom ::= TK_ID */
   129,  /* (217) expr_atom ::= TK_ID TK_DOT TK_ID */
   129,  /* (218) expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID */
   155,  /* (219) maybe_semi ::= */
   155,  /* (220) maybe_semi ::= TK_SEMI */
   166,  /* (221) table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP */
   166,  /* (222) table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP */
   166,  /* (223) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname */
   166,  /* (224) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP */
   166,  /* (225) table_constraint ::= TK_CHECK TK_LP expr TK_RP */
};

/* For rule J, yyRuleInfoNRhs[J] contains the negative of the number
** of symbols on the right-hand side of that rule. */
static const signed char yyRuleInfoNRhs[] = {
   -2,  /* (0) input ::= stmt_list maybe_semi */
   -1,  /* (1) stmt_list ::= stmt */
   -3,  /* (2) stmt_list ::= stmt_list TK_SEMI stmt */
   -1,  /* (3) stmt ::= select_stmt */
   -1,  /* (4) stmt ::= insert_stmt */
   -1,  /* (5) stmt ::= update_stmt */
   -1,  /* (6) stmt ::= delete_stmt */
   -1,  /* (7) stmt ::= create_stmt */
   -1,  /* (8) stmt ::= drop_stmt */
   -1,  /* (9) stmt ::= pragma_stmt */
   -1,  /* (10) stmt ::= begin_stmt */
   -1,  /* (11) stmt ::= commit_stmt */
   -1,  /* (12) stmt ::= rollback_stmt */
   -1,  /* (13) stmt ::= savepoint_stmt */
   -1,  /* (14) stmt ::= release_stmt */
   -1,  /* (15) stmt ::= attach_stmt */
   -1,  /* (16) stmt ::= detach_stmt */
   -1,  /* (17) stmt ::= explain_stmt */
   -2,  /* (18) select_stmt ::= with_clause select_stmt */
   -1,  /* (19) select_stmt ::= select_core */
   -3,  /* (20) select_stmt ::= select_core TK_UNION select_stmt */
   -4,  /* (21) select_stmt ::= select_core TK_UNION TK_ALL select_stmt */
   -3,  /* (22) select_stmt ::= select_core TK_INTERSECT select_stmt */
   -3,  /* (23) select_stmt ::= select_core TK_EXCEPT select_stmt */
  -10,  /* (24) select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part */
    0,  /* (25) distinct_opt ::= */
   -1,  /* (26) distinct_opt ::= TK_DISTINCT */
   -1,  /* (27) distinct_opt ::= TK_ALL */
   -1,  /* (28) sel_list ::= sel_item */
   -3,  /* (29) sel_list ::= sel_list TK_COMMA sel_item */
   -1,  /* (30) sel_item ::= TK_STAR */
   -3,  /* (31) sel_item ::= TK_ID TK_DOT TK_STAR */
   -1,  /* (32) sel_item ::= expr */
   -3,  /* (33) sel_item ::= expr TK_AS TK_ID */
   -2,  /* (34) sel_item ::= expr TK_ID */
    0,  /* (35) from_opt ::= */
   -2,  /* (36) from_opt ::= TK_FROM table_list */
   -1,  /* (37) table_list ::= table_ref */
   -3,  /* (38) table_list ::= table_list TK_COMMA table_ref */
   -4,  /* (39) table_list ::= table_list join_op table_ref join_constraint */
   -1,  /* (40) join_op ::= TK_JOIN */
   -2,  /* (41) join_op ::= TK_INNER TK_JOIN */
   -2,  /* (42) join_op ::= TK_LEFT TK_JOIN */
   -3,  /* (43) join_op ::= TK_LEFT TK_OUTER TK_JOIN */
   -2,  /* (44) join_op ::= TK_RIGHT TK_JOIN */
   -3,  /* (45) join_op ::= TK_RIGHT TK_OUTER TK_JOIN */
   -2,  /* (46) join_op ::= TK_FULL TK_JOIN */
   -3,  /* (47) join_op ::= TK_FULL TK_OUTER TK_JOIN */
   -2,  /* (48) join_op ::= TK_CROSS TK_JOIN */
   -2,  /* (49) join_op ::= TK_NATURAL TK_JOIN */
   -3,  /* (50) join_op ::= TK_NATURAL TK_LEFT TK_JOIN */
    0,  /* (51) join_constraint ::= */
   -2,  /* (52) join_constraint ::= TK_ON expr */
   -4,  /* (53) join_constraint ::= TK_USING TK_LP id_list TK_RP */
   -1,  /* (54) table_ref ::= TK_ID */
   -3,  /* (55) table_ref ::= TK_ID TK_DOT TK_ID */
   -3,  /* (56) table_ref ::= TK_ID TK_AS TK_ID */
   -2,  /* (57) table_ref ::= TK_ID TK_ID */
   -3,  /* (58) table_ref ::= TK_LP select_stmt TK_RP */
   -5,  /* (59) table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID */
    0,  /* (60) where_opt ::= */
   -2,  /* (61) where_opt ::= TK_WHERE expr */
    0,  /* (62) group_opt ::= */
   -3,  /* (63) group_opt ::= TK_GROUP TK_BY expr_list */
    0,  /* (64) having_opt ::= */
   -2,  /* (65) having_opt ::= TK_HAVING expr */
    0,  /* (66) order_opt ::= */
   -3,  /* (67) order_opt ::= TK_ORDER TK_BY order_list */
   -1,  /* (68) order_list ::= order_item */
   -3,  /* (69) order_list ::= order_list TK_COMMA order_item */
   -1,  /* (70) order_item ::= expr */
   -2,  /* (71) order_item ::= expr TK_ASC */
   -2,  /* (72) order_item ::= expr TK_DESC */
    0,  /* (73) limit_opt ::= */
   -2,  /* (74) limit_opt ::= TK_LIMIT expr */
    0,  /* (75) offset_part ::= */
   -2,  /* (76) offset_part ::= TK_OFFSET expr */
   -2,  /* (77) offset_part ::= TK_COMMA expr */
   -2,  /* (78) with_clause ::= TK_WITH cte_list */
   -3,  /* (79) with_clause ::= TK_WITH TK_RECURSIVE cte_list */
   -1,  /* (80) cte_list ::= cte */
   -3,  /* (81) cte_list ::= cte_list TK_COMMA cte */
   -5,  /* (82) cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP */
   -8,  /* (83) cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP */
   -5,  /* (84) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list */
   -6,  /* (85) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES */
   -5,  /* (86) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt */
   -5,  /* (87) insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list */
   -1,  /* (88) qname ::= TK_ID */
   -3,  /* (89) qname ::= TK_ID TK_DOT TK_ID */
    0,  /* (90) insert_cols ::= */
   -3,  /* (91) insert_cols ::= TK_LP id_list TK_RP */
   -4,  /* (92) values_list ::= TK_VALUES TK_LP expr_list TK_RP */
   -5,  /* (93) values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP */
   -5,  /* (94) update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt */
   -1,  /* (95) assign_list ::= assign */
   -3,  /* (96) assign_list ::= assign_list TK_COMMA assign */
   -3,  /* (97) assign ::= TK_ID TK_EQ expr */
   -4,  /* (98) delete_stmt ::= TK_DELETE TK_FROM qname where_opt */
   -8,  /* (99) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP */
   -7,  /* (100) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt */
  -10,  /* (101) create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP */
   -7,  /* (102) create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt */
    0,  /* (103) temp_opt ::= */
   -1,  /* (104) temp_opt ::= TK_TEMP */
   -1,  /* (105) temp_opt ::= TK_TEMPORARY */
    0,  /* (106) unique_opt ::= */
   -1,  /* (107) unique_opt ::= TK_UNIQUE */
    0,  /* (108) if_not_exists ::= */
   -3,  /* (109) if_not_exists ::= TK_IF TK_NOT TK_EXISTS */
   -1,  /* (110) table_body ::= table_elem */
   -3,  /* (111) table_body ::= table_body TK_COMMA table_elem */
   -1,  /* (112) table_elem ::= coldef */
   -1,  /* (113) table_elem ::= table_constraint */
   -3,  /* (114) coldef ::= TK_ID type_opt col_constraints */
    0,  /* (115) type_opt ::= */
   -1,  /* (116) type_opt ::= TK_ID */
   -4,  /* (117) type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP */
   -6,  /* (118) type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP */
    0,  /* (119) col_constraints ::= */
   -2,  /* (120) col_constraints ::= col_constraints col_constraint */
   -2,  /* (121) col_constraint ::= TK_PRIMARY TK_KEY */
   -3,  /* (122) col_constraint ::= TK_PRIMARY TK_KEY TK_ASC */
   -3,  /* (123) col_constraint ::= TK_PRIMARY TK_KEY TK_DESC */
   -3,  /* (124) col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR */
   -2,  /* (125) col_constraint ::= TK_NOT TK_NULL */
   -1,  /* (126) col_constraint ::= TK_UNIQUE */
   -2,  /* (127) col_constraint ::= TK_DEFAULT expr_atom */
   -4,  /* (128) col_constraint ::= TK_CHECK TK_LP expr TK_RP */
   -2,  /* (129) col_constraint ::= TK_REFERENCES qname */
   -5,  /* (130) col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP */
   -4,  /* (131) drop_stmt ::= TK_DROP TK_TABLE if_exists qname */
   -4,  /* (132) drop_stmt ::= TK_DROP TK_INDEX if_exists qname */
   -4,  /* (133) drop_stmt ::= TK_DROP TK_VIEW if_exists qname */
   -4,  /* (134) drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname */
    0,  /* (135) if_exists ::= */
   -2,  /* (136) if_exists ::= TK_IF TK_EXISTS */
   -2,  /* (137) pragma_stmt ::= TK_PRAGMA qname */
   -4,  /* (138) pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom */
   -5,  /* (139) pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP */
   -1,  /* (140) begin_stmt ::= TK_BEGIN */
   -2,  /* (141) begin_stmt ::= TK_BEGIN TK_TRANSACTION */
   -2,  /* (142) begin_stmt ::= TK_BEGIN TK_ID */
   -3,  /* (143) begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION */
   -1,  /* (144) commit_stmt ::= TK_COMMIT */
   -2,  /* (145) commit_stmt ::= TK_COMMIT TK_TRANSACTION */
   -1,  /* (146) commit_stmt ::= TK_END */
   -2,  /* (147) commit_stmt ::= TK_END TK_TRANSACTION */
   -1,  /* (148) rollback_stmt ::= TK_ROLLBACK */
   -2,  /* (149) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION */
   -3,  /* (150) rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID */
   -4,  /* (151) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID */
   -4,  /* (152) rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID */
   -2,  /* (153) savepoint_stmt ::= TK_SAVEPOINT TK_ID */
   -2,  /* (154) release_stmt ::= TK_RELEASE TK_ID */
   -3,  /* (155) release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID */
   -4,  /* (156) attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID */
   -5,  /* (157) attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID */
   -2,  /* (158) detach_stmt ::= TK_DETACH TK_ID */
   -3,  /* (159) detach_stmt ::= TK_DETACH TK_DATABASE TK_ID */
   -2,  /* (160) explain_stmt ::= TK_EXPLAIN stmt */
   -4,  /* (161) explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt */
   -1,  /* (162) id_list ::= TK_ID */
   -3,  /* (163) id_list ::= id_list TK_COMMA TK_ID */
   -1,  /* (164) expr_list ::= expr */
   -3,  /* (165) expr_list ::= expr_list TK_COMMA expr */
   -1,  /* (166) expr ::= expr_atom */
   -3,  /* (167) expr ::= TK_LP expr TK_RP */
   -3,  /* (168) expr ::= TK_LP select_stmt TK_RP */
   -2,  /* (169) expr ::= TK_NOT expr */
   -2,  /* (170) expr ::= TK_MINUS expr */
   -2,  /* (171) expr ::= TK_PLUS expr */
   -2,  /* (172) expr ::= TK_TILDE expr */
   -3,  /* (173) expr ::= expr TK_OR expr */
   -3,  /* (174) expr ::= expr TK_AND expr */
   -3,  /* (175) expr ::= expr TK_EQ expr */
   -3,  /* (176) expr ::= expr TK_NE expr */
   -3,  /* (177) expr ::= expr TK_LT expr */
   -3,  /* (178) expr ::= expr TK_LE expr */
   -3,  /* (179) expr ::= expr TK_GT expr */
   -3,  /* (180) expr ::= expr TK_GE expr */
   -3,  /* (181) expr ::= expr TK_PLUS expr */
   -3,  /* (182) expr ::= expr TK_MINUS expr */
   -3,  /* (183) expr ::= expr TK_STAR expr */
   -3,  /* (184) expr ::= expr TK_SLASH expr */
   -3,  /* (185) expr ::= expr TK_PERCENT expr */
   -3,  /* (186) expr ::= expr TK_CONCAT expr */
   -3,  /* (187) expr ::= expr TK_AMP expr */
   -3,  /* (188) expr ::= expr TK_PIPE expr */
   -3,  /* (189) expr ::= expr TK_LIKE expr */
   -3,  /* (190) expr ::= expr TK_IS TK_NULL */
   -4,  /* (191) expr ::= expr TK_IS TK_NOT TK_NULL */
   -3,  /* (192) expr ::= expr TK_NOT TK_NULL */
   -3,  /* (193) expr ::= expr TK_IS expr */
   -5,  /* (194) expr ::= expr TK_IN TK_LP expr_list TK_RP */
   -6,  /* (195) expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP */
   -6,  /* (196) expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP */
   -5,  /* (197) expr ::= expr TK_IN TK_LP select_stmt TK_RP */
   -5,  /* (198) expr ::= expr TK_BETWEEN expr TK_AND expr */
   -5,  /* (199) expr ::= TK_CASE expr_atom case_arms case_else TK_END */
   -4,  /* (200) expr ::= TK_CASE case_arms case_else TK_END */
   -3,  /* (201) expr ::= TK_ID TK_LP TK_RP */
   -4,  /* (202) expr ::= TK_ID TK_LP TK_STAR TK_RP */
   -4,  /* (203) expr ::= TK_ID TK_LP expr_list TK_RP */
   -5,  /* (204) expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP */
   -4,  /* (205) case_arms ::= TK_WHEN expr TK_THEN expr */
   -5,  /* (206) case_arms ::= case_arms TK_WHEN expr TK_THEN expr */
    0,  /* (207) case_else ::= */
   -2,  /* (208) case_else ::= TK_ELSE expr */
   -1,  /* (209) expr_atom ::= TK_NUMBER */
   -1,  /* (210) expr_atom ::= TK_STRING */
   -1,  /* (211) expr_atom ::= TK_BLOB */
   -1,  /* (212) expr_atom ::= TK_NULL */
   -1,  /* (213) expr_atom ::= TK_TRUE */
   -1,  /* (214) expr_atom ::= TK_FALSE */
   -1,  /* (215) expr_atom ::= TK_QMARK */
   -1,  /* (216) expr_atom ::= TK_ID */
   -3,  /* (217) expr_atom ::= TK_ID TK_DOT TK_ID */
   -5,  /* (218) expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID */
    0,  /* (219) maybe_semi ::= */
   -1,  /* (220) maybe_semi ::= TK_SEMI */
   -5,  /* (221) table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP */
   -4,  /* (222) table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP */
   -7,  /* (223) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname */
  -10,  /* (224) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP */
   -4,  /* (225) table_constraint ::= TK_CHECK TK_LP expr TK_RP */
};

static void yy_accept(yyParser*);  /* Forward Declaration */

/* Forward declarations for the action-time lookahead API
** (SqlParse_get_lookahead / SqlParse_clear_lookahead).  The definitions
** live just before void SqlParse() further down; these forward
** declarations let user action bodies inside yy_reduce reference
** the API without an out-of-order-symbol error.  P0-NEW-5. */
#ifndef YYEMPTY
# define YYEMPTY (-2)
#endif
int  SqlParse_get_lookahead(
  void *yyp,
  SqlParseTOKENTYPE *yyminor_out
#ifdef YYLOCATIONTYPE
  , YYLOCATIONTYPE *yyloc_out
#endif
);
void SqlParse_clear_lookahead(void *yyp);

/* P0-NEW-8: opt-in eager default-reduce drain.  After a SqlParse()
** call returns the parser may be in a state whose only available
** action is a reduce (independent of the next lookahead).  Bison's
** pull-mode fires those reduces eagerly between yylex calls; Lime's
** push-mode normally waits for the next push to confirm the
** reduction.  SqlParse_drain forces the eager-fire behavior on demand,
** letting drivers like ecpg's preproc -- whose action bodies have
** side effects (fprintf to base_yyout) that must precede the
** lexer's next echo of whitespace -- match Bison's timing.
**
** Loops while the top-of-stack state's default action is
** (a) a reduce and (b) unconditional (no token-specific entries
** would override it).  Stops on a shift state, an unconditional
** non-reduce default, or YY_ACCEPT.  Action bodies fired during
** drain see no lookahead (SqlParse_get_lookahead returns YYEMPTY).
** Safe to call between SqlParse() invocations; safe to call multiple
** times in a row (the second call is a no-op once the parser is
** quiescent). */
void SqlParse_drain(void *yyp SqlParseCTX_PDECL);

/*
** v0.6.0: per-rule reduce action callbacks.
**
** Each grammar rule's reduction code is emitted as its own
** static function `yy_rule_<N>(yy_reduce_ctx *ctx)` and
** dispatched through `yy_rule_reduce_fn[ruleno]` below.  This
** replaces Lemon's classical 30-year switch-on-yyruleno.
**
** Why the change:
**   1. JIT specialisation -- per-rule callbacks let the JIT
**      inline reduce code into the JIT-compiled trace,
**      eliminating the indirect call through yy_reduce.
**   2. Composition + extension -- merging two grammars now
**      concatenates `yy_rule_reduce_fn[]` arrays trivially
**      instead of doing source-level switch surgery.
**   3. Hot-rule annotation -- `__attribute__((hot))` on the
**      handful of rules a profile says are hottest is now
**      possible per-function (impossible per switch case).
**   4. Plugin overrides -- a future feature lets an extension
**      replace one rule's reduce action by overwriting one
**      slot in the dispatch array, no whole-grammar rebuild.
**
** ABI break vs v0.5.x: the generated .c file's reduce path
** changes shape but the runtime ABI (SqlParserSnapshot, the
** push-parser entry, the JIT) stays put.  See ROADMAP for
** the v0.6.0 charter.
*/
typedef struct yy_reduce_ctx {
  yyParser *yypParser;             /* The parser */
  yyStackEntry *yymsp;             /* Top of the parser's stack */
  int yyLookahead;                 /* Lookahead token or YYNOCODE */
  SqlParseTOKENTYPE yyLookaheadToken; /* Value of the lookahead token */
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE *yyloc_lhs_ptr;   /* &yyloc_lhs in caller; per-rule
                                   ** functions read AND write through
                                   ** this pointer so @$ assignments
                                   ** propagate to the LHS slot. */
#endif
} yy_reduce_ctx;

/********** Begin per-rule reduce action functions ****************************/
/* (0) input ::= stmt_list maybe_semi */
static void yy_rule_0(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 224 "sql_grammar.lime"
{
    pstate->stmts = yymsp[-1].minor.yy31;
    /* Route on the FIRST statement.  Set here (not in the per-statement
     * actions) because reductions happen inner-first: a nested SELECT
     * inside INSERT/CREATE VIEW/EXPLAIN would otherwise claim the kind
     * before the outer statement reduces. */
    if (yymsp[-1].minor.yy31) { pstate->kind = yymsp[-1].minor.yy31->kind; pstate->readonly = yymsp[-1].minor.yy31->readonly; }
}
#line 2350 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (1) stmt_list ::= stmt */
static void yy_rule_1(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 236 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2377 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (2) stmt_list ::= stmt_list TK_SEMI stmt */
static void yy_rule_2(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 237 "sql_grammar.lime"
{
    /* append S to the tail of L */
    if (yymsp[-2].minor.yy31 == NULL) { yylhsminor.yy31 = yymsp[0].minor.yy31; }
    else { sql_stmt_t *t = yymsp[-2].minor.yy31; while (t->next) t = t->next; t->next = yymsp[0].minor.yy31; yylhsminor.yy31 = yymsp[-2].minor.yy31; }
}
#line 2409 "sql_grammar.c"
  yymsp[-2].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (3) stmt ::= select_stmt */
static void yy_rule_3(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 243 "sql_grammar.lime"
{ yylhsminor.yy31 = AST(pstate, sql_stmt_t); if (yylhsminor.yy31) { yylhsminor.yy31->kind = SQL_KIND_SELECT; yylhsminor.yy31->readonly = 1; yylhsminor.yy31->u.select = yymsp[0].minor.yy21; } }
#line 2437 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (4) stmt ::= insert_stmt */
static void yy_rule_4(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 244 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2465 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (5) stmt ::= update_stmt */
static void yy_rule_5(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 245 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2493 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (6) stmt ::= delete_stmt */
static void yy_rule_6(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 246 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2521 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (7) stmt ::= create_stmt */
static void yy_rule_7(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 247 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2549 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (8) stmt ::= drop_stmt */
static void yy_rule_8(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 248 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2577 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (9) stmt ::= pragma_stmt */
static void yy_rule_9(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 249 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2605 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (10) stmt ::= begin_stmt */
static void yy_rule_10(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 250 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2633 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (11) stmt ::= commit_stmt */
static void yy_rule_11(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 251 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2661 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (12) stmt ::= rollback_stmt */
static void yy_rule_12(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 252 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2689 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (13) stmt ::= savepoint_stmt */
static void yy_rule_13(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 253 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2717 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (14) stmt ::= release_stmt */
static void yy_rule_14(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 254 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2745 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (15) stmt ::= attach_stmt */
static void yy_rule_15(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 255 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2773 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (16) stmt ::= detach_stmt */
static void yy_rule_16(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 256 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2801 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (17) stmt ::= explain_stmt */
static void yy_rule_17(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 257 "sql_grammar.lime"
{ yylhsminor.yy31 = yymsp[0].minor.yy31; }
#line 2829 "sql_grammar.c"
  yymsp[0].minor.yy31 = yylhsminor.yy31;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (18) select_stmt ::= with_clause select_stmt */
static void yy_rule_18(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 265 "sql_grammar.lime"
{
    if (yymsp[0].minor.yy21) { yymsp[0].minor.yy21->with = yymsp[-1].minor.yy201.list; yymsp[0].minor.yy21->with_recursive = yymsp[-1].minor.yy201.rec; }
    yylhsminor.yy21 = yymsp[0].minor.yy21;
}
#line 2860 "sql_grammar.c"
  yymsp[-1].minor.yy21 = yylhsminor.yy21;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (19) select_stmt ::= select_core */
static void yy_rule_19(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 269 "sql_grammar.lime"
{ yylhsminor.yy21 = yymsp[0].minor.yy21; }
#line 2888 "sql_grammar.c"
  yymsp[0].minor.yy21 = yylhsminor.yy21;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (20) select_stmt ::= select_core TK_UNION select_stmt */
static void yy_rule_20(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 270 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy21) { yymsp[-2].minor.yy21->setop = SX_SET_UNION; yymsp[-2].minor.yy21->rhs = yymsp[0].minor.yy21; } yylhsminor.yy21 = yymsp[-2].minor.yy21;
}
#line 2918 "sql_grammar.c"
  yymsp[-2].minor.yy21 = yylhsminor.yy21;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (21) select_stmt ::= select_core TK_UNION TK_ALL select_stmt */
static void yy_rule_21(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 273 "sql_grammar.lime"
{
    if (yymsp[-3].minor.yy21) { yymsp[-3].minor.yy21->setop = SX_SET_UNION_ALL; yymsp[-3].minor.yy21->rhs = yymsp[0].minor.yy21; } yylhsminor.yy21 = yymsp[-3].minor.yy21;
}
#line 2948 "sql_grammar.c"
  yymsp[-3].minor.yy21 = yylhsminor.yy21;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (22) select_stmt ::= select_core TK_INTERSECT select_stmt */
static void yy_rule_22(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 276 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy21) { yymsp[-2].minor.yy21->setop = SX_SET_INTERSECT; yymsp[-2].minor.yy21->rhs = yymsp[0].minor.yy21; } yylhsminor.yy21 = yymsp[-2].minor.yy21;
}
#line 2978 "sql_grammar.c"
  yymsp[-2].minor.yy21 = yylhsminor.yy21;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (23) select_stmt ::= select_core TK_EXCEPT select_stmt */
static void yy_rule_23(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 279 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy21) { yymsp[-2].minor.yy21->setop = SX_SET_EXCEPT; yymsp[-2].minor.yy21->rhs = yymsp[0].minor.yy21; } yylhsminor.yy21 = yymsp[-2].minor.yy21;
}
#line 3008 "sql_grammar.c"
  yymsp[-2].minor.yy21 = yylhsminor.yy21;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (24) select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part */
static void yy_rule_24(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 285 "sql_grammar.lime"
{
    yylhsminor.yy21 = AST(pstate, sql_select_t);
    if (yylhsminor.yy21) {
        yylhsminor.yy21->distinct = yymsp[-8].minor.yy274; yylhsminor.yy21->cols = yymsp[-7].minor.yy314; yylhsminor.yy21->from = yymsp[-6].minor.yy133; yylhsminor.yy21->where = yymsp[-5].minor.yy138;
        yylhsminor.yy21->group = yymsp[-4].minor.yy314; yylhsminor.yy21->having = yymsp[-3].minor.yy138; yylhsminor.yy21->order = yymsp[-2].minor.yy314; yylhsminor.yy21->limit = yymsp[-1].minor.yy138;
        yylhsminor.yy21->offset = yymsp[0].minor.yy138;
        yylhsminor.yy21->src = yymsp[-9].minor.yy0.p;   /* SELECT keyword; length resolved by the consumer */
    }
}
#line 3044 "sql_grammar.c"
  yymsp[-9].minor.yy21 = yylhsminor.yy21;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (25) distinct_opt ::= */
static void yy_rule_25(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 295 "sql_grammar.lime"
{ yymsp[1].minor.yy274 = 0; }
#line 3072 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (26) distinct_opt ::= TK_DISTINCT */
static void yy_rule_26(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 296 "sql_grammar.lime"
{ yymsp[0].minor.yy274 = 1; }
#line 3099 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (27) distinct_opt ::= TK_ALL */
static void yy_rule_27(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 297 "sql_grammar.lime"
{ yymsp[0].minor.yy274 = 0; }
#line 3126 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (28) sel_list ::= sel_item */
static void yy_rule_28(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 299 "sql_grammar.lime"
{ yylhsminor.yy314 = mk_list(pstate); if (yylhsminor.yy314 && yymsp[0].minor.yy280) { yylhsminor.yy314->head = yylhsminor.yy314->tail = yymsp[0].minor.yy280; yylhsminor.yy314->n = 1; } }
#line 3153 "sql_grammar.c"
  yymsp[0].minor.yy314 = yylhsminor.yy314;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (29) sel_list ::= sel_list TK_COMMA sel_item */
static void yy_rule_29(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 300 "sql_grammar.lime"
{
    yylhsminor.yy314 = yymsp[-2].minor.yy314; if (yylhsminor.yy314 && yymsp[0].minor.yy280) { if (yylhsminor.yy314->tail) yylhsminor.yy314->tail->next = yymsp[0].minor.yy280; else yylhsminor.yy314->head = yymsp[0].minor.yy280; yylhsminor.yy314->tail = yymsp[0].minor.yy280; yylhsminor.yy314->n++; }
}
#line 3183 "sql_grammar.c"
  yymsp[-2].minor.yy314 = yylhsminor.yy314;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (30) sel_item ::= TK_STAR */
static void yy_rule_30(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 304 "sql_grammar.lime"
{
    yymsp[0].minor.yy280 = AST(pstate, sql_exprlist_item_t);
    if (yymsp[0].minor.yy280) { yymsp[0].minor.yy280->expr = mk_expr(pstate, SX_E_STAR); }
}
#line 3214 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (31) sel_item ::= TK_ID TK_DOT TK_STAR */
static void yy_rule_31(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 308 "sql_grammar.lime"
{
    yylhsminor.yy280 = AST(pstate, sql_exprlist_item_t);
    if (yylhsminor.yy280) { yylhsminor.yy280->expr = mk_expr(pstate, SX_E_STAR);
             if (yylhsminor.yy280->expr) { yylhsminor.yy280->expr->name[0] = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy280->expr->nname = 1; } }
}
#line 3245 "sql_grammar.c"
  yymsp[-2].minor.yy280 = yylhsminor.yy280;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (32) sel_item ::= expr */
static void yy_rule_32(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 313 "sql_grammar.lime"
{ yylhsminor.yy280 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy280) yylhsminor.yy280->expr = yymsp[0].minor.yy138; }
#line 3273 "sql_grammar.c"
  yymsp[0].minor.yy280 = yylhsminor.yy280;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (33) sel_item ::= expr TK_AS TK_ID */
static void yy_rule_33(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 314 "sql_grammar.lime"
{
    yylhsminor.yy280 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy280) { yylhsminor.yy280->expr = yymsp[-2].minor.yy138; yylhsminor.yy280->alias = tok_str(yymsp[0].minor.yy0); }
}
#line 3303 "sql_grammar.c"
  yymsp[-2].minor.yy280 = yylhsminor.yy280;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (34) sel_item ::= expr TK_ID */
static void yy_rule_34(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 317 "sql_grammar.lime"
{
    yylhsminor.yy280 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy280) { yylhsminor.yy280->expr = yymsp[-1].minor.yy138; yylhsminor.yy280->alias = tok_str(yymsp[0].minor.yy0); }
}
#line 3333 "sql_grammar.c"
  yymsp[-1].minor.yy280 = yylhsminor.yy280;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (35) from_opt ::= */
static void yy_rule_35(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 321 "sql_grammar.lime"
{ yymsp[1].minor.yy133 = NULL; }
#line 3361 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (36) from_opt ::= TK_FROM table_list */
static void yy_rule_36(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 322 "sql_grammar.lime"
{ yymsp[-1].minor.yy133 = yymsp[0].minor.yy133; }
#line 3388 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (37) table_list ::= table_ref */
static void yy_rule_37(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 324 "sql_grammar.lime"
{ yylhsminor.yy133 = yymsp[0].minor.yy133; }
#line 3415 "sql_grammar.c"
  yymsp[0].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (38) table_list ::= table_list TK_COMMA table_ref */
static void yy_rule_38(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 325 "sql_grammar.lime"
{
    if (yymsp[0].minor.yy133) yymsp[0].minor.yy133->join = SX_J_NONE;
    if (yymsp[-2].minor.yy133 == NULL) { yylhsminor.yy133 = yymsp[0].minor.yy133; }
    else { sql_src_t *s = yymsp[-2].minor.yy133; while (s->next) s = s->next; s->next = yymsp[0].minor.yy133; yylhsminor.yy133 = yymsp[-2].minor.yy133; }
}
#line 3447 "sql_grammar.c"
  yymsp[-2].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (39) table_list ::= table_list join_op table_ref join_constraint */
static void yy_rule_39(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 330 "sql_grammar.lime"
{
    if (yymsp[-1].minor.yy133) { yymsp[-1].minor.yy133->join = (sql_join_type_t)yymsp[-2].minor.yy274; yymsp[-1].minor.yy133->on = yymsp[0].minor.yy138; }
    if (yymsp[-3].minor.yy133 == NULL) { yylhsminor.yy133 = yymsp[-1].minor.yy133; }
    else { sql_src_t *s = yymsp[-3].minor.yy133; while (s->next) s = s->next; s->next = yymsp[-1].minor.yy133; yylhsminor.yy133 = yymsp[-3].minor.yy133; }
}
#line 3479 "sql_grammar.c"
  yymsp[-3].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (40) join_op ::= TK_JOIN */
static void yy_rule_40(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 336 "sql_grammar.lime"
{ yymsp[0].minor.yy274 = SX_J_INNER; }
#line 3507 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (41) join_op ::= TK_INNER TK_JOIN */
static void yy_rule_41(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 337 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = SX_J_INNER; }
#line 3534 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (42) join_op ::= TK_LEFT TK_JOIN */
static void yy_rule_42(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 338 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = SX_J_LEFT; }
#line 3561 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (43) join_op ::= TK_LEFT TK_OUTER TK_JOIN */
static void yy_rule_43(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 339 "sql_grammar.lime"
{ yymsp[-2].minor.yy274 = SX_J_LEFT; }
#line 3588 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (44) join_op ::= TK_RIGHT TK_JOIN */
static void yy_rule_44(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 340 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = SX_J_RIGHT; }
#line 3615 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (45) join_op ::= TK_RIGHT TK_OUTER TK_JOIN */
static void yy_rule_45(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 341 "sql_grammar.lime"
{ yymsp[-2].minor.yy274 = SX_J_RIGHT; }
#line 3642 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (46) join_op ::= TK_FULL TK_JOIN */
static void yy_rule_46(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 342 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = SX_J_FULL; }
#line 3669 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (47) join_op ::= TK_FULL TK_OUTER TK_JOIN */
static void yy_rule_47(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 343 "sql_grammar.lime"
{ yymsp[-2].minor.yy274 = SX_J_FULL; }
#line 3696 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (48) join_op ::= TK_CROSS TK_JOIN */
static void yy_rule_48(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 344 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = SX_J_CROSS; }
#line 3723 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (49) join_op ::= TK_NATURAL TK_JOIN */
static void yy_rule_49(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 345 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = SX_J_NATURAL; }
#line 3750 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (50) join_op ::= TK_NATURAL TK_LEFT TK_JOIN */
static void yy_rule_50(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 346 "sql_grammar.lime"
{ yymsp[-2].minor.yy274 = SX_J_NATURAL; }
#line 3777 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (51) join_constraint ::= */
static void yy_rule_51(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 349 "sql_grammar.lime"
{ yymsp[1].minor.yy138 = NULL; }
#line 3804 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (52) join_constraint ::= TK_ON expr */
static void yy_rule_52(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 350 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = yymsp[0].minor.yy138; }
#line 3831 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (53) join_constraint ::= TK_USING TK_LP id_list TK_RP */
static void yy_rule_53(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 351 "sql_grammar.lime"
{ yymsp[-3].minor.yy138 = NULL; }
#line 3858 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (54) table_ref ::= TK_ID */
static void yy_rule_54(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 353 "sql_grammar.lime"
{
    yylhsminor.yy133 = AST(pstate, sql_src_t); if (yylhsminor.yy133) yylhsminor.yy133->table = tok_str(yymsp[0].minor.yy0);
}
#line 3887 "sql_grammar.c"
  yymsp[0].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (55) table_ref ::= TK_ID TK_DOT TK_ID */
static void yy_rule_55(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 356 "sql_grammar.lime"
{
    yylhsminor.yy133 = AST(pstate, sql_src_t); if (yylhsminor.yy133) { yylhsminor.yy133->schema = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy133->table = tok_str(yymsp[0].minor.yy0); }
}
#line 3917 "sql_grammar.c"
  yymsp[-2].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (56) table_ref ::= TK_ID TK_AS TK_ID */
static void yy_rule_56(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 359 "sql_grammar.lime"
{
    yylhsminor.yy133 = AST(pstate, sql_src_t); if (yylhsminor.yy133) { yylhsminor.yy133->table = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy133->alias = tok_str(yymsp[0].minor.yy0); }
}
#line 3947 "sql_grammar.c"
  yymsp[-2].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (57) table_ref ::= TK_ID TK_ID */
static void yy_rule_57(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 362 "sql_grammar.lime"
{
    yylhsminor.yy133 = AST(pstate, sql_src_t); if (yylhsminor.yy133) { yylhsminor.yy133->table = tok_str(yymsp[-1].minor.yy0); yylhsminor.yy133->alias = tok_str(yymsp[0].minor.yy0); }
}
#line 3977 "sql_grammar.c"
  yymsp[-1].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (58) table_ref ::= TK_LP select_stmt TK_RP */
static void yy_rule_58(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 365 "sql_grammar.lime"
{
    yylhsminor.yy133 = AST(pstate, sql_src_t);
    if (yylhsminor.yy133) { yylhsminor.yy133->subquery = yymsp[-1].minor.yy21; yylhsminor.yy133->subsrc = yymsp[-2].minor.yy0.p; yylhsminor.yy133->subsrclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 4008 "sql_grammar.c"
  yymsp[-2].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (59) table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID */
static void yy_rule_59(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 369 "sql_grammar.lime"
{
    yylhsminor.yy133 = AST(pstate, sql_src_t);
    if (yylhsminor.yy133) { yylhsminor.yy133->subquery = yymsp[-3].minor.yy21; yylhsminor.yy133->alias = tok_str(yymsp[0].minor.yy0);
             yylhsminor.yy133->subsrc = yymsp[-4].minor.yy0.p; yylhsminor.yy133->subsrclen = (uint32_t)((yymsp[-2].minor.yy0.p + yymsp[-2].minor.yy0.len) - yymsp[-4].minor.yy0.p); }
}
#line 4040 "sql_grammar.c"
  yymsp[-4].minor.yy133 = yylhsminor.yy133;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (60) where_opt ::= */
static void yy_rule_60(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 375 "sql_grammar.lime"
{ yymsp[1].minor.yy138 = NULL; }
#line 4068 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (61) where_opt ::= TK_WHERE expr */
static void yy_rule_61(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 376 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = yymsp[0].minor.yy138; }
#line 4095 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (62) group_opt ::= */
static void yy_rule_62(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 378 "sql_grammar.lime"
{ yymsp[1].minor.yy314 = NULL; }
#line 4122 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (63) group_opt ::= TK_GROUP TK_BY expr_list */
static void yy_rule_63(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 379 "sql_grammar.lime"
{ yymsp[-2].minor.yy314 = yymsp[0].minor.yy314; }
#line 4149 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (64) having_opt ::= */
static void yy_rule_64(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 381 "sql_grammar.lime"
{ yymsp[1].minor.yy138 = NULL; }
#line 4176 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (65) having_opt ::= TK_HAVING expr */
static void yy_rule_65(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 382 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = yymsp[0].minor.yy138; }
#line 4203 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (66) order_opt ::= */
static void yy_rule_66(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 384 "sql_grammar.lime"
{ yymsp[1].minor.yy314 = NULL; }
#line 4230 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (67) order_opt ::= TK_ORDER TK_BY order_list */
static void yy_rule_67(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 385 "sql_grammar.lime"
{ yymsp[-2].minor.yy314 = yymsp[0].minor.yy314; }
#line 4257 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (68) order_list ::= order_item */
static void yy_rule_68(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 387 "sql_grammar.lime"
{ yylhsminor.yy314 = mk_list(pstate); if (yylhsminor.yy314 && yymsp[0].minor.yy280) { yylhsminor.yy314->head = yylhsminor.yy314->tail = yymsp[0].minor.yy280; yylhsminor.yy314->n = 1; } }
#line 4284 "sql_grammar.c"
  yymsp[0].minor.yy314 = yylhsminor.yy314;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (69) order_list ::= order_list TK_COMMA order_item */
static void yy_rule_69(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 388 "sql_grammar.lime"
{
    yylhsminor.yy314 = yymsp[-2].minor.yy314; if (yylhsminor.yy314 && yymsp[0].minor.yy280) { if (yylhsminor.yy314->tail) yylhsminor.yy314->tail->next = yymsp[0].minor.yy280; else yylhsminor.yy314->head = yymsp[0].minor.yy280; yylhsminor.yy314->tail = yymsp[0].minor.yy280; yylhsminor.yy314->n++; }
}
#line 4314 "sql_grammar.c"
  yymsp[-2].minor.yy314 = yylhsminor.yy314;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (70) order_item ::= expr */
static void yy_rule_70(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 392 "sql_grammar.lime"
{ yylhsminor.yy280 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy280) { yylhsminor.yy280->expr = yymsp[0].minor.yy138; yylhsminor.yy280->sort = 1; } }
#line 4342 "sql_grammar.c"
  yymsp[0].minor.yy280 = yylhsminor.yy280;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (71) order_item ::= expr TK_ASC */
static void yy_rule_71(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 393 "sql_grammar.lime"
{ yylhsminor.yy280 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy280) { yylhsminor.yy280->expr = yymsp[-1].minor.yy138; yylhsminor.yy280->sort = 1; } }
#line 4370 "sql_grammar.c"
  yymsp[-1].minor.yy280 = yylhsminor.yy280;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (72) order_item ::= expr TK_DESC */
static void yy_rule_72(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 394 "sql_grammar.lime"
{ yylhsminor.yy280 = AST(pstate, sql_exprlist_item_t); if (yylhsminor.yy280) { yylhsminor.yy280->expr = yymsp[-1].minor.yy138; yylhsminor.yy280->sort = 2; } }
#line 4398 "sql_grammar.c"
  yymsp[-1].minor.yy280 = yylhsminor.yy280;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (73) limit_opt ::= */
static void yy_rule_73(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 396 "sql_grammar.lime"
{ yymsp[1].minor.yy138 = NULL; }
#line 4426 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (74) limit_opt ::= TK_LIMIT expr */
static void yy_rule_74(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 397 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = yymsp[0].minor.yy138; }
#line 4453 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (75) offset_part ::= */
static void yy_rule_75(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 399 "sql_grammar.lime"
{ yymsp[1].minor.yy138 = NULL; }
#line 4480 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (76) offset_part ::= TK_OFFSET expr */
static void yy_rule_76(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 400 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = yymsp[0].minor.yy138; }
#line 4507 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (77) offset_part ::= TK_COMMA expr */
static void yy_rule_77(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 401 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = yymsp[0].minor.yy138; }
#line 4534 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (78) with_clause ::= TK_WITH cte_list */
static void yy_rule_78(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 409 "sql_grammar.lime"
{ yymsp[-1].minor.yy201.list = yymsp[0].minor.yy319; yymsp[-1].minor.yy201.rec = 0; }
#line 4561 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (79) with_clause ::= TK_WITH TK_RECURSIVE cte_list */
static void yy_rule_79(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 410 "sql_grammar.lime"
{ yymsp[-2].minor.yy201.list = yymsp[0].minor.yy319; yymsp[-2].minor.yy201.rec = 1; }
#line 4588 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (80) cte_list ::= cte */
static void yy_rule_80(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 412 "sql_grammar.lime"
{ yylhsminor.yy319 = yymsp[0].minor.yy319; }
#line 4615 "sql_grammar.c"
  yymsp[0].minor.yy319 = yylhsminor.yy319;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (81) cte_list ::= cte_list TK_COMMA cte */
static void yy_rule_81(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 413 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy319 == NULL) { yylhsminor.yy319 = yymsp[0].minor.yy319; }
    else { sql_cte_t *s = yymsp[-2].minor.yy319; while (s->next) s = s->next; s->next = yymsp[0].minor.yy319; yylhsminor.yy319 = yymsp[-2].minor.yy319; }
}
#line 4646 "sql_grammar.c"
  yymsp[-2].minor.yy319 = yylhsminor.yy319;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (82) cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP */
static void yy_rule_82(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 418 "sql_grammar.lime"
{
    yylhsminor.yy319 = AST(pstate, sql_cte_t); if (yylhsminor.yy319) { yylhsminor.yy319->name = tok_str(yymsp[-4].minor.yy0); yylhsminor.yy319->select = yymsp[-1].minor.yy21; }
}
#line 4676 "sql_grammar.c"
  yymsp[-4].minor.yy319 = yylhsminor.yy319;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (83) cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP */
static void yy_rule_83(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 421 "sql_grammar.lime"
{
    yylhsminor.yy319 = AST(pstate, sql_cte_t); if (yylhsminor.yy319) { yylhsminor.yy319->name = tok_str(yymsp[-7].minor.yy0); yylhsminor.yy319->cols = yymsp[-5].minor.yy314; yylhsminor.yy319->select = yymsp[-1].minor.yy21; }
}
#line 4706 "sql_grammar.c"
  yymsp[-7].minor.yy319 = yylhsminor.yy319;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (84) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list */
static void yy_rule_84(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 427 "sql_grammar.lime"
{
    sql_insert_t *ins = yymsp[0].minor.yy284 ? yymsp[0].minor.yy284 : AST(pstate, sql_insert_t);
    yymsp[-4].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy31 && ins) {
        ins->schema = yymsp[-2].minor.yy32.schema; ins->table = yymsp[-2].minor.yy32.table; ins->cols = yymsp[-1].minor.yy314;
        yymsp[-4].minor.yy31->kind = SQL_KIND_INSERT; yymsp[-4].minor.yy31->u.insert = ins;
    }
}
#line 4741 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (85) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES */
static void yy_rule_85(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 435 "sql_grammar.lime"
{
    sql_insert_t *ins = AST(pstate, sql_insert_t);
    yymsp[-5].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-5].minor.yy31 && ins) {
        ins->schema = yymsp[-3].minor.yy32.schema; ins->table = yymsp[-3].minor.yy32.table; ins->cols = yymsp[-2].minor.yy314; ins->def_values = 1;
        yymsp[-5].minor.yy31->kind = SQL_KIND_INSERT; yymsp[-5].minor.yy31->u.insert = ins;
    }
}
#line 4775 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (86) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt */
static void yy_rule_86(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 443 "sql_grammar.lime"
{
    sql_insert_t *ins = AST(pstate, sql_insert_t);
    yymsp[-4].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy31 && ins) {
        ins->schema = yymsp[-2].minor.yy32.schema; ins->table = yymsp[-2].minor.yy32.table; ins->cols = yymsp[-1].minor.yy314; ins->select = yymsp[0].minor.yy21;
        yymsp[-4].minor.yy31->kind = SQL_KIND_INSERT; yymsp[-4].minor.yy31->u.insert = ins;
    }
}
#line 4809 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (87) insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list */
static void yy_rule_87(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 451 "sql_grammar.lime"
{
    sql_insert_t *ins = yymsp[0].minor.yy284 ? yymsp[0].minor.yy284 : AST(pstate, sql_insert_t);
    yymsp[-4].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy31 && ins) {
        ins->schema = yymsp[-2].minor.yy32.schema; ins->table = yymsp[-2].minor.yy32.table; ins->cols = yymsp[-1].minor.yy314; ins->replace = 1;
        yymsp[-4].minor.yy31->kind = SQL_KIND_INSERT; yymsp[-4].minor.yy31->u.insert = ins;
    }
}
#line 4843 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (88) qname ::= TK_ID */
static void yy_rule_88(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 461 "sql_grammar.lime"
{ yylhsminor.yy32.schema.p = NULL; yylhsminor.yy32.schema.len = 0; yylhsminor.yy32.table = tok_str(yymsp[0].minor.yy0); }
#line 4870 "sql_grammar.c"
  yymsp[0].minor.yy32 = yylhsminor.yy32;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (89) qname ::= TK_ID TK_DOT TK_ID */
static void yy_rule_89(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 462 "sql_grammar.lime"
{ yylhsminor.yy32.schema = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy32.table = tok_str(yymsp[0].minor.yy0); }
#line 4898 "sql_grammar.c"
  yymsp[-2].minor.yy32 = yylhsminor.yy32;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (90) insert_cols ::= */
static void yy_rule_90(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 464 "sql_grammar.lime"
{ yymsp[1].minor.yy314 = NULL; }
#line 4926 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (91) insert_cols ::= TK_LP id_list TK_RP */
static void yy_rule_91(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 465 "sql_grammar.lime"
{ yymsp[-2].minor.yy314 = yymsp[-1].minor.yy314; }
#line 4953 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (92) values_list ::= TK_VALUES TK_LP expr_list TK_RP */
static void yy_rule_92(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 467 "sql_grammar.lime"
{
    yymsp[-3].minor.yy284 = AST(pstate, sql_insert_t);
    if (yymsp[-3].minor.yy284) {
        yymsp[-3].minor.yy284->rows = (sql_exprlist_t **)sql_arena_alloc(pstate->arena, sizeof(sql_exprlist_t *));
        if (yymsp[-3].minor.yy284->rows) { yymsp[-3].minor.yy284->rows[0] = yymsp[-1].minor.yy314; yymsp[-3].minor.yy284->n_rows = 1; }
    }
}
#line 4986 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (93) values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP */
static void yy_rule_93(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 474 "sql_grammar.lime"
{
    yylhsminor.yy284 = yymsp[-4].minor.yy284;
    if (yylhsminor.yy284) {
        sql_exprlist_t **nr = (sql_exprlist_t **)sql_arena_alloc(pstate->arena,
            sizeof(sql_exprlist_t *) * (size_t)(yylhsminor.yy284->n_rows + 1));
        if (nr) {
            int i; for (i = 0; i < yylhsminor.yy284->n_rows; i++) nr[i] = yylhsminor.yy284->rows[i];
            nr[yylhsminor.yy284->n_rows] = yymsp[-1].minor.yy314; yylhsminor.yy284->rows = nr; yylhsminor.yy284->n_rows++;
        }
    }
}
#line 5023 "sql_grammar.c"
  yymsp[-4].minor.yy284 = yylhsminor.yy284;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (94) update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt */
static void yy_rule_94(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 488 "sql_grammar.lime"
{
    sql_update_t *up = AST(pstate, sql_update_t);
    yymsp[-4].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy31 && up) {
        up->schema = yymsp[-3].minor.yy32.schema; up->table = yymsp[-3].minor.yy32.table; up->sets = yymsp[-1].minor.yy88; up->where = yymsp[0].minor.yy138;
        yymsp[-4].minor.yy31->kind = SQL_KIND_UPDATE; yymsp[-4].minor.yy31->u.update = up;
    }
}
#line 5058 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (95) assign_list ::= assign */
static void yy_rule_95(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 497 "sql_grammar.lime"
{ yylhsminor.yy88 = yymsp[0].minor.yy88; }
#line 5085 "sql_grammar.c"
  yymsp[0].minor.yy88 = yylhsminor.yy88;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (96) assign_list ::= assign_list TK_COMMA assign */
static void yy_rule_96(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 498 "sql_grammar.lime"
{
    if (yymsp[-2].minor.yy88 == NULL) { yylhsminor.yy88 = yymsp[0].minor.yy88; }
    else { sql_assign_t *s = yymsp[-2].minor.yy88; while (s->next) s = s->next; s->next = yymsp[0].minor.yy88; yylhsminor.yy88 = yymsp[-2].minor.yy88; }
}
#line 5116 "sql_grammar.c"
  yymsp[-2].minor.yy88 = yylhsminor.yy88;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (97) assign ::= TK_ID TK_EQ expr */
static void yy_rule_97(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 503 "sql_grammar.lime"
{
    yylhsminor.yy88 = AST(pstate, sql_assign_t); if (yylhsminor.yy88) { yylhsminor.yy88->col = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy88->val = yymsp[0].minor.yy138; }
}
#line 5146 "sql_grammar.c"
  yymsp[-2].minor.yy88 = yylhsminor.yy88;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (98) delete_stmt ::= TK_DELETE TK_FROM qname where_opt */
static void yy_rule_98(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 509 "sql_grammar.lime"
{
    sql_delete_t *d = AST(pstate, sql_delete_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31 && d) {
        d->schema = yymsp[-1].minor.yy32.schema; d->table = yymsp[-1].minor.yy32.table; d->where = yymsp[0].minor.yy138;
        yymsp[-3].minor.yy31->kind = SQL_KIND_DELETE; yymsp[-3].minor.yy31->u.del = d;
    }
}
#line 5181 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (99) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP */
static void yy_rule_99(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 521 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-7].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-7].minor.yy31 && cr) {
        cr->kind = SX_CR_TABLE; cr->temp = yymsp[-6].minor.yy274; cr->if_not_exists = yymsp[-4].minor.yy274;
        cr->schema = yymsp[-3].minor.yy32.schema; cr->name = yymsp[-3].minor.yy32.table; cr->cols = yymsp[-1].minor.yy134;
        yymsp[-7].minor.yy31->kind = SQL_KIND_CREATE; yymsp[-7].minor.yy31->u.create = cr;
    }
}
#line 5216 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (100) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt */
static void yy_rule_100(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 531 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-6].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-6].minor.yy31 && cr) {
        cr->kind = SX_CR_TABLE_AS; cr->temp = yymsp[-5].minor.yy274; cr->if_not_exists = yymsp[-3].minor.yy274;
        cr->schema = yymsp[-2].minor.yy32.schema; cr->name = yymsp[-2].minor.yy32.table; cr->select = yymsp[0].minor.yy21;
        yymsp[-6].minor.yy31->kind = SQL_KIND_CREATE; yymsp[-6].minor.yy31->u.create = cr;
    }
}
#line 5251 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (101) create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP */
static void yy_rule_101(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 541 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-9].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-9].minor.yy31 && cr) {
        cr->kind = SX_CR_INDEX; cr->unique = yymsp[-8].minor.yy274; cr->if_not_exists = yymsp[-6].minor.yy274;
        cr->schema = yymsp[-5].minor.yy32.schema; cr->name = yymsp[-5].minor.yy32.table; cr->on_table = tok_str(yymsp[-3].minor.yy0);
        cr->index_cols = yymsp[-1].minor.yy314;
        yymsp[-9].minor.yy31->kind = SQL_KIND_CREATE; yymsp[-9].minor.yy31->u.create = cr;
    }
}
#line 5287 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (102) create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt */
static void yy_rule_102(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 552 "sql_grammar.lime"
{
    sql_create_t *cr = AST(pstate, sql_create_t);
    yymsp[-6].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-6].minor.yy31 && cr) {
        cr->kind = SX_CR_VIEW; cr->temp = yymsp[-5].minor.yy274; cr->if_not_exists = yymsp[-3].minor.yy274;
        cr->schema = yymsp[-2].minor.yy32.schema; cr->name = yymsp[-2].minor.yy32.table; cr->select = yymsp[0].minor.yy21;
        yymsp[-6].minor.yy31->kind = SQL_KIND_CREATE; yymsp[-6].minor.yy31->u.create = cr;
    }
}
#line 5322 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (103) temp_opt ::= */
static void yy_rule_103(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 565 "sql_grammar.lime"
{ yymsp[1].minor.yy274 = 0; }
#line 5349 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (104) temp_opt ::= TK_TEMP */
static void yy_rule_104(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 566 "sql_grammar.lime"
{ yymsp[0].minor.yy274 = 1; }
#line 5376 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (105) temp_opt ::= TK_TEMPORARY */
static void yy_rule_105(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 567 "sql_grammar.lime"
{ yymsp[0].minor.yy274 = 1; }
#line 5403 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (106) unique_opt ::= */
static void yy_rule_106(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 569 "sql_grammar.lime"
{ yymsp[1].minor.yy274 = 0; }
#line 5430 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (107) unique_opt ::= TK_UNIQUE */
static void yy_rule_107(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 570 "sql_grammar.lime"
{ yymsp[0].minor.yy274 = 1; }
#line 5457 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (108) if_not_exists ::= */
static void yy_rule_108(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 572 "sql_grammar.lime"
{ yymsp[1].minor.yy274 = 0; }
#line 5484 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (109) if_not_exists ::= TK_IF TK_NOT TK_EXISTS */
static void yy_rule_109(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 573 "sql_grammar.lime"
{ yymsp[-2].minor.yy274 = 1; }
#line 5511 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (110) table_body ::= table_elem */
static void yy_rule_110(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 585 "sql_grammar.lime"
{ yylhsminor.yy134 = yymsp[0].minor.yy134; }
#line 5538 "sql_grammar.c"
  yymsp[0].minor.yy134 = yylhsminor.yy134;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (111) table_body ::= table_body TK_COMMA table_elem */
static void yy_rule_111(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 586 "sql_grammar.lime"
{
    if (yymsp[0].minor.yy134 == NULL) { yylhsminor.yy134 = yymsp[-2].minor.yy134; }                    /* a constraint: no column */
    else if (yymsp[-2].minor.yy134 == NULL) { yylhsminor.yy134 = yymsp[0].minor.yy134; }
    else { sql_coldef_t *s = yymsp[-2].minor.yy134; while (s->next) s = s->next; s->next = yymsp[0].minor.yy134; yylhsminor.yy134 = yymsp[-2].minor.yy134; }
}
#line 5570 "sql_grammar.c"
  yymsp[-2].minor.yy134 = yylhsminor.yy134;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (112) table_elem ::= coldef */
static void yy_rule_112(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 592 "sql_grammar.lime"
{ yylhsminor.yy134 = yymsp[0].minor.yy134; }
#line 5598 "sql_grammar.c"
  yymsp[0].minor.yy134 = yylhsminor.yy134;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (113) table_elem ::= table_constraint */
static void yy_rule_113(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 593 "sql_grammar.lime"
{ yymsp[0].minor.yy134 = NULL; }
#line 5626 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (114) coldef ::= TK_ID type_opt col_constraints */
static void yy_rule_114(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 595 "sql_grammar.lime"
{
    yylhsminor.yy134 = AST(pstate, sql_coldef_t);
    if (yylhsminor.yy134) { yylhsminor.yy134->name = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy134->type = yymsp[-1].minor.yy52;
             yylhsminor.yy134->primary = (yymsp[0].minor.yy274 & 1) != 0; yylhsminor.yy134->notnull = (yymsp[0].minor.yy274 & 2) != 0;
             yylhsminor.yy134->unique = (yymsp[0].minor.yy274 & 4) != 0; }
}
#line 5658 "sql_grammar.c"
  yymsp[-2].minor.yy134 = yylhsminor.yy134;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (115) type_opt ::= */
static void yy_rule_115(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 603 "sql_grammar.lime"
{ yymsp[1].minor.yy52.p = NULL; yymsp[1].minor.yy52.len = 0; }
#line 5686 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (116) type_opt ::= TK_ID */
static void yy_rule_116(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 604 "sql_grammar.lime"
{ yylhsminor.yy52 = tok_str(yymsp[0].minor.yy0); }
#line 5713 "sql_grammar.c"
  yymsp[0].minor.yy52 = yylhsminor.yy52;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (117) type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP */
static void yy_rule_117(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 605 "sql_grammar.lime"
{ yylhsminor.yy52 = tok_str(yymsp[-3].minor.yy0); }
#line 5741 "sql_grammar.c"
  yymsp[-3].minor.yy52 = yylhsminor.yy52;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (118) type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP */
static void yy_rule_118(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 606 "sql_grammar.lime"
{ yylhsminor.yy52 = tok_str(yymsp[-5].minor.yy0); }
#line 5769 "sql_grammar.c"
  yymsp[-5].minor.yy52 = yylhsminor.yy52;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (119) col_constraints ::= */
static void yy_rule_119(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 610 "sql_grammar.lime"
{ yymsp[1].minor.yy274 = 0; }
#line 5797 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (120) col_constraints ::= col_constraints col_constraint */
static void yy_rule_120(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 611 "sql_grammar.lime"
{ yylhsminor.yy274 = yymsp[-1].minor.yy274 | yymsp[0].minor.yy274; }
#line 5824 "sql_grammar.c"
  yymsp[-1].minor.yy274 = yylhsminor.yy274;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (121) col_constraint ::= TK_PRIMARY TK_KEY */
static void yy_rule_121(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 613 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = 1; }
#line 5852 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (122) col_constraint ::= TK_PRIMARY TK_KEY TK_ASC */
static void yy_rule_122(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 614 "sql_grammar.lime"
{ yymsp[-2].minor.yy274 = 1; }
#line 5879 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (123) col_constraint ::= TK_PRIMARY TK_KEY TK_DESC */
static void yy_rule_123(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 615 "sql_grammar.lime"
{ yymsp[-2].minor.yy274 = 1; }
#line 5906 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (124) col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR */
static void yy_rule_124(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 616 "sql_grammar.lime"
{ yymsp[-2].minor.yy274 = 1; }
#line 5933 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (125) col_constraint ::= TK_NOT TK_NULL */
static void yy_rule_125(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 617 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = 2; }
#line 5960 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (126) col_constraint ::= TK_UNIQUE */
static void yy_rule_126(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 618 "sql_grammar.lime"
{ yymsp[0].minor.yy274 = 4; }
#line 5987 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (127) col_constraint ::= TK_DEFAULT expr_atom */
static void yy_rule_127(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 619 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = 0; }
#line 6014 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (128) col_constraint ::= TK_CHECK TK_LP expr TK_RP */
static void yy_rule_128(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 620 "sql_grammar.lime"
{ yymsp[-3].minor.yy274 = 0; }
#line 6041 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (129) col_constraint ::= TK_REFERENCES qname */
static void yy_rule_129(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 621 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = 0; }
#line 6068 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (130) col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP */
static void yy_rule_130(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 622 "sql_grammar.lime"
{ yymsp[-4].minor.yy274 = 0; }
#line 6095 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (131) drop_stmt ::= TK_DROP TK_TABLE if_exists qname */
static void yy_rule_131(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 632 "sql_grammar.lime"
{
    sql_drop_t *d = AST(pstate, sql_drop_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31 && d) { d->kind = SX_DR_TABLE; d->if_exists = yymsp[-1].minor.yy274; d->schema = yymsp[0].minor.yy32.schema; d->name = yymsp[0].minor.yy32.table;
                  yymsp[-3].minor.yy31->kind = SQL_KIND_DROP; yymsp[-3].minor.yy31->u.drop = d; }
}
#line 6127 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (132) drop_stmt ::= TK_DROP TK_INDEX if_exists qname */
static void yy_rule_132(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 638 "sql_grammar.lime"
{
    sql_drop_t *d = AST(pstate, sql_drop_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31 && d) { d->kind = SX_DR_INDEX; d->if_exists = yymsp[-1].minor.yy274; d->schema = yymsp[0].minor.yy32.schema; d->name = yymsp[0].minor.yy32.table;
                  yymsp[-3].minor.yy31->kind = SQL_KIND_DROP; yymsp[-3].minor.yy31->u.drop = d; }
}
#line 6159 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (133) drop_stmt ::= TK_DROP TK_VIEW if_exists qname */
static void yy_rule_133(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 644 "sql_grammar.lime"
{
    sql_drop_t *d = AST(pstate, sql_drop_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31 && d) { d->kind = SX_DR_VIEW; d->if_exists = yymsp[-1].minor.yy274; d->schema = yymsp[0].minor.yy32.schema; d->name = yymsp[0].minor.yy32.table;
                  yymsp[-3].minor.yy31->kind = SQL_KIND_DROP; yymsp[-3].minor.yy31->u.drop = d; }
}
#line 6191 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (134) drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname */
static void yy_rule_134(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 650 "sql_grammar.lime"
{
    sql_drop_t *d = AST(pstate, sql_drop_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31 && d) { d->kind = SX_DR_TRIGGER; d->if_exists = yymsp[-1].minor.yy274; d->schema = yymsp[0].minor.yy32.schema; d->name = yymsp[0].minor.yy32.table;
                  yymsp[-3].minor.yy31->kind = SQL_KIND_DROP; yymsp[-3].minor.yy31->u.drop = d; }
}
#line 6223 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (135) if_exists ::= */
static void yy_rule_135(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 658 "sql_grammar.lime"
{ yymsp[1].minor.yy274 = 0; }
#line 6250 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (136) if_exists ::= TK_IF TK_EXISTS */
static void yy_rule_136(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 659 "sql_grammar.lime"
{ yymsp[-1].minor.yy274 = 1; }
#line 6277 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (137) pragma_stmt ::= TK_PRAGMA qname */
static void yy_rule_137(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 663 "sql_grammar.lime"
{
    sql_pragma_t *p = AST(pstate, sql_pragma_t);
    yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-1].minor.yy31 && p) { p->schema = yymsp[0].minor.yy32.schema; p->name = yymsp[0].minor.yy32.table; yymsp[-1].minor.yy31->kind = SQL_KIND_PRAGMA; yymsp[-1].minor.yy31->readonly = 1; yymsp[-1].minor.yy31->u.pragma = p; }
}
#line 6308 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (138) pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom */
static void yy_rule_138(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 668 "sql_grammar.lime"
{
    sql_pragma_t *p = AST(pstate, sql_pragma_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31 && p) { p->schema = yymsp[-2].minor.yy32.schema; p->name = yymsp[-2].minor.yy32.table; p->value = yymsp[0].minor.yy138; yymsp[-3].minor.yy31->kind = SQL_KIND_PRAGMA; yymsp[-3].minor.yy31->u.pragma = p; }
}
#line 6339 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (139) pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP */
static void yy_rule_139(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 673 "sql_grammar.lime"
{
    sql_pragma_t *p = AST(pstate, sql_pragma_t);
    yymsp[-4].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy31 && p) { p->schema = yymsp[-3].minor.yy32.schema; p->name = yymsp[-3].minor.yy32.table; p->value = yymsp[-1].minor.yy138; yymsp[-4].minor.yy31->kind = SQL_KIND_PRAGMA; yymsp[-4].minor.yy31->u.pragma = p; }
}
#line 6370 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (140) begin_stmt ::= TK_BEGIN */
static void yy_rule_140(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 681 "sql_grammar.lime"
{ yymsp[0].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[0].minor.yy31) yymsp[0].minor.yy31->kind = SQL_KIND_BEGIN; }
#line 6397 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (141) begin_stmt ::= TK_BEGIN TK_TRANSACTION */
static void yy_rule_141(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 682 "sql_grammar.lime"
{ yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy31) yymsp[-1].minor.yy31->kind = SQL_KIND_BEGIN; }
#line 6424 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (142) begin_stmt ::= TK_BEGIN TK_ID */
static void yy_rule_142(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 683 "sql_grammar.lime"
{ yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy31) yymsp[-1].minor.yy31->kind = SQL_KIND_BEGIN; }
#line 6451 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (143) begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION */
static void yy_rule_143(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 684 "sql_grammar.lime"
{ yymsp[-2].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[-2].minor.yy31) yymsp[-2].minor.yy31->kind = SQL_KIND_BEGIN; }
#line 6478 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (144) commit_stmt ::= TK_COMMIT */
static void yy_rule_144(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 686 "sql_grammar.lime"
{ yymsp[0].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[0].minor.yy31) yymsp[0].minor.yy31->kind = SQL_KIND_COMMIT; }
#line 6505 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (145) commit_stmt ::= TK_COMMIT TK_TRANSACTION */
static void yy_rule_145(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 687 "sql_grammar.lime"
{ yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy31) yymsp[-1].minor.yy31->kind = SQL_KIND_COMMIT; }
#line 6532 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (146) commit_stmt ::= TK_END */
static void yy_rule_146(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 688 "sql_grammar.lime"
{ yymsp[0].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[0].minor.yy31) yymsp[0].minor.yy31->kind = SQL_KIND_COMMIT; }
#line 6559 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (147) commit_stmt ::= TK_END TK_TRANSACTION */
static void yy_rule_147(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 689 "sql_grammar.lime"
{ yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy31) yymsp[-1].minor.yy31->kind = SQL_KIND_COMMIT; }
#line 6586 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (148) rollback_stmt ::= TK_ROLLBACK */
static void yy_rule_148(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 691 "sql_grammar.lime"
{ yymsp[0].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[0].minor.yy31) yymsp[0].minor.yy31->kind = SQL_KIND_ROLLBACK; }
#line 6613 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (149) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION */
static void yy_rule_149(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 692 "sql_grammar.lime"
{ yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy31) yymsp[-1].minor.yy31->kind = SQL_KIND_ROLLBACK; }
#line 6640 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (150) rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID */
static void yy_rule_150(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 693 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-2].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-2].minor.yy31) { yymsp[-2].minor.yy31->kind = SQL_KIND_ROLLBACK; yymsp[-2].minor.yy31->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6670 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (151) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID */
static void yy_rule_151(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 697 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31) { yymsp[-3].minor.yy31->kind = SQL_KIND_ROLLBACK; yymsp[-3].minor.yy31->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6700 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (152) rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID */
static void yy_rule_152(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 701 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31) { yymsp[-3].minor.yy31->kind = SQL_KIND_ROLLBACK; yymsp[-3].minor.yy31->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6730 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (153) savepoint_stmt ::= TK_SAVEPOINT TK_ID */
static void yy_rule_153(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 706 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-1].minor.yy31) { yymsp[-1].minor.yy31->kind = SQL_KIND_SAVEPOINT; yymsp[-1].minor.yy31->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6760 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (154) release_stmt ::= TK_RELEASE TK_ID */
static void yy_rule_154(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 711 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-1].minor.yy31) { yymsp[-1].minor.yy31->kind = SQL_KIND_RELEASE; yymsp[-1].minor.yy31->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6790 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (155) release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID */
static void yy_rule_155(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 715 "sql_grammar.lime"
{
    sql_txn_t *x = AST(pstate, sql_txn_t);
    yymsp[-2].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-2].minor.yy31) { yymsp[-2].minor.yy31->kind = SQL_KIND_RELEASE; yymsp[-2].minor.yy31->u.txn = x; if (x) x->name = tok_str(yymsp[0].minor.yy0); } }
#line 6820 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (156) attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID */
static void yy_rule_156(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 720 "sql_grammar.lime"
{
    sql_attach_t *at = AST(pstate, sql_attach_t);
    yymsp[-3].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-3].minor.yy31 && at) { at->target = yymsp[-2].minor.yy138; at->alias = tok_str(yymsp[0].minor.yy0); yymsp[-3].minor.yy31->kind = SQL_KIND_ATTACH; yymsp[-3].minor.yy31->u.attach = at; }
}
#line 6851 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (157) attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID */
static void yy_rule_157(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 725 "sql_grammar.lime"
{
    sql_attach_t *at = AST(pstate, sql_attach_t);
    yymsp[-4].minor.yy31 = AST(pstate, sql_stmt_t);
    if (yymsp[-4].minor.yy31 && at) { at->target = yymsp[-2].minor.yy138; at->alias = tok_str(yymsp[0].minor.yy0); yymsp[-4].minor.yy31->kind = SQL_KIND_ATTACH; yymsp[-4].minor.yy31->u.attach = at; }
}
#line 6882 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (158) detach_stmt ::= TK_DETACH TK_ID */
static void yy_rule_158(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 731 "sql_grammar.lime"
{
    yymsp[-1].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[-1].minor.yy31) { yymsp[-1].minor.yy31->kind = SQL_KIND_DETACH; yymsp[-1].minor.yy31->u.detach = tok_str(yymsp[0].minor.yy0); }
}
#line 6911 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (159) detach_stmt ::= TK_DETACH TK_DATABASE TK_ID */
static void yy_rule_159(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 734 "sql_grammar.lime"
{
    yymsp[-2].minor.yy31 = AST(pstate, sql_stmt_t); if (yymsp[-2].minor.yy31) { yymsp[-2].minor.yy31->kind = SQL_KIND_DETACH; yymsp[-2].minor.yy31->u.detach = tok_str(yymsp[0].minor.yy0); }
}
#line 6940 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (160) explain_stmt ::= TK_EXPLAIN stmt */
static void yy_rule_160(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 738 "sql_grammar.lime"
{
    yymsp[-1].minor.yy31 = yymsp[0].minor.yy31; if (yymsp[-1].minor.yy31) { yymsp[-1].minor.yy31->explain = 1; yymsp[-1].minor.yy31->kind = SQL_KIND_EXPLAIN; yymsp[-1].minor.yy31->readonly = 1; }
}
#line 6969 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (161) explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt */
static void yy_rule_161(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 741 "sql_grammar.lime"
{
    yymsp[-3].minor.yy31 = yymsp[0].minor.yy31; if (yymsp[-3].minor.yy31) { yymsp[-3].minor.yy31->explain = 2; yymsp[-3].minor.yy31->kind = SQL_KIND_EXPLAIN; yymsp[-3].minor.yy31->readonly = 1; }
}
#line 6998 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (162) id_list ::= TK_ID */
static void yy_rule_162(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 747 "sql_grammar.lime"
{
    sql_expr_t *c = mk_expr(pstate, SX_E_COLUMN);
    if (c) { c->name[0] = tok_str(yymsp[0].minor.yy0); c->nname = 1; }
    yylhsminor.yy314 = list_add(pstate, NULL, c);
}
#line 7029 "sql_grammar.c"
  yymsp[0].minor.yy314 = yylhsminor.yy314;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (163) id_list ::= id_list TK_COMMA TK_ID */
static void yy_rule_163(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 752 "sql_grammar.lime"
{
    sql_expr_t *c = mk_expr(pstate, SX_E_COLUMN);
    if (c) { c->name[0] = tok_str(yymsp[0].minor.yy0); c->nname = 1; }
    yylhsminor.yy314 = list_add(pstate, yymsp[-2].minor.yy314, c);
}
#line 7061 "sql_grammar.c"
  yymsp[-2].minor.yy314 = yylhsminor.yy314;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (164) expr_list ::= expr */
static void yy_rule_164(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 758 "sql_grammar.lime"
{ yylhsminor.yy314 = list_add(pstate, NULL, yymsp[0].minor.yy138); }
#line 7089 "sql_grammar.c"
  yymsp[0].minor.yy314 = yylhsminor.yy314;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (165) expr_list ::= expr_list TK_COMMA expr */
static void yy_rule_165(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 759 "sql_grammar.lime"
{ yylhsminor.yy314 = list_add(pstate, yymsp[-2].minor.yy314, yymsp[0].minor.yy138); }
#line 7117 "sql_grammar.c"
  yymsp[-2].minor.yy314 = yylhsminor.yy314;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (166) expr ::= expr_atom */
static void yy_rule_166(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 761 "sql_grammar.lime"
{ yylhsminor.yy138 = yymsp[0].minor.yy138; }
#line 7145 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (167) expr ::= TK_LP expr TK_RP */
static void yy_rule_167(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 762 "sql_grammar.lime"
{
    yylhsminor.yy138 = yymsp[-1].minor.yy138;
    /* Grow the span to include the parentheses so a parenthesized
     * select item is named with them, as SQLite does ((a+1)*2). */
    if (yylhsminor.yy138) { yylhsminor.yy138->src = yymsp[-2].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 7178 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (168) expr ::= TK_LP select_stmt TK_RP */
static void yy_rule_168(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 768 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_SUBQUERY);
    if (yylhsminor.yy138) { yylhsminor.yy138->sel = yymsp[-1].minor.yy21; yylhsminor.yy138->src = yymsp[-2].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 7209 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (169) expr ::= TK_NOT expr */
static void yy_rule_169(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 772 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = mk_unary(pstate, TK_NOT, yymsp[0].minor.yy138); }
#line 7237 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (170) expr ::= TK_MINUS expr */
static void yy_rule_170(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 773 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = mk_unary(pstate, TK_MINUS, yymsp[0].minor.yy138); }
#line 7264 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (171) expr ::= TK_PLUS expr */
static void yy_rule_171(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 774 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = mk_unary(pstate, TK_PLUS, yymsp[0].minor.yy138); }
#line 7291 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (172) expr ::= TK_TILDE expr */
static void yy_rule_172(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 775 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = mk_unary(pstate, TK_TILDE, yymsp[0].minor.yy138); }
#line 7318 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (173) expr ::= expr TK_OR expr */
static void yy_rule_173(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 776 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_OR, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7345 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (174) expr ::= expr TK_AND expr */
static void yy_rule_174(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 777 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_AND, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7373 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (175) expr ::= expr TK_EQ expr */
static void yy_rule_175(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 778 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_EQ, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7401 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (176) expr ::= expr TK_NE expr */
static void yy_rule_176(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 779 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_NE, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7429 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (177) expr ::= expr TK_LT expr */
static void yy_rule_177(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 780 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_LT, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7457 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (178) expr ::= expr TK_LE expr */
static void yy_rule_178(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 781 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_LE, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7485 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (179) expr ::= expr TK_GT expr */
static void yy_rule_179(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 782 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_GT, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7513 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (180) expr ::= expr TK_GE expr */
static void yy_rule_180(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 783 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_GE, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7541 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (181) expr ::= expr TK_PLUS expr */
static void yy_rule_181(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 784 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_PLUS, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7569 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (182) expr ::= expr TK_MINUS expr */
static void yy_rule_182(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 785 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_MINUS, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7597 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (183) expr ::= expr TK_STAR expr */
static void yy_rule_183(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 786 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_STAR, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7625 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (184) expr ::= expr TK_SLASH expr */
static void yy_rule_184(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 787 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_SLASH, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7653 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (185) expr ::= expr TK_PERCENT expr */
static void yy_rule_185(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 788 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_PERCENT, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7681 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (186) expr ::= expr TK_CONCAT expr */
static void yy_rule_186(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 789 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_CONCAT, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7709 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (187) expr ::= expr TK_AMP expr */
static void yy_rule_187(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 790 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_AMP, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7737 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (188) expr ::= expr TK_PIPE expr */
static void yy_rule_188(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 791 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_PIPE, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7765 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (189) expr ::= expr TK_LIKE expr */
static void yy_rule_189(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 792 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_LIKE, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7793 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (190) expr ::= expr TK_IS TK_NULL */
static void yy_rule_190(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 793 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_IS_NULL); if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-2].minor.yy138; yylhsminor.yy138->ival = 0; } }
#line 7821 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (191) expr ::= expr TK_IS TK_NOT TK_NULL */
static void yy_rule_191(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 794 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_IS_NULL); if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-3].minor.yy138; yylhsminor.yy138->ival = 1; } }
#line 7849 "sql_grammar.c"
  yymsp[-3].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (192) expr ::= expr TK_NOT TK_NULL */
static void yy_rule_192(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 795 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_IS_NULL); if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-2].minor.yy138; yylhsminor.yy138->ival = 1; } }
#line 7877 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (193) expr ::= expr TK_IS expr */
static void yy_rule_193(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 796 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_binary(pstate, TK_IS, yymsp[-2].minor.yy138, yymsp[0].minor.yy138); }
#line 7905 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (194) expr ::= expr TK_IN TK_LP expr_list TK_RP */
static void yy_rule_194(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 797 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_IN_LIST); if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-4].minor.yy138; yylhsminor.yy138->list = yymsp[-1].minor.yy314; }
}
#line 7935 "sql_grammar.c"
  yymsp[-4].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (195) expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP */
static void yy_rule_195(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 800 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_IN_LIST); if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-5].minor.yy138; yylhsminor.yy138->list = yymsp[-1].minor.yy314; yylhsminor.yy138->ival = 1; }
}
#line 7965 "sql_grammar.c"
  yymsp[-5].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (196) expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP */
static void yy_rule_196(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 803 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_IN_SELECT);
    if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-5].minor.yy138; yylhsminor.yy138->sel = yymsp[-1].minor.yy21; yylhsminor.yy138->ival = 1;
             yylhsminor.yy138->src = yymsp[-2].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 7997 "sql_grammar.c"
  yymsp[-5].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (197) expr ::= expr TK_IN TK_LP select_stmt TK_RP */
static void yy_rule_197(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 808 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_IN_SELECT);
    if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-4].minor.yy138; yylhsminor.yy138->sel = yymsp[-1].minor.yy21; yylhsminor.yy138->src = yymsp[-2].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 8028 "sql_grammar.c"
  yymsp[-4].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (198) expr ::= expr TK_BETWEEN expr TK_AND expr */
static void yy_rule_198(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 812 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_BETWEEN); if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-4].minor.yy138; yylhsminor.yy138->b = yymsp[-2].minor.yy138; yylhsminor.yy138->c = yymsp[0].minor.yy138; }
}
#line 8058 "sql_grammar.c"
  yymsp[-4].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (199) expr ::= TK_CASE expr_atom case_arms case_else TK_END */
static void yy_rule_199(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 815 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_CASE);
    if (yylhsminor.yy138) { yylhsminor.yy138->a = yymsp[-3].minor.yy138; yylhsminor.yy138->arms = yymsp[-2].minor.yy28; yylhsminor.yy138->els = yymsp[-1].minor.yy138;
             yylhsminor.yy138->src = yymsp[-4].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-4].minor.yy0.p); }
}
#line 8090 "sql_grammar.c"
  yymsp[-4].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (200) expr ::= TK_CASE case_arms case_else TK_END */
static void yy_rule_200(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 820 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_CASE);
    if (yylhsminor.yy138) { yylhsminor.yy138->arms = yymsp[-2].minor.yy28; yylhsminor.yy138->els = yymsp[-1].minor.yy138;
             yylhsminor.yy138->src = yymsp[-3].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-3].minor.yy0.p); }
}
#line 8122 "sql_grammar.c"
  yymsp[-3].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (201) expr ::= TK_ID TK_LP TK_RP */
static void yy_rule_201(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 825 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_FUNC); if (yylhsminor.yy138) { yylhsminor.yy138->name[0] = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy138->nname = 1; }
    if (yylhsminor.yy138) { yylhsminor.yy138->src = yymsp[-2].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 8153 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (202) expr ::= TK_ID TK_LP TK_STAR TK_RP */
static void yy_rule_202(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 829 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_FUNC); if (yylhsminor.yy138) { yylhsminor.yy138->name[0] = tok_str(yymsp[-3].minor.yy0); yylhsminor.yy138->nname = 1; yylhsminor.yy138->ival = 2; }
    if (yylhsminor.yy138) { yylhsminor.yy138->src = yymsp[-3].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-3].minor.yy0.p); }
}
#line 8184 "sql_grammar.c"
  yymsp[-3].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (203) expr ::= TK_ID TK_LP expr_list TK_RP */
static void yy_rule_203(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 833 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_FUNC); if (yylhsminor.yy138) { yylhsminor.yy138->name[0] = tok_str(yymsp[-3].minor.yy0); yylhsminor.yy138->nname = 1; yylhsminor.yy138->list = yymsp[-1].minor.yy314; }
    if (yylhsminor.yy138) { yylhsminor.yy138->src = yymsp[-3].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-3].minor.yy0.p); }
}
#line 8215 "sql_grammar.c"
  yymsp[-3].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (204) expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP */
static void yy_rule_204(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 837 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_FUNC); if (yylhsminor.yy138) { yylhsminor.yy138->name[0] = tok_str(yymsp[-4].minor.yy0); yylhsminor.yy138->nname = 1; yylhsminor.yy138->list = yymsp[-1].minor.yy314; yylhsminor.yy138->ival = 1; }
    if (yylhsminor.yy138) { yylhsminor.yy138->src = yymsp[-4].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-4].minor.yy0.p); }
}
#line 8246 "sql_grammar.c"
  yymsp[-4].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (205) case_arms ::= TK_WHEN expr TK_THEN expr */
static void yy_rule_205(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 842 "sql_grammar.lime"
{
    yymsp[-3].minor.yy28 = AST(pstate, sql_case_arm_t); if (yymsp[-3].minor.yy28) { yymsp[-3].minor.yy28->when = yymsp[-2].minor.yy138; yymsp[-3].minor.yy28->then = yymsp[0].minor.yy138; }
}
#line 8276 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (206) case_arms ::= case_arms TK_WHEN expr TK_THEN expr */
static void yy_rule_206(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 845 "sql_grammar.lime"
{
    sql_case_arm_t *arm = AST(pstate, sql_case_arm_t);
    if (arm) { arm->when = yymsp[-2].minor.yy138; arm->then = yymsp[0].minor.yy138; }
    if (yymsp[-4].minor.yy28 == NULL) { yylhsminor.yy28 = arm; }
    else { sql_case_arm_t *s = yymsp[-4].minor.yy28; while (s->next) s = s->next; s->next = arm; yylhsminor.yy28 = yymsp[-4].minor.yy28; }
}
#line 8308 "sql_grammar.c"
  yymsp[-4].minor.yy28 = yylhsminor.yy28;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (207) case_else ::= */
static void yy_rule_207(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 852 "sql_grammar.lime"
{ yymsp[1].minor.yy138 = NULL; }
#line 8336 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (208) case_else ::= TK_ELSE expr */
static void yy_rule_208(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 853 "sql_grammar.lime"
{ yymsp[-1].minor.yy138 = yymsp[0].minor.yy138; }
#line 8363 "sql_grammar.c"
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (209) expr_atom ::= TK_NUMBER */
static void yy_rule_209(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 855 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_NUMBER); if (yylhsminor.yy138) { yylhsminor.yy138->lit = tok_str(yymsp[0].minor.yy0); span_tok(yylhsminor.yy138, yymsp[0].minor.yy0); } }
#line 8390 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (210) expr_atom ::= TK_STRING */
static void yy_rule_210(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 856 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_STRING); if (yylhsminor.yy138) { yylhsminor.yy138->lit = tok_str_unquote(yymsp[0].minor.yy0); span_tok(yylhsminor.yy138, yymsp[0].minor.yy0); } }
#line 8418 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (211) expr_atom ::= TK_BLOB */
static void yy_rule_211(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 857 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_BLOB);   if (yylhsminor.yy138) { yylhsminor.yy138->lit = tok_str(yymsp[0].minor.yy0); span_tok(yylhsminor.yy138, yymsp[0].minor.yy0); } }
#line 8446 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (212) expr_atom ::= TK_NULL */
static void yy_rule_212(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 858 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_NULL); span_tok(yylhsminor.yy138, yymsp[0].minor.yy0); }
#line 8474 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (213) expr_atom ::= TK_TRUE */
static void yy_rule_213(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 859 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_BOOL); if (yylhsminor.yy138) { yylhsminor.yy138->ival = 1; span_tok(yylhsminor.yy138, yymsp[0].minor.yy0); } }
#line 8502 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (214) expr_atom ::= TK_FALSE */
static void yy_rule_214(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 860 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_BOOL); if (yylhsminor.yy138) { yylhsminor.yy138->ival = 0; span_tok(yylhsminor.yy138, yymsp[0].minor.yy0); } }
#line 8530 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (215) expr_atom ::= TK_QMARK */
static void yy_rule_215(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 861 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_PARAM); span_tok(yylhsminor.yy138, yymsp[0].minor.yy0); }
#line 8558 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (216) expr_atom ::= TK_ID */
static void yy_rule_216(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 862 "sql_grammar.lime"
{ yylhsminor.yy138 = mk_expr(pstate, SX_E_COLUMN); if (yylhsminor.yy138) { yylhsminor.yy138->name[0] = tok_str(yymsp[0].minor.yy0); yylhsminor.yy138->nname = 1; span_tok(yylhsminor.yy138, yymsp[0].minor.yy0); } }
#line 8586 "sql_grammar.c"
  yymsp[0].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (217) expr_atom ::= TK_ID TK_DOT TK_ID */
static void yy_rule_217(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 863 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_COLUMN); if (yylhsminor.yy138) { yylhsminor.yy138->name[0] = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy138->name[1] = tok_str(yymsp[0].minor.yy0); yylhsminor.yy138->nname = 2;
                                              yylhsminor.yy138->src = yymsp[-2].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-2].minor.yy0.p); }
}
#line 8617 "sql_grammar.c"
  yymsp[-2].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (218) expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID */
static void yy_rule_218(yy_reduce_ctx *yy_ctx){
  yyParser *yypParser = yy_ctx->yypParser;
  yyStackEntry *yymsp = yy_ctx->yymsp;
  int yyLookahead = yy_ctx->yyLookahead;
  SqlParseTOKENTYPE yyLookaheadToken = yy_ctx->yyLookaheadToken;
  /* Zero-init: a copy rule (LHS ::= RHS, action `A = B`) reads
  ** yylhsminor on a path GCC's data-flow can't prove is
  ** write-dominated, tripping -Wmaybe-uninitialized /
  ** -Wuninitialized under strict downstream flags.  Bison
  ** likewise defines the LHS slot on entry to every action.
  ** An initializer (not memset) keeps this a declaration so it
  ** stays ahead of the other locals (-Wdeclaration-after-stmt).
  ** {0} zero-inits the union's first member (always the scalar
  ** `int yyinit`), which is well-defined and warning-clean. */
  YYMINORTYPE yylhsminor = {0};
#ifdef YYLOCATIONTYPE
  YYLOCATIONTYPE yyloc_lhs = *yy_ctx->yyloc_lhs_ptr;
#endif
  SqlParseCTX_FETCH
  SqlParseARG_FETCH
  (void)yypParser; (void)yymsp; (void)yyLookahead;
  (void)yyLookaheadToken; (void)yylhsminor;
#ifdef YYLOCATIONTYPE
  (void)yyloc_lhs;
#endif
#line 867 "sql_grammar.lime"
{
    yylhsminor.yy138 = mk_expr(pstate, SX_E_COLUMN); if (yylhsminor.yy138) { yylhsminor.yy138->name[0] = tok_str(yymsp[-4].minor.yy0); yylhsminor.yy138->name[1] = tok_str(yymsp[-2].minor.yy0); yylhsminor.yy138->name[2] = tok_str(yymsp[0].minor.yy0); yylhsminor.yy138->nname = 3;
                                              yylhsminor.yy138->src = yymsp[-4].minor.yy0.p; yylhsminor.yy138->srclen = (uint32_t)((yymsp[0].minor.yy0.p + yymsp[0].minor.yy0.len) - yymsp[-4].minor.yy0.p); }
}
#line 8648 "sql_grammar.c"
  yymsp[-4].minor.yy138 = yylhsminor.yy138;
#ifdef YYLOCATIONTYPE
  /* Commit any @$ writes back to yy_reduce's local
  ** so the post-action stack-shift sees them. */
  *yy_ctx->yyloc_lhs_ptr = yyloc_lhs;
#endif
}

/* (219) maybe_semi ::= */
static void yy_rule_219(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (220) maybe_semi ::= TK_SEMI */
static void yy_rule_220(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (221) table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP */
static void yy_rule_221(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (222) table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP */
static void yy_rule_222(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (223) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname */
static void yy_rule_223(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (224) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP */
static void yy_rule_224(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* (225) table_constraint ::= TK_CHECK TK_LP expr TK_RP */
static void yy_rule_225(yy_reduce_ctx *yy_ctx){
  (void)yy_ctx;
}

/* Dispatch table -- one entry per rule, parallel to
** yyRuleInfoLhs[] / yyRuleInfoNRhs[].  Composition merges
** grammars by concatenating these arrays. */
static void (*const yy_rule_reduce_fn[])(yy_reduce_ctx *) = {
  yy_rule_0,  /* (0) input ::= stmt_list maybe_semi */
  yy_rule_1,  /* (1) stmt_list ::= stmt */
  yy_rule_2,  /* (2) stmt_list ::= stmt_list TK_SEMI stmt */
  yy_rule_3,  /* (3) stmt ::= select_stmt */
  yy_rule_4,  /* (4) stmt ::= insert_stmt */
  yy_rule_5,  /* (5) stmt ::= update_stmt */
  yy_rule_6,  /* (6) stmt ::= delete_stmt */
  yy_rule_7,  /* (7) stmt ::= create_stmt */
  yy_rule_8,  /* (8) stmt ::= drop_stmt */
  yy_rule_9,  /* (9) stmt ::= pragma_stmt */
  yy_rule_10,  /* (10) stmt ::= begin_stmt */
  yy_rule_11,  /* (11) stmt ::= commit_stmt */
  yy_rule_12,  /* (12) stmt ::= rollback_stmt */
  yy_rule_13,  /* (13) stmt ::= savepoint_stmt */
  yy_rule_14,  /* (14) stmt ::= release_stmt */
  yy_rule_15,  /* (15) stmt ::= attach_stmt */
  yy_rule_16,  /* (16) stmt ::= detach_stmt */
  yy_rule_17,  /* (17) stmt ::= explain_stmt */
  yy_rule_18,  /* (18) select_stmt ::= with_clause select_stmt */
  yy_rule_19,  /* (19) select_stmt ::= select_core */
  yy_rule_20,  /* (20) select_stmt ::= select_core TK_UNION select_stmt */
  yy_rule_21,  /* (21) select_stmt ::= select_core TK_UNION TK_ALL select_stmt */
  yy_rule_22,  /* (22) select_stmt ::= select_core TK_INTERSECT select_stmt */
  yy_rule_23,  /* (23) select_stmt ::= select_core TK_EXCEPT select_stmt */
  yy_rule_24,  /* (24) select_core ::= TK_SELECT distinct_opt sel_list from_opt where_opt group_opt having_opt order_opt limit_opt offset_part */
  yy_rule_25,  /* (25) distinct_opt ::= */
  yy_rule_26,  /* (26) distinct_opt ::= TK_DISTINCT */
  yy_rule_27,  /* (27) distinct_opt ::= TK_ALL */
  yy_rule_28,  /* (28) sel_list ::= sel_item */
  yy_rule_29,  /* (29) sel_list ::= sel_list TK_COMMA sel_item */
  yy_rule_30,  /* (30) sel_item ::= TK_STAR */
  yy_rule_31,  /* (31) sel_item ::= TK_ID TK_DOT TK_STAR */
  yy_rule_32,  /* (32) sel_item ::= expr */
  yy_rule_33,  /* (33) sel_item ::= expr TK_AS TK_ID */
  yy_rule_34,  /* (34) sel_item ::= expr TK_ID */
  yy_rule_35,  /* (35) from_opt ::= */
  yy_rule_36,  /* (36) from_opt ::= TK_FROM table_list */
  yy_rule_37,  /* (37) table_list ::= table_ref */
  yy_rule_38,  /* (38) table_list ::= table_list TK_COMMA table_ref */
  yy_rule_39,  /* (39) table_list ::= table_list join_op table_ref join_constraint */
  yy_rule_40,  /* (40) join_op ::= TK_JOIN */
  yy_rule_41,  /* (41) join_op ::= TK_INNER TK_JOIN */
  yy_rule_42,  /* (42) join_op ::= TK_LEFT TK_JOIN */
  yy_rule_43,  /* (43) join_op ::= TK_LEFT TK_OUTER TK_JOIN */
  yy_rule_44,  /* (44) join_op ::= TK_RIGHT TK_JOIN */
  yy_rule_45,  /* (45) join_op ::= TK_RIGHT TK_OUTER TK_JOIN */
  yy_rule_46,  /* (46) join_op ::= TK_FULL TK_JOIN */
  yy_rule_47,  /* (47) join_op ::= TK_FULL TK_OUTER TK_JOIN */
  yy_rule_48,  /* (48) join_op ::= TK_CROSS TK_JOIN */
  yy_rule_49,  /* (49) join_op ::= TK_NATURAL TK_JOIN */
  yy_rule_50,  /* (50) join_op ::= TK_NATURAL TK_LEFT TK_JOIN */
  yy_rule_51,  /* (51) join_constraint ::= */
  yy_rule_52,  /* (52) join_constraint ::= TK_ON expr */
  yy_rule_53,  /* (53) join_constraint ::= TK_USING TK_LP id_list TK_RP */
  yy_rule_54,  /* (54) table_ref ::= TK_ID */
  yy_rule_55,  /* (55) table_ref ::= TK_ID TK_DOT TK_ID */
  yy_rule_56,  /* (56) table_ref ::= TK_ID TK_AS TK_ID */
  yy_rule_57,  /* (57) table_ref ::= TK_ID TK_ID */
  yy_rule_58,  /* (58) table_ref ::= TK_LP select_stmt TK_RP */
  yy_rule_59,  /* (59) table_ref ::= TK_LP select_stmt TK_RP TK_AS TK_ID */
  yy_rule_60,  /* (60) where_opt ::= */
  yy_rule_61,  /* (61) where_opt ::= TK_WHERE expr */
  yy_rule_62,  /* (62) group_opt ::= */
  yy_rule_63,  /* (63) group_opt ::= TK_GROUP TK_BY expr_list */
  yy_rule_64,  /* (64) having_opt ::= */
  yy_rule_65,  /* (65) having_opt ::= TK_HAVING expr */
  yy_rule_66,  /* (66) order_opt ::= */
  yy_rule_67,  /* (67) order_opt ::= TK_ORDER TK_BY order_list */
  yy_rule_68,  /* (68) order_list ::= order_item */
  yy_rule_69,  /* (69) order_list ::= order_list TK_COMMA order_item */
  yy_rule_70,  /* (70) order_item ::= expr */
  yy_rule_71,  /* (71) order_item ::= expr TK_ASC */
  yy_rule_72,  /* (72) order_item ::= expr TK_DESC */
  yy_rule_73,  /* (73) limit_opt ::= */
  yy_rule_74,  /* (74) limit_opt ::= TK_LIMIT expr */
  yy_rule_75,  /* (75) offset_part ::= */
  yy_rule_76,  /* (76) offset_part ::= TK_OFFSET expr */
  yy_rule_77,  /* (77) offset_part ::= TK_COMMA expr */
  yy_rule_78,  /* (78) with_clause ::= TK_WITH cte_list */
  yy_rule_79,  /* (79) with_clause ::= TK_WITH TK_RECURSIVE cte_list */
  yy_rule_80,  /* (80) cte_list ::= cte */
  yy_rule_81,  /* (81) cte_list ::= cte_list TK_COMMA cte */
  yy_rule_82,  /* (82) cte ::= TK_ID TK_AS TK_LP select_stmt TK_RP */
  yy_rule_83,  /* (83) cte ::= TK_ID TK_LP id_list TK_RP TK_AS TK_LP select_stmt TK_RP */
  yy_rule_84,  /* (84) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols values_list */
  yy_rule_85,  /* (85) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols TK_DEFAULT TK_VALUES */
  yy_rule_86,  /* (86) insert_stmt ::= TK_INSERT TK_INTO qname insert_cols select_stmt */
  yy_rule_87,  /* (87) insert_stmt ::= TK_REPLACE TK_INTO qname insert_cols values_list */
  yy_rule_88,  /* (88) qname ::= TK_ID */
  yy_rule_89,  /* (89) qname ::= TK_ID TK_DOT TK_ID */
  yy_rule_90,  /* (90) insert_cols ::= */
  yy_rule_91,  /* (91) insert_cols ::= TK_LP id_list TK_RP */
  yy_rule_92,  /* (92) values_list ::= TK_VALUES TK_LP expr_list TK_RP */
  yy_rule_93,  /* (93) values_list ::= values_list TK_COMMA TK_LP expr_list TK_RP */
  yy_rule_94,  /* (94) update_stmt ::= TK_UPDATE qname TK_SET assign_list where_opt */
  yy_rule_95,  /* (95) assign_list ::= assign */
  yy_rule_96,  /* (96) assign_list ::= assign_list TK_COMMA assign */
  yy_rule_97,  /* (97) assign ::= TK_ID TK_EQ expr */
  yy_rule_98,  /* (98) delete_stmt ::= TK_DELETE TK_FROM qname where_opt */
  yy_rule_99,  /* (99) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_LP table_body TK_RP */
  yy_rule_100,  /* (100) create_stmt ::= TK_CREATE temp_opt TK_TABLE if_not_exists qname TK_AS select_stmt */
  yy_rule_101,  /* (101) create_stmt ::= TK_CREATE unique_opt TK_INDEX if_not_exists qname TK_ON TK_ID TK_LP id_list TK_RP */
  yy_rule_102,  /* (102) create_stmt ::= TK_CREATE temp_opt TK_VIEW if_not_exists qname TK_AS select_stmt */
  yy_rule_103,  /* (103) temp_opt ::= */
  yy_rule_104,  /* (104) temp_opt ::= TK_TEMP */
  yy_rule_105,  /* (105) temp_opt ::= TK_TEMPORARY */
  yy_rule_106,  /* (106) unique_opt ::= */
  yy_rule_107,  /* (107) unique_opt ::= TK_UNIQUE */
  yy_rule_108,  /* (108) if_not_exists ::= */
  yy_rule_109,  /* (109) if_not_exists ::= TK_IF TK_NOT TK_EXISTS */
  yy_rule_110,  /* (110) table_body ::= table_elem */
  yy_rule_111,  /* (111) table_body ::= table_body TK_COMMA table_elem */
  yy_rule_112,  /* (112) table_elem ::= coldef */
  yy_rule_113,  /* (113) table_elem ::= table_constraint */
  yy_rule_114,  /* (114) coldef ::= TK_ID type_opt col_constraints */
  yy_rule_115,  /* (115) type_opt ::= */
  yy_rule_116,  /* (116) type_opt ::= TK_ID */
  yy_rule_117,  /* (117) type_opt ::= TK_ID TK_LP TK_NUMBER TK_RP */
  yy_rule_118,  /* (118) type_opt ::= TK_ID TK_LP TK_NUMBER TK_COMMA TK_NUMBER TK_RP */
  yy_rule_119,  /* (119) col_constraints ::= */
  yy_rule_120,  /* (120) col_constraints ::= col_constraints col_constraint */
  yy_rule_121,  /* (121) col_constraint ::= TK_PRIMARY TK_KEY */
  yy_rule_122,  /* (122) col_constraint ::= TK_PRIMARY TK_KEY TK_ASC */
  yy_rule_123,  /* (123) col_constraint ::= TK_PRIMARY TK_KEY TK_DESC */
  yy_rule_124,  /* (124) col_constraint ::= TK_PRIMARY TK_KEY TK_AUTOINCR */
  yy_rule_125,  /* (125) col_constraint ::= TK_NOT TK_NULL */
  yy_rule_126,  /* (126) col_constraint ::= TK_UNIQUE */
  yy_rule_127,  /* (127) col_constraint ::= TK_DEFAULT expr_atom */
  yy_rule_128,  /* (128) col_constraint ::= TK_CHECK TK_LP expr TK_RP */
  yy_rule_129,  /* (129) col_constraint ::= TK_REFERENCES qname */
  yy_rule_130,  /* (130) col_constraint ::= TK_REFERENCES qname TK_LP id_list TK_RP */
  yy_rule_131,  /* (131) drop_stmt ::= TK_DROP TK_TABLE if_exists qname */
  yy_rule_132,  /* (132) drop_stmt ::= TK_DROP TK_INDEX if_exists qname */
  yy_rule_133,  /* (133) drop_stmt ::= TK_DROP TK_VIEW if_exists qname */
  yy_rule_134,  /* (134) drop_stmt ::= TK_DROP TK_TRIGGER if_exists qname */
  yy_rule_135,  /* (135) if_exists ::= */
  yy_rule_136,  /* (136) if_exists ::= TK_IF TK_EXISTS */
  yy_rule_137,  /* (137) pragma_stmt ::= TK_PRAGMA qname */
  yy_rule_138,  /* (138) pragma_stmt ::= TK_PRAGMA qname TK_EQ expr_atom */
  yy_rule_139,  /* (139) pragma_stmt ::= TK_PRAGMA qname TK_LP expr_atom TK_RP */
  yy_rule_140,  /* (140) begin_stmt ::= TK_BEGIN */
  yy_rule_141,  /* (141) begin_stmt ::= TK_BEGIN TK_TRANSACTION */
  yy_rule_142,  /* (142) begin_stmt ::= TK_BEGIN TK_ID */
  yy_rule_143,  /* (143) begin_stmt ::= TK_BEGIN TK_ID TK_TRANSACTION */
  yy_rule_144,  /* (144) commit_stmt ::= TK_COMMIT */
  yy_rule_145,  /* (145) commit_stmt ::= TK_COMMIT TK_TRANSACTION */
  yy_rule_146,  /* (146) commit_stmt ::= TK_END */
  yy_rule_147,  /* (147) commit_stmt ::= TK_END TK_TRANSACTION */
  yy_rule_148,  /* (148) rollback_stmt ::= TK_ROLLBACK */
  yy_rule_149,  /* (149) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION */
  yy_rule_150,  /* (150) rollback_stmt ::= TK_ROLLBACK TK_TO TK_ID */
  yy_rule_151,  /* (151) rollback_stmt ::= TK_ROLLBACK TK_TRANSACTION TK_TO TK_ID */
  yy_rule_152,  /* (152) rollback_stmt ::= TK_ROLLBACK TK_TO TK_SAVEPOINT TK_ID */
  yy_rule_153,  /* (153) savepoint_stmt ::= TK_SAVEPOINT TK_ID */
  yy_rule_154,  /* (154) release_stmt ::= TK_RELEASE TK_ID */
  yy_rule_155,  /* (155) release_stmt ::= TK_RELEASE TK_SAVEPOINT TK_ID */
  yy_rule_156,  /* (156) attach_stmt ::= TK_ATTACH expr_atom TK_AS TK_ID */
  yy_rule_157,  /* (157) attach_stmt ::= TK_ATTACH TK_DATABASE expr_atom TK_AS TK_ID */
  yy_rule_158,  /* (158) detach_stmt ::= TK_DETACH TK_ID */
  yy_rule_159,  /* (159) detach_stmt ::= TK_DETACH TK_DATABASE TK_ID */
  yy_rule_160,  /* (160) explain_stmt ::= TK_EXPLAIN stmt */
  yy_rule_161,  /* (161) explain_stmt ::= TK_EXPLAIN TK_QUERY TK_PLAN stmt */
  yy_rule_162,  /* (162) id_list ::= TK_ID */
  yy_rule_163,  /* (163) id_list ::= id_list TK_COMMA TK_ID */
  yy_rule_164,  /* (164) expr_list ::= expr */
  yy_rule_165,  /* (165) expr_list ::= expr_list TK_COMMA expr */
  yy_rule_166,  /* (166) expr ::= expr_atom */
  yy_rule_167,  /* (167) expr ::= TK_LP expr TK_RP */
  yy_rule_168,  /* (168) expr ::= TK_LP select_stmt TK_RP */
  yy_rule_169,  /* (169) expr ::= TK_NOT expr */
  yy_rule_170,  /* (170) expr ::= TK_MINUS expr */
  yy_rule_171,  /* (171) expr ::= TK_PLUS expr */
  yy_rule_172,  /* (172) expr ::= TK_TILDE expr */
  yy_rule_173,  /* (173) expr ::= expr TK_OR expr */
  yy_rule_174,  /* (174) expr ::= expr TK_AND expr */
  yy_rule_175,  /* (175) expr ::= expr TK_EQ expr */
  yy_rule_176,  /* (176) expr ::= expr TK_NE expr */
  yy_rule_177,  /* (177) expr ::= expr TK_LT expr */
  yy_rule_178,  /* (178) expr ::= expr TK_LE expr */
  yy_rule_179,  /* (179) expr ::= expr TK_GT expr */
  yy_rule_180,  /* (180) expr ::= expr TK_GE expr */
  yy_rule_181,  /* (181) expr ::= expr TK_PLUS expr */
  yy_rule_182,  /* (182) expr ::= expr TK_MINUS expr */
  yy_rule_183,  /* (183) expr ::= expr TK_STAR expr */
  yy_rule_184,  /* (184) expr ::= expr TK_SLASH expr */
  yy_rule_185,  /* (185) expr ::= expr TK_PERCENT expr */
  yy_rule_186,  /* (186) expr ::= expr TK_CONCAT expr */
  yy_rule_187,  /* (187) expr ::= expr TK_AMP expr */
  yy_rule_188,  /* (188) expr ::= expr TK_PIPE expr */
  yy_rule_189,  /* (189) expr ::= expr TK_LIKE expr */
  yy_rule_190,  /* (190) expr ::= expr TK_IS TK_NULL */
  yy_rule_191,  /* (191) expr ::= expr TK_IS TK_NOT TK_NULL */
  yy_rule_192,  /* (192) expr ::= expr TK_NOT TK_NULL */
  yy_rule_193,  /* (193) expr ::= expr TK_IS expr */
  yy_rule_194,  /* (194) expr ::= expr TK_IN TK_LP expr_list TK_RP */
  yy_rule_195,  /* (195) expr ::= expr TK_NOT TK_IN TK_LP expr_list TK_RP */
  yy_rule_196,  /* (196) expr ::= expr TK_NOT TK_IN TK_LP select_stmt TK_RP */
  yy_rule_197,  /* (197) expr ::= expr TK_IN TK_LP select_stmt TK_RP */
  yy_rule_198,  /* (198) expr ::= expr TK_BETWEEN expr TK_AND expr */
  yy_rule_199,  /* (199) expr ::= TK_CASE expr_atom case_arms case_else TK_END */
  yy_rule_200,  /* (200) expr ::= TK_CASE case_arms case_else TK_END */
  yy_rule_201,  /* (201) expr ::= TK_ID TK_LP TK_RP */
  yy_rule_202,  /* (202) expr ::= TK_ID TK_LP TK_STAR TK_RP */
  yy_rule_203,  /* (203) expr ::= TK_ID TK_LP expr_list TK_RP */
  yy_rule_204,  /* (204) expr ::= TK_ID TK_LP TK_DISTINCT expr_list TK_RP */
  yy_rule_205,  /* (205) case_arms ::= TK_WHEN expr TK_THEN expr */
  yy_rule_206,  /* (206) case_arms ::= case_arms TK_WHEN expr TK_THEN expr */
  yy_rule_207,  /* (207) case_else ::= */
  yy_rule_208,  /* (208) case_else ::= TK_ELSE expr */
  yy_rule_209,  /* (209) expr_atom ::= TK_NUMBER */
  yy_rule_210,  /* (210) expr_atom ::= TK_STRING */
  yy_rule_211,  /* (211) expr_atom ::= TK_BLOB */
  yy_rule_212,  /* (212) expr_atom ::= TK_NULL */
  yy_rule_213,  /* (213) expr_atom ::= TK_TRUE */
  yy_rule_214,  /* (214) expr_atom ::= TK_FALSE */
  yy_rule_215,  /* (215) expr_atom ::= TK_QMARK */
  yy_rule_216,  /* (216) expr_atom ::= TK_ID */
  yy_rule_217,  /* (217) expr_atom ::= TK_ID TK_DOT TK_ID */
  yy_rule_218,  /* (218) expr_atom ::= TK_ID TK_DOT TK_ID TK_DOT TK_ID */
  yy_rule_219,  /* (219) maybe_semi ::= */
  yy_rule_220,  /* (220) maybe_semi ::= TK_SEMI */
  yy_rule_221,  /* (221) table_constraint ::= TK_PRIMARY TK_KEY TK_LP id_list TK_RP */
  yy_rule_222,  /* (222) table_constraint ::= TK_UNIQUE TK_LP id_list TK_RP */
  yy_rule_223,  /* (223) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname */
  yy_rule_224,  /* (224) table_constraint ::= TK_FOREIGN TK_KEY TK_LP id_list TK_RP TK_REFERENCES qname TK_LP id_list TK_RP */
  yy_rule_225,  /* (225) table_constraint ::= TK_CHECK TK_LP expr TK_RP */
};
/********** End per-rule reduce action functions ******************************/

/*
** Perform a reduce action and the shift that must immediately
** follow the reduce.
**
** The yyLookahead and yyLookaheadToken parameters provide reduce actions
** access to the lookahead token (if any).  The yyLookahead will be YYNOCODE
** if the lookahead token has already been consumed.  As this procedure is
** only called from one place, optimizing compilers will in-line it, which
** means that the extra parameters have no performance impact.
*/
static YYACTIONTYPE yy_reduce(
  yyParser *yypParser,         /* The parser */
  unsigned int yyruleno,       /* Number of the rule by which to reduce */
  int yyLookahead,             /* Lookahead token, or YYNOCODE if none */
  SqlParseTOKENTYPE yyLookaheadToken  /* Value of the lookahead token */
  SqlParseCTX_PDECL                   /* %extra_context */
){
  int yygoto;                     /* The next state */
  YYACTIONTYPE yyact;             /* The next action */
  yyStackEntry *yymsp;            /* The top of the parser's stack */
  int yysize;                     /* Amount to pop the stack */
#ifdef YYLOCATIONTYPE
  /* P0-NEW-7: per-reduce LHS-location local.  Bound to @$ /
  ** @<lhsalias> in user action bodies, so the action sees
  ** Bison's documented pre-action default and may overwrite
  ** it.  Computed below before the action switch; committed
  ** to the LHS slot's yyloc after the stack adjustment. */
  YYLOCATIONTYPE yyloc_lhs;
# ifndef YYRHSLOC
#  define YYRHSLOC(Rhs, K) ((Rhs)[K])
# endif
#endif
  (void)yyLookahead;
  (void)yyLookaheadToken;
  yymsp = yypParser->yytos;

#ifdef YYLOCATIONTYPE
  /* Compute the LHS location BEFORE the action body, matching
  ** Bison's documented YYLLOC_DEFAULT ordering (P0-NEW-7).  The
  ** action body's @$ writes go to yyloc_lhs and are preserved;
  ** see P0-NEW-6 for why this matters (ecpg's preproc.y has 70+
  ** non-trivial @$ assignments that build source-text
  ** accumulation across each reduce). */
  {
    int yyN = -yyRuleInfoNRhs[yyruleno];   /* nrhs >= 0 */
# ifdef YYLLOC_DEFAULT
    /* User-defined override.  Build a 0-indexed YYLOCATIONTYPE
    ** array per Bison's signature: Rhs[i] for i in 1..N is the
    ** i-th RHS's location, Rhs[0] is the slot below the rule
    ** (used for empty-rule fallback). */
    YYLOCATIONTYPE yyloc_rhs[YYNRHS_MAX + 1];
    int yi;
    yyloc_rhs[0] = yymsp[-yyN].yyloc;
    for( yi=1; yi<=yyN; yi++ ){
      yyloc_rhs[yi] = yymsp[yi-yyN].yyloc;
    }
    YYLLOC_DEFAULT(yyloc_lhs, yyloc_rhs, yyN);
# else
    /* Built-in default: Rhs[1] for non-empty, lookahead for
    ** empty.  Same observable result as the prior post-action
    ** slot-reuse logic. */
    if( yyN == 0 ){
      yyloc_lhs = yypParser->yyLookaheadLoc;
    }else{
      yyloc_lhs = yymsp[1-yyN].yyloc;
    }
# endif
  }
#endif

  /* v0.6.0: dispatch the rule's reduction via the per-rule
  ** function pointer table.  The bookkeeping below (LHS lookup,
  ** stack pop+push, location commit) is unchanged. */
  assert( yyruleno<sizeof(yyRuleInfoLhs)/sizeof(yyRuleInfoLhs[0]) );
  assert( yyruleno<(sizeof(yy_rule_reduce_fn)/sizeof(yy_rule_reduce_fn[0])) );
  {
    yy_reduce_ctx yy_ctx;
    yy_ctx.yypParser = yypParser;
    yy_ctx.yymsp = yymsp;
    yy_ctx.yyLookahead = yyLookahead;
    yy_ctx.yyLookaheadToken = yyLookaheadToken;
#ifdef YYLOCATIONTYPE
    yy_ctx.yyloc_lhs_ptr = &yyloc_lhs;
#endif
    yy_rule_reduce_fn[yyruleno](&yy_ctx);
  }

  yygoto = yyRuleInfoLhs[yyruleno];
  yysize = yyRuleInfoNRhs[yyruleno];
  yyact = yy_find_reduce_action(yymsp[yysize].stateno,(YYCODETYPE)yygoto);

  /* There are no SHIFTREDUCE actions on nonterminals because the table
  ** generator has simplified them to pure REDUCE actions. */
  assert( !(yyact>YY_MAX_SHIFT && yyact<=YY_MAX_SHIFTREDUCE) );

  /* It is not possible for a REDUCE to be followed by an error */
  assert( yyact!=YY_ERROR_ACTION );

  yymsp += yysize+1;
  yypParser->yytos = yymsp;
  yymsp->stateno = (YYACTIONTYPE)yyact;
  yymsp->major = (YYCODETYPE)yygoto;
#ifdef YYLOCATIONTYPE
  /* P0-NEW-7: commit the LHS location computed before the
  ** action body (and possibly overwritten by the action via
  ** @$ / @<lhsalias>) to the LHS slot's yyloc field. */
  yymsp->yyloc = yyloc_lhs;
#endif
  yyTraceShift(yypParser, yyact, "... then shift");
  return yyact;
}

/*
** The following code executes when the parse fails
*/
#ifndef YYNOERRORRECOVERY
static void yy_parse_failed(
  yyParser *yypParser           /* The parser */
){
  SqlParseARG_FETCH
  SqlParseCTX_FETCH
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE,"%sFail!\n",yyTracePrompt);
  }
#endif
  while( yypParser->yytos>yypParser->yystack ) yy_pop_parser_stack(yypParser);
  /* Here code is inserted which will be executed whenever the
  ** parser fails */
/************ Begin %parse_failure code ***************************************/
#line 102 "sql_grammar.lime"

    pstate->error = 1;
    if (!pstate->err_msg) pstate->err_msg = "parse failure";
#line 9059 "sql_grammar.c"
/************ End %parse_failure code *****************************************/
  SqlParseARG_STORE /* Suppress warning about unused %extra_argument variable */
  SqlParseCTX_STORE
}
#endif /* YYNOERRORRECOVERY */

/*
** The following code executes when a syntax error first occurs.
**
** Inside the user-supplied %syntax_error block these locals are bound:
**
**   yymajor   -- token code of the offending lookahead.  0 for EOF.
**   yyminor   -- token value (SqlParseTOKENTYPE) of the offending
**                lookahead.  Also available as the macro TOKEN.
**   yyloc     -- source location of the offending lookahead, when the
**                grammar declares %locations and parsing is driven by
**                SqlParseLoc().  Also available as the macro TOKEN_LOC.
**                Zero-initialised when locations are not threaded.
**   yypParser -- the parser handle (yyParser *).
**
** These bindings let %syntax_error implementations distinguish, for
** example, "error at end of input" (yymajor==0) from "error at or
** near <token>" (yymajor==some_terminal) the way bison's yyerror
** does via *yychar / *yylloc.
*/
static void yy_syntax_error(
  yyParser *yypParser,           /* The parser */
  int yymajor,                   /* The major type of the error token */
  SqlParseTOKENTYPE yyminor         /* The minor type of the error token */
#ifdef YYLOCATIONTYPE
  ,YYLOCATIONTYPE yyloc          /* Source location of the error token */
#endif
){
  SqlParseARG_FETCH
  SqlParseCTX_FETCH
  /* When the user does not supply a %syntax_error block, yymajor /
  ** yyminor are unused.  Reference them via a (void) cast so generated
  ** parsers compile cleanly under -Wunused-parameter without forcing
  ** an explicit pragma in every emitted file. */
  (void)yymajor;
  (void)yyminor;
#define TOKEN yyminor
#ifdef YYLOCATIONTYPE
#define TOKEN_LOC yyloc
  (void)yyloc;
#endif
/************ Begin %syntax_error code ****************************************/
#line 95 "sql_grammar.lime"

    (void)yymajor;
    (void)yyminor;
    pstate->error = 1;
    if (!pstate->err_msg) pstate->err_msg = "syntax error";
#line 9113 "sql_grammar.c"
/************ End %syntax_error code ******************************************/
  SqlParseARG_STORE /* Suppress warning about unused %extra_argument variable */
  SqlParseCTX_STORE
}

/*
** The following is executed when the parser accepts
*/
static void yy_accept(
  yyParser *yypParser           /* The parser */
){
  SqlParseARG_FETCH
  SqlParseCTX_FETCH
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE,"%sAccept!\n",yyTracePrompt);
  }
#endif
#ifndef YYNOERRORRECOVERY
  yypParser->yyerrcnt = -1;
#endif
  assert( yypParser->yytos==yypParser->yystack );
  /* Here code is inserted which will be executed whenever the
  ** parser accepts */
/*********** Begin %parse_accept code *****************************************/
/*********** End %parse_accept code *******************************************/
  SqlParseARG_STORE /* Suppress warning about unused %extra_argument variable */
  SqlParseCTX_STORE
}

/* The main parser program.
** The first argument is a pointer to a structure obtained from
** "SqlParseAlloc" which describes the current state of the parser.
** The second argument is the major token number.  The third is
** the minor token.  The fourth optional argument is whatever the
** user wants (and specified in the grammar) and is available for
** use by the action routines.
**
** Inputs:
** <ul>
** <li> A pointer to the parser (an opaque structure.)
** <li> The major token number.
** <li> The minor token number.
** <li> An option argument of a grammar-specified type.
** </ul>
**
** Outputs:
** None.
*/

/* Helper macro for calling yy_syntax_error with optional location argument.
**
** The yyloc argument is the source location of the *offending lookahead*,
** not of the stack-top symbol.  SqlParseLoc() stashes the lookahead's
** location in yypParser->yyLookaheadLoc on each call; the macro reads it
** from there.  This matches Bison's *yylloc semantics in yyerror() --
** the location of the token that the parser couldn't accept -- and is
** what callers writing PostgreSQL-compatible error messages need.
**
** When the pull-parser entry point SqlParse() is used (no location
** threading), or when a syntax error fires before the first SqlParseLoc()
** call, yyLookaheadLoc is zero-initialized -- callers should treat
** that as "location unknown". */
#ifdef YYLOCATIONTYPE
# define YY_SYNTAX_ERROR(P,M,m) \
    yy_syntax_error(P,M,m, (P)->yyLookaheadLoc)
#else
# define YY_SYNTAX_ERROR(P,M,m) yy_syntax_error(P,M,m)
#endif

/*
** Action-time lookahead access (P0-NEW-5).  Equivalent to bison's
** yychar / yyclearin from inside an action body.
**
** Use case: empty productions whose action wants to consume the
** parser's pending lookahead and reach into the lexer for further
** tokens.  PostgreSQL's plpgsql pl_gram.y has a dozen rules of the
** form
**     decl_datatype : empty { $$ = read_datatype(yychar, ...); yyclearin; }
** that all rely on this idiom.
**
** SqlParse_get_lookahead() returns the externally-visible token code
** the parser is about to shift -- the same value the caller passed
** to SqlParse() / SqlParseLoc() and the same value the user-emitted
** #defines compare against.  Returns YYEMPTY (-2) if no SqlParse() call
** is currently in progress.  yyminor_out and yyloc_out are optional;
** pass NULL to skip.  yyloc_out is only written when the parser was
** built with %locations (YYLOCATIONTYPE defined); otherwise it is
** ignored.
**
** SqlParse_clear_lookahead() tells the dispatch loop the action body
** consumed the lookahead via SqlParse_get_lookahead() and pulled
** further tokens from the lexer itself.  The current SqlParse() call
** returns without shifting.  The driver's next SqlParse() call provides
** whatever the lexer has advanced to, treated as a fresh lookahead.
*/
int SqlParse_get_lookahead(
  void *yyp,                          /* The parser */
  SqlParseTOKENTYPE *yyminor_out         /* (out) Token value, or NULL */
#ifdef YYLOCATIONTYPE
  , YYLOCATIONTYPE *yyloc_out         /* (out) Token location, or NULL */
#endif
){
  yyParser *yypParser = (yyParser*)yyp;
  if( yyminor_out!=0 ){
    *yyminor_out = yypParser->yyLookaheadMinor;
  }
#ifdef YYLOCATIONTYPE
  if( yyloc_out!=0 ){
    *yyloc_out = yypParser->yyLookaheadLoc;
  }
#endif
  return yypParser->yyLookaheadMajor;
}

void SqlParse_clear_lookahead(void *yyp){
  yyParser *yypParser = (yyParser*)yyp;
  yypParser->yyLookaheadCleared = 1;
}

/* P0-NEW-8: opt-in eager default-reduce drain.  See forward-decl
** comment above for the semantics.  Implementation: at each step,
** check if the top state's yy_default[] entry is an unconditional
** reduce (no token-specific entries would override it for any
** lookahead).  If so, fire it via yy_reduce; loop.  Otherwise stop.
**
** Detecting "unconditional" requires scanning the state's region of
** yy_lookahead[] for any entry that matches its position-relative
** token code -- O(YYNTOKEN) per drain step.  This is acceptable for
** the use case (one drain per token in the driver loop); a future
** optimization could emit a per-state bitmap at parser-build time. */
void SqlParse_drain(void *yyp SqlParseCTX_PDECL){
  yyParser *yypParser = (yyParser*)yyp;
  SqlParseCTX_FETCH
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE, "%sDrain entry, top=%d\n",
            yyTracePrompt, yypParser->yytos->stateno);
  }
#endif
  for(;;){
    YYACTIONTYPE state = yypParser->yytos->stateno;
    YYACTIONTYPE def;
    int base, i, unconditional;
    SqlParseTOKENTYPE dummy_tok;
    unsigned int ruleno;

    /* Case 1: state is a deferred reduce from a prior
    ** SHIFTREDUCE action.  In that case yy_shift stashed
    ** stateno = (real_state) + (YY_MIN_REDUCE - YY_MIN_SHIFTREDUCE)
    ** so the next SqlParse() call would re-enter the dispatch loop
    ** and fire the reduce on the new lookahead.  Fire it here
    ** instead -- with no lookahead in hand, exactly the
    ** semantics the caller asked for. */
    if( state >= YY_MIN_REDUCE && state <= YY_MAX_REDUCE ){
      ruleno = (unsigned int)(state - YY_MIN_REDUCE);
#ifndef NDEBUG
      if( yyTraceFILE ){
        fprintf(yyTraceFILE,
            "%sDrain firing deferred SHIFTREDUCE rule %u\n",
            yyTracePrompt, ruleno);
      }
#endif
      memset(&dummy_tok, 0, sizeof(dummy_tok));
      yy_reduce(yypParser, ruleno, YYNOCODE, dummy_tok SqlParseCTX_PARAM);
      continue;
    }

    /* Case 2: state is a normal shift state.  If its default
    ** action is an unconditional reduce (no token-specific
    ** entries would override it for any lookahead), fire that
    ** reduce now.  Otherwise stop -- a real lookahead is
    ** required to disambiguate. */
    if( state > YY_MAX_SHIFT ) break;       /* error/accept etc. */

    def = yy_default[state];
    if( def < YY_MIN_REDUCE || def > YY_MAX_REDUCE ) break;

    if( state > YY_SHIFT_COUNT ){
      /* Out of yy_shift_ofst[] range -- no specific actions
      ** possible, so the default is unconditional. */
      unconditional = 1;
    }else{
      base = yy_shift_ofst[state];
      unconditional = 1;
      for( i=0; i<YYNTOKEN; i++ ){
        int j = base + i;
        if( j>=0 && j<YY_ACTTAB_COUNT && yy_lookahead[j]==(YYCODETYPE)i ){
          unconditional = 0;
          break;
        }
      }
    }
    if( !unconditional ) break;

    ruleno = (unsigned int)(def - YY_MIN_REDUCE);
#ifndef NDEBUG
    if( yyTraceFILE ){
      fprintf(yyTraceFILE,
          "%sDrain firing unconditional default reduce by rule %u\n",
          yyTracePrompt, ruleno);
    }
#endif
    memset(&dummy_tok, 0, sizeof(dummy_tok));
    yy_reduce(yypParser, ruleno, YYNOCODE, dummy_tok SqlParseCTX_PARAM);
    /* Loop. */
  }
#ifndef NDEBUG
  if( yyTraceFILE ){
    fprintf(yyTraceFILE, "%sDrain exit, top=%d\n",
            yyTracePrompt, yypParser->yytos->stateno);
  }
#endif
}

void SqlParse(void*,int,SqlParseTOKENTYPE SqlParseARG_PDECL);
void SqlParse(
  void *yyp,                   /* The parser */
  int yymajor,                 /* The major token code number */
  SqlParseTOKENTYPE yyminor       /* The value for the token */
  SqlParseARG_PDECL               /* Optional %extra_argument parameter */
){
  YYMINORTYPE yyminorunion;
  YYACTIONTYPE yyact;   /* The parser action. */
#if !defined(YYERRORSYMBOL) && !defined(YYNOERRORRECOVERY)
  int yyendofinput;     /* True if we are at the end of input */
#endif
#ifdef YYERRORSYMBOL
  int yyerrorhit = 0;   /* True if yymajor has invoked an error */
#endif
  yyParser *yypParser = (yyParser*)yyp;  /* The parser */
  int yymajorExternal;
  SqlParseCTX_FETCH
  SqlParseARG_STORE

  /* Stash the external lookahead first thing so action code that
  ** runs in any reduce sequence can read it via
  ** SqlParse_get_lookahead().  Done before the %first_token range
  ** check so that even %syntax_error fired on an out-of-range
  ** code can see the offending token via the same API.  P0-NEW-5. */
  yypParser->yyLookaheadMajor = yymajor;
  yypParser->yyLookaheadMinor = yyminor;
  yypParser->yyLookaheadCleared = 0;

  /* %first_token N -- the user passes the externally-visible token
  ** code (with the offset applied per the emitted #defines).  Convert
  ** to the internal action-table index by subtracting the offset.
  ** EOF (0) is preserved.  When YYFIRSTTOKEN is 0 (the default), this
  ** entire block is a no-op that the optimiser deletes.
  **
  ** If the external code is out of range -- e.g. an ASCII '+' (43)
  ** sneaking through to a parser declared with %first_token 258 --
  ** fire %syntax_error directly and return.  Indexing the action
  ** table at a negative or out-of-range slot would be undefined
  ** behaviour. */
  yymajorExternal = yymajor;
#if YYFIRSTTOKEN > 0
  if( yymajor != 0 ){
    yymajor -= YYFIRSTTOKEN;
    if( yymajor < 0 || yymajor >= YYNTOKEN ){
      YYMINORTYPE yyminorunion_local;
      memset(&yyminorunion_local, 0, sizeof(yyminorunion_local));
      yyminorunion_local.yy0 = yyminor;
#ifndef NDEBUG
      if( yyTraceFILE ){
        fprintf(yyTraceFILE, "%sExternal token code %d outside "
                "[YYFIRSTTOKEN..YYFIRSTTOKEN+YYNTOKEN); rejecting.\n",
                yyTracePrompt, yymajorExternal);
      }
#endif
      YY_SYNTAX_ERROR(yypParser, yymajorExternal, yyminor);
      (void)yyminorunion_local;
      yypParser->yyLookaheadMajor = -2;
      yypParser->yyLookaheadCleared = 0;
      SqlParseARG_STORE;
      return;
    }
  }
#endif

  assert( yypParser->yytos!=0 );
#if !defined(YYERRORSYMBOL) && !defined(YYNOERRORRECOVERY)
  yyendofinput = (yymajor==0);
#endif

  yyact = yypParser->yytos->stateno;
#ifndef NDEBUG
  if( yyTraceFILE ){
    if( yyact < YY_MIN_REDUCE ){
      fprintf(yyTraceFILE,"%sInput '%s' in state %d\n",
              yyTracePrompt,yyTokenName[yymajor],yyact);
    }else{
      fprintf(yyTraceFILE,"%sInput '%s' with pending reduce %d\n",
              yyTracePrompt,yyTokenName[yymajor],yyact-YY_MIN_REDUCE);
    }
  }
#endif

  while(1){ /* Exit by "break" */
    assert( yypParser->yytos>=yypParser->yystack );
    assert( yyact==yypParser->yytos->stateno );
    yyact = yy_find_shift_action((YYCODETYPE)yymajor,yyact);
    if( yyact >= YY_MIN_REDUCE ){
      unsigned int yyruleno = yyact - YY_MIN_REDUCE; /* Reduce by this rule */
#ifndef NDEBUG
      assert( yyruleno<(int)(sizeof(yyRuleName)/sizeof(yyRuleName[0])) );
      if( yyTraceFILE ){
        int yysize = yyRuleInfoNRhs[yyruleno];
        if( yysize ){
          fprintf(yyTraceFILE, "%sReduce %d [%s]%s, pop back to state %d.\n",
            yyTracePrompt,
            yyruleno, yyRuleName[yyruleno],
            yyruleno<YYNRULE_WITH_ACTION ? "" : " without external action",
            yypParser->yytos[yysize].stateno);
        }else{
          fprintf(yyTraceFILE, "%sReduce %d [%s]%s.\n",
            yyTracePrompt, yyruleno, yyRuleName[yyruleno],
            yyruleno<YYNRULE_WITH_ACTION ? "" : " without external action");
        }
      }
#endif /* NDEBUG */

      /* Check that the stack is large enough to grow by a single entry
      ** if the RHS of the rule is empty.  This ensures that there is room
      ** enough on the stack to push the LHS value */
      if( yyRuleInfoNRhs[yyruleno]==0 ){
#ifdef YYTRACKMAXSTACKDEPTH
        if( (int)(yypParser->yytos - yypParser->yystack)>yypParser->yyhwm ){
          yypParser->yyhwm++;
          assert( yypParser->yyhwm ==
                  (int)(yypParser->yytos - yypParser->yystack));
        }
#endif
        if( yypParser->yytos>=yypParser->yystackEnd ){
          if( yyGrowStack(yypParser) ){
            yyStackOverflow(yypParser);
            break;
          }
        }
      }
      yyact = yy_reduce(yypParser,yyruleno,yymajor,yyminor SqlParseCTX_PARAM);
      /* If the reduce action called SqlParse_clear_lookahead() the user
      ** action consumed the token via SqlParse_get_lookahead() and read
      ** further tokens from the lexer themselves.  Skip the rest of
      ** the dispatch (no shift) and return.  The driver's next
      ** SqlParse() call provides whatever the lexer has advanced to.
      ** P0-NEW-5. */
      if( yypParser->yyLookaheadCleared ){
#ifndef NDEBUG
        if( yyTraceFILE ){
          fprintf(yyTraceFILE,
            "%sLookahead consumed by action; returning without shift.\n",
            yyTracePrompt);
        }
#endif
        yypParser->yyLookaheadMajor = -2;
        yypParser->yyLookaheadCleared = 0;
        SqlParseARG_STORE;
        return;
      }
    }else if( yyact <= YY_MAX_SHIFTREDUCE ){
      yy_shift(yypParser,yyact,(YYCODETYPE)yymajor,yyminor);
#ifndef YYNOERRORRECOVERY
      yypParser->yyerrcnt--;
#endif
      break;
    }else if( LIME_UNLIKELY(yyact==YY_ACCEPT_ACTION) ){
      yypParser->yytos--;
      yy_accept(yypParser);
      return;
    }else{
      assert( yyact == YY_ERROR_ACTION );
      yyminorunion.yy0 = yyminor;
#ifdef YYERRORSYMBOL
      int yymx;
#endif
#ifndef NDEBUG
      if( yyTraceFILE ){
        fprintf(yyTraceFILE,"%sSyntax Error!\n",yyTracePrompt);
      }
#endif
#ifdef YYERRORSYMBOL
      /* A syntax error has occurred.
      ** The response to an error depends upon whether or not the
      ** grammar defines an error token "ERROR".  
      **
      ** This is what we do if the grammar does define ERROR:
      **
      **  * Call the %syntax_error function.
      **
      **  * Begin popping the stack until we enter a state where
      **    it is legal to shift the error symbol, then shift
      **    the error symbol.
      **
      **  * Set the error count to three.
      **
      **  * Begin accepting and shifting new tokens.  No new error
      **    processing will occur until three tokens have been
      **    shifted successfully.
      **
      */
      if( yypParser->yyerrcnt<0 ){
        /* yymajorExternal is the offset-corrected token code that the
        ** caller passed to SqlParse(); user %syntax_error code expects to
        ** see this rather than the internal action-table index. */
        YY_SYNTAX_ERROR(yypParser,yymajorExternal,yyminor);
      }
      yymx = yypParser->yytos->major;
      if( yymx==YYERRORSYMBOL || yyerrorhit ){
#ifndef NDEBUG
        if( yyTraceFILE ){
          fprintf(yyTraceFILE,"%sDiscard input token %s\n",
             yyTracePrompt,yyTokenName[yymajor]);
        }
#endif
        yy_destructor(yypParser, (YYCODETYPE)yymajor, &yyminorunion);
        yymajor = YYNOCODE;
      }else{
        while( yypParser->yytos > yypParser->yystack ){
          yyact = yy_find_reduce_action(yypParser->yytos->stateno,
                                        YYERRORSYMBOL);
          if( yyact<=YY_MAX_SHIFTREDUCE ) break;
          yy_pop_parser_stack(yypParser);
        }
        if( yypParser->yytos <= yypParser->yystack || yymajor==0 ){
          yy_destructor(yypParser,(YYCODETYPE)yymajor,&yyminorunion);
          yy_parse_failed(yypParser);
#ifndef YYNOERRORRECOVERY
          yypParser->yyerrcnt = -1;
#endif
          yymajor = YYNOCODE;
        }else if( yymx!=YYERRORSYMBOL ){
          yy_shift(yypParser,yyact,YYERRORSYMBOL,yyminor);
        }
      }
      yypParser->yyerrcnt = 3;
      yyerrorhit = 1;
      if( yymajor==YYNOCODE ) break;
      yyact = yypParser->yytos->stateno;
#elif defined(YYNOERRORRECOVERY)
      /* If the YYNOERRORRECOVERY macro is defined, then do not attempt to
      ** do any kind of error recovery.  Instead, simply invoke the syntax
      ** error routine and continue going as if nothing had happened.
      **
      ** Applications can set this macro (for example inside %include) if
      ** they intend to abandon the parse upon the first syntax error seen.
      */
      YY_SYNTAX_ERROR(yypParser,yymajorExternal,yyminor);
      yy_destructor(yypParser,(YYCODETYPE)yymajor,&yyminorunion);
      break;
#else  /* YYERRORSYMBOL is not defined */
      /* This is what we do if the grammar does not define ERROR:
      **
      **  * Report an error message, and throw away the input token.
      **
      **  * If the input token is $, then fail the parse.
      **
      ** As before, subsequent error messages are suppressed until
      ** three input tokens have been successfully shifted.
      **
      ** If YYERRORSYNC is defined, use panic-mode recovery:
      ** discard tokens until a synchronization token is found,
      ** then resume parsing from the current state.
      */
      if( yypParser->yyerrcnt<=0 ){
        YY_SYNTAX_ERROR(yypParser,yymajorExternal,yyminor);
      }
      yypParser->yyerrcnt = 3;
#ifdef YYERRORSYNC
      /* Panic-mode recovery: discard tokens until we find a sync token */
      if( yymajor!=0 && (YYCODETYPE)yymajor<(YYCODETYPE)(sizeof(yy_is_sync_token)/sizeof(yy_is_sync_token[0]))
          && yy_is_sync_token[(YYCODETYPE)yymajor] ){
        /* Current token IS a sync token -- resume parsing here */
        break;
      }
      /* Not a sync token -- discard and continue */
      yy_destructor(yypParser,(YYCODETYPE)yymajor,&yyminorunion);
      yymajor = YYNOCODE;
      break;
#else /* !YYERRORSYNC */
      yy_destructor(yypParser,(YYCODETYPE)yymajor,&yyminorunion);
      if( yyendofinput ){
        yy_parse_failed(yypParser);
#ifndef YYNOERRORRECOVERY
        yypParser->yyerrcnt = -1;
#endif
      }
      break;
#endif /* YYERRORSYNC */
#endif /* YYERRORSYMBOL */
    }
  }
#ifndef NDEBUG
  if( yyTraceFILE ){
    yyStackEntry *i;
    char cDiv = '[';
    fprintf(yyTraceFILE,"%sReturn. Stack=",yyTracePrompt);
    for(i=&yypParser->yystack[1]; i<=yypParser->yytos; i++){
      fprintf(yyTraceFILE,"%c%s", cDiv, yyTokenName[i->major]);
      cDiv = ' ';
    }
    fprintf(yyTraceFILE,"]\n");
  }
#endif
  /* Clear action-time lookahead state on SqlParse() exit (P0-NEW-5).
  ** Calls to SqlParse_get_lookahead() outside an active SqlParse() are
  ** defined to return YYEMPTY. */
  yypParser->yyLookaheadMajor = -2;
  yypParser->yyLookaheadCleared = 0;
  return;
}

#ifdef YYLOCATIONTYPE
/*
** Location-aware version of SqlParse().
** Same as SqlParse() but also stores the source location for the shifted token.
** Use this entry point when the %locations directive is active.
**
** The location is stored on the parse stack alongside the token value.
** During reductions, a YYLOC(N) macro can access the location of the
** Nth RHS symbol, and the result location is computed by merging the
** locations of the first and last RHS symbols.
*/
void SqlParseLoc(void*,int,SqlParseTOKENTYPE,YYLOCATIONTYPE SqlParseARG_PDECL);
void SqlParseLoc(
  void *yyp,                   /* The parser */
  int yymajor,                 /* The major token code number */
  SqlParseTOKENTYPE yyminor,      /* The value for the token */
  YYLOCATIONTYPE yyloc         /* Source location of the token */
  SqlParseARG_PDECL               /* Optional %extra_argument parameter */
){
  yyParser *yypParser = (yyParser*)yyp;
  /* Stash the lookahead's location so %syntax_error can see it via
  ** yyLookaheadLoc / TOKEN_LOC even when the parser fails on this
  ** token (no shift happens, so yytos->yyloc would still be the
  ** previously-shifted token's location). */
  yypParser->yyLookaheadLoc = yyloc;
  SqlParse(yyp, yymajor, yyminor SqlParseARG_PARAM);
  /* After SqlParse shifts the token, patch the location on top of stack */
  if( yypParser->yytos > yypParser->yystack ){
    yypParser->yytos->yyloc = yyloc;
  }
}
#endif /* YYLOCATIONTYPE */

/*
** Return the fallback token corresponding to canonical token iToken, or
** 0 if iToken has no fallback.
*/
int SqlParseFallback(int);
int SqlParseFallback(int iToken){
#ifdef YYFALLBACK
  assert( iToken<(int)(sizeof(yyFallback)/sizeof(yyFallback[0])) );
  return yyFallback[iToken];
#else
  (void)iToken;
  return 0;
#endif
}

/*
** Return a heap-allocated string listing the tokens that would be
** valid in the current parser state.  The caller must free() the
** returned string.  Returns NULL if the parser is NULL or on
** allocation failure.
**
** The output is a comma-separated list like: "SELECT, INSERT, UPDATE".
** This is useful for producing human-readable error messages.
**
** See also SqlParseExpectedTokens() for a structured (array) form and
** SqlParseTokenName() for looking up individual token names.
*/
char *SqlParseExpectedTokensString(void*);
char *SqlParseExpectedTokensString(void *yyp){
  yyParser *yypParser = (yyParser*)yyp;
  YYACTIONTYPE stateno;
  char *buf = 0;
  size_t buf_len = 0;
  size_t buf_cap = 0;
  int first = 1;
  int i;
  if( yypParser==0 || yypParser->yytos==0 ) return 0;
  stateno = yypParser->yytos->stateno;

  for(i=0; i<YYNTOKEN; i++){
    YYACTIONTYPE act;
#if YY_SHIFT_COUNT >= 0
    int j = i + yy_shift_ofst[stateno];
    if( j>=0 && j<YY_ACTTAB_COUNT && yy_lookahead[j]==(YYCODETYPE)i ){
      act = yy_action[j];
    }else{
      act = yy_default[stateno];
    }
#else
    act = yy_default[stateno];
#endif
    if( act!=YY_ERROR_ACTION && act!=yy_default[stateno] ){
      const char *name = yyTokenName[i];
      size_t nlen = 0;
      const char *p = name;
      size_t needed;
      while( *p ) { nlen++; p++; }

      /* Need space for ", " + name + NUL */
      needed = buf_len + (first ? 0 : 2) + nlen + 1;
      if( needed > buf_cap ){
        size_t new_cap = (buf_cap == 0) ? 128 : buf_cap * 2;
        char *tmp;
        if( new_cap < needed ) new_cap = needed;
        tmp = (char*)realloc(buf, new_cap);
        if( tmp==0 ){
          free(buf);
          return 0;
        }
        buf = tmp;
        buf_cap = new_cap;
      }
      if( !first ){
        buf[buf_len++] = ',';
        buf[buf_len++] = ' ';
      }
      memcpy(buf + buf_len, name, nlen);
      buf_len += nlen;
      buf[buf_len] = 0;
      first = 0;
    }
  }
  return buf;
}


/* ====================================================================
**  RFC 0059 Diagnostics API
**  --------------------------------------------------------------------
**  These three functions let a host produce rich error messages:
**
**    SqlParseTokenName(code)      -> string name for a token code
**    SqlParseState(parser)        -> current parser state number
**    SqlParseExpectedTokens(...)  -> array of token codes valid at a state
**
**  Together with Token.length from the tokenizer, this is enough to
**  build Rust-compiler-style diagnostics with caret underlines and
**  "expected one of ..." hints.
** ================================================================== */

/*
** Return the string name of a terminal token code, or NULL if the
** code is out of range.  Names come from the yyTokenName[] table.
*/
const char *SqlParseTokenName(int);
const char *SqlParseTokenName(int tokenCode){
  if( tokenCode < 0 || tokenCode >= (int)YYNTOKEN ) return 0;
  return yyTokenName[tokenCode];
}

/*
** Return the current parser state number, or -1 if the parser handle
** is invalid.  For a freshly-initialized parser, returns 0 (the
** initial/start state).
**
** The returned value is meaningful only as an input to
** SqlParseExpectedTokens().  Host code should not attach semantic meaning
** to the number itself -- it is an internal identifier and may change
** whenever the grammar is regenerated.
*/
int SqlParseState(void*);
int SqlParseState(void *yyp){
  yyParser *yypParser = (yyParser*)yyp;
  if( yypParser==0 || yypParser->yytos==0 ) return -1;
  return (int)yypParser->yytos->stateno;
}

/*
** Fill `out` with the token codes that would be valid at state
** `stateno` and return the count written (up to `max`).  If `out` is
** NULL or `max` is 0, returns the count that would have been written,
** which lets the caller size a buffer correctly on a second call.
**
** The returned codes can be passed to SqlParseTokenName() to obtain
** human-readable names.  Codes are returned in ascending numeric
** order.
**
** Typical usage from a %syntax_error callback:
**
**   int s = SqlParseState(yypParser);
**   int n = SqlParseExpectedTokens(s, NULL, 0);
**   int *codes = malloc(n * sizeof(int));
**   SqlParseExpectedTokens(s, codes, n);
**   for( int i = 0; i < n; i++ ){
**       fprintf(stderr, "  expected: %s\n", SqlParseTokenName(codes[i]));
**   }
**   free(codes);
*/
int SqlParseExpectedTokens(int,int*,int);
int SqlParseExpectedTokens(int stateno, int *out, int max){
  int count = 0;
  int i;

  if( stateno < 0 ) return 0;
#if YY_SHIFT_COUNT >= 0
  if( stateno > YY_SHIFT_COUNT ) return 0;
#endif

  for(i = 0; i < YYNTOKEN; i++){
    YYACTIONTYPE act;
#if YY_SHIFT_COUNT >= 0
    int j = i + yy_shift_ofst[stateno];
    if( j >= 0 && j < YY_ACTTAB_COUNT && yy_lookahead[j] == (YYCODETYPE)i ){
      act = yy_action[j];
    }else{
      act = yy_default[stateno];
    }
#else
    act = yy_default[stateno];
#endif
    if( act != YY_ERROR_ACTION && act != yy_default[stateno] ){
      if( out && count < max ){
        out[count] = i;
      }
      count++;
    }
  }
  return count;
}
